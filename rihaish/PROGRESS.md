# PROGRESS.md — Rihaish

**Current build-step:** 35 — amenities-booking — DONE (2026-07-13)
**Last completed:** 35 — amenities-booking — DONE (2026-07-13)
**Next:** 36 — (see ARCHITECTURE.md build order)

### Recent steps

- **35 — amenities-booking** — DONE (2026-07-13) — feature `amenities.core` (+ sub-features `amenities.billing`/`amenities.approval`; deps residents.registry/core.notifications/core.worker). Bookable shared spaces with a dedicated AMENITY_MANAGER role. DOUBLE-BOOKING IMPOSSIBLE via per-amenity Postgres ADVISORY TRANSACTION LOCK + transactional overlap check respecting `maxConcurrentBookings` (a naive UNIQUE index can't express concurrency>1); concurrency vitest proves exactly-one-succeeds, loser → `slot_taken` 409. RATE APPROVAL GATE: manager proposes (PENDING) → admin approves (`amenities.rate.approve`, gated by `amenities.approval`); `selectRate` prices ONLY an APPROVED, effective-dated rate (specificity SPECIFIC_SLOT>WEEKDAY/WEEKEND>ALL, then latest effectiveFrom); a DRAFT/PENDING/REJECTED rate prices nothing; approve CLOSES the prior current rate's effectiveTo (effective-dating, never in-place edit); amount+deposit SNAPSHOTTED so a later rate change never re-prices (vitest). Booking money → its OWN Invoice + InvoiceLine `chargeHeadCode="amenity"` + LedgerEntry(INVOICE,DEBIT), so the resident portal shows a clearly SEPARATE labelled line, never merged into maintenance (vitest). `billing.ledger` deliberately NOT a hard dep — pricing is a RUNTIME check (`isEnabled(billing.ledger)&&isEnabled(amenities.billing)`); either off → free/unpriced bookings, no invoice (vitest). Security deposit tracked as its OWN ledger entries (ADJUSTMENT DEBIT hold at approval, CREDIT refund at completion); withhold needs a mandatory audited reason; free-window cancel writes a REVERSAL credit of the invoice + deposit release, after the window the charge stands. AMENITY_MANAGER hard-scoped to amenities they run (`assertDeskAccess` → `amenity_forbidden` 403 on others; vitest). Booking number `RUFI-BKG-2026-0042` gapless per (society,year) via AmenityBookingSequence. Migration `20260713370000_amenities_booking`: 3 enums + Amenity/AmenitySlot/AmenityRate/AmenityBooking/AmenityFeedback (societyId+deletedAt scoped) + AmenityBookingSequence (pass-through). `lib/amenities/`: pure `rules.ts` (16 vitest) + constants/actor/http + `service.ts`. `schemas/amenities.ts`. API `/api/amenities`(GET/POST)+`/[id]`(GET/PATCH)+`/[id]/{slots,rates,availability,feedback}`, `/api/amenity-slots/[id]`(DELETE), `/api/amenity-rates/[id]/approve`, `/api/bookings`(GET/POST)+`/[id]/{approve,reject,cancel,complete,feedback}`, `/api/me/bookings`. Perms `amenities.{read,manage,slot.manage,booking.approve,rate.propose}`→amenities.core, `rate.approve`→amenities.approval; residents browse/book/feedback with NO perm. Notif codes amenity.booking.{requested,approved,rejected}/amenity.rate.approved under `announcements` (en+ur). UI `amenities` page (nav `calendarCheck`, feature-gated, hideOnSharedDevice): Simple resident cards+availability(unavailable GREYED not hidden)+request+my-bookings+feedback; Pro desk bookings table+approve/reject/complete(withhold-deposit)+amenity CRUD+slot/blackout editor+rate approval workflow+feedback browser. i18n en+ur `amenities.*` (177 keys, parity 0-diff). Integration test proves 6 acceptance criteria; e2e `amenities-booking.spec` (401 all endpoints). No cron. lint+typecheck clean; rules 16/16, rbac 8/8.

- **34 — surveys** — DONE (2026-07-13) — feature `surveys.core` (deps residents.registry/core.notifications). OPINION collection, NOT a poll: the atomic unit is the RESPONDENT. ANONYMITY IS REAL yet drafts resume + turnout is known — reconciled by `SurveyParticipation` (pass-through, records WHO opened/submitted WITHOUT linking to ANSWERS; anon submit SEVERS `draftResponseId`). One-response via participation.hasSubmitted honouring `allowMultipleResponses`. Required validation SERVER-side + branching-aware. Anonymity-preserving export (no respondent column, rows ordered by SHA-256 of responseId). `lib/surveys/` pure rules + service; API `/api/surveys` + `/api/me/surveys`. UI Simple one-question-per-screen + Pro builder/charts/free-text/export. lint+typecheck clean.

- **33 — polls-voting** — DONE (2026-07-13) — feature `polls.core` (deps residents.registry/core.notifications). BINDING votes. INTEGRITY: one-vote-per-flat is a DB fact (`@@unique([pollId,flatId])`). ANONYMITY real (anon poll `voterUserId=NULL`) yet `VoteTurnout` (pass-through, flat-keyed) records turnout. Verifiable receipt hash. RESULTS 403 until close for EVERY role incl SOCIETY_ADMIN; quorum-not-met → INCONCLUSIVE. Eligibility snapshotted at open. Cancel voids result + audit + notify-all; votes immutable. `lib/polls/` pure rules (30+ vitest) + service; API `/api/polls` + `/api/me/polls`. UI Simple ballot+receipt+animated results + Pro builder/turnout dashboard/chase list. lint+typecheck clean.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
