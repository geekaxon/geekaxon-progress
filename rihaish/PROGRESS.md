# PROGRESS.md — Rihaish

**Current build-step:** 27 — staff-operations-console — DONE (2026-07-12)
**Last completed:** 27 — staff-operations-console — DONE (2026-07-12)
**Next:** 28 — (see ARCHITECTURE.md build order)

### Recent steps

- **27 — staff-operations-console** — DONE (2026-07-12) — feature `staff.console` (deps `complaints`/`staff.directory`/`branding.pwa` — spec's `complaints.core`/`pwa.core` map to those), perm `staff.console.read` (gated by `staff.console`, seeded to STAFF role), nav `myJobs` (main group, listChecks, hideOnSharedDevice). NO new tables — a hard-scoped surface over step-26 tickets. `lib/staff-console/`: pure `rules.ts` (classifyActiveJob today/upcoming by society-local day; compareJobs priority→SLA-due→FIFO, 11 vitest) + `constants.ts` (JOB_BUCKETS, ACTIVE/COMPLETED status sets) + `actor.ts` (requireStaffConsoleActor: auth ∩ perm ∩ feature; runRead/WriteScope mirror complaints) + `http.ts` + `service.ts` — every read filters `assignedStaffId = me` (the active StaffMember linked to the caller), every action calls `requireMyJob` → `not_your_job` 403 by direct id; buckets today/assigned/upcoming (in-memory classify+sort, ≤500 active) & completed (DB-paginated, RESOLVED/CLOSED + rating); actions delegate to complaints `transitionTicket`/`addNote` (start ASSIGNED→IN_PROGRESS, hold→ON_HOLD reason-required, resolve→RESOLVED note-required triggers rating, note+photo) + own `requestReassign` (sets reassignNeeded + REASSIGN_FLAGGED event + committee notify); every action writes a TicketEvent + notifies the resident. API `/api/staff/me/jobs?bucket=` + `/:id` + `/:id/{start,note,hold,resolve,reassign-request}` (POST writes send `X-Offline-Queue` → SW outbox queues + Background-Sync replays; illegal transition on replay → 409 conflict, never silent overwrite). UI mobile-first Simple-only: `my-jobs` page (feature+staff-record gated, graceful NoConsole) → console with bottom tabs Today·Assigned·Upcoming·Done·More (global pwa bottom bar suppressed on route), big flat job cards (call/WhatsApp, SLA countdown, photo hint, rating in Done) + job-detail drawer (photos, resident contact, thread, sticky action bar; disabled-with-explanation when reassigned away). i18n en+ur `staffConsole.*` (37 keys) + `nav.myJobs`, RTL logical props. e2e `staff-operations-console.spec` (401 gates on every endpoint; 403 hard-scope enforced in service). Graceful degradation: feature off → complaints unaffected, committee updates status manually.

- **26 — complaints-service-requests** — DONE (2026-07-12) — feature `complaints` (registered as `complaints` to match the forward-declared nav/pwa/rbac wiring; spec's `complaints.core` maps to it) + sub-features `complaints.sla`, `complaints.public` (deps residents.registry/staff.directory/core.notifications/core.storage/core.worker). Perms `complaints.read/.assign/.manage/.category.manage` (seeded COMMITTEE_MEMBER read+assign, MANAGER all); resident raise/track/rate tier is member-only (no perm). Migration `20260712290000_complaints_service_requests`: `ServiceCategory` (tree, per-cat slaHours+defaultAssignee, soft-delete) + `Ticket` (auto-scoped; gapless per-society number, **separate seq per kind** via `TicketSequence` atomic upsert; status machine OPEN→ASSIGNED→IN_PROGRESS→RESOLVED→CLOSED +ON_HOLD/REOPENED/CANCELLED) + `TicketEvent` (append-only audit thread) + `TicketRating` (1–5, feeds step 36) + `TicketUpvote` (public board) + `ComplaintSettings` (visibility default, business-hours, reopen/auto-close windows). Pure rules unit-tested (`rules.ts` status machine/numbering/reopen-window; `sla.ts` business-hours due-date + escalation ladder — 27 vitest). Worker `complaints.sla-escalate` now real: warn→manager→committee escalation (`complaints.sla` only) + auto-close RESOLVED-unrated past 7d + flag orphaned assignees. API `/api/complaints` (overview/list/board/dashboard/suggest/public + POST raise), `/:id` (detail+thread, PATCH meta), `/:id/{assign,transition,note,rate,reopen,upvote}`, `/bulk-assign`, `/categories`(+/:id), `/settings`, `/export`. UI Simple (report tiles+camera photo, my-tickets timeline+rate, committee kanban drag-to-transition) & Pro (ticket table+filters+export, animated SLA dashboard, category manager, settings, public board+upvotes); ticket drawer = audit thread + role actions. i18n en+ur `complaints.*`, RTL. Assignee suggestions from `StaffMember.skills`. Edge cases: flat-not-held 403, private ticket 403 cross-resident, no-assignee SLA starts at creation, SLA-disabled graceful, reopen>30d → new linked ticket. e2e auth-gate spec.

- **25 — staff-directory-attendance** — DONE (2026-07-12) — `staff.directory` feature + `staff.attendance` sub-feature (deps residents.registry/core.auth/core.storage), perms `staff.directory.read/.manage`, `staff.attendance.read/.mark`, `staff.leave.approve` (seeded to MANAGER/COMMITTEE), nav `staff` (idCard, manage), ui-mode module `staff`. Migration `20260712280000_staff_directory_attendance`: `StaffMember` (scoped: societyId+deletedAt → soft-delete keeps attendance/leave history; unique `(societyId, employeeCode)`; optional login/CNIC-enc/salaryMinor BigInt/skills[]) + `Attendance` (pass-through; unique `(societyId, staffId, date)`) + `LeaveRequest` (pass-through) + enums `EmploymentType`/`Shift`/`AttendanceStatus`/`AttendanceMethod`(MANUAL|QR|SELF — **geofence [DECIDE AT BUILD] DEFERRED**)/`LeaveStatus`. Attendance dates are **society-local** (`lib/staff/dates.ts`: local day ↔ 00:00Z key); night-shift check-out after midnight updates the open check-in-day row. Pure rules (`lib/staff/rules.ts`, unit-tested): future date blocked, past date requires a reason (audited `attendance.backdated`), employment window (`isEmployedOn`), attendance % (half-day 0.5, holiday excluded). CNIC encrypted/masked(last-3)/view-audited (society-toggleable, shared residents flag); salary→BigInt minor units feeds step 30. `listStaffBySkill` is the step-26 assignment-suggestion hook. API `/api/staff` (GET directory / `?scope=manage` table, POST), `/staff/:id` (GET detail+CNIC audit, PATCH, DELETE soft), `/staff/export`, `/api/attendance` (roster `?date=`), `/attendance/mark` (bulk), `/attendance/check-in`, `/attendance/grid` (+`/export` xlsx), `/api/leave-requests` (GET/POST), `/leave-requests/:id` PATCH (approve marks LEAVE days). UI Simple (staff cards call/WhatsApp + one-tap mobile roster + reason-gated back-date) & Pro (staff table+export, monthly attendance grid w/ % + Excel export, leave approvals); detail drawer + add/edit modal; i18n en+ur `staff.*`+`nav.staff`+`uiModes.module.staff`, RTL. Unit: `dates`/`rules`; e2e `staff-directory-attendance.spec` (auth gates on every surface). Graceful degradation: directory works with attendance disabled.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
