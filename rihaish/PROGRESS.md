# PROGRESS.md — Rihaish

**Current build-step:** 41 — cctv — DONE (2026-07-13)
**Last completed:** 41 — cctv — DONE (2026-07-13)
**Next:** 42 — (see ARCHITECTURE.md build order)

### Recent steps

- **41 — cctv** — DONE (2026-07-13) — feature `cctv.core` (deps `residents.registry`, `core.storage`; notifications/gatepass/emergency are RUNTIME integrations, NOT DAG edges — snapshots degrade to nothing when absent, no module depends on CCTV). Bring-your-own, tiered CCTV where RIHAISH NEVER CARRIES VIDEO (architecture test asserts no media/streaming lib imported by service/token/keys/actor/rules and the stream URL points at the SOCIETY edge, never at us). The security heart is a pure Ed25519 view-token engine (`token.ts`): SHORT-LIVED, single-society/single-camera/single-user, `jti`-replay-protected, signed — the society edge verifies with Rihaish's PUBLIC key; expired/tampered/foreign-key/replayed all rejected (7 vitest). SAFEGUARDING enforced in code (pure `rules.ts`, 7 vitest): only COMMON cameras are ever resident-grantable — `createGrant` rejects RESTRICTED/PRIVATE to any resident subject for EVERY caller incl. SOCIETY_ADMIN, AND `resolveAccessibleCameras` re-filters at access time (defense in depth). Ex-tenant nightmare closed two ways: resident-subject access requires an ACTIVE occupancy (an ended occupancy → zero cameras automatically) AND `revokeGrantsForEndedOccupancy` (wired best-effort + feature-gated into residents `updateOccupancy`/endDate) revokes explicit USER grants. Live view SURVIVES a READ_ONLY society (`runLiveScope` never read-only — cutting security cameras over a billing dispute is unacceptable); only CONFIG writes freeze (`runConfigScope`). Every view → APPEND-ONLY `CameraViewLog` (no delete path; member sees own history, committee sees all); watermark = viewer name+flat+time, non-removable DOM overlay. Snapshots: agent PUSHES tiny jpegs (the ONLY image data touching storage) — rate-limited (1/interval), 100 KB cap, through `storeFile` so counted against `maxStorageMb`. Migration `20260713430000_cctv`: `CctvMode`/`AgentStatus`/`CameraClass`/`GrantSubject`/`SnapshotReason` enums + `CctvIntegration`/`CctvSettings` (pass-through) + `CctvAgent`/`Camera`/`CameraGroup`/`CameraAccessGrant`/`CameraViewLog`/`CameraSnapshot`. `lib/cctv/` constants/token/keys(env Ed25519 PEM, dev-ephemeral fallback)/rules/actor/http/service; `schemas/cctv.ts`. API `/api/cctv/integration`(GET/PUT)+`/settings`+`/agents/{enroll,heartbeat}`+`/view-log`, `/api/cameras`(GET/POST)+`/[id]`(PATCH/DELETE)+`/[id]/token`(mint)+`/[id]/snapshot`(agent push), `/api/camera-groups`, `/api/camera-grants`(+`/[id]` DELETE revoke), `/api/me/cameras`. Perms `cctv.view`(COMMITTEE_MEMBER+MANAGER+GUARD)/`cctv.manage`(MANAGER,admin `*`)→cctv.core; a resident's OWN access needs NO perm. Nav `cctv` (icon `Cctv`, main group, NOT hidden on shared device — guard needs the gate tile), ui-mode `cctv`. UI Simple = mobile camera wall of snapshot tiles → watermarked live-view modal (mint token, LINK opens vendor tab); Pro = cameras/grants/agents tables + append-only view-log audit (CSV) + honest tier capability matrix/setup guide. Env `CCTV_TOKEN_{PRIVATE,PUBLIC}_KEY`+`_KEY_ID` (optional, dev-ephemeral; in .env.example). i18n en+ur `cctv.*` (99 keys) + `nav.cctv`, parity 0-diff. Integration test (`cctv.integration.test.ts`, DB-backed): safeguarding refusal, occupancy-end auto-revoke + ex-tenant zero-access + mint-denied, signed edge-URL token verify, snapshot rate/size cap + storage accounting, no-media-lib architecture assertion. lint+typecheck clean; pure token+rules tests (14) green locally.

- **40 — public-site** — DONE (2026-07-13) — feature `platform.public_site` (deps `platform.billing`, `core.shell`). The apex marketing site (rihaish.pk). APEX-ONLY BY CONSTRUCTION: new pure `isMarketingPath` + a middleware gate 404 a society host on `/`, `/features`, `/pricing`, `/demo`, `/about`, `/legal/*` (generic 404, reveals nothing); `/api/leads` re-checks the host in-handler. STATIC/ISR: every marketing page renders with no DB read (getTranslations + setRequestLocale only), so a DB outage never takes the site down; the lead form is the one dynamic surface and degrades to a 503 "email us" instead of pretending it saved. Route group `app/[locale]/(marketing)/` (layout + landing/features/pricing/demo/about + legal/{privacy,terms,data-protection}); old placeholder root page removed. Lead capture is layered: apex host → in-memory per-IP rate limit (survives DB down) → honeypot + time-trap + stateless arithmetic challenge (`checkSpam`, pure) → ONE shared Zod schema (`schemas/public-site.ts`, client+server) → `createLead` (unscoped platform write, authoritative) → best-effort email/WhatsApp heads-up (`notifyPlatformOfLead`, never fails the lead). A lead for an EXISTING society is flagged (`flagExistingSocieties`, case-insensitive) NOT rejected. New `Lead` model + `LeadStatus` (migration `20260713420000_public_site`, platform-level, no societyId/deletedAt). Platform console: `/platform/leads` page + `LeadsTable` (status pipeline + notes editor) + API `/api/platform/leads`(GET list)+`/[id]`(PATCH), nav `leads` (icon `Inbox`), perms reuse `platform.society.{read,manage}`. SEO: `app/sitemap.ts` (both locales + hreflang alternates), `app/robots.ts` (disallow /app /api /platform), per-page `buildMetadata` (canonical + hreflang + OG/Twitter), JSON-LD `SoftwareApplication`, `(marketing)/opengraph-image.tsx` (next/og, emerald/gold, no staging token). Privacy-respecting analytics only (self-hosted Plausible/Umami via `ANALYTICS_SCRIPT_URL`, first-party, opt-in; no third-party tracker). Product preview is a token-free MOCK (no staging screenshot/token embedded). Pure flat-count calculator (`quoteMonthly`, 184→₨9,200/mo, volume tiers + monthly floor + annual discount, 6 vitest). Env: PLATFORM_ALERT_{EMAIL,PHONE}, PLATFORM_WA_{PHONE_NUMBER_ID,ACCESS_TOKEN}, ANALYTICS_{SCRIPT_URL,SITE_ID} (all optional, in .env.example). i18n en+ur `marketing.*` (landing/features/pricing/demo/about + 3 legal docs incl. CNIC handling + child-exit safeguarding) + `platform.leads`/`nav.leads` — genuine Urdu, RTL, parity 0-diff (incl. array lengths). Tests: pricing (6), spam (6), rate-limit (4), `isMarketingPath` (2), leads DB-backed integration (lands-as-NEW / existing-society-flagged-not-rejected / pipeline). lint+typecheck clean. NOTE (recorded): [DECIDE AT BUILD] trademark/domain clearance for "Rihaish" is a business/legal action, not code — surfaced on the pricing page ("clearance is on us") but the actual filing is out of code scope; real staging screenshots await a screenshot pipeline (mock preview ships meanwhile, deliberately token-free).

- **39 — reports-dashboards** — DONE (2026-07-13) — feature `reports.core` (deps `core.tables`, `core.notifications`; every DATA source — billing/complaints/gatepass/amenities/staff/announcements/occupancy/meters — is a RUNTIME `isEnabled` check, NOT a DAG edge, so a report/widget disappears cleanly when its owning module is off). A READ-ONLY analytics layer over every module. 14 reports (7 financial + 7 operational), each `filters → summary tiles → chart → CSV/Excel/PDF export`. RECONCILIATION IS BY CONSTRUCTION — financial builders reuse the owning module's authoritative helpers (`balanceOf`, `getAccountSummary`) or read the ledger directly: `computeCollection.collectedMinor` = Σ ledger PAYMENT credits, `computeArrears.totalMinor` = Σ positive flat balances (aged by each flat's oldest due date so headline == bucket sum), income−expense via `getAccountSummary` (opening+income−expense=closing). Integration test proves all three EXACT to the minor unit + entitlement-filtering + disabled-report refusal. Exports reuse step-07 kit (`buildInlineExport`/`planExportMode`, PII sensitivity taken from the SERVER result never the client, Urdu font via `loadUrduFont`); > 1,000 rows → `reports.export` worker job → `storeFile` + signed URL emailed. Scheduled monthly report cron `reports.scheduled.monthly` (`0 3 1 * *`, society-scoped so it runs in society tz, `mutating:false` delivery) gated by `ReportSettings.scheduledMonthly` (new pass-through model, migration `20260713410000_reports_dashboards`). 4 role home dashboards (committee/resident/guard/amenity-manager) via `buildDashboard`+`homeRole`, each widget entitlement-gated (absent when module off — never an empty box), animated on mount; home `page.tsx` now renders the live role dashboard (guest → placeholder). `lib/reports/` constants/rules(+8 vitest)/types/actor/http/service/registry/builders/dashboards; `schemas/reports.ts`. API `/api/reports`(GET list), `/[key]`(GET run), `/[key]/export`(POST inline/202-queued), `/schedule`(GET/PUT). Perms `reports.{read,manage}`→reports.core; COMMITTEE/ACCOUNTANT read, MANAGER both, admin `*`. Notif category `reports` + templates `reports.scheduled.ready`/`reports.export.ready` (en+ur IN_APP/EMAIL). Nav `reports` (icon `barChart3`, manage group), ui-mode `reports` (committee). i18n en+ur `reports.*` + `nav.reports` + notification-category labels (parity 0-diff). lint+typecheck clean. NOTE (deliberate, recorded): platform L0 reports (MRR/ARR) and the defaulters "send reminder to all" one-click action were left out of scope; the defaulters report still surfaces contact phones.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
