# PROGRESS.md — Rihaish

**Current build-step:** 43 — utility-schedules-notices — DONE (2026-07-13)
**Last completed:** 43 — utility-schedules-notices — DONE (2026-07-13)
**Next:** 44 — (see ARCHITECTURE.md build order)

### Recent steps

- **43 — utility-schedules-notices** — DONE (2026-07-13) — feature `utility.schedules` (+ sub-features `utility.outages`, `utility.external_notices`; deps `residents.registry`/`core.notifications`/`core.storage`/`core.worker`). Tracks TIME (when power goes, gas comes, water arrives), NOT bills — INDEPENDENT of step 29 but REUSES `UtilityProvider`. WALL-CLOCK IS THE HARD RULE: slot `startTime`/`endTime` are society-local "HH:mm" strings, never built into a `Date`; the pure rules compare minutes-of-day against `zonedParts(now, tz)` so a 14:00 shed is 14:00 in any deploy timezone (25 vitest incl. same-local-time-across-Karachi/London, midnight-spanning 22:00–02:00 → two display segments + one reminder, Eid `isExclusion` suppression, Today-strip next-event, overlap warning, duration stats). NEVER touches the ledger (architecture.test, 6, mirrors step 29 — no `@/lib/billing`/`@/lib/charges`/`ledgerEntry`/`invoice.create` in `lib/schedules/*`). Reminders: `sweepScheduleReminders` (worker `schedules.reminders`, `*/15`, society-scoped, mutating→skipped in READ_ONLY, feature-gated) fires per-user opt-in SLOT reminders within lead window (dedupe via `ScheduleSlotReminderLog` unique(userId,slotId,dayKey)) + service-NOTICE `remindBeforeMinutes` offsets + at-start (dedupe via `remindersSent[]`/`startReminderSent`), STOPS on resolve/cancel. NOTE (cross-tenant fix): `ScheduleReminder`/`ScheduleSlot`/`ServiceNoticeConfirmation`/`ScheduleSlotReminderLog` are pass-through (societyId, NO deletedAt → NOT auto-scoped) so the sweep filters `societyId` explicitly and all other reads key off already-scoped parent ids. Migration `20260713450000_utility_schedules_notices`: enums `ScheduleKind`/`ServiceNoticeKind`/`NoticeSeverity`/`ServiceNoticeStatus` + `UtilitySchedule`(scoped soft-delete; providerId NULLABLE — widened from spec so GENERATOR/LIFT internal schedules need no provider) + `ScheduleSlot`/`ServiceNotice`(scoped)/`ServiceNoticeConfirmation`/`ScheduleReminder`/`ScheduleSlotReminderLog`; UtilityProvider gets `schedules`/`serviceNotices` back-rels. `lib/schedules/` constants/rules(+test)/actor/http/service/architecture.test/integration.test; `schemas/schedules.ts`. API `/api/utility/schedules`(GET overview|?scope=manage list/POST)+`/[id]`(GET/PATCH/DELETE)+`/[id]/slots`(POST bulk-replace)+`/[id]/remind`(POST resident pref)+`/today`(GET strip), `/api/service-notices`(GET/POST)+`/[id]`(GET)+`/{publish,resolve,confirm}`+`/quick-outage`(two-tap)+`/export`(PDF outage-duration report via buildInlineExport, audited), `/api/me/schedules`. Perms `schedules.read`(COMMITTEE+MANAGER+ACCOUNTANT-no; seeded COMMITTEE_MEMBER)/`schedules.manage`(MANAGER,admin `*`)→utility.schedules; resident view/remind/confirm needs NO perm. Sub-feature gate: external-authority notices (authority set OR GOVERNMENT/ROAD_WORK/CLOSURE)→`utility.external_notices`, else outage-family→`utility.outages`; off→`feature_disabled` (integration test 4 proves outage refused while timetable still works). Nav `schedules` (icon `calendarClock`, main group, hideOnSharedDevice), ui-mode `schedules` (resident→SIMPLE). UI Simple=resident Today strip (next-event chips + live outage banners w/ "mine is out too" confirm) + my-schedule cards (kind-coloured, remind-me toggle, provider-PDF link, week grid); committee tabs schedules-table(overlap warn)/notices(publish/resolve, report-outage 2-tap, export PDF)/create(schedule+slot editor w/ exclusion dates). Notif templates (category `utility`, en+ur) `schedule.slot.reminder`/`service.notice.{published,reminder,resolved}`. i18n en+ur `schedules.*` (100 keys) + `nav.schedules`, parity 0-diff. Integration test (DB-backed, skipIf no DATABASE_URL, 4): slot reminder once/day + Eid suppression; notice reminder stops on resolve; "mine too" aggregates + idempotent; outage refused when sub-feature off. lint+typecheck clean; rules(25)+architecture(6) green locally. NOTE (recorded): NO scraper/auto-sync is built (spec's honest constraint — `externalSourceUrl`/`lastCheckedAt` reserved for a future per-provider importer, no migration needed); tap-drag grid is a row-editor (functional equivalent); the Today strip lives in the schedules page (global home-screen wiring is a future enhancement).

- **42 — permission-requests** — DONE (2026-07-13) — feature `permits.core` (+ sub-features `permits.charges`, `permits.fasttrack`; deps `residents.registry`/`core.notifications`/`core.storage`; `billing.ledger` + `gatepass` are RUNTIME `isEnabled` checks, NOT DAG edges → graceful degradation). Turns a phone call to an absent committee member into a request with a status. THE FUNERAL FAST-TRACK: a `PermitType.isFastTrack` type auto-approves on submission, can NEVER carry a fee/deposit (rejected at the DATA LAYER via pure `fastTrackChargeViolation` — enforced in `createPermitType`/`patchPermitType`), notifies committee+guard (roles) and (opt-in) the whole society via the `emergency` category (`ignorePreferences`, cannot be muted, plain/gentle language), and best-effort raises a BEREAVEMENT announcement (step 24, direct `Announcement`+`BereavementDetail` insert). READ_ONLY EXCEPTION (explicit+tested): `runWriteScope` never sets scope-readOnly (so a funeral is always writable); the SERVICE re-imposes the freeze (`assertSocietyWritable` → `ReadOnlyError`/423) for every NON-fast-track write — normal permit blocked, funeral exempt. COMMON-AREA CONFLICT impossible: per-area Postgres advisory xact lock (`pg_advisory_xact_lock(hashtext(society:area)::int8)`) + `overlappingAreaCount` overlap check inside the create/submit/approve tx → two tents on one lawn serialize, one wins, loser `area_taken` (409). Fee/deposit SNAPSHOT at approval (`snapshotCharge`; a later type-fee change never re-prices); money → ledger as a SEPARATE `permits` invoice line + a separate debris-deposit DEBIT hold; deposit refunded (CREDIT) on complete/refund-deposit unless withheld with an audited reason; unpriced+free when `billing.ledger`/`permits.charges` off. RENOVATION permits emit STAFF_RECURRING worker gate passes (step 28 `createPass`, feature-gated + try/catch → silent no-op when gatepass off). Guard console gets an active-funeral banner (`getActiveFunerals`, ±1 day window) so no mourner is turned away. Gapless numbering `RUFI-PMT-2026-0042` (`PermitSequence` atomic upsert). Migration `20260713440000_permit_requests`: `PermitStatus` enum + `PermitType`/`PermitRequest` (auto-scoped, soft-delete infra — never hard-deleted) + `PermitEvent` (append-only audit, pass-through) + `PermitSequence`. `lib/permits/` constants/rules(+rules.test 40 assertions: overlap/window/fast-track-no-charge/snapshot/status-machine)/actor/http/service; `schemas/permits.ts`. API `/api/permit-types`(GET/POST)+`/[id]`(PATCH), `/api/permits`(GET desk/POST create), `/[id]`(GET detail+timeline)+`/{submit,approve,reject,cancel,complete,refund-deposit}`, `/api/permits/availability`, `/api/me/permits`. Perms `permits.read`(COMMITTEE+MANAGER+GUARD)/`permits.approve`(COMMITTEE+MANAGER)/`permits.manage`(MANAGER,admin `*`) → permits.core; a resident's OWN create/submit/cancel needs NO perm. Nav `permits` (icon `stamp`, main group, NOT hidden on shared device — guard needs the funeral banner), ui-mode `permits`. UI Simple = resident icon tiles (Tent/Renovation/Shifting/Funeral/Milad/Party) → request form (fee shown BEFORE submit, NEVER for a funeral; gentle funeral copy + announce-to-society toggle) + my-requests status list; committee approvals inbox (one-tap approve/reject) + area calendar + funeral banner; Pro = permit table (status filter) + permit-type editor (fees/deposits/noise windows/allowed areas) + complete/refund actions. i18n en+ur `permits.*` (106 keys) + `nav.permits`, parity 0-diff. Notif templates `permit.{submitted,approved,rejected}` (announcements) + `permit.funeral.{committee,society}` (emergency, gentle EN+UR incl. Inna lillahi). Integration test (`permits.integration.test.ts`, DB-backed, 5): fast-track no-fee refusal + funeral auto-approve; funeral works under READ_ONLY while normal permit 423s; two-permit same-area concurrency (exactly one wins); approved chargeable → one `permits` ledger DEBIT snapshot + deposit refund CREDIT; renovation emits STAFF_RECURRING passes when gatepass on, silent when off. lint+typecheck clean. NOTE (recorded): noise-window violations are surfaced as rule text only (a complaint, step 26 — the software states the rule, does not police it); worker pass persons are placeholder-named (`Worker N`) — the crew's real names are a future enhancement; overrun (ran past endAt) is surfaced via `isOverrun` on the DTO, deposit never auto-forfeited (a human decides).

- **41 — cctv** — DONE (2026-07-13) — feature `cctv.core` (deps `residents.registry`, `core.storage`; notifications/gatepass/emergency are RUNTIME integrations, NOT DAG edges — snapshots degrade to nothing when absent, no module depends on CCTV). Bring-your-own, tiered CCTV where RIHAISH NEVER CARRIES VIDEO (architecture test asserts no media/streaming lib imported by service/token/keys/actor/rules and the stream URL points at the SOCIETY edge, never at us). The security heart is a pure Ed25519 view-token engine (`token.ts`): SHORT-LIVED, single-society/single-camera/single-user, `jti`-replay-protected, signed — the society edge verifies with Rihaish's PUBLIC key; expired/tampered/foreign-key/replayed all rejected (7 vitest). SAFEGUARDING enforced in code (pure `rules.ts`, 7 vitest): only COMMON cameras are ever resident-grantable — `createGrant` rejects RESTRICTED/PRIVATE to any resident subject for EVERY caller incl. SOCIETY_ADMIN, AND `resolveAccessibleCameras` re-filters at access time (defense in depth). Ex-tenant nightmare closed two ways: resident-subject access requires an ACTIVE occupancy (an ended occupancy → zero cameras automatically) AND `revokeGrantsForEndedOccupancy` (wired best-effort + feature-gated into residents `updateOccupancy`/endDate) revokes explicit USER grants. Live view SURVIVES a READ_ONLY society (`runLiveScope` never read-only — cutting security cameras over a billing dispute is unacceptable); only CONFIG writes freeze (`runConfigScope`). Every view → APPEND-ONLY `CameraViewLog` (no delete path; member sees own history, committee sees all); watermark = viewer name+flat+time, non-removable DOM overlay. Snapshots: agent PUSHES tiny jpegs (the ONLY image data touching storage) — rate-limited (1/interval), 100 KB cap, through `storeFile` so counted against `maxStorageMb`. Migration `20260713430000_cctv`: `CctvMode`/`AgentStatus`/`CameraClass`/`GrantSubject`/`SnapshotReason` enums + `CctvIntegration`/`CctvSettings` (pass-through) + `CctvAgent`/`Camera`/`CameraGroup`/`CameraAccessGrant`/`CameraViewLog`/`CameraSnapshot`. `lib/cctv/` constants/token/keys(env Ed25519 PEM, dev-ephemeral fallback)/rules/actor/http/service; `schemas/cctv.ts`. API `/api/cctv/integration`(GET/PUT)+`/settings`+`/agents/{enroll,heartbeat}`+`/view-log`, `/api/cameras`(GET/POST)+`/[id]`(PATCH/DELETE)+`/[id]/token`(mint)+`/[id]/snapshot`(agent push), `/api/camera-groups`, `/api/camera-grants`(+`/[id]` DELETE revoke), `/api/me/cameras`. Perms `cctv.view`(COMMITTEE_MEMBER+MANAGER+GUARD)/`cctv.manage`(MANAGER,admin `*`)→cctv.core; a resident's OWN access needs NO perm. Nav `cctv` (icon `Cctv`, main group, NOT hidden on shared device — guard needs the gate tile), ui-mode `cctv`. UI Simple = mobile camera wall of snapshot tiles → watermarked live-view modal (mint token, LINK opens vendor tab); Pro = cameras/grants/agents tables + append-only view-log audit (CSV) + honest tier capability matrix/setup guide. Env `CCTV_TOKEN_{PRIVATE,PUBLIC}_KEY`+`_KEY_ID` (optional, dev-ephemeral; in .env.example). i18n en+ur `cctv.*` (99 keys) + `nav.cctv`, parity 0-diff. Integration test (`cctv.integration.test.ts`, DB-backed): safeguarding refusal, occupancy-end auto-revoke + ex-tenant zero-access + mint-denied, signed edge-URL token verify, snapshot rate/size cap + storage accounting, no-media-lib architecture assertion. lint+typecheck clean; pure token+rules tests (14) green locally.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
