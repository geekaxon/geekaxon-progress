# PROGRESS.md — Rihaish

**Current build-step:** 20 — resident-finance
**Last completed:** 19 — payments-receipts — DONE (2026-07-12)
**Next:** 20 — resident-finance

### Recent steps

- **19 — payments-receipts** — DONE (2026-07-12) — `billing.payments` feature (deps billing.ledger/core.storage) + perm `billing.payment.read` (new) & existing `billing.payment.record`, nav `payments` (banknote icon), RBAC (COMMITTEE_MEMBER read, ACCOUNTANT read+record). Migration `20260712230000_payments_receipts`: enums `PaymentMethod{CASH,CHEQUE,BANK_TRANSFER,ONLINE}`/`PaymentProvider{MANUAL,STRIPE}`/`PaymentStatus{PENDING_VERIFICATION,CLEARED,BOUNCED,VOID}`/`AllocationMode{OLDEST_FIRST,SPECIFIC_INVOICE}`; `Payment`/`Receipt` (societyId+deletedAt scoped, Payment_flatId_fkey + Receipt_paymentId_fkey) + infra `PaymentSettings`/`ReceiptSequence`. **Money is the ledger — a Payment row has no money**: CLEARED writes `LedgerEntry(PAYMENT,CREDIT)` (one per settled invoice carrying invoiceId + one advance); bounce/void writes matching `REVERSAL,DEBIT` (never edits originals) so invoices re-open. Pure logic (unit-tested): `allocation` (OLDEST_FIRST/target, partial/advance gating, 10 tests), `money-words` (South-Asian lakh/crore, currency names, 6). `payments.ts`: settings, `recordPayment` (CLEARED, allocate→credits→gapless receipt via atomic ReceiptSequence upsert→recompute invoice status→enqueue receipt-pdf→notify), `submitPayment` (PENDING, proof-gated, no ledger), `verifyPayment` (approve/reject; `SELECT…FOR UPDATE` row-lock + clear in one txn → second verifier gets `already_verified`; verifier may edit amount, audited), `bouncePayment`/`voidPayment` (REVERSAL + once-only bounce fee + void receipt keeps number), list/queue/detail/overview/export/getReceiptRef. Worker: `runReceiptPdf` (branded Urdu-capable PDF via `receipt-pdf.ts` + `pdf-lib`, amount in figures+words, stored via `storeFile`, links `Receipt.pdfFileId`; dedupeKey idempotent) registered `billing.receipt-pdf` (non-mutating). Templates `billing.payment.reverted`/`rejected` (en+ur). API (8): `/api/payments`(overview+POST record), `/list`, `/queue`, `/settings`(GET/PATCH), `/submit`, `/[id]`(GET/PATCH approve|reject|bounce|void), `/[id]/receipt`(signed URL or 202), `/export`. UI via `useUiModule("payments")`: Simple record modal + verification queue (slip thumb, approve/reject); Pro payment DataTable (filters/export, bounce/void confirm, receipt download) + settings modal. i18n en+ur parity (payments ns, 101 keys). Gates green: typecheck, lint(0), 489 unit tests, build (8 routes + page en/ur). Integration `payments.integration.test.ts` (cleared credits/settles/receipt, partial→PARTIALLY_PAID, excess→credit, bounce reversal never mutates + fee once + receipt voided keeps number, submission off-ledger + already_verified, gapless receipts under concurrency, void) DB-gated skip. e2e `payments-receipts.spec` (11 unauth 401s + page no-500). **Note:** SPECIFIC_INVOICE allocation supported end-to-end in service/API; the record modal defaults OLDEST_FIRST (per-flat invoice picker deferred to step 20 resident-finance). Receipt PDF passes `logo:null` (society name + emerald branding render; logo-bytes loader deferred).

- **18 — ledger-invoicing** — DONE (2026-07-12) — `billing.ledger` feature + perms `billing.invoice.read/create`, nav `billing`, RBAC wired. Migration `20260712220000_ledger_invoicing`: `Invoice`(+lines snapshot)/`InvoiceLine`/`LedgerEntry` (scoped) + infra `InvoiceSequence`/`BillingSettings`. **Money is the ledger — no `paidAmount`**; `balance = Σdebit−Σcredit`. Partial unique `(societyId,flatId,period) WHERE period NOT NULL` = idempotency. Pure logic: `balance`/`numbering`/`aging`/`late-fee`. `commitRun` (per-flat txn: Invoice+lines+INVOICE debit + atomic seq bump; advance auto-apply; marks specials APPLIED), `cancelInvoice` (REVERSAL, keeps number). Worker `runBillingMonthly`+`runBillingLateFee`. Ledger append-only architecture test. API (9), UI Simple+Pro. i18n 104 keys. Gates: 473 unit tests, build. DB-gated `run.integration`, e2e `ledger-invoicing.spec`.

- **16 — residents-occupancy** — DONE (2026-07-12) — `residents.registry` (isCore) + perms `society.residents.read/manage`, nav `residents`. Migration `20260712200000_residents_occupancy`: `FlatOccupancy` (endDate=null=current; history preserved), `AuthorizedPickupPerson`, `ResidentInvite`. Pure logic: `occupancy`/`invite-token`/`csv`. `service.ts`: invite/accept/transfer/import/export/getFlatPanel/pickup CRUD. UI Simple+Pro. i18n 121 keys. Gates: 408 unit tests, build. e2e `residents-occupancy.spec`.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
