# PROGRESS.md — Rihaish

**Current build-step:** 29 — utility-bill-notices — DONE (2026-07-12)
**Last completed:** 29 — utility-bill-notices — DONE (2026-07-12)
**Next:** 30 — (see ARCHITECTURE.md build order)

### Recent steps

- **29 — utility-bill-notices** — DONE (2026-07-12) — feature `utility.notices` (deps residents.registry/core.notifications/core.storage/core.worker). A REMINDER with per-flat paid/unpaid state — NOT billing. HARD RULE enforced by `lib/utility/architecture.test.ts`: the module may not import `@/lib/billing`/`@/lib/charges` — a utility amount never touches the maintenance ledger. Migration `20260712310000_utility_bill_notices`: `UtilityProvider` (unique per-society code, presets SSGC/K-Electric/KW&SB/PTCL/StormFiber/Nayatel), `UtilityBillNotice` (dedicated `UtilityTargetType` ALL/BLOCK/CATEGORY/FLAT_LIST, `reminderDaysBefore` Int[]=[7,3,1], `remindAfterDue`, DRAFT/PUBLISHED/CLOSED), `UtilityBillFlat` (per-flat state: optional amountMinor/consumerNumber/billFileId, PENDING/PAID/DISMISSED, `lastReminderOn` day-dedupe, `amountNotifiedAt`, `revertReason`) — all auto-scoped (societyId+deletedAt). `lib/utility/`: pure `rules.ts` (`reminderDue` — stops on paid/dismissed/due-pass unless remindAfterDue; zoned day math; vitest) + targeting/actor/http/columns/constants + `service.ts` (providers CRUD+seed, notice create→publish fan-out (one row per targeted flat)→close, patch/bulk amounts, mark-paid resident-or-committee, dismiss, audited committee revert, `myUtilityBills`, worker `sweepUtilityReminders`). Perms `utility.read` (COMMITTEE_MEMBER+MANAGER) / `utility.manage` (MANAGER); residents mark their own flat with NO perm. API `/api/utility/{providers,providers/seed,providers/[id],notices,notices/[id]{,/publish,/close,/bulk},flats/[id]{,/mark-paid,/dismiss,/revert},export}` + `/api/me/utility-bills`. Worker cron `utility.reminders` (0 10 * * *) — replaced the stub. Notif codes utility.notice.posted/reminder/amount_added (en+ur, all stress "not a maintenance charge"). UI `utility` page (nav `zap`, structurally SEPARATE from finance): Simple resident bill cards (due badge, amount if known, mark-paid+slip, dismiss, grey-out) + committee notices list/create-wizard, Pro notice table + per-notice flat matrix (bulk amount entry, progress bar, revert). ui-module `utility` (Simple default). i18n en+ur `utility.*`+`nav.utility`. e2e `utility-bill-notices.spec` (401 on every endpoint). lint+typecheck clean.

- **28 — gate-pass** — DONE (2026-07-12) — feature `gatepass` (spec's `gatepass.core` maps to it; forward-declared nav/rbac/ui-modes reused) + per-type sub-features `gatepass.item_exit/.child_exit/.visitor/.delivery/.staff_recurring` and `gatepass.cnic` (deps residents.registry/core.storage/core.notifications/core.worker/branding.pwa). The security module — things LEAVING the building are primary. Perms `gatepass.verify` (guard console, seeded to GUARD), `.read` (committee register, COMMITTEE_MEMBER+MANAGER), `.manage` (settings, MANAGER); residents create/approve with NO perm (member tier). Migration `20260712300000_gate_pass`: `Pass` (auto-scoped societyId+deletedAt; gapless per-society number via `PassSequence` atomic upsert; signed society-bound expiring `qrToken` + 6-digit `shortCode`) + pass-through `PassPerson` (CNIC enc/masked, child photo off by default), `PassItem`, `GateEvent` (ALWAYS stamps guardUserId — the point; reuses `DeviceKind`), `GatePassSettings`. New enums PassType/PassStatus/PersonRole/PassDirection/VerifyMethod/ItemExitApprover/DeliveryMode. `lib/gate-pass/`: pure `rules.ts` (approver policy, co-sign gate, verify decision, numbering — vitest) + `token.ts` (HMAC sign/verify, society-bound + expiring — vitest proves cross-society + expiry + tamper rejection) + constants/actor/http/columns + `service.ts` (create→approve/reject/cancel, guard verify QR/CODE/SEARCH → GateEvent + notify resident, quick delivery log, manual entry, currently-inside, register/event Pro tables, export with child-exclusion, worker `expirePasses`). Vendored dependency-free QR encoder (`qrcodegen.ts`) — no new npm dep. API `/api/gate-pass` (overview/register/events/pickups + POST create), `/:id` (+approve/reject/cancel), `/settings`, `/export`, `/guard/{verify,search,events,delivery,manual,inside}` (hard-scoped to gatepass.verify). Worker cron `gatepass.expire` (*/15). Notification codes gatepass.approved/rejected/approval_request/visitor_arrived/item_exited/child_exited/delivery_logged (en+ur). UI: resident/committee page (`gate-pass`, type tiles→create form, my passes, approvals, QR+shortCode drawer, Pro register+settings) + guard console (`gate`, mobile Urdu-first: big Scan-QR/Code/Search/Delivery buttons, camera capture, 30-min idle lock, offline-queue writes via X-Offline-Queue→202, currently-inside; global bottom bar suppressed). i18n en+ur `gatePass.*`/`gate.*`+`nav.gatePass`, RTL logical props. e2e `gate-pass.spec` (401 on every endpoint). lint+typecheck clean.

- **27 — staff-operations-console** — DONE (2026-07-12) — feature `staff.console` (deps `complaints`/`staff.directory`/`branding.pwa`), perm `staff.console.read` (STAFF role), nav `myJobs`. NO new tables — a hard-scoped surface over step-26 tickets. `lib/staff-console/` pure rules (classify today/upcoming, priority→SLA→FIFO) + actor (auth ∩ perm ∩ feature) + service (every read filters `assignedStaffId = me`, actions → `not_your_job` 403). API `/api/staff/me/jobs` + actions (offline-queue via SW). UI mobile Simple-only console (bottom tabs, big cards, job drawer). i18n en+ur. e2e auth gates. Graceful: feature off → committee updates status manually.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
