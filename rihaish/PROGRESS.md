# PROGRESS.md — Rihaish

**Current build-step:** 19 — payments-receipts
**Last completed:** 18 — ledger-invoicing — DONE (2026-07-12)
**Next:** 19 — payments-receipts

### Recent steps

- **18 — ledger-invoicing** — DONE (2026-07-12) — `billing.ledger` feature (deps billing.charges/core.worker/core.notifications) + perms `billing.invoice.read/create` (gated by billing.ledger), nav `billing` (feature+perm gated), RBAC roles wired. Migration `20260712220000_ledger_invoicing`: enums `InvoiceStatus`/`LedgerType{INVOICE,PAYMENT,REVERSAL,LATE_FEE,ADJUSTMENT,OPENING_BALANCE,WRITE_OFF}`/`Direction`/`LateFeeMode`; `Invoice`(+lines snapshot)/`InvoiceLine`/`LedgerEntry` (societyId+deletedAt scoped) + infra `InvoiceSequence`/`BillingSettings`. **Money is the ledger — no `paidAmount` column**; `balance = Σdebit−Σcredit`. Partial unique index `(societyId,flatId,period) WHERE period NOT NULL` = idempotency under concurrency. **Fixed missing `Invoice_flatId_fkey` FK** (schema declared `flat Flat @relation onDelete:Cascade` but migration omitted it — drift vs repo convention). Pure logic (unit-tested): `balance` (balanceOf/paymentStatus/overlayOverdue/advanceToApply, 15 tests), `numbering` (format tokens `{prefix}/{YYYY}/{MM}/{seq:N}`, 9), `aging` (0–30/31–60/61–90/90+, clock-injected, 5), `late-fee` (FLAT minor / PERCENT bps floored, grace deadline, per-month key, 6). `service.ts`: settings CRUD, `previewRun` (writes nothing) + `commitRun` (per-flat txn: Invoice+lines+INVOICE debit + atomic `INSERT…ON CONFLICT` seq bump → gapless, rollback-safe; **advance auto-application** via matched CREDIT+DEBIT so the invoice issues PAID/PARTIALLY_PAID while the advance still absorbs it; P2002 race → skip; marks scheduled specials APPLIED; notifies after commit), invoice list/detail/export, `cancelInvoice` (REVERSAL credit, keeps number, never deletes), `getFlatStatement`+`getAgingReport`, opening-balance add/lock. Worker: `runBillingMonthly` (manual `payload.period` unconditional / cron only when `autoGenerate`, READ_ONLY skipped) + `runBillingLateFee` (one fee per invoice per month, idempotent by ledger lookup) replaced the step-17/19 stubs. **Ledger append-only enforced by architecture test** (grep forbids `.ledgerEntry.update/delete/upsert`). API (9): `/api/billing`(overview), `/settings`(GET/PATCH), `/runs/preview`+`/runs/commit`(202 queued), `/invoices`(list)+`/[id]`(GET/PATCH cancel)+`/export`, `/ledger`(flat statement), `/opening-balance`(POST/PATCH lock). UI via `useUiModule("billing")`: Simple generate→preview→confirm + plain flat statement; Pro invoice DataTable (filters/bulk/export) + flat-ledger drawer (running balance) + aging card + settings modal. i18n en+ur parity (billing ns, 104 keys). Gates green: typecheck, lint(0), 473 unit tests, build (9 routes + page en/ur). Integration test `run.integration.test.ts` (concurrency/idempotency/advance/cancel-keeps-number) DB-gated skip like `queue.integration`. e2e `ledger-invoicing.spec` (12 unauth 401s + page no-500).

- **16 — residents-occupancy** — DONE (2026-07-12) — `residents.registry` (isCore, deps flats.registry/core.auth/core.notifications) + perms `society.residents.read/manage`, nav `residents`. Migration `20260712200000_residents_occupancy`: enum `Relation{OWNER,OCCUPANT}`; `FlatOccupancy` (scoped; `endDate=null`=current; history preserved — a transfer closes the old row + opens a new one), `AuthorizedPickupPerson` (scoped; CNIC enc/mask), `ResidentInvite` (single-use token, no deletedAt). Pure logic: `occupancy` (derive status + plan transfer), `invite-token` (7d TTL, single-use), `csv` (header-validated, atomic). `service.ts`: invite/resend/accept(public), transfer/update/suspend, directory/summary/list/export, `getFlatPanel`, pickup CRUD, `importResidents`. API: `/api/residents`(+invite), `/api/residents/[id]`, `/invite/[token]/accept`(PUBLIC)+`/import`+`/export`, `/api/flats/[id]/occupancy/transfer`+`/pickup-persons`. UI via `useUiModule("residents")`: Simple card directory + invite wizard; Pro DataTable + shared `FlatPanelDrawer`. i18n en+ur (residents ns, 121 keys). Gates green: typecheck, lint(0), 408 unit tests, build. e2e `residents-occupancy.spec`.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
