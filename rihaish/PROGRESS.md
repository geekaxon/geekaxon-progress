# PROGRESS.md — Rihaish

**Current build-step:** 39 — reports-dashboards — DONE (2026-07-13)
**Last completed:** 39 — reports-dashboards — DONE (2026-07-13)
**Next:** 40 — (see ARCHITECTURE.md build order)

### Recent steps

- **39 — reports-dashboards** — DONE (2026-07-13) — feature `reports.core` (deps `core.tables`, `core.notifications`; every DATA source — billing/complaints/gatepass/amenities/staff/announcements/occupancy/meters — is a RUNTIME `isEnabled` check, NOT a DAG edge, so a report/widget disappears cleanly when its owning module is off). A READ-ONLY analytics layer over every module. 14 reports (7 financial + 7 operational), each `filters → summary tiles → chart → CSV/Excel/PDF export`. RECONCILIATION IS BY CONSTRUCTION — financial builders reuse the owning module's authoritative helpers (`balanceOf`, `getAccountSummary`) or read the ledger directly: `computeCollection.collectedMinor` = Σ ledger PAYMENT credits, `computeArrears.totalMinor` = Σ positive flat balances (aged by each flat's oldest due date so headline == bucket sum), income−expense via `getAccountSummary` (opening+income−expense=closing). Integration test proves all three EXACT to the minor unit + entitlement-filtering + disabled-report refusal. Exports reuse step-07 kit (`buildInlineExport`/`planExportMode`, PII sensitivity taken from the SERVER result never the client, Urdu font via `loadUrduFont`); > 1,000 rows → `reports.export` worker job → `storeFile` + signed URL emailed. Scheduled monthly report cron `reports.scheduled.monthly` (`0 3 1 * *`, society-scoped so it runs in society tz, `mutating:false` delivery) gated by `ReportSettings.scheduledMonthly` (new pass-through model, migration `20260713410000_reports_dashboards`). 4 role home dashboards (committee/resident/guard/amenity-manager) via `buildDashboard`+`homeRole`, each widget entitlement-gated (absent when module off — never an empty box), animated on mount; home `page.tsx` now renders the live role dashboard (guest → placeholder). `lib/reports/` constants/rules(+8 vitest)/types/actor/http/service/registry/builders/dashboards; `schemas/reports.ts`. API `/api/reports`(GET list), `/[key]`(GET run), `/[key]/export`(POST inline/202-queued), `/schedule`(GET/PUT). Perms `reports.{read,manage}`→reports.core; COMMITTEE/ACCOUNTANT read, MANAGER both, admin `*`. Notif category `reports` + templates `reports.scheduled.ready`/`reports.export.ready` (en+ur IN_APP/EMAIL). Nav `reports` (icon `barChart3`, manage group), ui-mode `reports` (committee). i18n en+ur `reports.*` + `nav.reports` + notification-category labels (parity 0-diff). lint+typecheck clean. NOTE (deliberate, recorded): platform L0 reports (MRR/ARR) and the defaulters "send reminder to all" one-click action were left out of scope; the defaulters report still surfaces contact phones.

- **38 — emergency-alert** — DONE (2026-07-13) — feature `emergency.core` (deps residents.registry/core.notifications; pwa.push+gatepass are runtime/soft, NOT hard edges — the module degrades, never goes silent). A resident presses one button → the guard + committee know instantly. THE ALERT ROW IS PERSISTED FIRST (the guard console reads it), THEN notifications fire, so a provider outage can never un-raise it. Notifications IGNORE the resident's prefs (new `notify({ignorePreferences})` + pure `deliverableChannels` = societyChannels ∩ template, in-app always retained; you cannot mute an emergency — proven: a committee user with emergency push OFF still gets a PUSH row). READ-ONLY EXCEPTION (explicit + tested): `runWriteScope` NEVER sets read-only, and the worker escalation re-enters a `withSociety(readOnly:false)` scope, so a society frozen for non-payment still raises + processes alerts. Status machine (pure `rules.ts`, 20 vitest) is a one-way ratchet ACTIVE→ACKNOWLEDGED→RESOLVED with FALSE_ALARM from either open state; RESOLVED/FALSE_ALARM terminal — an alert can NEVER be deleted (no delete path; `FALSE_ALARM` is the only mis-tap closure) and never re-opens. Escalation cron `emergency.escalate` (`* * * * *`, society-scoped, `mutating:false` so it runs even READ_ONLY) fires ONCE per alert nobody acknowledged within `requireAcknowledgeMinutes` (default 2): re-notify, widen roles, warn block neighbours for FIRE/GAS_LEAK if configured. No guard online at raise → recipient list widened immediately to committee+admin and the resident screen says so. Migration `20260713400000_emergency_alert`: EmergencyKind+AlertStatus enums + EmergencyAlert/EmergencyContact (societyId+deletedAt scoped; deletedAt stays null forever) + EmergencyEvent (immutable audit trail, pass-through) + EmergencySettings (pass-through). `lib/emergency/` constants/rules/actor/http/service; `schemas/emergency.ts`. API `/api/emergency/alerts`(GET own/all + POST raise), `/alerts/[id]/{acknowledge,resolve,false-alarm}`, `/contacts`(GET all/POST)+`/[id]`(PATCH/DELETE), `/settings`(PUT). Perms `emergency.{respond,manage}`→emergency.core; GUARD+COMMITTEE_MEMBER get respond, MANAGER both; residents RAISE + see own + read the contact directory with NO perm. Notif `emergency.alert.raised` (category `emergency`, prefs-ignored; en+ur IN_APP/PUSH/SMS templates already seeded step 11). UI page `emergency` (nav `siren`, main group, NOT hidden on shared device — the guard is the responder; ui-mode `emergency`): Simple = red SOS (TWO TAPS MAX — tap SOS, tap a kind tile = sent; optional inline note never costs a tap) + "Help is being notified" live ack status (polls) + OFFLINE QUEUE (a failed raise is held, shows "Not sent yet", retries on `online`/interval — never looks sent when it isn't) + always-on emergency-contact directory (one-tap `tel:`); responder overlay = full-screen, audible (WebAudio siren), persistent, cannot dismiss without acknowledging, one-tap call to the resident; Pro = alerts table (status/kind/block filters, CSV export) + response-time/false-alarm-rate metrics + contact-directory editor + escalation policy. i18n en+ur `emergency.*` (deep parity 0-diff). Pure rules 20 vitest + pure `deliverableChannels` test; integration test (`emergency.integration.test.ts`) proves prefs-ignored/push-despite-off, READ_ONLY raise+ack+resolve, escalate-once-never-acked, cannot-delete/FALSE_ALARM-only-closure, contact directory. lint+typecheck clean.

- **37 — meter-readings** — DONE (2026-07-13) — feature `meters.core` (deps flats.registry/billing.charges/staff.directory). Sub-metered utilities (WATER/GAS/ELECTRICITY/GENERATOR) billed by consumption, feeding the charge engine's `METERED` heads. PURE rules (`lib/meters/rules.ts`, 20 vitest): ROLLOVER-safe consumption (6-digit 999,980→000,020 = 40 units via "wrap only when smaller-magnitude than the drop", else DISPUTED consumption 0 — never billed silently); SLAB tariff (telescoping bands + fixed charge, hand-checked 120u→251,000); effective-dated `selectTariff` (never edited in place); labelled `estimateConsumption` (avg last 3, basis 0 → never a made-up number); `isAnomalous` (>2× recent avg). Billing bridge `lib/meters/billing.ts` `meteredLinesForRun(period,now,societyId)` prices CONFIRMED readings → per-flat `ResolveMetered` lines; `resolveCharges` extended with an optional `metered[]` param (backward-compatible, default []): a line bills, absence → `metered_unavailable` warning (feature OFF → no lines → head skipped cleanly, never a silent zero). Invoice COMMIT marks consumed CONFIRMED readings BILLED (immutable) in the SAME txn; feature-gated in `loadRunContext` via `isEnabled(meters.core)`. Service enforces BILLED immutability (no confirm/edit/dispute/round-overwrite path). Migration `20260713390000_meter_readings`: MeterKind+ReadingStatus enums + Meter/MeterTariff/MeterReading (societyId+deletedAt scoped, Decimal readings/BigInt money) + MeterSettings (pass-through, photoRequired/estimateMissing). `lib/meters/` constants/rules/actor/http/service/billing; `schemas/meters.ts`. API `/api/meters`(GET/POST)+`/[id]`(PATCH), `/api/meter-tariffs`(GET/POST), `/api/meters/readings`(GET?period)+`/bulk`(round)+`/bulk-confirm`+`/[id]`(PATCH pre-confirm)+`/[id]/confirm`+`/[id]/dispute`, `/api/meters/settings`(PUT), `/api/me/meters`(GET own usage). Perms `meters.{read,record,manage}`→meters.core; COMMITTEE(read+record)/MANAGER(all)/STAFF(record, walks the round); residents see OWN usage no-perm. Notif `meter.reading.disputed`→resident (announcements). UI page `meter-readings` (nav `droplets`, ui-mode `meterReadings`): Simple = Urdu-first mobile reading round (previous, big number, camera, save&next) + resident own-usage cards (latest priced, meter photo, 6-month Sparkline, estimated label); Pro = readings table (period/kind/status/block filters, anomaly highlight, confirm/dispute, bulk confirm, CSV export) + slab tariff editor + meter CRUD + policy toggles. i18n en+ur `meterReadings.*` (115 keys, parity 0-diff). Integration test (`meters.integration.test.ts`) proves rollover/dispute/slab-pricing/estimate-label/BILLED-immutable/missing-warns. lint+typecheck clean; rules 20/20, resolve 17/17 (unchanged), registries 37/37.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
