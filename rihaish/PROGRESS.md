# PROGRESS.md — Rihaish

**Current build-step:** 31 — document-vault — DONE (2026-07-12)
**Last completed:** 31 — document-vault — DONE (2026-07-12)
**Next:** 32 — (see ARCHITECTURE.md build order)

### Recent steps

- **31 — document-vault** — DONE (2026-07-12) — feature `documents.core` (deps core.storage/residents.registry). Bylaws/minutes/notices/NOCs/floor-plans in one place, PRIVATE BY DEFAULT: every download resolves to a short-lived signed URL (step 10 adapter) — no public path — and every open/download is logged to `DocumentAccessLog`. Migration `20260712330000_document_vault`: `DocVisibility` enum (COMMITTEE_ONLY/ALL_RESIDENTS/ROLE_SCOPED/FLAT_SCOPED), `DocumentFolder` (self-ref parent tree, visibility+allowedRoles, sortOrder) + `Document` (fileId→StoredFile, versioning via self-ref `supersedesDocumentId` — NEVER overwritten, old rows retained; `flatId` for FLAT_SCOPED, per-doc `visibility?` override, publishedAt/expiresAt) both auto-scoped (societyId+deletedAt) + `DocumentAccessLog` (pass-through infra, no deletedAt, scoped explicitly — the immutable "who read the minutes" trail). `lib/documents/`: pure `rules.ts` (`resolveVisibility` document→folder→default COMMITTEE_ONLY; `canViewDocument`/`canViewFolder` — FLAT_SCOPED limited to a flat's CURRENT occupants; `isVisibleInFeed` publish+expiry; `supersededIds`/`isHeadVersion`; `matchesSearch` title/desc/tags — vitest `rules.test.ts` proves the privacy trap: previous occupant (endDate set → out of currentFlatIds) refused, new occupant never gains old occupant's docs) + constants/actor/http/columns + `service.ts` (folders CRUD+seed+soft-delete-with-docs, documents create/update/soft-delete, `newVersion` links supersedes + retains old, `listDocuments` resident feed [head+feed-visible+visibility], `listDocumentsTable` committee in-memory paginate, `getDownload` [visibility gate → 403 on guessed FLAT_SCOPED id, signed URL + access log], `listVersions` chain walk, `getAccessLog`, `bulkVisibility`; FLAT_SCOPED occupancy resolved from OPEN FlatOccupancy holdings). Perms `documents.read` (COMMITTEE_MEMBER+MANAGER) / `documents.manage` (MANAGER); residents browse/open per visibility with NO perm (member tier). API `/api/documents` (feed / `?scope=list` table / `?scope=overview`; POST create), `/folders`(+`/[id]` PATCH/DELETE, `/seed`), `/[id]`(GET/PATCH/DELETE), `/[id]/{download,new-version,versions,access-log}`, `/bulk-visibility`. UI `documents` page (nav `folderArchive`, main group, feature-gated): resident folder tiles→doc cards (icon-by-mime, search, open in mobile PDF/image viewer, download) + committee Manage tab (folder bar seed/create, Pro table with version history drawer, per-doc access-log drawer, add-document + new-version modals, delete). ui-module `documents` pre-registered (resident/SIMPLE). i18n en+ur `documents.*`+`nav.documents` (parity verified). e2e `documents-vault.spec` (401 on every endpoint). 50 MB PDF handled by storage layer's 10 MB cap (FileTooLargeError). No cron, no notifications (spec requires neither). lint+typecheck clean.

- **30 — expenses-accounts** — DONE (2026-07-12) — feature `expenses.core` (deps billing.ledger/core.storage; staff.directory soft-dep, runtime-checked). The society's books — "where the union's money went". HARD RULE enforced by `lib/expenses/architecture.test.ts`: no `@/lib/billing`/`@/lib/charges` import, no `ledgerEntry`/`invoice.create` — an expense never touches a flat's maintenance ledger. Migration `20260712320000_expenses_accounts`: `ExpenseHead` (self-ref parent→sub-head, unique per-society code), `Vendor` (soft-delete via isActive; expenses against inactive vendor allowed+flagged), `Expense` (gapless per-(society,prefix,year,month) voucher `RUFI-EXP-2026-07-0031` via atomic `ExpenseSequence` upsert; never edited past DRAFT, never deleted — VOID keeps the number; `PaymentMethod` reused CASH/CHEQUE/BANK_TRANSFER; attachments StoredFile ids), `Budget` (unique per head+period), infra `ExpenseSettings`(requireApproval/approvalThresholdMinor/publishToResidents/voucherPrefix) + `ExpenseSequence`. `lib/expenses/`: pure `rules.ts` (voucher format, `requiresApproval` threshold, `accountSummary` opening+income−expense=closing, `budgetVariance`, `isUnsupported` — vitest) + actor/http/columns/constants + `service.ts` (heads CRUD+seed, vendors CRUD, expense create→submit→approve/reject→mark-paid→void, budgets upsert+variance, account summary reading CLEARED payments as income (aggregate, never a ledger write), resident transparency (head-level only, no vendor/salary), salary draft batch gated on staff.directory). Perms `expenses.read` (COMMITTEE/MANAGER/ACCOUNTANT) / `.record` (ACCOUNTANT/MANAGER) / `.approve` (COMMITTEE/MANAGER) / `.manage` (MANAGER); residents see published transparency with NO perm. API `/api/{expense-heads,vendors,budgets}` (+[id]), `/api/expenses` (+[id]{,/submit,/approve,/reject,/void,/mark-paid}, /salary-draft, /settings), `/api/accounts/summary`, `/api/me/society-accounts`. UI `expenses` page (nav `wallet`, main group, feature-gated): committee console (Simple add-expense head-tiles→amount→receipt, month summary card Collected/Spent/Balance, expense list Simple cards/Pro table, approval queue, budgets vs actual bars, vendors, settings) + resident transparency (spend-by-head bars, published-only). ui-module `expenses` was pre-registered (committee/PRO). i18n en+ur `expenses.*`+`nav.expenses`. e2e `expenses-accounts.spec` (401 on every endpoint). No cron, no notifications (spec requires neither). lint+typecheck clean.

- **29 — utility-bill-notices** — DONE (2026-07-12) — feature `utility.notices` (deps residents.registry/core.notifications/core.storage/core.worker). A REMINDER with per-flat paid/unpaid state — NOT billing. HARD RULE enforced by `lib/utility/architecture.test.ts`: the module may not import `@/lib/billing`/`@/lib/charges` — a utility amount never touches the maintenance ledger. Migration `20260712310000_utility_bill_notices`: `UtilityProvider` (unique per-society code, presets SSGC/K-Electric/KW&SB/PTCL/StormFiber/Nayatel), `UtilityBillNotice` (dedicated `UtilityTargetType` ALL/BLOCK/CATEGORY/FLAT_LIST, `reminderDaysBefore` Int[]=[7,3,1], `remindAfterDue`, DRAFT/PUBLISHED/CLOSED), `UtilityBillFlat` (per-flat state: optional amountMinor/consumerNumber/billFileId, PENDING/PAID/DISMISSED, `lastReminderOn` day-dedupe, `amountNotifiedAt`, `revertReason`) — all auto-scoped (societyId+deletedAt). `lib/utility/`: pure `rules.ts` (`reminderDue` — stops on paid/dismissed/due-pass unless remindAfterDue; zoned day math; vitest) + targeting/actor/http/columns/constants + `service.ts` (providers CRUD+seed, notice create→publish fan-out (one row per targeted flat)→close, patch/bulk amounts, mark-paid resident-or-committee, dismiss, audited committee revert, `myUtilityBills`, worker `sweepUtilityReminders`). Perms `utility.read` (COMMITTEE_MEMBER+MANAGER) / `utility.manage` (MANAGER); residents mark their own flat with NO perm. API `/api/utility/{providers,providers/seed,providers/[id],notices,notices/[id]{,/publish,/close,/bulk},flats/[id]{,/mark-paid,/dismiss,/revert},export}` + `/api/me/utility-bills`. Worker cron `utility.reminders` (0 10 * * *) — replaced the stub. Notif codes utility.notice.posted/reminder/amount_added (en+ur, all stress "not a maintenance charge"). UI `utility` page (nav `zap`, structurally SEPARATE from finance): Simple resident bill cards (due badge, amount if known, mark-paid+slip, dismiss, grey-out) + committee notices list/create-wizard, Pro notice table + per-notice flat matrix (bulk amount entry, progress bar, revert). ui-module `utility` (Simple default). i18n en+ur `utility.*`+`nav.utility`. e2e `utility-bill-notices.spec` (401 on every endpoint). lint+typecheck clean.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
