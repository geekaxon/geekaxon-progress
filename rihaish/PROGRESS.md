# PROGRESS.md — Rihaish

**Current build-step:** 37 — meter-readings — DONE (2026-07-13)
**Last completed:** 37 — meter-readings — DONE (2026-07-13)
**Next:** 38 — (see ARCHITECTURE.md build order)

### Recent steps

- **37 — meter-readings** — DONE (2026-07-13) — feature `meters.core` (deps flats.registry/billing.charges/staff.directory). Sub-metered utilities (WATER/GAS/ELECTRICITY/GENERATOR) billed by consumption, feeding the charge engine's `METERED` heads. PURE rules (`lib/meters/rules.ts`, 20 vitest): ROLLOVER-safe consumption (6-digit 999,980→000,020 = 40 units via "wrap only when smaller-magnitude than the drop", else DISPUTED consumption 0 — never billed silently); SLAB tariff (telescoping bands + fixed charge, hand-checked 120u→251,000); effective-dated `selectTariff` (never edited in place); labelled `estimateConsumption` (avg last 3, basis 0 → never a made-up number); `isAnomalous` (>2× recent avg). Billing bridge `lib/meters/billing.ts` `meteredLinesForRun(period,now,societyId)` prices CONFIRMED readings → per-flat `ResolveMetered` lines; `resolveCharges` extended with an optional `metered[]` param (backward-compatible, default []): a line bills, absence → `metered_unavailable` warning (feature OFF → no lines → head skipped cleanly, never a silent zero). Invoice COMMIT marks consumed CONFIRMED readings BILLED (immutable) in the SAME txn; feature-gated in `loadRunContext` via `isEnabled(meters.core)`. Service enforces BILLED immutability (no confirm/edit/dispute/round-overwrite path). Migration `20260713390000_meter_readings`: MeterKind+ReadingStatus enums + Meter/MeterTariff/MeterReading (societyId+deletedAt scoped, Decimal readings/BigInt money) + MeterSettings (pass-through, photoRequired/estimateMissing). `lib/meters/` constants/rules/actor/http/service/billing; `schemas/meters.ts`. API `/api/meters`(GET/POST)+`/[id]`(PATCH), `/api/meter-tariffs`(GET/POST), `/api/meters/readings`(GET?period)+`/bulk`(round)+`/bulk-confirm`+`/[id]`(PATCH pre-confirm)+`/[id]/confirm`+`/[id]/dispute`, `/api/meters/settings`(PUT), `/api/me/meters`(GET own usage). Perms `meters.{read,record,manage}`→meters.core; COMMITTEE(read+record)/MANAGER(all)/STAFF(record, walks the round); residents see OWN usage no-perm. Notif `meter.reading.disputed`→resident (announcements). UI page `meter-readings` (nav `droplets`, ui-mode `meterReadings`): Simple = Urdu-first mobile reading round (previous, big number, camera, save&next) + resident own-usage cards (latest priced, meter photo, 6-month Sparkline, estimated label); Pro = readings table (period/kind/status/block filters, anomaly highlight, confirm/dispute, bulk confirm, CSV export) + slab tariff editor + meter CRUD + policy toggles. i18n en+ur `meterReadings.*` (115 keys, parity 0-diff). Integration test (`meters.integration.test.ts`) proves rollover/dispute/slab-pricing/estimate-label/BILLED-immutable/missing-warns. lint+typecheck clean; rules 20/20, resolve 17/17 (unchanged), registries 37/37.

- **36 — staff-performance** — DONE (2026-07-13) — feature `staff.performance` (deps staff.directory/complaints/staff.console). Track staff on what residents EXPERIENCE. Snapshots COMPUTED MONTHLY BY THE WORKER (`staff.performance.snapshot` cron, `0 1 1 * *`, society-scoped, finalises the just-ended month; idempotent per (society,staff,period)) and STORED — a report read is a cheap lookup, NEVER a scan across a year of tickets (proven: leaderboard empty before the run). Composite `score` = weighted avg of five 0-100 sub-scores (speed/rating/sla/reopen/attendance) via society-configurable `PerformanceWeights` (the weights ARE the policy, VISIBLE, live-preview editor). HONESTY RULES in pure `rules.ts` (14 vitest incl. hand-computed fixture 88.5): <3 rated tickets → `insufficient_data` score NULL (never a misleading number); society collects NO ratings → `no_ratings`, falls back to speed+SLA (UI banner); a metric with no data is EXCLUDED and its weight RENORMALISED (attendance drops out when `staff.attendance` off). Rating ≤2 → `reviewFlag` (surfaced, humans decide — not an auto-penalty). Resolution credit → ticket's FINAL assignee (`assignedStaffId`); reassignment a SEPARATE signal from `assigned` audit events; reopens map to final assignee; partial-period flag when staff joined/left mid-month. Performance NEVER visible to residents; a worker sees only their OWN (ops console `/api/staff/me/performance`, resolved from userId, hidden when feature off). Migration `20260713380000_staff_performance`: StaffPerformanceSnapshot + PerformanceWeights (both pass-through infra, filtered societyId explicitly). `lib/staff-performance/` rules+service+actor+http+constants; `schemas/staff-performance.ts`. API `/api/staff/[id]/performance`, `/performance/leaderboard`, `/performance/weights`(GET/PUT), `/me/performance`. Perms `staff.performance.{read,manage}`→COMMITTEE(read)/MANAGER(both). UI page `staff-performance` (nav `gauge`, ui-mode `staffPerformance`): Simple leaderboard cards (animated score ring, rating stars, trend arrow, rank badges) + Pro table/weights-editor(live browser preview via shared `computeScore`)/per-staff detail drawer (12-month animated LineCharts + tickets behind the numbers); worker's own card in `my-jobs` console. i18n en+ur `staffPerformance.*`. Integration test (2 societies) proves worker-computed/stored + score + insufficient + own-view + zero-ratings fallback. lint+typecheck clean; rules 14/14.

- **35 — amenities-booking** — DONE (2026-07-13) — feature `amenities.core` (+ sub-features `amenities.billing`/`amenities.approval`; deps residents.registry/core.notifications/core.worker). Bookable shared spaces with a dedicated AMENITY_MANAGER role. DOUBLE-BOOKING IMPOSSIBLE via per-amenity Postgres ADVISORY TRANSACTION LOCK + transactional overlap check respecting `maxConcurrentBookings` (a naive UNIQUE index can't express concurrency>1); concurrency vitest proves exactly-one-succeeds, loser → `slot_taken` 409. RATE APPROVAL GATE: manager proposes (PENDING) → admin approves (`amenities.rate.approve`, gated by `amenities.approval`); `selectRate` prices ONLY an APPROVED, effective-dated rate (specificity SPECIFIC_SLOT>WEEKDAY/WEEKEND>ALL, then latest effectiveFrom); a DRAFT/PENDING/REJECTED rate prices nothing; approve CLOSES the prior current rate's effectiveTo (effective-dating, never in-place edit); amount+deposit SNAPSHOTTED so a later rate change never re-prices (vitest). Booking money → its OWN Invoice + InvoiceLine `chargeHeadCode="amenity"` + LedgerEntry(INVOICE,DEBIT), so the resident portal shows a clearly SEPARATE labelled line, never merged into maintenance (vitest). `billing.ledger` deliberately NOT a hard dep — pricing is a RUNTIME check (`isEnabled(billing.ledger)&&isEnabled(amenities.billing)`); either off → free/unpriced bookings, no invoice (vitest). Security deposit tracked as its OWN ledger entries (ADJUSTMENT DEBIT hold at approval, CREDIT refund at completion); withhold needs a mandatory audited reason; free-window cancel writes a REVERSAL credit of the invoice + deposit release, after the window the charge stands. AMENITY_MANAGER hard-scoped to amenities they run (`assertDeskAccess` → `amenity_forbidden` 403 on others; vitest). Booking number `RUFI-BKG-2026-0042` gapless per (society,year) via AmenityBookingSequence. Migration `20260713370000_amenities_booking`: 3 enums + Amenity/AmenitySlot/AmenityRate/AmenityBooking/AmenityFeedback (societyId+deletedAt scoped) + AmenityBookingSequence (pass-through). `lib/amenities/`: pure `rules.ts` (16 vitest) + constants/actor/http + `service.ts`. `schemas/amenities.ts`. UI `amenities` page. i18n en+ur. Integration + e2e tests. lint+typecheck clean; rules 16/16, rbac 8/8.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
