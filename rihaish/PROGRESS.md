# PROGRESS.md — Rihaish

**Current build-step:** 23 — (next per ARCHITECTURE build order)
**Last completed:** 22 — platform-billing — DONE (2026-07-12)
**Next:** 23 — (see ARCHITECTURE.md build order)

### Recent steps

- **22 — platform-billing** — DONE (2026-07-12) — `platform.billing` feature (deps platform.console, flats.registry, core.worker), perms `platform.billing.read/.manage` (PLATFORM_ADMIN/SUPPORT), platform-shell nav `billing` (wallet). Migration `20260712250000_platform_billing`: `PricingProfile` (per-flat|lumpsum, effective-dated, superseded-never-edited), `PlatformInvoice` (flatCount+rate SNAPSHOT, idempotent @@unique(societyId,period), "RIH-YYYY-MM-####"), `PlatformPayment` (MANUAL v1 + STRIPE seam, reuses `PaymentStatus`), `PlatformBillingSettings` (singleton dunning knobs), `PlatformInvoiceSequence` (gapless). **Completely separate tables/module/UI from society billing.** `lib/platform-billing`: pure `math` (per-flat vs lumpsum, bp-discount floored in minor units, effective-profile selection, MRR normalise, invoice status, grace, reminder-day) + `numbering`, `adapter` (PaymentProviderAdapter seam — MANUAL only, mock-Stripe test proves no-schema-change), `service` (settings, pricing set/supersede, monthly run snapshotting flat count via unscoped `platformDb()`, record payment→adapter capture→reconcile→restore, bounce/void reversal, grace-check dunning→READ_ONLY + reminders + restore, manual extension), `dashboard` (MRR/ARR/collection/overdue/revenue-by-plan), `invoice-pdf`, `http`. Worker: real `platform.grace-check` (daily) + new `platform.billing-run` (monthly `0 3 1 * *`), both platform-scoped. Notifications: new `platform_billing` category + 5 templates (en+ur), reaches SOCIETY_ADMIN + nominated recipients. API: `/api/platform/billing/{dashboard,invoices,invoices/[id],invoices/[id]/pdf,payments,payments/[id]/reverse,settings,run}` + `/societies/[id]/{pricing,extension}`. UI: platform billing page (tiles + revenue-by-plan bar chart + invoices table with filters/PDF/record-payment/run) + per-society PricingPanel in society detail. Read-only = writes 423, reads work, residents keep dues. i18n en+ur parity. Tests: `math.test`/`numbering.test`/`adapter.test` (pure), DB-gated `run.integration.test` (snapshot immutability, idempotent, grace→read-only→restore, bounce, extension, table separation), e2e `platform-billing.spec` (apex 401 / society-host 404 / page no-500). Updated policy/templates/registry tests for the new category + cron.

- **21 — society-onboarding-wizard** — DONE (2026-07-12) — `platform.onboarding` feature, perms `platform.onboarding.read/.manage`, nav `setup`. Migration `SocietyOnboarding` (resumable draft over an already-ACTIVE tenant; launch = completion + opening-balance LOCK). Full-screen wizard, 10 steps, i18n en+ur. Unit + DB-gated + e2e.

- **20 — resident-finance** — DONE (2026-07-12) — `billing.resident_portal` feature, nav `finance` (feature-gated NO permission), resident-scoped read/submit over 18–19. `assertHoldsFlat`→403; branded invoice PDF. 7 hard-scoped `/api/me/*` routes. Simple+Pro. Unit + integration + e2e.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
