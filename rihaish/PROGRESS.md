# PROGRESS.md — Rihaish

**Current build-step:** 21 — society-onboarding-wizard
**Last completed:** 20 — resident-finance — DONE (2026-07-12)
**Next:** 21 — society-onboarding-wizard

### Recent steps

- **20 — resident-finance** — DONE (2026-07-12) — `billing.resident_portal` feature (deps billing.payments), nav `finance` (wallet icon, main group, feature-gated NO permission — residents hold none, hideOnSharedDevice). **No migration** — a resident-scoped read/submit layer over steps 18–19. `me-actor.ts`: `requireMeFinanceActor` (auth + feature `isEnabled`, NO RBAC; occupancy is the authz) reusing `runReadScope`/`runWriteScope`; carries activeFlatId + societyReadOnly. `me-finance.ts`: **THE gate `assertHoldsFlat` → 403** on every flat-scoped read (a resident can never reach another flat, even by id); `getFinanceSummary` (per-flat headline + combined total, balance = `balanceOf` = step-18 `balance(flatId)` exactly), `getMyStatement` (running-balance ledger + invoices-with-lines + receipts + aging + **pending/rejected submissions** for status transparency, date-range filter), `assertHoldsInvoice`/`assertHoldsPayment`, `submitMyPayment` = step-19 `submitPayment` (off-ledger PENDING). `invoice-pdf.ts`: on-demand branded Urdu-capable invoice PDF (pdf-lib, no worker). API (7, all hard-scoped): `/api/me/finance/summary`, `/api/me/invoices`(?flatId), `/api/me/invoices/[id]/pdf`, `/api/me/ledger`(?flatId&from&to), `/api/me/ledger/export`(pdf/xlsx/csv via generic export engine), `/api/me/receipts/[id]/pdf`(signed URL or 202), `/api/me/payments`(POST submit+proof, runWriteScope→423 READ_ONLY). UI via `useUiModule("finance")` (audience resident→SIMPLE default): Simple = colour-coded balance card + Pay/Upload (FileUpload slip, camera) + plain-language timeline + receipts; Pro = aging tiles + date filter + export + running-balance statement table + invoices/receipts. Multi-flat in-page switcher (posts `/api/me/active-flat`) + combined card. READ_ONLY banner disables submit. Utilities (29)/amenities (35) render as separate labelled sections — never mixed into maintenance balance. i18n en+ur parity (finance ns + nav.finance). Unit `invoice-pdf.test.ts` (valid PDF en/ur/cancelled). Integration `me-finance.integration.test.ts` DB-gated (cross-flat 403, headline == balance(flatId), submit off-ledger reaches queue + visible pending). e2e `resident-finance.spec` (7 unauth 401s + page no-500). **Deferred:** owner-may-see-occupant-invoices society toggle (default yes implemented); utilities/amenities data (steps 29/35).

- **19 — payments-receipts** — DONE (2026-07-12) — `billing.payments` feature + perm `billing.payment.read`/`.record`, nav `payments`. Migration `20260712230000_payments_receipts`: `Payment`/`Receipt` + infra `PaymentSettings`/`ReceiptSequence`. **Money is the ledger — a Payment row has no money**: CLEARED writes `LedgerEntry(PAYMENT,CREDIT)`; bounce/void writes matching `REVERSAL,DEBIT`. Pure `allocation`/`money-words`. `payments.ts`: recordPayment/submitPayment/verifyPayment (FOR UPDATE row-lock)/bounce/void. Worker `runReceiptPdf`. API (8), UI Simple+Pro. Gates green: 489 unit tests, build. Integration + e2e.

- **18 — ledger-invoicing** — DONE (2026-07-12) — `billing.ledger` feature + perms `billing.invoice.read/create`, nav `billing`, RBAC wired. Migration `20260712220000_ledger_invoicing`: `Invoice`(+lines snapshot)/`InvoiceLine`/`LedgerEntry` (scoped) + infra `InvoiceSequence`/`BillingSettings`. **Money is the ledger — no `paidAmount`**; `balance = Σdebit−Σcredit`. Pure logic: `balance`/`numbering`/`aging`/`late-fee`. `commitRun`, `cancelInvoice` (REVERSAL, keeps number). Worker `runBillingMonthly`+`runBillingLateFee`. API (9), UI Simple+Pro. Gates: 473 unit tests, build. DB-gated `run.integration`, e2e.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
