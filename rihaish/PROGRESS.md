# PROGRESS.md — Rihaish

**Current build-step:** 09 — user-account
**Last completed:** 08 — platform-console — DONE (2026-07-12)
**Next:** 09 — user-account

### Recent steps

- **08 — platform-console** — DONE (2026-07-12) — `platform.console` registered (platform-only, `isCore:false`, deps core.entitlements/tables/shell). Schema: `Session.impersonationWrite`/`impersonationReason` (+migration). `lib/platform/*` (all in the `db.unscoped()` allow-list): `guard.requirePlatform` (apex + platform-user + permission), cross-society `runPlatformList` (reuses `buildListQuery`), `societies` (list+counts/last-activity, create+role-seed+plan+limits, update; limits on the `core.tenancy` limits JSON), `detail` (feature catalogue with DAG deps/dependents + impersonation targets), `plans` (CRUD+societyCount), `audit` (slug-enriched cross-society viewer), `dashboard`. **Impersonation (security-critical):** pure `assertCanImpersonate` (apex-only, platform-only, no-nesting, society-target-only, never another platform user, never self) + `startImpersonation` (30-min read-only session stamped `impersonatedBy`, enter audit), `enableImpersonationWrite` (reason-required, separate audit), `exitImpersonation` (exit audit + destroy); `impersonationReadOnly(ctx)` read-only-by-default; cookie widened to parent domain to reach the society host. API: societies GET/POST + `[id]` GET/PATCH, entitlements PUT (preview/enable/disable+cascade/setPlan/setLimits), `[id]/impersonate` POST, plans GET/POST + `[id]` PATCH, audit GET; neutral `/api/impersonate/exit`+`/write` (banner lives on the society host where `/api/platform/*` is 404). UI on the table+form kits: platform shell + sign-in gate, dashboard (tiles+charts), societies table + New-Society modal, society detail (profile/plan/limits/entitlement DAG switches/impersonate), plans table + modal, audit table; society `ImpersonationBanner` now a persistent client control (Enable-write reason modal + Exit). i18n en+ur. Gates green: typecheck, lint(0), 207 unit tests, build; runtime smoke — sign-in gate 200 (EN+UR RTL), `/platform` 404 on society host, platform API 401 unauth / 404 on society host, impersonate exit 200 / write 401.
- **07 — data-table-export-kit** — DONE (2026-07-12) — `core.tables` registered (isCore, deps core.forms). Server contract `lib/tables/*`: `buildListQuery`/`runList` (society-scoped by construction, pageSize cap 100), in-memory parity path, export engine (CSV RFC-4180 + formula-guard, exceljs RTL, pdf-lib Urdu, inline≤1000/queued>1000, PII→`tables.export.pii`+audit). `<DataTable>` (TanStack v8): search/filters/column-vis/density/list⇄card/selection+bulk/saved-views/row-actions/sticky+pinned/states/RTL. Chart primitives (Recharts) + a11y fallback + PNG/PDF. Dev `/design-system/tables`. All gates green (197 unit tests).
- **06 — form-input-kit** — DONE (2026-07-12) — `core.forms` registered (isCore, deps core.shell). Pure layer `lib/forms/*` (phone→E.164, CNIC mask, token mask, money bigint) + shared Zod (`schemas/*`); 9 shadcn primitives; form kit `components/form/*` (unified `<FieldShell>`, `<Form>`+unsaved guard, Text/Number/Money/Masked/Phone, `<Select>`, virtualised `<Combobox>`+`<MultiSelect>`, Date/Range/Time, Checkbox/Radio/Switch, `<FileDropzone>`, `<Modal>`/`<Drawer>`/`<ConfirmDialog>`, `<FormActions>`). Dev `/design-system`. ESLint bans alert/confirm/prompt. All gates green (164 unit tests).

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
