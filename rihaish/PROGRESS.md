# PROGRESS.md — Rihaish

**Current build-step:** 08 — platform-console
**Last completed:** 07 — data-table-export-kit — DONE (2026-07-12)
**Next:** 08 — platform-console

### Recent steps

- **07 — data-table-export-kit** — DONE (2026-07-12) — `core.tables` registered (isCore, deps core.forms). Server contract `lib/tables/*`: `buildListQuery` (Prisma where/orderBy/skip/take; pageSize hard-cap 100; filters text/number/dateRange/enum/boolean; global search OR; sort/filter whitelisted by column config; never emits societyId/deletedAt — the enforced `db` injects them so every list is society-scoped by construction), `runList` (throws TenantScopeError with no scope), `queryInMemory`/`filterSortInMemory` (parity path for demo+export). Export engine `lib/tables/export/*`: CSV (BOM + RFC-4180 + formula-injection guard), Excel (exceljs, RTL sheet), branded PDF (pdf-lib + embedded Noto Naskh for Urdu, page numbers, RTL), inline≤1000/queued>1000 seam, PII detect → permission `tables.export.pii` + `AuditLog` with sensitive column list (`buildInlineExport`). `<DataTable>` (TanStack v8) `components/table/*`: debounced search, per-column filters, column visibility, density, list⇄card (card default mobile), row selection+bulk, saved views (localStorage adapter, server seam), row actions, sticky header + pinned first column, skeleton/empty/error, RTL. Chart primitives `components/charts/*` (Recharts): line/bar/stacked/area/donut/sparkline, animated, tokenised `--chart-1..5`, a11y data-table fallback, PNG/PDF export. Dev-only `/design-system/tables` + API (6,000 in-mem flats) — runtime-verified: cap→100, filtered CSV==on-screen total (1000), >1000→202 queued, CNIC no-perm→403, Urdu PDF/xlsx generated, EN+UR(RTL) render 200. Fix: `tailwind.config.ts` `require()`→ESM import (dev-server was broken for all pages under ESM loader). All gates green (197 unit tests, lint, build).
- **06 — form-input-kit** — DONE (2026-07-12) — `core.forms` registered (isCore, deps core.shell). Pure layer `lib/forms/*` (phone→E.164 PK-default, CNIC `#####-#######-#` paste-format, generic token mask, money-input bigint-only) + shared Zod `schemas/common.ts`/`schemas/demo.ts` (one schema client+server; phone/cnic/money transforms) all unit-tested. 9 shadcn primitives (label, input, textarea, select, popover, checkbox, radio-group, switch, calendar-rdp9). Form kit `components/form/*`: unified `<FieldShell>` anatomy, `<Form>`+unsaved-changes guard, Text/TextArea/Number/Money/Masked/Phone fields, `<Select>`, virtualised searchable `<Combobox>` (500+, debounce 250ms) + `<MultiSelect>` (chips/select-all), Date/DateRange/Time pickers (Intl Urdu+RTL), Checkbox/Radio/Switch, `<FileDropzone>` (camera), `<Modal>`/`<Drawer>`/`<ConfirmDialog>` (no double-submit), `<FormActions>`. Dev-only `/design-system` route (Simple/Pro, EN+UR RTL smoke-tested 200) + demo API on shared schema. ESLint bans alert/confirm/prompt. All gates green (164 unit tests, lint, build).
- **05 — app-shell** — DONE (2026-07-11) — `core.shell` registered (isCore, deps core.auth+core.entitlements). Pure nav registry `lib/nav.ts` (role×entitlement gate `visibleNav`/`groupNav`, guard shared-device hide) unit-tested; 9 shadcn/ui primitives (dropdown-menu, tooltip, dialog, sheet, command/cmdk, skeleton, sonner, scroll-area, separator) RTL-logical; society app shell `app/[locale]/app/layout.tsx` (host→society or 404, `buildShellData` assembles identity+entitlements+gated nav, cookie sidebar state) → client frame `AppShellClient` (Radix DirectionProvider, collapsible sidebar+tooltips, top bar 3 toggles+search+flat-switcher+bell+user-menu, ⌘K palette, mobile drawer, states PageSkeleton/EmptyState/ErrorState/Forbidden, read-only + impersonation banners, sonner Toaster); loading/error boundaries; `lib/server-auth.ts` RSC identity. All gates green (125 unit tests).

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
