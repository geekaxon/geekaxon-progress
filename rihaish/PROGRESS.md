# PROGRESS.md — Rihaish

**Current build-step:** 12 — worker-scheduler
**Last completed:** 11 — notifications-core — DONE (2026-07-12)
**Next:** 12 — worker-scheduler

### Recent steps

- **11 — notifications-core** — DONE (2026-07-12) — `core.notifications` registered (isCore, deps core.account/core.storage) + sub-features `integrations.email`/`integrations.sms`/`integrations.whatsapp` and the deferred `auth.otp` (deps integrations.sms — DAG now satisfied). Migration `20260712150000_notifications_core`: `SocietyIntegration` (AES-256-GCM `configEnc`, `isActive` only after test), `NotificationTemplate` (societyId null = platform default), `Notification` (per user×channel delivery ledger), `NotificationRecipientRule` + enums `IntegrationKind`/`Channel`/`NotifStatus`. All four are pass-through models → tenant isolation enforced EXPLICITLY (every query scopes `societyId`). `lib/notifications/*`: pure `channels` (pref ∩ society ∩ template, emergency floors in-app+push), `templates` (8-code EN+UR catalogue, one per category, `{{var}}` renderer), `config` (per-provider Zod + encrypt/decrypt + `redactStatus` — secrets never leave the server), `adapters` (in-app/email/sms/whatsapp/push behind injectable transports; Twilio/pk-sms-gw/wa-cloud request builders), `integrations` (configure→encrypt, `testIntegration` flips active only on live-send ok, `availableChannels`), `templates-db` (resolve/override/reset, in-code fallback for unseeded DB), `audience` (users/roles/all/residents/recipient-rule; block/flat = step 15-16 stubs), `service` (`notify` = resolve→render→enqueue rows, in-app DELIVERED, external QUEUED, no-address SUPPRESSED, NEVER sends inline; `notifySafe`; notification-centre list/unread/markRead), `dispatch` (worker drain, 3-attempt backoff→FAILED, +delivery log), `recipient-rules`. API `/api/notifications` (+`[id]/read`, `/read-all`), `/api/society/integrations`(+`[kind]` PUT/DELETE, `[kind]/test`), `/api/society/templates`, `/api/society/recipient-rules`. UI: live notification bell + centre (badge/list/mark-read, degrades to empty on 401), `/app/settings` Simple/Pro (integration cards+status chip+Send test, template editor w/ variable chips+EN/UR preview, recipient rules). i18n en+ur. Gates green: typecheck, lint(0), 294 unit tests (+29: channels/config/templates/adapters), build (all new routes). e2e `notifications.spec` (unauth 401s). Dispatcher + real SMTP/push transports wired by worker (12)/push (23).
- **10 — storage-media** — DONE (2026-07-12) — `core.storage` registered (isCore, deps core.tenancy/core.forms). Schema (migration `20260712140000_storage_media`): `StoredFile` (auto tenant-scoped: has `societyId`+`deletedAt` → isolation + soft-delete free), `Visibility` enum, `Society.maxStorageMb`. `lib/storage/*`: pure `mime` (magic-byte sniff + executable reject), `keys` (scoped key mint/thumb/safe-key), `signing` (HMAC blob tokens + upload tickets), `limits`/`retention` (quota + purge policy), `s3-sigv4` (AWS-vector-verified presigner), adapters `local`/`s3`(R2)/`gdrive` behind one `StorageAdapter` (switch = one env var) via `factory`, `image` (sharp: EXIF/GPS strip, auto-orient, WebP + 320px thumb, HEIC decode), `service` (sniff→process→quota→dedup→store, signed-URL access, soft-delete, per-society retention sweep). API `/api/files/sign` (ticket), `/api/files` (multipart upload), `/api/files/[id]` GET+DELETE, `/api/files/blob` (only byte path; unsigned/guessed/expired→403). Client `<FileUpload>` (progress/preview/camera/toast) + dev `/design-system/files`. sharp@0.35.3 added (native arm64), `serverExternalPackages`. Account avatar-delete TODO now soft-deletes the object. i18n en+ur. Gates green: typecheck, lint(0), 265 unit tests (+43: adapter contract local≡s3, sigv4 vector, sniff/keys/signing/limits/retention/image), build (all `/api/files/*`). e2e `files.spec` (unauth 401s + blob 403/traversal, skips unseeded).
- **09 — user-account** — DONE (2026-07-12) — `core.account` registered (isCore, deps core.auth/core.forms). Migration `20260712130000_user_account`: `UserProfile`, `NotificationPreference` (keyed by `userId`), `Society.cnicCapture`, `Session.ip`/`userAgent`. `lib/crypto.ts` AES-256-GCM at-rest + `maskCnic`. `lib/account/*` (notifications-policy, service, actor). API `/api/me` + `/password`/`/pin`/`/sessions`/`/notification-preferences`. UI `/app/account` (Simple/Pro): masked-CNIC profile w/ re-auth, security+sessions, locked notification matrix, prefs. i18n en+ur. Gates green (222 unit tests); e2e `account.spec`.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
