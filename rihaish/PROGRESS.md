# PROGRESS.md — Rihaish

**Current build-step:** 18 — ledger-invoicing
**Last completed:** 17 — charge-engine — DONE (2026-07-12)
**Next:** 18 — ledger-invoicing

### Recent steps

- **17 — charge-engine** — DONE (2026-07-12) — `billing.charges` feature (entitled, deps flats.registry/residents.registry) + perms `billing.charges.read/manage` (ACCOUNTANT read+manage; MANAGER/COMMITTEE read), nav `charges` (coins icon), ui-module `charges` (committee→PRO). Migration `20260712210000_charge_engine`: enums `ChargeKind{RECURRING,ONE_OFF,METERED}`/`TargetType`/`SplitMode`/`SpecialChargeStatus`; `ChargeHead`/`RateRule`/`SpecialCharge` (all societyId+deletedAt scoped; amounts BigInt minor). Pure logic (unit-tested, 29 tests): `resolve` (specificity score flatId>cat+occ>cat>occ>block>default, effective-dating by PERIOD START, tie-break latest effectiveFrom→createdAt→id, no-match→warning never silent-zero, METERED→warning), `split` (SPLIT_EQUALLY remainder → first N flats take extra minor unit; always reconciles exactly — followed the stated RULE, not the spec's looser example arithmetic), `period` (YYYY-MM→UTC month start). `service.ts`: head CRUD, `createRateRule` (auto-supersedes the open same-signature rule: sets old effectiveTo+supersededBy, NEVER edits its amount — reproducible history), rule history, special-charge CRUD (APPLIED=immutable 409), `resolveTargetFlats` (ordered live flats — soft-deleted skipped; incl. non-billable so a construction levy can target under-construction flats), `simulate` (same pure resolve the invoice run will call → outputs match), `getOverview`. Money crosses wire as strings (no BigInt in JSON). API: `/api/charges`(GET overview), `/api/charges/simulate`(POST), `/api/charge-heads`(CRUD), `/api/rate-rules`(GET list/history+POST supersede+DELETE), `/api/special-charges`(GET list/all+POST+PATCH+DELETE cancel), `/api/special-charges/preview`(POST live count). UI via `useUiModule("charges")`: Simple maintenance-charges screen (per-occupancy amount editors that write occupancy rules) + one-time wizard; Pro rate-rule DataTable (specificity/history/current-vs-superseded, delete) + rate simulator (lines+source+warnings+total) + special-charge builder w/ live target preview ("bills N flats, total ₨X") + charge-head manager. i18n en+ur parity (charges ns, 110 keys + nav). Gates green: typecheck, lint(0), 437 unit tests (+29), build (6 new routes + page en/ur). e2e `charge-engine.spec` (12 unauth 401s + page no-500).

- **16 — residents-occupancy** — DONE (2026-07-12) — `residents.registry` (isCore, deps flats.registry/core.auth/core.notifications) + perms `society.residents.read/manage`, nav `residents`. Migration `20260712200000_residents_occupancy`: enum `Relation{OWNER,OCCUPANT}`; `FlatOccupancy` (scoped; `endDate=null`=current; history preserved — a transfer closes the old row + opens a new one), `AuthorizedPickupPerson` (scoped; CNIC enc/mask), `ResidentInvite` (single-use token, no deletedAt). Pure logic: `occupancy` (derive status + plan transfer), `invite-token` (7d TTL, single-use), `csv` (header-validated, atomic). `service.ts`: invite/resend/accept(public), transfer/update/suspend, directory/summary/list/export, `getFlatPanel`, pickup CRUD, `importResidents`. API: `/api/residents`(+invite), `/api/residents/[id]`, `/invite/[token]/accept`(PUBLIC)+`/import`+`/export`, `/api/flats/[id]/occupancy/transfer`+`/pickup-persons`. UI via `useUiModule("residents")`: Simple card directory + invite wizard; Pro DataTable + shared `FlatPanelDrawer`. i18n en+ur (residents ns, 121 keys). Gates green: typecheck, lint(0), 408 unit tests, build. e2e `residents-occupancy.spec`.

- **15 — society-structure** — DONE (2026-07-12) — `flats.registry` registered (isCore, deps core.tenancy/core.tables/core.forms) + perms `society.structure.read/manage`, nav `structure`, ui-module `structure` (committee→PRO). Migration `20260712190000_society_structure`: enums `OccupancyStatus/BillTo/ParkingMode/VehicleType`; `Block/Floor/FlatCategory/Flat/ParkingSlot/Vehicle` (EVERY row carries societyId+deletedAt → scoping auto-pins/soft-deletes; uniqueness excludes deletedAt so a merged flat's `displayCode` stays reserved) + `SocietySettings` (PK=societyId, infra pass-through). Pure logic (unit-tested): `flat-code` (`{block}/{number}/{floor}` template → society-unique displayCode), `bulk` (atomic plan, dup/`maxFlats` guard, floorPrefixed/sequential numbering), `csv` (RFC-4180 parse + row-level validation, no partial import). `service.ts`: scoped CRUD (tenant-scoping layer stamps societyId → `tenantCreate` helper), `getGridData` (Simple lattice), `listFlats/Parking/VehiclesQuery` (data-table kit), `listFlatsForExport`, `getCsvContext`, `importFlats` (one txn). API: `/api/structure`(GET overview+PATCH settings), `/api/blocks|floors|flat-categories|parking-slots|vehicles`(CRUD), `/api/flats`(list/detail/CRUD), `/api/flats/bulk|import|export` (export = first real `buildInlineExport` caller). UI via `useUiModule("structure")`: Simple block→floor→flat grid colour-coded by occupancy + drawer; Pro flat DataTable (filters/CSV import+export) + Parking/Vehicles tabs (parking hidden unless mode=ASSIGNED); shared flat drawer (edit + vehicles add/del + parking) + bulk "Set up my building" + CSV import (dry-run preview) + setup modal (blocks/floors/categories/settings). Fixed schema null-acceptance for optional fields (client sends `||null`). i18n en+ur (structure ns, 38 keys + nav). Gates green: typecheck, lint(0), 383 unit tests, build (11 new routes + page en/ur). e2e `society-structure.spec` (13 unauth 401s + page no-500).

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
