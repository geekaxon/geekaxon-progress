# PROGRESS.md — Rihaish

**Current build-step:** 44 — seasonal-events-qurbani — DONE (2026-07-13)
**Last completed:** 44 — seasonal-events-qurbani — DONE (2026-07-13)
**Next:** 45 — (see ARCHITECTURE.md build order)

### Recent steps

- **44 — seasonal-events-qurbani** — DONE (2026-07-13) — feature `seasonal.core` (+ sub-features `seasonal.charges`/`seasonal.spaces`/`seasonal.vendors`; deps `residents.registry`/`core.notifications`/`core.storage`; `billing.ledger` + `staff.directory` are RUNTIME `isEnabled` checks, NOT DAG edges → graceful degradation). Generic `SeasonalEvent` machinery, Qurbani first-class. FEES ARE SNAPSHOTTED PER LINE at registration (`rules.buildPermitLines`) — a 2027 permit is forever priced at 2027's fee even after next year's fee changes (proven by pure + DB tests). Approved permit → ONE itemised ledger invoice under a `qurbani` head (one `InvoiceLine` per animal type + one DEBIT), flows through the normal payment/receipt/arrears machinery; FREE + un-invoiced when `billing.ledger`/`seasonal.charges` off (`totalFeeMinor` 0). SPACE DOUBLE-ALLOCATION IMPOSSIBLE — per-space `pg_advisory_xact_lock(hashtext(society:seasonal-space:id))` + in-tx re-check (`assertAreaFree`-equiv via `canAllocateSpace`) → two permits on one spot serialize, one wins (`space_taken` 409). AUDITABLE BALLOT — deterministic `xmur3`→`mulberry32` seeded Fisher-Yates (`seededShuffle`/`runBallot`, no Date/Math.random), reproducible from recorded seed; append-only `SeasonalBallot` retains seed+result+entrants, a re-run DEMANDS a reason (`reason_required`) and BOTH runs kept. Gapless per-event numbering `RUFI-QRB-2027-0042` (`SeasonalPermitSequence` atomic upsert keyed societyId+eventId). MOON-SIGHTING SHIFT: `shiftEventToEid` slides arrival window + departure deadline by the Eid delta (registration stamps untouched), committee does nothing, residents notified. GUARD BOARD: per-flat APPROVED animal counts + arrival window (unpermitted animal stopped at gate). "Copy last year" clones types+spaces with fees editable (`copyEvent`). Migration `20260713460000_seasonal_events_qurbani`: enums `SeasonalKind`/`SeasonalStatus`/`SeasonalPermitStatus`(renamed — `PermitStatus` taken by step 42)/`SeasonalSpaceMode`/`SeasonalVendorKind`/`SeasonalTaskStatus` + `SeasonalEvent`/`SeasonalPermitType`/`SeasonalPermit`/`SeasonalSpace`/`SeasonalVendor`/`SeasonalTask` (auto-scoped soft-delete) + `SeasonalPermitLine`/`SeasonalVendorRating`/`SeasonalBallot`/`SeasonalPermitSequence` (pass-through — societyId, NO deletedAt; always keyed off a society-unique parent id eventId/permitId/vendorId, no cross-tenant leak). `lib/seasonal/` constants/rules(+rules.test 9 groups: snapshot/ballot-reproducible/space-guard/window/status/date-shift/rating)/actor/http/service/seasonal.integration.test(DB-backed, 4: snapshot-holds-across-fee-change + itemised debit, space concurrency exactly-one-wins, ballot seed recorded + re-run needs reason, free-when-billing-off + guard board). `schemas/seasonal.ts`. API `/api/seasonal/events`(GET/POST)+`/[id]`(PATCH)+`/copy`+`/[id]/shift-date`+`/[id]/permit-types`(GET/POST)+`/[id]/permits`(GET desk/POST register)+`/[id]/spaces`(GET/POST)+`/[id]/ballot`(GET history/POST run)+`/[id]/guard`+`/[id]/board`, `/api/seasonal/permit-types/[id]`(PATCH/DELETE), `/api/seasonal/permits/[id]`(GET)+`/{approve,reject,cancel,complete}`, `/api/seasonal/spaces/[id]/allocate`, `/api/seasonal/vendors`(GET/POST)+`/[id]`(PATCH)+`/[id]/rate`, `/api/seasonal/tasks`(GET/POST)+`/[id]`(PATCH), `/api/me/seasonal`. Perms `seasonal.read`(COMMITTEE_MEMBER+MANAGER+GUARD — guard needs the animal board)/`seasonal.manage`(MANAGER, admin `*`)→seasonal.core; a resident's OWN register/cancel/rate needs NO perm. Nav `seasonal` (icon `beef` — added to nav-icons, main group, NOT hidden on shared device), ui-mode `seasonal` (resident→SIMPLE). UI Simple=resident "Register your Qurbani" fee-shown animal tiles + quantity steppers + live total + rules-accept + status cards + butcher list w/ star ratings; committee approvals inbox + live board + guard board. Pro=tabbed permits/spaces/vendors/tasks/ballot/types/events tables + event wizard ("copy last year") + ballot runner + confirm-Eid shift. Notif templates (category `announcements`, en+ur) `seasonal.permit.{submitted,approved,rejected}`/`seasonal.space.allocated`/`seasonal.date.shifted`. i18n en+ur `seasonal.*` (189 keys) + `nav.seasonal`, parity 0-diff. lint+typecheck clean; ballot determinism sanity-checked standalone. NOTE (recorded): spec sub-feature `seasonal.permits` folded into `seasonal.core` (registration IS the core function, not a separable toggle); `eventDate` stored (not live-derived) — step 45 Hijri calendar will drive `shiftEventDate` when built; vendor photo upload wired via `photoFileId` field only (uploader is a future enhancement); ballot allocates only currently-free BALLOT-mode spaces.

- **43 — utility-schedules-notices** — DONE (2026-07-13) — feature `utility.schedules` (+ sub-features `utility.outages`, `utility.external_notices`; deps `residents.registry`/`core.notifications`/`core.storage`/`core.worker`). Tracks TIME (when power goes, gas comes, water arrives), NOT bills — INDEPENDENT of step 29 but REUSES `UtilityProvider`. WALL-CLOCK IS THE HARD RULE: slot `startTime`/`endTime` are society-local "HH:mm" strings, never built into a `Date`; the pure rules compare minutes-of-day against `zonedParts(now, tz)`. NEVER touches the ledger (architecture.test). Reminders via worker `schedules.reminders`. Migration `20260713450000_utility_schedules_notices`. lint+typecheck clean.

- **42 — permission-requests** — DONE (2026-07-13) — feature `permits.core` (+ sub-features `permits.charges`, `permits.fasttrack`). THE FUNERAL FAST-TRACK: auto-approve, never a fee/deposit (rejected at data layer), works under READ_ONLY, notifies committee+guard+(opt-in) society via `emergency`. Common-area conflict impossible (per-area advisory lock + overlap check). Fee/deposit SNAPSHOT at approval → ledger. Renovation → STAFF_RECURRING gate passes. Migration `20260713440000_permit_requests`. lint+typecheck clean.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
