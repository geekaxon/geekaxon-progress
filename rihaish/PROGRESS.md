# PROGRESS.md — Rihaish

**Current build-step:** 15 — society-structure
**Last completed:** 14 — ui-modes — DONE (2026-07-12)
**Next:** 15 — society-structure

### Recent steps

- **14 — ui-modes** — DONE (2026-07-12) — `ui.modes` registered (entitled, deps core.shell/core.account). Migration `20260712180000_ui_modes`: enum `UiMode {SIMPLE,PRO}`, `SocietyModuleUiPolicy` (@@unique[societyId,module]; infra, societyId + no deletedAt → scoping passes through, service scopes explicitly) + `UserModuleUiPref` (@@unique[userId,module]; retained across entitlement loss, never deleted). `lib/ui-modes/*`: `modules` (forward-declared registry of 12 mode-supporting modules w/ audience→built-in default: resident=SIMPLE, committee=PRO; `dashboard` = reference), pure `resolve` (`resolveMode` exact order pref→policy-default→built-in, `canUserChoose`=entitled∧override), `service` (getPolicyMap/getInterfaceConfig/setPolicy+audit, getUserPrefMap/setUserPref w/ defense-in-depth gate throwing `not_allowed`, resolveUserModes/resolveModeMap). Shell: `ShellData.uiModes` map (buildShellData→resolveModeMap for belonging user), client `UiModeProvider`/`useUiModule`(registers active module)/`useUiModes`/`useActiveMode`; top-bar `ModeToggle` self-hides unless active module `canChoose`, switches instant client-side + background PATCH persist (revert+toast on fail). API `/api/me/ui-modes` (GET resolved modules, PATCH set pref), `/api/society/ui-modes` (GET config+entitled, PUT policy — settings.read/update). UI: settings **Interface** panel (per-module default segmented + let-users-choose switch disabled w/ note when not entitled, side-by-side preview), account **Interface** prefs panel (only override-allowed modules, synced via shared context), reference **dashboard** rebuilt as Simple (big guided cards) / Pro (dense metric grid) via `useUiModule`. i18n en+ur (uiModes + dashboard metrics). Gates green: typecheck, lint(0), 356 unit tests (+12: resolve order/entitlement-off/canChoose/registry), build (both new routes). e2e `ui-modes.spec` (unauth 401s, dashboard no-500).

- **13 — branding-domains** — DONE (2026-07-12) — `branding.core` registered (isCore, deps core.tenancy/core.storage/core.shell) + `branding.custom_domain`/`branding.pwa`. Migration `20260712170000_branding_domains`: `SocietyBranding` (PK=societyId; infra, no deletedAt → scoping passes through) + `SocietyDomain` verify columns. `lib/branding/*`: pure contrast (WCAG AA auto-correct), tokens (`brandCss` injects only `--primary`/-foreground/--ring scoped :root+.dark), domains (subdomain + custom-host normalize, TXT challenge, injectable resolver), service (upsert w/ asset-ownership check, cached brand-CSS, domain bind/verify + sweep). Worker `domains.verify` cron. API `/api/society/branding`, `/api/society/domains`(+`[id]`). `GET /manifest.webmanifest` per host. Layout injects scoped brand `<style>` + **Powered by Rihaish** footer. Gates green: typecheck, lint(0), 344 unit tests, build. e2e `branding-domains.spec`.
- **12 — worker-scheduler** — DONE (2026-07-12) — `core.worker` registered (isCore, deps core.notifications). Migration `20260712160000_worker_scheduler`: `Job` (Postgres queue, `dedupeKey` idempotency), `CronSchedule` (per-society tz), `WorkerHeartbeat`. `lib/worker/*`: pure cron (tz-aware), backoff (→DEAD), registry (9 crons), queue (`FOR UPDATE SKIP LOCKED`), scheduler, runtime (concurrency pool, `withSociety`, READ_ONLY skip, graceful drain). Worker TS via `tsx`; `/api/health` DB-heartbeat. API `/api/platform/jobs`(+retry/kill), `/api/society/automation`(+run-now). Gates green: typecheck, lint(0), 314 unit tests, build, worker boots+SIGTERM-drains. e2e `worker-scheduler.spec`.

> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
