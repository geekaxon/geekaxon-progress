# PROGRESS.md — Rihaish

**Current build-step:** 34 — surveys — DONE (2026-07-13)
**Last completed:** 34 — surveys — DONE (2026-07-13)
**Next:** 35 — (see ARCHITECTURE.md build order)

### Recent steps

- **34 — surveys** — DONE (2026-07-13) — feature `surveys.core` (deps residents.registry/core.notifications). OPINION collection, NOT a poll: the atomic unit is the RESPONDENT (a user), branching + rating/NPS/free-text allowed, no binding winner. ANONYMITY IS REAL yet drafts resume + turnout is known — reconciled by `SurveyParticipation` (pass-through infra, `@@id(surveyId,userId)`, societyId no deletedAt): records WHO opened/submitted (turnout, one-response, cross-device resume) WITHOUT linking to ANSWERS — on submit of an anonymous survey `draftResponseId` is SEVERED so the `SurveyResponse` (respondentUserId NULL) is unrecoverable (mirrors VoteTurnout). One-response enforced via participation.hasSubmitted honouring `allowMultipleResponses` — DELIBERATELY NOT the spec's `@@unique(surveyId,respondentUserId)` (can't express "only when !allowMultiple" + collapses to NULL for every anon row); decision recorded. Required validation SERVER-side, branching-aware (`validateResponse`: a required question hidden by an unmet condition is exempt). Closing-mid-response → 10-min grace to submit an EXISTING draft (`canSubmitAt`), then discarded. Anonymity-preserving export: anon CSV has NO respondent/flat/submittedAt column AT ALL + rows ordered by SHA-256 hash of responseId (not submission order/time) — proven in vitest. Migration `20260713360000_surveys`: `ALTER TYPE Eligibility ADD VALUE 'ROLE'` (survey-only audience; polls never offer it), SurveyStatus/QuestionType enums, Survey+SurveyQuestion+SurveyResponse+SurveyAnswer (all societyId+deletedAt scoped) + SurveyParticipation (pass-through). `lib/surveys/`: pure `rules.ts` (isQuestionVisible/validateResponse/validateAnswerValue/summarizeQuestion[choice/scale+NPS/text, zero-safe]/npsScore/isUserEligible/buildExport/toCsv/stableOrderKey — vitest rules.test) + constants/actor(SHARED-device block)/http + `service.ts` (create/patch/setQuestions[DRAFT-only replace]/open[notify audience]/close/listSurveys/getSurvey[+resume draftAnswers]/respond[save-draft|submit, anon severs link]/getResults[committee immediate, resident per flag, free-text committee-only, byBlock]/exportResponses[CSV]/listMySurveys/listBlocks/listRoles[from ROLE_SEEDS]). Perms `surveys.read`(COMMITTEE/MANAGER)/`surveys.manage`(MANAGER); residents see+respond with NO perm. API `/api/surveys`(GET/POST), `/[id]`(GET/PATCH), `/[id]/{questions,open,close,respond}`(POST), `/[id]/{results,export}`(GET), `/api/me/surveys`(GET). Notif codes survey.opened/results_published under `announcements` (en+ur IN_APP+PUSH). UI `surveys` page (nav `listChecks`, main, feature-gated, hideOnSharedDevice): Simple resident one-question-per-screen (progress bar, tap-scale RATING/NPS widgets, save-&-finish-later, branching skips hidden Qs, anon note, thank-you) + Pro committee builder (meta + question editor: type/required/options/min-max/branching, reorder up/down as DnD-equivalent) + per-question charts (animated bars/histogram/NPS) + free-text browser w/ search + response-rate-by-block + CSV export + open/close. ui-module `surveys` (resident/SIMPLE). i18n en+ur `surveys.*`+`nav.surveys` (126 leaf keys, parity verified). Integration test proves 5 DB-backed acceptance criteria (anon severed, draft resume, required server-side, branching exempt, one-response, anon export); e2e `surveys.spec` (401 on every endpoint). No cron. lint+typecheck clean.

- **33 — polls-voting** — DONE (2026-07-13) — feature `polls.core` (deps residents.registry/core.notifications). BINDING votes, not a survey. INTEGRITY: one-vote-per-flat is a DB fact (`@@unique([pollId,flatId])`) — `castVote` relies on it, a double-vote (incl. two residents of one flat) fails at the DB → P2002 → `already_voted` 409, proven by a raw-insert integration test. The FLAT is the atomic voting unit for every eligibility mode (design decision); eligibility governs only which flats are eligible + which occupant casts. ANONYMITY is real: anonymous poll stores `voterUserId=NULL` on `Vote` (WHAT), yet `VoteTurnout` (pass-through infra, flat-keyed, no userId) records WHO/turnout — chase non-voters without knowing choices. Every voter gets a verifiable receipt hash (sha256 over pollId|flatId|sortedOptions|voteId-nonce). RESULTS 403 until close for EVERY role incl SOCIETY_ADMIN (`canSeeResults`); quorum-not-met → INCONCLUSIVE, never a winner (`tally`). Eligibility snapshotted at open (`eligibleFlatCount`); server time authoritative for the window (`endsAt` exclusive). Cancel voids result (status CANCELLED) + audit + notify-all; votes immutable, never deleted (`deletedAt` scoping-only, never set). Migration `20260713350000_polls_voting`: PollKind/Eligibility/PollStatus enums, Poll+Vote (both scoped), VoteTurnout (pass-through). `lib/polls/`: pure `rules.ts` (isOpenAt/effectiveStatus/canSeeResults/isFlatEligible/voterMayCast/validateBallot/tally/computeReceiptHash/verifyReceipt — 30+ vitest) + constants/actor(SHARED-device block)/http + `service.ts`. Perms `polls.read`(COMMITTEE/MANAGER)/`polls.manage`(MANAGER); residents see+vote with NO perm. API `/api/polls`(GET/POST), `/[id]`(GET/PATCH), `/[id]/{open,close,cancel,vote}`(POST), `/[id]/{results,turnout}`(GET), `/api/me/polls`(GET). Notif codes poll.opened/results_published/cancelled under `announcements` category (en+ur IN_APP+PUSH). UI `polls` page (nav `vote`, main, feature-gated, hideOnSharedDevice): Simple resident ballot (big option cards, "vote is anonymous", confirm "you cannot change this", receipt with hash, animated result bars) + Pro committee builder + live turnout dashboard (never live results) + who-hasn't-voted chase list + open/close/cancel. ui-module `polls` (resident/SIMPLE). i18n en+ur `polls.*`+`nav.polls`. Integration test proves all 5 DB-backed acceptance criteria; e2e `polls-voting.spec` (401 on every endpoint). No cron. lint+typecheck clean.

- **32 — messaging-chat** — DONE (2026-07-13) — feature `chat.core` (deps residents.registry/core.notifications/core.storage/branding.pwa[=pwa.core]). THE two-way channel residents open the app for. ROLE-ADDRESSED threads: a `ThreadParticipant` row with `userId=null`+`roleTarget` addresses "the Committee" — any CURRENT holder replies, thread survives a committee change (access derives from live role membership, pure `canAccessThread`). Resident-to-resident DIRECT/GROUP OFF by default (`ChatSettings`, 403 when disabled, not hidden). SOFT-DELETE STAYS VISIBLE: removal sets `removedAt` (NOT the scoping `deletedAt` which would hide it) → still shows "message removed"; no hard delete anywhere. Guard on SHARED device → no chat access (403). Migration `20260712340000_messaging_chat`: `ThreadKind`/`ThreadStatus` enums, `Thread`+`Message` (both societyId+deletedAt scoped), `ThreadParticipant` (pass-through child, `@@unique(threadId,userId)`), `ChatSettings` singleton. `lib/chat/`: pure `rules.ts` (access/reply, role-target satisfaction, resident-to-resident gate, redactMessage, unreadCount, notifiableUserIds muted-silent, effectiveStatus auto-close — 24 vitest) + constants/actor(SHARED-device block)/http + `service.ts` (threads create/list[resident+committee inbox filters+search]/patch[mute self, close/assign committee], messages cursor-paginated+notifySafe per message respecting mutes incl. role holders, markRead upsert cursor, removeMessage soft, unreadTotal, exportThread). Perms `chat.read`(COMMITTEE/MANAGER/AMENITY_MANAGER)/`chat.manage`(MANAGER) gated by chat.core; residents open/read/send with NO perm. API `/api/threads`(GET/POST), `/[id]`(GET/PATCH), `/[id]/messages`(GET/POST), `/[id]/read`(POST), `/[id]/export`(GET), `/api/messages/[id]`(DELETE soft), `/api/chat/{overview,settings}`. Notif code `chat.message.received` (already in catalogue, en+ur IN_APP+PUSH). UI `chat` page (nav `messageCircle`, main, feature-gated, hideOnSharedDevice): Simple resident WhatsApp-list + "Message the committee" + thread view (composer, attach, remove own, mute) ; Pro committee inbox (filters unread/status/kind, search, assign/close, export, settings drawer). ui-module `chat` (resident/SIMPLE). i18n en+ur `chat.*`+`nav.chat` (parity verified). e2e `messaging-chat.spec` (401 on every endpoint). Bottom-tab unread surfaced via `/api/chat/overview` unreadTotal + per-thread badges (shell tab-badge wiring deferred). No cron. lint+typecheck clean; chat rules 24/24.


> Full history: PROGRESS-HISTORY.md · Build order: ARCHITECTURE.md
