> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **26 — Phase 2: Phlebotomist Field App.** ✅ DONE/APPROVED (2026-07-05) — the phlebotomist's **offline field PWA** over the SAME home-collection bookings (25). It lists **my visits** (assigned to me, route-ordered by assignment sequence then slot) enriched with the patient to call, the tests to draw, the sample barcode + whether proof exists, then drives the doorstep flow — **en route** → **collect** (barcode ties the physical sample to the lab order via `LabService.collectSample`, 27; a prior sample tolerated; then → COLLECTED with patient WhatsApp) → **proof** (patient signature + photo) → collected — and flags a **failed draw** (→ FAILED, office re-opens as a recollection). **Every mutation is OFFLINE-IDEMPOTENT** (10, `SyncService.runIdempotent` keyed on the `Idempotency-Key` header — a replayed action returns the same result and never double-posts; the `collection_proofs.clientActionId` unique index is a second line of defence). It **OWNS only the 1 new table** `collection_proofs`; the visit list + status timeline (HomeCollectionService), the sample tie (LabService) and the patient identity (PatientService) are reached through their services, never mutated. Access is narrowed to the caller's **own assignments** (a visit not assigned to me → 404). NEW migration `…220000` (1 table + `HomeStatus` reuse; `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed + global-unique clientActionId proven in a new `phlebotomist-isolation` pglite spec). **Flag+perm REUSED — no catalog change** (SAME `lab.homeCollection` + `home.collection.manage`; PHLEBOTOMIST/RIDER hold it). Pure `@mp/shared/phlebotomist` (`FIELD_STATUSES`/`isFieldStatus` = EN_ROUTE/FAILED only; `routeSequenceOf`/`compareVisits` route ordering). API `PhlebotomistModule` (**declarative** class `@RequireFeature('lab.homeCollection')`+`@RequirePermission('home.collection.manage')` → FeatureGuard 404 THEN RolesGuard 403): `/phlebotomist/visits`, `/visits/:id/status`, `/visits/:id/collect`, `/visits/:id/proof` (all mutations require `Idempotency-Key`). Web EN+UR `/phlebotomist` (large-touch offline PWA: route-ordered visit list, call/navigate, collect panel = barcode + pointer signature pad + photo capture; tries server then falls to the outbox; `noindex`; manifest shortcut). Gates green: typecheck 26/26, lint 15/15 (0 warn), build 15/15 (`/phlebotomist` route 3.9 kB / 136 kB First Load), jest **api 515/515 [+17]** + **db 126/126 [+4 phlebotomist-isolation]** + i18n 19/19 parity. Vertical smoke: lab-preset tenant→404; LAB_TECH (flag on, no manage)→403; visits list = only my assignments in route order; COLLECTED via status→400 (rides collect); missing Idempotency-Key→400; replayed collect/proof→same result, one sample/one proof; visit not mine→404.
- **Prior completed build-step:** **25 — Phase 2: Home Collection (Lab).** ✅ DONE/APPROVED (2026-07-05) — the lab at the patient's home. A **booking** (tests + address + slot) is **verified against a serviceable area** (`verifyServiceArea` — unknown/inactive area or a pin outside the served list → refused with a clear reason; else the area's extra home fee), given a **capacity-bounded 2-hour slot** (`daySlots`/`slotAvailability`/`isSlotOpen` — a full slot is refused; failed/re-opened bookings don't count against capacity), spins up a **real lab order via 19** (`LabService.createOrder` — prep WhatsApp + tests validated), records the booking with the fee, **bills the home fee to an invoice via 21** (`BillingService.createInvoice` source OTHER, **best-effort** — a lab-catalog-only tenant with no billing still books), **assigns a phlebotomist** (BOOKED→ASSIGNED + route sequence for 26), and walks a **status timeline** (`HOME_STATUS_FLOW`/`canTransitionHome`: BOOKED→ASSIGNED→EN_ROUTE→COLLECTED→RECEIVED; FAILED→RECOLLECT) with a **patient WhatsApp at each patient-facing step** (09, best-effort/degrades). A **failed draw re-opens as a recollection** (original→RECOLLECT, a fresh BOOKED booking linked `recollectOfId`, no re-charge; new sample linked in 27). It **OWNS only its 3 tables** — lab/billing/patient reached through their services, never mutated. NEW migration `…210000` (3 tables `service_areas`/`home_collections`/`phlebotomist_assignments` + `HomeStatus` enum; `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven in a new `home-collection-isolation` pglite spec). **Flag+perm pre-existed** (`lab.homeCollection` dependsOn `lab.catalog`; `home.collection.manage` held by PHLEBOTOMIST/RIDER/TENANT_OWNER) — **no catalog change**. Pure `@mp/shared/home-collection` (`HOME_STATUS_FLOW`/`canTransitionHome`/`nextHomeStatuses`/`notifiesPatient`; `verifyServiceArea`/`pinsOf`; `daySlots`/`slotAvailability`/`isSlotOpen`). API `HomeCollectionModule` (**declarative** class-level `@RequireFeature('lab.homeCollection')`+`@RequirePermission('home.collection.manage')` → FeatureGuard 404 THEN RolesGuard 403): `/home-collections`(book+list),`/:id`,`/:id/assign`,`/:id/status`,`/:id/recollect`,`/verify`,`/slots`,`/areas`(POST+GET+PATCH). Web EN+UR `/home-collection` (Simple=Book+Bookings status board; Pro adds Areas mgmt + phlebotomist assignment; live slot picker; `noindex`). Gates green: typecheck 26/26, lint 15/15 (0 warn), build 15/15 (`/home-collection` route 2.6 kB), jest **api 498/498 [+24]** + **db 122/122 [+5 home-collection-isolation]** + i18n 19/19 parity. Vertical smoke: lab-preset tenant (homeCollection off)→404; LAB_TECH (flag on, no manage)→403; non-serviceable pin→400; full slot→400; charge 300→invoice total 300; FAILED→recollect→new BOOKED linked, original RECOLLECT.
- **Current build-step:** **27 — Phase 2: Sample & Test Tracking.** Read `specs/27-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/27-sample-tracking.md`
- **Branch state:** `feat/26-phlebotomist-app` complete (awaiting controller merge to `staging`), stacked on `feat/25-home-collection` → `feat/24-admin-dashboard` → `feat/23-accounts-finance` → `feat/22-doctor-commission` → `feat/21-billing-cashier` → `feat/20-pharmacy-pos` → `feat/19-laboratory` → `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **26 — Phlebotomist Field App** ✅ 2026-07-05 — the phlebotomist's **offline field PWA** over the SAME home-collection bookings (25). Lists **my visits** (route-ordered by assignment sequence then slot, enriched w/ patient/tests/barcode/proof-flag), drives the doorstep flow **en route → collect (barcode ties the sample to the lab order via `LabService.collectSample` 27; then COLLECTED + patient WhatsApp) → proof (signature+photo) → collected**, flags a **failed draw** (→ FAILED). **Every mutation OFFLINE-IDEMPOTENT** (10, `SyncService.runIdempotent` on the `Idempotency-Key` header; `collection_proofs.clientActionId` unique = 2nd line). OWNS only 1 new table `collection_proofs`; visits/status (HomeCollectionService), sample tie (LabService), patient (PatientService) via services. Narrowed to caller's **own assignments** (not mine→404). NEW migration `…220000` (1 table; `apply_tenant_rls`, T1/T2+WITH-CHECK+fail-closed+global-unique clientActionId proven `phlebotomist-isolation`). **Flag+perm REUSED — no catalog change** (SAME `lab.homeCollection`+`home.collection.manage`; PHLEBOTOMIST/RIDER). Pure `@mp/shared/phlebotomist` (`FIELD_STATUSES`=EN_ROUTE/FAILED, `isFieldStatus`, `routeSequenceOf`/`compareVisits`). API `PhlebotomistModule` (class `@RequireFeature`+`@RequirePermission` → 404 THEN 403): `/phlebotomist/visits`,`/visits/:id/status`,`/visits/:id/collect`,`/visits/:id/proof` (mutations require `Idempotency-Key`). Web EN+UR `/phlebotomist` (large-touch offline PWA: route-ordered list, call/navigate, collect panel = barcode + pointer signature pad + photo; server-then-outbox; manifest shortcut). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/phlebotomist` 3.9 kB), jest api 515/515 (+17) + db 126/126 (+4 phlebotomist-isolation) + i18n 19/19. Smoke: lab-preset→404, LAB_TECH→403, visits=only my assignments route-ordered, COLLECTED-via-status→400, no Idempotency-Key→400, replayed collect/proof→same result+one sample/proof, not-mine→404.
- **25 — Home Collection (Lab)** ✅ 2026-07-05 — the lab at the patient's home. A **booking** (tests+address+slot) is **verified against a serviceable area** (`verifyServiceArea` — unknown/inactive/pin-outside-list→refused w/ reason; else extra home fee), given a **capacity-bounded 2-hour slot** (`daySlots`/`slotAvailability`/`isSlotOpen` — full slot refused), spins up a **real lab order via 19** (prep WhatsApp), **bills the fee to an invoice via 21** (source OTHER, **best-effort** — catalog-only lab still books), **assigns a phlebotomist** (BOOKED→ASSIGNED + route seq for 26), walks a **status timeline** (`HOME_STATUS_FLOW`/`canTransitionHome`: BOOKED→ASSIGNED→EN_ROUTE→COLLECTED→RECEIVED; FAILED→RECOLLECT) w/ **patient WhatsApp per patient-facing step** (09, degrades); a **failed draw re-opens as a recollection** (original→RECOLLECT, fresh BOOKED linked `recollectOfId`, no re-charge; sample linked in 27). OWNS only its 3 tables (lab/billing/patient via their services, never mutated). NEW migration `…210000` (3 tables + `HomeStatus` enum; `apply_tenant_rls`, T1/T2+WITH-CHECK+fail-closed proven `home-collection-isolation`). Flag `lab.homeCollection`(dependsOn `lab.catalog`) + perm `home.collection.manage`(PHLEBOTOMIST/RIDER/OWNER) **pre-existed — no catalog change**. Pure `@mp/shared/home-collection` (`HOME_STATUS_FLOW`/`canTransitionHome`/`notifiesPatient`, `verifyServiceArea`/`pinsOf`, `daySlots`/`slotAvailability`/`isSlotOpen`). API `HomeCollectionModule` (**declarative** class `@RequireFeature`+`@RequirePermission` → FeatureGuard 404 THEN RolesGuard 403): `/home-collections`(book+list),`/:id`,`/:id/assign`,`/:id/status`,`/:id/recollect`,`/verify`,`/slots`,`/areas`(POST+GET+PATCH). Web EN+UR `/home-collection` (Simple=Book+status board; Pro adds Areas mgmt + assignment; live slot picker). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/home-collection` 2.6 kB), jest api 498/498 (+24) + db 122/122 (+5 home-collection-isolation) + i18n 19/19 parity. Smoke: lab-preset→404, LAB_TECH→403, non-serviceable pin→400, full slot→400, charge 300→invoice 300, FAILED→recollect→new BOOKED linked+original RECOLLECT.
- **24 — Admin Dashboard & Owner Control** ✅ 2026-07-05 — the owner's daily cockpit: a **READ-MOSTLY aggregation over 19–23** whose **card set adapts to enabled flags** (pharmacy→sales/stock-loss/expiry, no lab/doctor; lab→TAT/volume/pending-approvals, no pharmacy; clinic→footfall/doctors+commission). **summary** (sales by source, collections vs pending, udhaar, gross profit = sales−pharmacy-COGS, cash position) · **alerts** (stock-loss value, low-stock, cashier variance, overdue AR ≥30d, pending lab approvals, audit highlights, expiry stub till 28) · **activity** (footfall, doctor productivity+commission, lab volume+avg TAT, top medicines). Cached 30 s; owns no source data (only writes `dashboard_prefs`). NEW migration `…200000` (1 table + `apply_tenant_rls`, T1/T2 proven `dashboard-isolation`). Always present, base `reports.view`; sensitive cards need extra perm (cash→`accounts.view`, audit→`audit.view`). Pure `@mp/shared/dashboard`. API `DashboardModule`: `/dashboard/summary`,`/alerts`,`/activity`,`/export`,`/prefs`. Web EN+UR `/dashboard`. Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 474/474 (+26) + db 117/117 (+3) + i18n parity.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
