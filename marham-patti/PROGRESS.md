> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **19 — Phase 1: Laboratory (Orders + Smart Reports).** ✅ DONE/APPROVED (2026-07-04) — the LIS: catalog (tests w/ **prep instructions** + packages + **smart result templates** = params/units/**age+gender ranges**) → order (from a referral 18 or direct) → sample (**barcode** collect/receive) → result (**auto-flagged** high/low/**critical** by the patient's age & gender, 14) → **pathologist approval (sign)** → **branded report** (07) + WhatsApp (09) + **TAT** + **re-test** + **amend-with-reason** (versioned, audited). NEW migration `…150000` (8 tables: `lab_tests`[@@unique tenant,code]/`lab_packages`/`result_templates`/`lab_orders`/`lab_order_tests`/`samples`[global-unique barcode]/`results`/`report_approvals` + `LabStatus` & `SampleStatus` enums; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Flags `lab.catalog`/`lab.results` (dependsOn) + perms `lab.order`/`lab.result.enter`/`lab.result.approve` already in the catalogs — **no catalog change**. Pure `@mp/shared/lab` (`evaluateResults` = the ONE smart-template evaluator server+client share w/ `pickRange` age+gender selection & NORMAL/HIGH/LOW/CRITICAL flagging, `computeTatHours`, `ageYears`, `isDeliverable`, lifecycle guards; reused additive `TEST` `TimelineKind`). API `LabModule` (4 controllers) — **split gating**: catalog/orders/samples `@RequireFeature('lab.catalog')`+`lab.order`; templates/results/approval/report method-overridden to `lab.results`+`lab.result.enter`/`lab.result.approve` (`getAllAndOverride` handler-wins) → **404 when the vertical lacks the flag** (vertical smoke) + catalog-only tenant 404s resulting. Endpoints: tests/packages CRUD (importable via 12, idempotent by `code`); result-templates CRUD; `POST /lab/orders` (validate patient, expand packages, best-effort prep WhatsApp); order worklist/detail; `collect`(barcode)→`samples/receive`(scan); `retest`(links `retestOfId`); `results` (evaluate + auto-flag + `criticalAlert`, → RESULTED when all done); `approve` (sign, refuses pre-result, → APPROVED+`reportedAt`+report WhatsApp, degrades); `amend` (reason-required, versioned, audited); `report` (branded 07, `draft` unless signed); `GET /patients/:id/lab-orders` (portal). MEDICAL `TimelineRegistry` contributor → `TEST` (medical view only). Web EN+UR `/lab` (Worklist: collect/receive + per-test result entry w/ pre-filled ranges + **live flags** via shared evaluator + Approve/Amend/**print branded report**/Re-test; New order; Catalog manager). Gates green: typecheck 26/26, lint 15/15, build 15/15 (`/lab` route), jest **api 321/321 [+25]** + **db 94/94 [+4 lab-isolation]** + i18n 19/19 parity. Perf ➖ (internal LIS tool, `robots:noindex`).
- **Current build-step:** **20 — Phase 1: Pharmacy POS.** Read `specs/20-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/20-pharmacy-pos.md`
- **Branch state:** `feat/19-laboratory` complete (awaiting controller merge to `staging`), stacked on `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. Step 20 branch stacks on it if not yet merged. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **19 — Laboratory (Orders + Smart Reports)** ✅ 2026-07-04 — the LIS: catalog (tests+**prep**+packages+**smart result templates** w/ **age+gender ranges**) → order → sample (**barcode**) → result (**auto-flagged** high/low/**critical** by patient age+gender, 14) → **pathologist sign** → **branded report** (07) + WhatsApp (09) + **TAT** + **re-test** + **amend-with-reason** (versioned, audited). NEW migration `…150000` (8 tables + `LabStatus`/`SampleStatus` enums; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Flags `lab.catalog`/`lab.results` + perms `lab.order`/`lab.result.enter`/`lab.result.approve` already in catalogs (no change). Pure `@mp/shared/lab` (`evaluateResults` = ONE evaluator server+client share via `pickRange` age+gender + NORMAL/HIGH/LOW/CRITICAL, `computeTatHours`, `ageYears`, `isDeliverable`; reused additive `TEST` `TimelineKind`). API `LabModule` (4 controllers) **split-gated**: catalog/orders/samples `lab.catalog`+`lab.order`, templates/results/approval method-overridden to `lab.results`+`lab.result.enter`/`lab.result.approve` → **404 vertical smoke** + catalog-only 404s resulting; tests/packages/templates CRUD (import 12 idempotent by `code`); order create (validate patient, expand packages, prep WhatsApp) + worklist/detail; `collect`(barcode)/`samples/receive`(scan); `retest`; `results`(auto-flag+`criticalAlert`, →RESULTED); `approve`(sign, refuses pre-result, →APPROVED+`reportedAt`+WhatsApp, degrades); `amend`(reason-required, versioned, audited); `report`(branded, `draft` unless signed); `patients/:id/lab-orders`. MEDICAL `TimelineRegistry` → `TEST` (medical view only). Web EN+UR `/lab` (Worklist collect/receive+result entry w/ **live flags**+Approve/Amend/print report/Re-test; New order; Catalog). Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 321/321 (+25) + db 94/94 (+4 lab-isolation) + i18n 19/19 parity. Perf ➖ (internal LIS tool).
- **18 — E-Prescription (Paper-Friendly + Privacy)** ✅ 2026-07-04 — the prescription pad for paper-first AND digital doctors: per-doctor **privacy** (`sendToPatient=false` → staff/pharmacy only, hidden 404 from non-`rx.viewInternal` incl. patient), **dual prescription** (INTERNAL vs BUY_OUTSIDE), TEST lines → lab, **allergy check** vs 14. NEW migration `…140000` (3 tables + the MINIMAL `medicines` model 20/28 extend additively; `DispenseMode`+`PrescriptionItemKind` enums; DEFENSIVE `pg_trgm` GIN → ILIKE fallback on pglite; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Pure `@mp/shared/prescription` (`medicineLabel`, `partitionPrescription` = ONE routing split, `findAllergyConflicts` = pure 14 matcher, `summarizePrescription`); added `PRESCRIPTION` to additive `TimelineKind`. API `PrescriptionsModule` PRESENT when EITHER `clinic.consultation` OR `pharmacy.pos` (programmatic **404 when neither**) + `rx.write`; medicines CRUD (fuzzy, import 12); `POST /prescriptions` (allergy 409 unless `allergyOverrideReason`, audited); `GET :id` privacy-gated + routing (`pharmacyAvailable`/`labAvailable` at runtime); patient list hides private; `repeat`; `print` (07 letterhead, dual copies when mixed); `send` (WhatsApp 09, blocked when private, degrades). MEDICAL `TimelineRegistry` contributor → prescription (medical view only). Web EN+UR `/prescriptions`. Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 296/296 (+22) + db 90/90 (+4 prescription-isolation) + i18n 19/19 parity. Perf ➖ (internal tool).
- **17 — Doctor Consultation (Comfort Levels)** ✅ 2026-07-04 — the clinical encounter, built to RESPECT how each doctor works: per-doctor comfort level (L0 Full Paper … L3 Full Digital, on `Doctor.comfortLevel` from 15) drives the UI, system always captures encounter + routes referrals. NEW migration `…130000` (4 tables: encounters/diagnoses/consult_templates/doctor_favorites + `EncounterStatus` enum; all `apply_tenant_rls`, T1/T2 proven). Pure `@mp/shared/consultation` (`clampComfortLevel`/`usesPaper` = ONE L0–L3 mapping, `summarizeEncounter`, `ReferralSeed`/`buildReferralSeed` = stable 18/19 contract); added `ENCOUNTER` to additive `TimelineKind`. API `ConsultationModule` (4 controllers) `@RequireFeature('clinic.consultation')` (**404 when off**) + `@RequirePermission('consult.write')`; encounters CRUD + diagnoses; `GET /patients/:id/history` (vitals 16 + allergies 14 + prior encounters); templates+favorites CRUD; `refer/lab|pharmacy` → **SEED** w/ downstream `available`; `ai-assist` **assist-only+logged+`ai.clinical`-gated**; L0 `print-header`. MEDICAL `TimelineRegistry` contributor → encounter+FOLLOWUP (medical view only). Web EN+UR `/consultation`. Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 274/274 (+27) + db 86/86 (+4 consultation-isolation) + i18n 19/19 parity. Perf ➖ (internal tool).

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
