> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **18 — Phase 1: E-Prescription (Paper-Friendly + Privacy).** ✅ DONE/APPROVED (2026-07-04) — the prescription pad that works for paper-first AND digital doctors, respects per-doctor **privacy** (`sendToPatient=false` → staff/pharmacy only, hidden 404 from anyone without `rx.viewInternal` incl. the patient), splits the **dual prescription** (INTERNAL dispensing vs BUY_OUTSIDE), routes TEST lines to the lab, and runs an **allergy check** vs the patient record (14). NEW migration `…140000` (3 tables + the MINIMAL `medicines` model that 20/28 extend additively: `medicines`/`prescriptions`/`prescription_items` + `DispenseMode` INTERNAL/BUY_OUTSIDE & `PrescriptionItemKind` MEDICINE/TEST enums; `pg_trgm` GIN indexes created DEFENSIVELY so pglite falls back to ILIKE; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Pure `@mp/shared/prescription` (`medicineLabel`, `partitionPrescription` = the ONE dual-dispense + test routing split server+client share, `findAllergyConflicts` = the pure 14 matcher, `summarizePrescription`, `isDispenseMode`/`isPrescriptionItemKind`); added `PRESCRIPTION` to the additive `TimelineKind`. API `PrescriptionsModule` — surface PRESENT when EITHER `clinic.consultation` OR `pharmacy.pos` is on (programmatic **404 when neither** = vertical smoke), then `rx.write`; `GET/POST /medicines` (fuzzy, importable via 12); `POST /prescriptions` (allergy 409 unless `allergyOverrideReason`, audited); `GET :id` (privacy-gated + routing w/ `pharmacyAvailable`/`labAvailable` read at runtime — never commits to unbuilt 19/20); `GET /patients/:id/prescriptions` (hides private from non-staff); `POST :id/repeat`; `POST :id/print` (07 letterhead, dual copies when modes mix); `POST :id/send` (WhatsApp 09, **blocked when private**, degrades cleanly). Registered as a MEDICAL `TimelineRegistry` contributor → prescription on the patient timeline **only in the medical view**. Web EN+UR `/prescriptions` (medicine search + favorites 17, per-item dosage/timing EN+UR/Roman + dispense-mode + medicine/test kind, prominent privacy toggle, inline allergy warnings + override reason, live routing preview, paper-photo image key, letterhead print window w/ dual copies, repeat/send). Gates green: typecheck 26/26, lint 15/15, build 15/15 (`/prescriptions` route), jest **api 296/296 [+22]** + **db 90/90 [+4 prescription-isolation]** + i18n 19/19 parity. Perf ➖ (internal tool, `robots:noindex`, no public surface).
- **Current build-step:** **19 — Phase 1: Laboratory.** Read `specs/19-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/19-laboratory.md`
- **Branch state:** `feat/18-eprescription` complete (awaiting controller merge to `staging`), stacked on `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. Step 19 branch stacks on it if not yet merged. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **18 — E-Prescription (Paper-Friendly + Privacy)** ✅ 2026-07-04 — the prescription pad for paper-first AND digital doctors: per-doctor **privacy** (`sendToPatient=false` → staff/pharmacy only, hidden 404 from non-`rx.viewInternal` incl. patient), **dual prescription** (INTERNAL vs BUY_OUTSIDE), TEST lines → lab, **allergy check** vs 14. NEW migration `…140000` (3 tables + the MINIMAL `medicines` model 20/28 extend additively; `DispenseMode`+`PrescriptionItemKind` enums; DEFENSIVE `pg_trgm` GIN → ILIKE fallback on pglite; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Pure `@mp/shared/prescription` (`medicineLabel`, `partitionPrescription` = ONE routing split, `findAllergyConflicts` = pure 14 matcher, `summarizePrescription`); added `PRESCRIPTION` to additive `TimelineKind`. API `PrescriptionsModule` PRESENT when EITHER `clinic.consultation` OR `pharmacy.pos` (programmatic **404 when neither**) + `rx.write`; medicines CRUD (fuzzy, import 12); `POST /prescriptions` (allergy 409 unless `allergyOverrideReason`, audited); `GET :id` privacy-gated + routing (`pharmacyAvailable`/`labAvailable` at runtime, no commit to unbuilt 19/20); patient list hides private; `repeat`; `print` (07 letterhead, dual copies when mixed); `send` (WhatsApp 09, blocked when private, degrades). MEDICAL `TimelineRegistry` contributor → prescription (medical view only). Web EN+UR `/prescriptions` (search+favorites, per-item dosage/timing/dispense/kind, privacy toggle, inline allergy + override, live routing preview, paper-photo key, letterhead print + dual copies). Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 296/296 (+22) + db 90/90 (+4 prescription-isolation) + i18n 19/19 parity. Perf ➖ (internal tool).
- **17 — Doctor Consultation (Comfort Levels)** ✅ 2026-07-04 — the clinical encounter, built to RESPECT how each doctor works: per-doctor comfort level (L0 Full Paper … L3 Full Digital, on `Doctor.comfortLevel` from 15) drives the UI, system always captures encounter + routes referrals. NEW migration `…130000` (4 tables: encounters/diagnoses/consult_templates/doctor_favorites + `EncounterStatus` enum; all `apply_tenant_rls`, T1/T2 proven). Pure `@mp/shared/consultation` (`clampComfortLevel`/`usesPaper` = ONE L0–L3 mapping, `summarizeEncounter`, `ReferralSeed`/`buildReferralSeed` = stable 18/19 contract); added `ENCOUNTER` to additive `TimelineKind`. API `ConsultationModule` (4 controllers) `@RequireFeature('clinic.consultation')` (**404 when off**) + `@RequirePermission('consult.write')`; encounters CRUD (comfort captured) + diagnoses; `GET /patients/:id/history` (one-click vitals 16 + allergies 14 + prior encounters); templates+favorites CRUD; `refer/lab|pharmacy` → **SEED** w/ downstream `available` (no commit to unbuilt 18/19); `ai-assist` **assist-only+logged+`ai.clinical`-gated**, degrades cleanly; L0 `print-header`. MEDICAL `TimelineRegistry` contributor → encounter+FOLLOWUP (medical view only). Web EN+UR `/consultation`. Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 274/274 (+27) + db 86/86 (+4 consultation-isolation) + i18n 19/19 parity. Perf ➖ (internal tool).
- **16 — Vitals Station (Nurse)** ✅ 2026-07-04 — pre-consultation nurse reading, built around the paper habit: type→print a FILLED A4, or print a BLANK header sheet and write by pen (photo→AI read deferred to 35; manual until then). NEW migration `…120000` (1 table `vitals` + `VitalsSource` enum; all values optional, Decimals, `apply_tenant_rls`+`branch_id`, T1/T2 proven). Pure `@mp/shared/vitals` (`computeBmi` = ONE server+client formula, `summarizeVitals`, `SUGAR_TYPES`); added `VITALS` to additive `TimelineKind`. API `VitalsModule`: whole controller `@RequireFeature('clinic.consultation')` (**404 when off** = vertical smoke) + `@RequirePermission('vitals.enter')`; `POST /vitals` **offline-idempotent** (`runIdempotent`, required `Idempotency-Key`, P2002 backstop) with **BMI always recomputed server-side**; `GET /vitals`; `GET /vitals/sheet?blank=` (filled/blank A4 header from patients 14 + appointments 15); `POST /vitals/from-photo` → DRAFT (aiAvailable:false, no silent commit). NEW decoupled `TimelineRegistry` in PatientsModule → vitals on the patient timeline **only in the medical view**. Web EN+UR `/vitals`. Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 247/247 (+20) + db 82/82 (+3 vitals-isolation) + i18n 19/19 parity.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
