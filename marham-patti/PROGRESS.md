> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **BUILD COMPLETE (1–39).** The specced product is build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39) — **ALL DONE**. This is the entire build. There are no further specced steps. The project is now in the **launch / test / stabilization** phase, not the build phase.
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **39 — AI Suite: Cost Control & Quality Routing (`ai.suite`).** ✅ DONE/APPROVED (2026-07-05) — makes the assist-only AI suite **economically safe** (completes the build, 1–39). Operationalizes the **35** budget primitive with a **live spend meter**, **per-task quality-tier routing** (quality-first, cost shown) and **hard budget caps** with a configurable **breach behavior**. **Extends 35** (no new module): `AiBudget` gains `warnPct` (default 80) + `onBreach BreachAction` (default BLOCK); NEW append-only **`AiSpendLog`** ledger (one row per completed `runAiTask` call: taskType/model/cost/at) — tenant-scoped RLS 02. Migration `…330000` (ALTER budget + CREATE ledger; existing rows adopt safe defaults). NEW enum `BreachAction {WARN THROTTLE BLOCK}` exported from `@mp/db`. **Pure `@mp/shared/ai-gateway` cost-control core:** `budgetGate(spent,cap,onBreach,warnPct)` → `{gate: ALLOW|WARN|THROTTLE|BLOCK, throttled, warning, over}` (no cap ⇒ ALLOW; under-cap-but-≥warn% ⇒ WARN; at/over cap ⇒ the `onBreach` action; **unknown action fail-safe→BLOCK**), `budgetMeter` (usedPct/remaining/warnThreshold, null w/o cap), `THROTTLE_TIER`=FAST, `normalizeWarnPct` [0,100]; reuses commission's `isPeriodKey`/`periodRange` for the ledger window. **`runAiTask` enforcement (still THE point):** consults the gate BEFORE the call → **BLOCK** records a FAILED task + 403 (spend untouched) · **WARN** runs (soft cap) · **THROTTLE** forces FAST tier · then logs an `AiSpendLog` line after the call. **Controller (all `ai.budget.manage` except GET budget=`ai.use`):** `GET /ai/spend?period=` (live meter: total+calls+byTaskType+byModel, validated `YYYY-MM`, 400 else), `GET /ai/routing` (every AI_TASK_TYPE with quality-first defaultTier + override + per-1k rate) + `PUT /ai/routing` (upsert config), extended `PUT /ai/budget` (merge-on-write cap/warnPct/onBreach; ≥1 field else 400), `GET /ai/budget` (extended view); `/ai/config` kept as back-compat alias. **OWNS 1 new table** `AiSpendLog`. **Flag REUSED (`ai.suite`) — NO catalog change; NO new permission** (reuses `ai.budget.manage`). **Web:** NEW `/admin/ai-cost` (Simple=meter+cap form; Pro adds by-task/by-model tables + per-task routing table w/ tier select + animated usage gauge) + a self-probing **AI cost card on the 24 dashboard** (renders nothing without `ai.suite`/`ai.budget.manage`); EN+UR keys (+38 pairs). Gates green: turbo typecheck **27/27** + lint **15/15** (0 err; 1 pre-existing doctor-portal warn), build+test **23/23**, jest **api 864/864 [+11]** + **db 178/178 [+5 new `ai-cost-control-isolation`]** + i18n 19/19 parity. Vertical smoke (e2e over the real graph + faked 35 repo): budget controls merge (cap survives a warn%/breach-only PUT)+`warnThreshold` computed; empty/bad-breach/out-of-range→400; **onBreach BLOCK→403 FAILED over cap · WARN→runs DONE · THROTTLE→runs on `gateway-fast`**; live meter aggregates by task+model (calls=2, other period→0, malformed→400, `ai.use`-only RECEPTION→403); routing lists quality-first defaults + PUT clinical.cds→FAST changes the model used; RLS proven T1/T2 in db (spend/cap never cross-tenant, WITH CHECK blocks foreign insert/update, no-context→0 rows).
- **Prior completed build-step:** **38 — AI Suite: Feedback & Learning (`ai.suite`).** ✅ DONE/APPROVED (2026-07-05) — closes the AI loop SAFELY: a 👍/👎 (+ optional structured `correction`) on any 35 `AiSuggestion` feeds per-tenant memory/rules; NON-safety→`AiMemory` (confidence rises w/ recurrence, 0.4<0.7 floor, 2nd=0.8 ACTIVE); SAFETY-CRITICAL→`AiRule` born PROPOSED, never auto-active, human→ACTIVE/REJECTED. 36 `rx.read` folds ACTIVE mappings (keeps mustVerify). OWNS 3 tables, migration `…320000`, NEW `ai.rules.review`. Gates: api 853/853 + db 173/173 + i18n 19/19.
- **Current build-step:** **NONE — the build (1–39) is COMPLETE.** There is no next build-step. Do **not** attempt to build a step 40 or beyond: no such spec exists, and a missing/empty spec is always a hard stop, never a cue to guess or improvise. The build phase is finished.
- **Next-step pointer:** → **NONE. Build complete at 39.** (If, in the future, a genuinely new capability is required, a human will first author a full, authoritative `specs/40-<slug>.md` — data model, endpoints, Simple/Pro faces, acceptance — and only then update this pointer to it. Until such a spec exists, there is nothing to build.)
- **Branch state:** `feat/39-ai-cost-control` complete (awaiting controller merge to `staging`), stacked on `feat/38-ai-feedback-learning` → `feat/37-ai-suite-ops` → `feat/36-ai-suite-clinical` → `feat/35-ai-gateway` → `feat/34-online-pharmacy` → `feat/33-doctor-portal-ai` → `feat/32-patient-ai-assistant` → `feat/31-patient-app` → `feat/30-branch-transfer` → `feat/29-purchase-suppliers` → `feat/28-pharmacy-inventory` → `feat/27-sample-tracking` → `feat/26-phlebotomist-app` → `feat/25-home-collection` → `feat/24-admin-dashboard` → `feat/23-accounts-finance` → `feat/22-doctor-commission` → `feat/21-billing-cashier` → `feat/20-pharmacy-pos` → `feat/19-laboratory` → `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **Launch hotfix — production `next build` (staging) fix** ✅ 2026-07-06 (build stays complete at 39; stabilization, not a new step) — **found via the FIRST production deploy:** dev/CI turbo gates were green but `deploy.sh`'s real `next build` static-generation died with `<Html> should not be imported outside of pages/_document` prerendering `/404` + `/_error`. **Root cause:** the App Router (`apps/web/app`; no `pages/` dir, zero `next/document` imports) had NO error surface, so Next fell back to its internal **Pages-Router** error page (which imports `<Html>`) and that prerender failed. **Fix:** added real App-Router error pages — `app/not-found.tsx` (server; EN+UR + RTL via `serverLocale`/`tr`; @mp/ui brand tokens + `Button`), `app/error.tsx` (client, in-layout, `useI18n`), `app/global-error.tsx` (self-contained: own `<html>`, brand CSS vars, cookie-driven locale) — making the Pages fallback unreachable; +7 EN+UR key pairs (parity kept). **Also:** killed the "non-standard NODE_ENV" warning per ARCHITECTURE — `NODE_ENV` is now ALWAYS a standard value; `deploy.sh` carries the deploy env in **APP_ENV** (`staging` on staging) and **pins `NODE_ENV=production`** for the build; `ecosystem.config.js` inlines `APP_ENV` so the runtime screenshot-token gate (keys off `APP_ENV/NODE_ENV==='staging'`, **fail-closed**) still fires on staging + denies on prod (proven by `screenshot.spec`). Minor: `packages/ui`→`"type":"module"` (clears the `tailwind-preset.js` module-type warning; `jest.config.js`→`.cjs`). **Verified the deploy.sh way** (`NODE_ENV=production APP_ENV=staging` full `turbo build`): **15/15 build OK, `/_not-found` generated, no `<Html>`/prerender/NODE_ENV/preset warnings**; live 404 renders EN "Page not found"/UR "صفحہ نہیں ملا" (RTL) with brand tokens. Gates: typecheck **27/27**, lint **15/15** (0 err; 1 pre-existing `@mp/api` warn), jest **api 864/864 + db 178/178 + ui 38/38 + i18n 19/19 parity**.
- **39 — AI Suite: Cost Control & Quality Routing (`ai.suite`)** ✅ 2026-07-05 — makes the AI suite **economically safe** + **completes the build (1–39)**. Operationalizes the 35 budget: **live spend meter** + **per-task quality-tier routing** (quality-first, cost shown) + **hard caps** with a **breach behavior**. Extends 35 (no new module): `AiBudget`+`warnPct`(80)/`onBreach BreachAction`(BLOCK); NEW append-only **`AiSpendLog`** ledger (tenant-scoped RLS 02); migration `…330000`; enum `BreachAction {WARN THROTTLE BLOCK}` from `@mp/db`. Pure `@mp/shared/ai-gateway`: `budgetGate`→`{ALLOW|WARN|THROTTLE|BLOCK,…}` (no-cap⇒ALLOW; ≥warn%⇒WARN; over-cap⇒onBreach; unknown**fail-safe→BLOCK**), `budgetMeter`, `THROTTLE_TIER`=FAST, `normalizeWarnPct`; reuses commission `isPeriodKey`/`periodRange`. `runAiTask` gates BEFORE the call (BLOCK→FAILED+403 spend-untouched · WARN→runs · THROTTLE→FAST) then logs a spend line. Controller (`ai.budget.manage`; GET budget=`ai.use`): `GET /ai/spend?period=` (total+calls+byTask+byModel, 400 on bad `YYYY-MM`), `GET/PUT /ai/routing`, extended `PUT /ai/budget` (merge-on-write, ≥1 field else 400), `/ai/config` kept as alias. **OWNS 1 table** `AiSpendLog`. **Flag REUSED — NO catalog/permission change.** Web: NEW `/admin/ai-cost` (Simple meter+cap; Pro +by-task/model +routing table +animated gauge) + self-probing AI cost card on the **24 dashboard**; EN+UR (+38). Gates: turbo 27/27 + 15/15, build+test 23/23, jest **api 864/864 (+11)** + **db 178/178 (+5 `ai-cost-control-isolation`)** + i18n 19/19. Smoke: budget merge (cap survives warn/breach-only PUT), empty/bad→400, BLOCK→403 FAILED over cap, WARN→DONE, THROTTLE→`gateway-fast`, meter aggregates(calls=2, other period→0, `ai.use`-only→403), routing lists quality-first defaults + PUT cds→FAST changes model, RLS T1/T2 (spend/cap never cross-tenant, WITH CHECK blocks, no-context→0).
- **38 — AI Suite: Feedback & Learning (`ai.suite`)** ✅ 2026-07-05 — closes the AI loop SAFELY: a 👍/👎 (+ optional `correction`) on any 35 `AiSuggestion` feeds per-tenant memory/rules — NON-safety→`AiMemory` (confidence rises w/ recurrence: 0.4<0.7 floor, 2nd=0.8 ACTIVE); SAFETY-CRITICAL (clinical/dosing/…; unknown**fail-safe→critical**)→`AiRule` born PROPOSED, never auto-active, human→ACTIVE/REJECTED. NEW `AiFeedbackModule` (`ai.suite` FIRST→`ai.use`; rule review new `ai.rules.review`, safety-critical review ALSO needs `ai.clinical`→403). 36 `rx.read` folds ACTIVE mappings (`applyMedicineMappings`, keeps mustVerify, `@Optional()`). OWNS 3 tables `AiFeedback`/`AiMemory`/`AiRule` (+ `Rating`/`RuleStatus`), migration `…320000`. Flag REUSED; NO web surface. Gates: api 853/853 (+29) + db 173/173 (+6 `ai-feedback-isolation`) + i18n 19/19.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
