> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **37 — AI Suite: Ops (`ai.ops`).** ✅ DONE/APPROVED (2026-07-05) — the five **advisory** operational AI capabilities, each a 35 task type, each **a human acts**: `predict.noshow` (score upcoming appointments 15 from each patient's REAL attendance → reminder/overbook hints; mild prior smooths sparse history), `predict.demand` (reorder hints from sales velocity 20/28 vs on-hand → feeds 29 purchase suggestions; sized to a cover target), `predict.expiry` (batches 28 unlikely to sell before expiry given velocity → promote/return hints; already-expired first), `analytics.ask` (**GROUNDED** Q&A over READ-ONLY finance/ops aggregates 21/23 — cites the exact figures + deltas, NEVER invents one, NEVER raw/cross-tenant), `ops.chat` (staff how-to/lookup from a curated book + honest fallback) + a **call-center voice SCAFFOLD** (disabled hook points, deferred to 43 — no AI call, no action). NEW **OpsAiModule** (`@RequireFeature('ai.ops')` FIRST → per-insight SOURCE flag checked in the service [`predict.noshow`→`clinic.appointments`, `predict.demand`/`predict.expiry`→`pharmacy.inventory`, `analytics.ask`/`ops.chat`→none] → `@RequirePermission('ai.use')`): `POST /ai/ops/{predict-noshow,predict-demand,predict-expiry,analytics-ask,ops-chat}` + `GET /ai/ops/call-center/scaffold`. Each POST: gate surface → READ the tenant's OWN read-only aggregates → compute the DETERMINISTIC advisory floor (pure `@mp/shared`) → route through **35 `AiGatewayService.runAiTask`** (LOGGED + BUDGETED; `floor`→suggestion `output.result`) → PENDING suggestion the 24/29/28/15 surfaces render; the human decides via the **35** decision surface. **OWNS NO tables** — REUSES 35's `AiTask`/`AiSuggestion` + its OWN tenant-scoped read-only repo (appointments 15 / sales-velocity+on-hand 20/28 / batches 28 / finance aggregates 21/23). NEW pure `@mp/shared/ops-ai` (`OPS_TASKS` + `opsSurfaceFlagFor`, `predictNoShows`/`predictDemand`/`predictExpiryRisk`/`answerAnalytics`/`opsChatReply` + `callCenterScaffold` + per-task policies; `asOf` passed in — no `Date.now()`); 5 ops task types added additively to `AI_TASK_TYPES`+`DEFAULT_TASK_TIER` (advisory: predictions→FAST, `analytics.ask`→BALANCED). **NO migration (no new tables/enums). Flags REUSED (`ai.ops` pre-existed since 35; `full`=FLAG_KEYS auto-includes) — NO catalog change. Permission REUSED — `ai.use` already held by TENANT_OWNER/ADMIN/MANAGER (owner/manager-facing ops); NO grant change.** NO web surface (backend suite; dashboard 24 Insights cards + 29 reorder + 28/15 call it). Gates green: turbo typecheck **27/27** + lint **15/15** (0 err; 1 pre-existing doctor-portal warn), test **20/20** + build **15/15**, jest **api 824/824 [+28]** + **db 167/167** (isolation inherited — 37 owns no tables; reads go through `runWithTenant` RLS) + i18n 19/19 parity. Vertical smoke (e2e over the real graph + REAL 35 mock provider): no token→401; no-`ai.ops`→404; per-insight SOURCE flag (T_PARTIAL `ai.ops` on but `pharmacy.inventory` off → predict-demand/expiry 404, predict-noshow/analytics/chat 200); run needs `ai.use`(RECEPTION→403); predict.noshow→scored (flaky patient HIGH first) + DONE task + PENDING suggestion carrying the advisory floor + audited; PENDING not applicable→approve→applicable; predict.demand reorder (100 = 10/day·14−40) + well-stocked skipped; predict.expiry slow-mover flagged, fast-mover excluded; analytics.ask grounded citations (profit 90←150 DOWN) + empty→400; ops.chat matched how-to + empty→400; call-center scaffold hooks all disabled + NO gateway task.
- **Prior completed build-step:** **36 — AI Suite: Clinical (`ai.clinical`).** ✅ DONE/APPROVED (2026-07-05) — the six **assist-only** clinical AI capabilities, each a 35 task type + each HUMAN-CONFIRMED: `rx.read`, `medicine.search`, `interaction.check` (advisory over REAL 14/18; never blocks), `lab.explain` (EN+UR+Roman, informational), `result.flag` (pathologist SIGNS 19), `note.summarize` (doctor edits). NEW `ClinicalAiModule` (`@RequireFeature('ai.clinical')` FIRST → per-task MODULE flag [`pharmacy.aiReader`/`pharmacy.pos`/`clinic.consultation`/`lab.results`] → `@RequirePermission('ai.use')`): `POST /ai/clinical/{rx-read,…,note-summarize}`, each computing a DETERMINISTIC `@mp/shared` floor then routing through **35 `runAiTask`** → PENDING suggestion. **OWNS NO tables** (reuses 35 + own read repo). NEW pure `@mp/shared/clinical-ai`; 6 task types added additively (quality-first). **NO migration. Flags REUSED. `ai.use` GRANTED additively to PHARMACIST/SALESMAN/PATHOLOGIST/LAB_TECH.** Gates: turbo 42/42 + 23/23, jest api 796/796 (+28) + db 167/167 + i18n 19/19.
- **Current build-step:** **38 — AI Suite: Feedback & Learning.** Read `specs/38-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/38-ai-feedback-learning.md`
- **Branch state:** `feat/37-ai-suite-ops` complete (awaiting controller merge to `staging`), stacked on `feat/36-ai-suite-clinical` → `feat/35-ai-gateway` → `feat/34-online-pharmacy` → `feat/33-doctor-portal-ai` → `feat/32-patient-ai-assistant` → `feat/31-patient-app` → `feat/30-branch-transfer` → `feat/29-purchase-suppliers` → `feat/28-pharmacy-inventory` → `feat/27-sample-tracking` → `feat/26-phlebotomist-app` → `feat/25-home-collection` → `feat/24-admin-dashboard` → `feat/23-accounts-finance` → `feat/22-doctor-commission` → `feat/21-billing-cashier` → `feat/20-pharmacy-pos` → `feat/19-laboratory` → `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **37 — AI Suite: Ops (`ai.ops`)** ✅ 2026-07-05 — the five **advisory** operational AI capabilities, each a 35 task type + each a human acts: `predict.noshow` (score upcoming 15 appts from real attendance → reminder/overbook), `predict.demand` (velocity 20/28 vs on-hand → 29 reorder hints), `predict.expiry` (batches 28 unlikely to sell before expiry → promote/return), `analytics.ask` (**GROUNDED** Q&A over READ-ONLY aggregates 21/23 — cites figures+deltas, never invents, never raw/cross-tenant), `ops.chat` (staff how-to) + a call-center voice **SCAFFOLD** (disabled hooks, deferred to 43). NEW `OpsAiModule` (`@RequireFeature('ai.ops')` FIRST → per-insight SOURCE flag in service [`clinic.appointments`/`pharmacy.inventory`; assistants none] → `@RequirePermission('ai.use')`): `POST /ai/ops/{predict-noshow,predict-demand,predict-expiry,analytics-ask,ops-chat}` + `GET /ai/ops/call-center/scaffold`, each POST reading the tenant's OWN read-only aggregates → DETERMINISTIC `@mp/shared` floor → **35 `runAiTask`** (`floor`→suggestion) → PENDING suggestion; human acts via the 35 decision surface. **OWNS NO tables** (reuses 35 + own read-only repo appts/velocity/batches/finance). NEW pure `@mp/shared/ops-ai` (`predictNoShows`/`predictDemand`/`predictExpiryRisk`/`answerAnalytics`/`opsChatReply`+`callCenterScaffold`+policies; `asOf` passed in); 5 task types added additively (advisory: predictions→FAST, analytics→BALANCED). **NO migration. Flags REUSED (`ai.ops` pre-existed; `full`=FLAG_KEYS). `ai.use` already held by OWNER/ADMIN/MANAGER — NO grant change.** NO web surface. Gates: turbo typecheck 27/27 + lint 15/15, test 20/20 + build 15/15, jest api 824/824 (+28) + db 167/167 + i18n 19/19. Smoke: no token→401, no-`ai.ops`→404, per-insight SOURCE flag (T_PARTIAL demand/expiry→404, noshow/analytics/chat→200), `ai.use`(RECEPTION→403), predict.noshow scored(flaky HIGH first)+PENDING+audited+PENDING-not-applicable→approve→applicable, predict.demand reorder(100)+skip-stocked, predict.expiry slow-mover flagged/fast-mover excluded, analytics.ask grounded citations(profit 90←150 DOWN)+empty→400, ops.chat matched+empty→400, call-center scaffold hooks-disabled+NO gateway task.
- **36 — AI Suite: Clinical (`ai.clinical`)** ✅ 2026-07-05 — the six **assist-only** clinical AI capabilities, each a 35 task type + each HUMAN-CONFIRMED: `rx.read`, `medicine.search`, `interaction.check` (advisory over REAL 14/18; never blocks), `lab.explain` (EN+UR+Roman, informational), `result.flag` (pathologist SIGNS 19), `note.summarize` (doctor edits). NEW `ClinicalAiModule` (`@RequireFeature('ai.clinical')` FIRST → per-task MODULE flag in service [`pharmacy.aiReader`/`pharmacy.pos`/`clinic.consultation`/`lab.results`] → `@RequirePermission('ai.use')`): `POST /ai/clinical/{rx-read,…,note-summarize}`, each computing a DETERMINISTIC `@mp/shared` floor then routing through **35 `runAiTask`** (new opt `floor`→suggestion `output.result`) → PENDING suggestion; human decides via the 35 decision surface. **OWNS NO tables** (reuses 35 + own read repo for allergies/meds/catalog). NEW pure `@mp/shared/clinical-ai`; 6 task types added additively (quality-first). **NO migration. Flags REUSED. `ai.use` GRANTED additively to PHARMACIST/SALESMAN/PATHOLOGIST/LAB_TECH.** Gates: turbo 42/42 + 23/23, jest api 796/796 (+28) + db 167/167 + i18n 19/19. Smoke: no token→401, no-`ai.clinical`→404, per-surface module flag, `ai.use`(RECEPTION→403), rx.read must-verify+PENDING+audited→approve→applicable, medicine.search brand+alt+intent, interaction.check real meds/allergies→HIGH+DRUG_ALLERGY, lab.explain EN/UR/Roman, result.flag pathologistSigns, note.summarize draftOnly.
- **35 — AI Suite: AI Gateway (`@mp/ai`)** ✅ 2026-07-05 — THE provider-agnostic, **assist-only-by-construction** primitive every AI surface (32/33/34/36/37) routes through, behind the `ai.suite` master flag (absent→404). **HITL: `runAiTask` only returns a PENDING `AiSuggestion`; applied ONLY after a human APPROVED/EDITED (`canApplySuggestion`). Per-tenant keys are SECRETS in the env/secret store, NEVER the DB.** NEW `AiModule`: `POST /ai/tasks`(Idempotency-Key→10 dedupe), `/ai/suggestions/:id/decision`, `GET /ai/{suggestions,tasks,budget,config}`. `runAiTask`: route (quality-first)→budget BLOCK over cap (FAILED task; spend untouched)→provider→persist `AiTask`(tokens/cost/latency)→accrue `spent`→PENDING `AiSuggestion`. OWNS 4 tables (no key col). Migration `…310000`. Gates: api 768/768 (+37) + db 167/167 (+6 isolation) + i18n 19/19.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
