> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **24 — Phase 2: Admin Dashboard & Owner Control.** ✅ DONE/APPROVED (2026-07-05) — the owner's daily cockpit: a **READ-MOSTLY aggregation over 19–23** whose **card set adapts to the tenant's enabled flags** (a pharmacy-only tenant sees sales/stock-loss/expiry but NO lab/doctor cards; a lab-only tenant sees TAT/test-volume/pending-approvals but no pharmacy cards; a clinic sees footfall/doctors+commission). Three sections: **summary** (today's sales by source, collections vs pending, **udhaar**, gross **profit** = sales−pharmacy-COGS, **cash position** — full=Σ CASH/BANK journal balance, lite=cash-ledger running balance), **alerts** (**stock-loss** value from DAMAGE/EXPIRE/−ADJUST movements×cost — the founding pain; **low-stock** ≤ threshold; **cashier shift variance** — the theft tripwire; **overdue udhaar/AR** ≥30d; **pending lab approvals** RESULTED; **audit highlights** — sensitive-action prefixes; **expiry** stub until 28 batches), **activity** (footfall tokens+new patients, doctor productivity+commission, lab volume+**avg TAT**, top medicines). Aggregates **cached in-app 30 s** (perf budget); every figure aggregated read-only through a tenant-scoped repo (the dashboard OWNS no source data → can't corrupt 19–23; the ONLY table it writes is per-user `dashboard_prefs` layout). NEW migration `…200000` (1 table `dashboard_prefs`, `@@unique(tenant,user)`; `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven in a new `dashboard-isolation` pglite spec). **Always present** (core owner control, no module flag) — base perm `reports.view`; **sensitive cards need the caller's extra perm** (cash→`accounts.view`, audit→`audit.view`) resolved per caller. Perms/flags all pre-existing — **no catalog change**. Pure `@mp/shared/dashboard` (`DASHBOARD_CARDS`/`visibleCards`/`cardRelevant` — the flag-adaptive core; `salesBySource`/`stockLossValue`/`shiftVarianceAlerts`/`overdueTotal`/`lowStockRows`/`averageTat`/`topBy`/`grossProfit`/`sumBy`). API `DashboardModule` (programmatic `reports.view` gate then per-caller sensitive-perm resolution): `/dashboard/summary`,`/alerts`,`/activity`,`/export`(CSV),`/prefs`(GET+PUT). Web EN+UR `/dashboard` (Simple=KPIs+alerts strip w/ drill links; Pro adds full grid + **dynamically-imported animated sales-by-source chart** + activity tables + CSV export; date/branch filter; optional/`noindex`). Gates green: typecheck 26/26, lint 15/15 (0 warn), build 15/15 (`/dashboard` route 4.17 kB), jest **api 474/474 [+26]** + **db 117/117 [+3 dashboard-isolation]** + i18n parity. Vertical smoke: pharmacy tenant→no lab/doctor cards; lab tenant→no pharmacy cards; MANAGER (no accounts/audit.view)→no cash/audit cards; NURSE (no reports.view)→403; numbers reconcile with seeded 21/23 (sales 600, collected 200, profit 560, cash 400, stock-loss 60, overdue 500).
- **Prior completed build-step:** **23 — Phase 2: Accounts & Finance (Lite / Full).** ✅ DONE/APPROVED (2026-07-05) — the books in two flag-chosen, vertical-aware modes. **LITE** (`accounting.lite`, default-on): `CashLedger` cash IN/OUT + running balance + by-category summary + udhaar visibility (read-only from invoices). **FULL** (`accounting.full`): a **vertical-aware chart** (`chartSeedFor`; **a lab never gets COGS**, a pharmacy does) → an **IDEMPOTENT poster** turning the SAME outbox 21/22 emit into balanced `JournalEntry`/`JournalLine` (`@@unique(tenant,source,sourceRefId)`+P2002 → **re-emit never double-posts**) → **reports** (trial balance Σd=Σc, P&L, balance sheet, day book, AR aging) → **manual journal** + **opening balances** → **fiscal period** create+**close** locks it. NEW migration `…190000` (5 tables + 4 enums; `apply_tenant_rls`, T1/T2+WITH-CHECK+fail-closed proven). Pure `@mp/shared/accounts`. API `AccountsModule` (mode-flag 404 FIRST then perm). Web EN+UR `/accounts` (LITE one screen; FULL Reports/Chart/Journal/Periods + animated P&L chart). Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 448/448 + db 114/114 (+6 accounts-isolation) + i18n 19/19.
- **Current build-step:** **25 — Phase 2: Home Collection (Lab).** Read `specs/25-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/25-home-collection.md`
- **Branch state:** `feat/24-admin-dashboard` complete (awaiting controller merge to `staging`), stacked on `feat/23-accounts-finance` → `feat/22-doctor-commission` → `feat/21-billing-cashier` → `feat/20-pharmacy-pos` → `feat/19-laboratory` → `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **24 — Admin Dashboard & Owner Control** ✅ 2026-07-05 — the owner's daily cockpit: a **READ-MOSTLY aggregation over 19–23** whose **card set adapts to enabled flags** (pharmacy→sales/stock-loss/expiry, no lab/doctor; lab→TAT/volume/pending-approvals, no pharmacy; clinic→footfall/doctors+commission). **summary** (sales by source, collections vs pending, udhaar, gross profit = sales−pharmacy-COGS, cash position — full=Σ CASH/BANK journals, lite=cash-ledger balance) · **alerts** (stock-loss value from DAMAGE/EXPIRE/−ADJUST×cost, low-stock, cashier variance, overdue AR ≥30d, pending lab approvals, audit highlights, expiry stub till 28) · **activity** (footfall, doctor productivity+commission, lab volume+avg TAT, top medicines). Aggregates **cached 30 s** (perf); dashboard OWNS no source data (only writes `dashboard_prefs`). NEW migration `…200000` (1 table + `apply_tenant_rls`, T1/T2+WITH-CHECK+fail-closed proven `dashboard-isolation`). **Always present**, base `reports.view`; **sensitive cards need extra perm** (cash→`accounts.view`, audit→`audit.view`) per caller. No catalog change. Pure `@mp/shared/dashboard` (`DASHBOARD_CARDS`/`visibleCards`, `salesBySource`/`stockLossValue`/`shiftVarianceAlerts`/`overdueTotal`/`averageTat`/`topBy`/`grossProfit`). API `DashboardModule`: `/dashboard/summary`,`/alerts`,`/activity`,`/export`(CSV),`/prefs`(GET+PUT). Web EN+UR `/dashboard` (Simple=KPIs+alerts strip w/ drill links; Pro adds grid + **dynamically-imported animated sales chart** + activity tables + export; date/branch filter). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/dashboard` route), jest api 474/474 (+26) + db 117/117 (+3 dashboard-isolation) + i18n parity. Smoke: pharmacy→no lab/doctor cards, lab→no pharmacy cards, MANAGER→no cash/audit cards, NURSE→403; numbers reconcile w/ 21/23.
- **23 — Accounts & Finance (Lite / Full)** ✅ 2026-07-05 — the books in two flag-chosen, vertical-aware modes. **LITE** (`accounting.lite`, default-on): `CashLedger` cash IN/OUT + running balance + by-category period summary + udhaar visibility (read-only from invoices), no double-entry. **FULL** (`accounting.full`): a **vertical-aware chart** (`chartSeedFor` — base always; pharmacy Inventory/COGS/Sales, lab Lab Income, clinic Consult Income, commission expense/payable; **a lab never gets COGS**) → an **IDEMPOTENT poster** turning the SAME outbox 21/22 emit into balanced `JournalEntry`/`JournalLine` (`@@unique(tenant,source,sourceRefId)`+P2002-catch → **re-emit never double-posts**; missing code lazily materialised; `postedAt` marks consumed) → **reports** (pure `@mp/shared`: trial balance Σd=Σc, P&L, balance sheet Assets=Liab+Equity+RetEarn, day book, AR aging) fresh after auto-post → **manual journal** (unbalanced→400, closed period→400) + **opening balances** (→ Opening Equity, idempotent) → **fiscal period** create+**close** locks it. NEW migration `…190000` (5 tables + 4 enums; all `apply_tenant_rls`, T1/T2+WITH-CHECK+fail-closed proven). Perms/flags already in catalogs (no change). Pure `@mp/shared/accounts`. API `AccountsModule` (**mode-flag 404 FIRST then perm**; full-only→404 when full off): `/accounts/mode`,`/cash`,`/chart`,`/seed`,`/journal`,`/opening`,`/reports`,`/ledger`,`/periods`(+close). Web EN+UR `/accounts` (mode auto-detected: LITE one screen; FULL Reports/Chart/Journal/Periods + **animated P&L chart**). Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 448/448 (+33) + db 114/114 (+6 accounts-isolation) + i18n 19/19. Vertical smoke: lab chart no COGS, pharmacy→lite, full→full w/ COGS, both-flags-off→404, ADMIN (no perm)→403, re-emit posts ONE journal.
- **22 — Doctor Commission & Payouts** ✅ 2026-07-05 — per-doctor, per-tenant-configurable earnings: `CommissionConfig` (SALARY/PER_PATIENT/PERCENT(consult|lab|pharmacy)/HYBRID/REFERRAL) w/ **effective-date rate history** → a **sweep** assembling the period's real activity READ-ONLY from encounters(17)/lab(19)/pharmacy(20)/**billing revenue**(21 PAID+PARTIAL) into **idempotent `CommissionEntry`** (each `sourceRefId` embeds its basis; `@@unique(tenant,doctor,sourceType,sourceRefId)` → recompute never double-credits) → **report** grouped by source + **doctor self-view** `/commission/my` (flag-only, matches admin) → **payout** (DRAFT→APPROVED→PAID; on PAID posts a **balanced expense** `AccountingEvent` to 23, idempotent `commission:<id>` outbox, `journalRef` set). **Allowed bases DERIVED from enabled verticals** (no extra table; disallowed rate→403). NEW migration `…180000` (3 tables + `CommissionType`/`CommSource`/`PayoutStatus`; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Perm `commission.manage` + flag `clinic.commission` already in catalogs (no change). Pure `@mp/shared/commission` (`commissionEntriesFor`, `periodKeyOf`/`periodRange`, `allowedBasesFor`, `buildCommissionLines`). API `CommissionModule` (**flag 404 FIRST then perm**): `/commission/configs`,`/doctors`,`/recompute`,`/report`,`/my`,`/payouts`(+approve/pay). Web EN+UR `/commission` (admin Config/Payouts/Report + **animated breakdown chart**; doctor My-Earnings; role auto-detected). Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 415/415 (+28) + db 108/108 (+5 commission-isolation) + i18n 19/19 parity. Vertical smoke: Lab-only→404, clinic rejects lab rate/allows consult, DOCTOR→403 admin but 200 `/my`.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
