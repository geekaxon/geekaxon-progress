> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty, an infra problem is not code-fixable, **or the spec itself mandates a halt (step 48 §6 does — obey it).**

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS **becoming a healthcare PLATFORM/marketplace** · first tenant Ganatra Clinic = Full suite; 4–5 tenants incl. a standalone pharmacy + standalone lab at launch · built by AboveNext)
- **Phase:** 1–39 launch scope COMPLETE · **Phase 4 (40–44) COMPLETE** (AppShell + session, staging demo seed, branded entrances, auth/dashboard polish, design-system consistency — merged to staging). Now entering **PHASE 5 — PLATFORM & MARKETPLACE (45–53)**: design v2 (Inter/surfaces/theming/skeletons) → platform identity (one account across tenants — the most safety-critical step) → marketplace discovery/booking → platform settlement & commission (**MANDATORY OWNER HALT after 48**) → field pools → vendor SaaS console → subscriptions & AI modes → mobile app experience → tenant addressing. Specs 45–53 authored + companion **`specs/45-53-CODEREF.md`** (read it each step; re-read live files — its structure notes are a snapshot).
- **SaaS model:** every capability is a feature flag bundled into vertical presets with per-tenant overrides; **51 adds subscription-package CEILINGS above presets.** Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag. **RLS is sacred: cross-tenant assembly happens ONLY via per-tenant scoped reads under platform scope — the "Ganatra test" (no tenant ever sees another tenant's records/relationships) is a hard gate from 46 onward.**
- **Last completed build-step:** **47 — Marketplace Discovery & Booking (`feature/47-marketplace-discovery`).** ✅ DONE (2026-07-11) — the foodpanda storefront OVER the platform (46): browse every clinic/lab/pharmacy + book/order from any. NEW published-copy tables `MarketplaceListing` + `MarketplaceDoctor`/`Test`/`Product` (projections; discovery reads ONLY these, never live tables) + a THIRD RLS helper `apply_marketplace_rls` (tenant reads+writes OWN; platform scope reads ALL — discovery). Additive provenance: `ApptSource += MARKETPLACE`, new `OrderSource` on `LabOrder`/`OnlineOrder`. NestJS `marketplace` module: tenant publication (Settings→Marketplace, capability-aware sync + toggle + publish), platform-scoped discovery reads (public `@Public` `/marketplace/discover` + `/tenant/:slug`), and the booking ORCHESTRATOR (`/platform/marketplace/book/{appointment,lab,pharmacy}`) that link-if-firsts (find-or-create tenant Patient by phone → `ensureMarketplaceLink`) then calls the SAME tenant services (15/19/25/34) under `runWithTenant` stamped `source:MARKETPLACE` — forks nothing (added optional `source`/`patientId` seams only). Web: patient Browse tab in `(platform)`, public `/discover` + `/discover/[slug]` (server shell + light island, ≥budget), tenant Settings→Marketplace island; EN/UR parity. Seed extended: 2 standalone tenants (pharmacy + lab) + Ganatra listing published. HARD GATES green — DB marketplace-isolation (9, Ganatra floor for discovery: tenant sees only own listing; platform reads all; tenant-owned writes; platform can't forge a listing) extends 46; API booking spec (10: link-if-first + reuse, source stamped, publication/flag negatives, PATIENT-only, discovery flag-gating). Gates: typecheck/lint+drift/test (db 221 +9 · api 904 +10 · i18n parity 19)/build (web `/discover` 202 KB < 250 budget) all green. Full detail in PROGRESS-HISTORY.md.
- **Current build-step:** **48 — Platform Settlement & Commission.** ⏳ PENDING — spec: `specs/48-platform-settlement.md` (+ `specs/45-53-CODEREF.md`). Branch `feature/48-platform-settlement`. **⚠️ MANDATORY OWNER HALT after 48 (§6) — end `[HUMAN_REQUIRED]`.**
- **Next-step pointer:** → **`specs/48-platform-settlement.md`** (platform ledger/commission/settlement; the 47 booking `paymentMode` seam gains `PLATFORM_ONLINE`; **step 48 ENDS with `[HUMAN_REQUIRED]` + pointer → HOLD for owner verification**) → owner re-points to → 49 field-pools → 50 saas-console → 51 subscriptions-ai-modes → 52 mobile-app-experience → 53 tenant-addressing; order ends at 53 — beyond that, missing spec = hard stop. **The Ganatra test is a hard gate from 46 onward — 48 EXTENDS the 46/47 isolation suites (the platform ledger must never leak tenant relationships).**
- **Branch state:** `feat/01`…`feat/39` + `feature/40`…`feature/44` complete and merged to `staging` (post git-filter-repo history cleanup; remote re-added; staging deploy green). Phase 5 stacks `feature/45-design-system-v2` → `feature/46-platform-identity` onward.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **47 — Marketplace Discovery & Booking** ✅ (Phase 5) — foodpanda storefront over the platform: published-copy `MarketplaceListing`+doctor/test/product projections (discovery reads ONLY these) + 3rd RLS helper `apply_marketplace_rls` (tenant R/W own; platform reads all); `ApptSource+=MARKETPLACE`, new `OrderSource` on lab/online orders. `marketplace` API module: tenant publication (Settings→Marketplace), public `@Public` discovery reads, and the booking orchestrator (`/platform/marketplace/book/*`) that link-if-firsts then calls the SAME 15/19/25/34 services under `runWithTenant` stamped `source:MARKETPLACE` (forks nothing). Web: patient Browse tab + public `/discover(/[slug])` + Settings→Marketplace (EN/UR). Seed +2 standalone tenants (pharmacy+lab). Ganatra floor extended to discovery (db 9) + booking negatives/link-reuse (api 10); gates green (db 221 · api 904 · parity 19 · web `/discover` 202 KB).
- **46 — Platform Identity** ✅ (Phase 5) — one account ABOVE tenants: platform-scope RLS tables (`PlatformUser`/`Otp`/`RefreshToken`) + tenant-filtered link tables (`PatientLink`/`WorkerLink`/`ShareGrant`) via new `apply_platform_rls`/`apply_platform_link_rls` + `runWithPlatformScope`; `platform` API module (OTP login w/ `typ:'platform'` tokens + `PlatformAuthGuard`, merged cross-tenant timeline as a per-link `runWithTenant` orchestration, claim flow, audited consented `ShareGrant` doctor-view); web `mp.platform` session + `/login/platform` + `(platform)` timeline app (EN/UR parity). Ganatra test proven at DB (14) + API e2e (16); staff auth untouched (894 api tests green).
- **45 — Design System v2** ✅ (Phase 5) — fixed the "teal-on-teal wall": neutral grey/white light + true-dark-neutral surfaces with a 3-level elevation scale (teal → accent only), self-hosted subset Inter (serif retired from chrome, UR untouched), theme engine (Light/Dark/System + explicit `.light` class) with 6 AA-safe palette presets & AA-guarded custom accent, skeleton primitive set (card/table/chart/kpis/avatar) + EmptyStates across dashboard + all 4 entrances, additive `BrandingProfile` theming columns + `Settings→Personalization`. Contrast token-pair matrix extended (both modes); all gates green.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
