> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **38 — AI Suite: Feedback & Learning (`ai.suite`).** ✅ DONE/APPROVED (2026-07-05) — closes the AI loop **SAFELY**: a 👍/👎 (+ optional structured `correction`) on any **35** `AiSuggestion` feeds **per-tenant** memory/rules that tune the tenant's OWN later tasks — with **safety-critical (clinical) changes gated behind human review** and **no unsafe autonomous retraining**. NEW **AiFeedbackModule** (`@RequireFeature('ai.suite')` FIRST → RBAC: feedback/reads `ai.use`, rule review `ai.rules.review`; a **safety-critical rule review ALSO needs `ai.clinical`** — checked in the service → 403): `POST /ai/suggestions/:id/feedback` (records `AiFeedback` against the tenant's OWN 35 suggestion, verified via `AiGatewayService.getSuggestion`), `GET /ai/feedback/{memory,rules}`, `POST /ai/feedback/rules/:id/review`. **Learning derivation** (pure `@mp/shared/ai-feedback` `deriveLearning`): a correction's `domain` classifies it — **NON-safety** (terminology/mapping/ops) → upsert `AiMemory` whose `confidence` rises with **recurrence** (a one-off `0.4` stays BELOW the `0.7` activation floor; the 2nd occurrence `0.8` → ACTIVE); **SAFETY-CRITICAL** (`clinical`/`dosing`/`interaction`/`diagnosis`/`prescription`; **unknown/empty domain fail-safe→critical**) → propose an `AiRule` **born `PROPOSED`** that can **NEVER auto-activate** — a human reviews → `ACTIVE`/`REJECTED`. **36 integration:** `ClinicalAiService.rxRead` now folds in the tenant's **ACTIVE** medicine-name mappings (`applyMedicineMappings`) — relabels a learned scrawl→brand + records `mappedFrom`/`mappedByMemory`, but keeps `mustVerify:true` and changes **no safety flag** (assist-only; injected `@Optional()`). **OWNS 3 tables** `AiFeedback`/`AiMemory`/`AiRule` (+ enums `Rating`/`RuleStatus`), all tenant-scoped (RLS 02) — **no cross-tenant learning**. Migration `…320000`. NEW permission **`ai.rules.review`** granted additively to OWNER(scope)/ADMIN/DOCTOR/PHARMACIST — NO other grant change. **Flag REUSED (`ai.suite`) — NO catalog change.** NO web surface (backend suite; 36 rx.read + a future review UI call it). Gates green: turbo typecheck **27/27** + lint **15/15** (0 err; 1 pre-existing doctor-portal warn), build+test **23/23**, jest **api 853/853 [+29]** + **db 173/173 [+6 new `ai-feedback-isolation`]** + i18n 19/19 parity. Vertical smoke (e2e over the real graph + faked 35/clinical/feedback repos): no token→401; no-`ai.suite`→404 (feedback+memory+rules); feedback needs `ai.use`(RECEPTION→403); bare 👍/👎 recorded+audited (no learning w/o correction); unknown suggestion→404; bad rating/correction→400; **recurring non-safety mapping → 2nd time ACTIVE memory → next `rx.read` relabels `augmntn 625`→`Augmentin 625` (still `mustVerify`)**; INACTIVE (0.4) mapping NOT applied; **dosing correction → PROPOSED rule stays PROPOSED (never auto-active), no memory written**; rule review needs `ai.rules.review`(SALESMAN→403); admin activates→`ACTIVE`+audited→re-review 409; **clinical rule review on ai.suite-but-not-ai.clinical tenant→403**; unknown rule→404; **one tenant's memory never visible to another** (app-level; RLS proven in db).
- **Prior completed build-step:** **37 — AI Suite: Ops (`ai.ops`).** ✅ DONE/APPROVED (2026-07-05) — the five **advisory** operational AI capabilities, each a 35 task type + each a human acts: `predict.noshow`, `predict.demand` (→29 reorder), `predict.expiry`, `analytics.ask` (**GROUNDED** over READ-ONLY 21/23; never invents), `ops.chat` + a call-center voice **SCAFFOLD** (disabled hooks, deferred to 43). NEW `OpsAiModule` (`@RequireFeature('ai.ops')` FIRST → per-insight SOURCE flag [`clinic.appointments`/`pharmacy.inventory`] → `@RequirePermission('ai.use')`), each POST reading the tenant's OWN read-only aggregates → DETERMINISTIC `@mp/shared` floor → **35 `runAiTask`** → PENDING suggestion. **OWNS NO tables** (reuses 35 + own read repo). NEW pure `@mp/shared/ops-ai`; 5 task types added additively. **NO migration. Flags/permission REUSED.** Gates: turbo 27/27 + 15/15, jest api 824/824 (+28) + db 167/167 + i18n 19/19.
- **Current build-step:** **39 — AI Suite: Cost Control.** Read `specs/39-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/39-ai-cost-control.md`
- **Branch state:** `feat/38-ai-feedback-learning` complete (awaiting controller merge to `staging`), stacked on `feat/37-ai-suite-ops` → `feat/36-ai-suite-clinical` → `feat/35-ai-gateway` → `feat/34-online-pharmacy` → `feat/33-doctor-portal-ai` → `feat/32-patient-ai-assistant` → `feat/31-patient-app` → `feat/30-branch-transfer` → `feat/29-purchase-suppliers` → `feat/28-pharmacy-inventory` → `feat/27-sample-tracking` → `feat/26-phlebotomist-app` → `feat/25-home-collection` → `feat/24-admin-dashboard` → `feat/23-accounts-finance` → `feat/22-doctor-commission` → `feat/21-billing-cashier` → `feat/20-pharmacy-pos` → `feat/19-laboratory` → `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **38 — AI Suite: Feedback & Learning (`ai.suite`)** ✅ 2026-07-05 — closes the AI loop SAFELY: a 👍/👎 (+ optional structured `correction`) on any **35** `AiSuggestion` feeds **per-tenant** memory/rules that tune the tenant's OWN later tasks — safety-critical changes gated behind human review; no unsafe autonomous retraining. NEW `AiFeedbackModule` (`@RequireFeature('ai.suite')` FIRST → RBAC: feedback/reads `ai.use`, rule review new `ai.rules.review`; safety-critical rule review ALSO needs `ai.clinical`→403): `POST /ai/suggestions/:id/feedback` (records `AiFeedback` vs the tenant's OWN suggestion via `getSuggestion`), `GET /ai/feedback/{memory,rules}`, `POST /ai/feedback/rules/:id/review`. Pure `@mp/shared/ai-feedback` `deriveLearning`: NON-safety correction → `AiMemory` whose `confidence` rises with recurrence (one-off 0.4 < 0.7 floor; 2nd = 0.8 ACTIVE); SAFETY-CRITICAL (`clinical`/`dosing`/`interaction`/…; unknown/empty **fail-safe→critical**) → `AiRule` born `PROPOSED`, NEVER auto-active, human reviews→`ACTIVE`/`REJECTED`. **36 integration:** `rx.read` folds in ACTIVE medicine mappings (`applyMedicineMappings`: relabels scrawl→brand + `mappedFrom`/`mappedByMemory`, keeps `mustVerify`, no safety-flag change; `@Optional()`). **OWNS 3 tables** `AiFeedback`/`AiMemory`/`AiRule` (+ enums `Rating`/`RuleStatus`), tenant-scoped RLS — no cross-tenant learning. Migration `…320000`. NEW `ai.rules.review` granted OWNER(scope)/ADMIN/DOCTOR/PHARMACIST. **Flag REUSED (`ai.suite`).** NO web surface. Gates: turbo typecheck 27/27 + lint 15/15, build+test 23/23, jest api 853/853 (+29) + db 173/173 (+6 `ai-feedback-isolation`) + i18n 19/19. Smoke: no token→401, no-`ai.suite`→404, feedback needs `ai.use`(RECEPTION→403), bare 👍/👎 recorded+audited (no learning), unknown suggestion→404, bad rating/correction→400, recurring mapping→2nd ACTIVE→next rx.read relabels(still mustVerify), inactive(0.4) not applied, dosing→PROPOSED stays(no memory), review needs `ai.rules.review`(SALESMAN→403), admin activates→ACTIVE+audited→409 re-review, clinical review on ai.suite-not-ai.clinical→403, unknown rule→404, cross-tenant memory invisible.
- **37 — AI Suite: Ops (`ai.ops`)** ✅ 2026-07-05 — the five **advisory** operational AI capabilities, each a 35 task type + each a human acts: `predict.noshow`, `predict.demand` (→29 reorder), `predict.expiry`, `analytics.ask` (**GROUNDED** over READ-ONLY 21/23; never invents), `ops.chat` + a call-center voice **SCAFFOLD** (disabled hooks, deferred to 43). NEW `OpsAiModule` (`@RequireFeature('ai.ops')` FIRST → per-insight SOURCE flag in service [`clinic.appointments`/`pharmacy.inventory`; assistants none] → `@RequirePermission('ai.use')`), each POST reading the tenant's OWN read-only aggregates → DETERMINISTIC `@mp/shared` floor → **35 `runAiTask`** (`floor`→suggestion) → PENDING suggestion; human acts via the 35 decision surface. **OWNS NO tables** (reuses 35 + own read-only repo). NEW pure `@mp/shared/ops-ai`; 5 task types added additively. **NO migration. Flags/permission REUSED.** Gates: turbo 27/27 + 15/15, test 20/20, jest api 824/824 (+28) + db 167/167 + i18n 19/19. Smoke: no token→401, no-`ai.ops`→404, per-insight SOURCE flag, `ai.use`(RECEPTION→403), predict.noshow scored(flaky HIGH first)+PENDING+audited, predict.demand reorder(100)+skip-stocked, predict.expiry slow flagged/fast excluded, analytics.ask grounded(profit 90←150 DOWN)+empty→400, ops.chat matched+empty→400, call-center scaffold disabled+NO gateway task.
- **36 — AI Suite: Clinical (`ai.clinical`)** ✅ 2026-07-05 — the six **assist-only** clinical AI capabilities, each a 35 task type + each HUMAN-CONFIRMED: `rx.read`, `medicine.search`, `interaction.check` (advisory over REAL 14/18; never blocks), `lab.explain` (EN+UR+Roman), `result.flag` (pathologist SIGNS 19), `note.summarize` (doctor edits). NEW `ClinicalAiModule` (`@RequireFeature('ai.clinical')` FIRST → per-task MODULE flag [`pharmacy.aiReader`/`pharmacy.pos`/`clinic.consultation`/`lab.results`] → `@RequirePermission('ai.use')`): `POST /ai/clinical/{rx-read,…,note-summarize}`, each computing a DETERMINISTIC `@mp/shared` floor then routing through **35 `runAiTask`** (opt `floor`→suggestion `output.result`) → PENDING suggestion. **OWNS NO tables** (reuses 35 + own read repo). NEW pure `@mp/shared/clinical-ai`; 6 task types added additively. **NO migration. Flags REUSED. `ai.use` GRANTED additively to PHARMACIST/SALESMAN/PATHOLOGIST/LAB_TECH.** Gates: turbo 42/42 + 23/23, jest api 796/796 (+28) + db 167/167 + i18n 19/19.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
