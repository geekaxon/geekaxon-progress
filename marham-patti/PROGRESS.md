> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **31 — Phase 3: Patient App (PWA).** ✅ DONE/APPROVED (2026-07-05) — **opens Phase 3** — the patient's OWN phone-facing surface behind the `patient.app` flag (absent for tenants not offering a patient app → 404). A patient is a **`User` with role PATIENT** (03 OTP), linked to a **`Patient` record by phone**; the surface exposes ONLY **self + linked family** (14 guardian dependents + directed relations) — the ownership rule is ABSOLUTE, never widened to the tenant. NEW **PatientAppModule** (`@RequireFeature('patient.app')` 404 FIRST, then `@Roles(Role.PATIENT)` — role-gated, NOT permission-gated, since a patient holds ZERO RBAC perms; staff→403): `GET /me/profile` (self + family), `PATCH /me/profile`, `GET/POST /me/family` (add a dependent = a `Patient guardianId=self` + a `PatientRelation`); `GET /me/doctors`, `GET/POST /me/appointments` (**book (15)** = create Appointment source BOOKED → lands in the staff queue); `GET /me/reports` (lab orders w/ a `viewable` flag) + `GET /me/reports/:id` (**visible ONLY once the pathologist signs it — APPROVED/DELIVERED**; a draft or another patient's order → indistinguishable 404, privacy 18/19); `GET /me/prescriptions` (patient-visible only — `sendToPatient=true`) + `POST /me/prescriptions/upload` (an image key → the tenant pharmacy ingests via its existing `from-prescription`); `GET /me/service-areas`, `GET /me/lab-tests`, `GET/POST /me/home-collections` (**book a home test (25)** = create LabOrder ORDERED + tests + HomeCollection BOOKED, charge from the ServiceArea); `GET /me/notifications` (by the patient's phone), `GET /me/refill-reminders` (computed from dispensed courses); `POST /me/devices` (register a PWA device for push, idempotent). It **OWNS only 2 tiny tables** (`patient_devices`, `patient_uploads`); patients+relations (14) / appointments+doctors (15) / lab (19) / prescriptions (18) / home collections+areas (25) / notification logs (09) are read + (for a booking) additively created through its OWN tenant-scoped repo (like 28/29/30 borrow tables), **never importing the owning module**. Pure `@mp/shared/patient-app` (`familyScopeIds`/`isInFamilyScope` — the absolute self/family scope; `isReportVisibleToPatient`; `computeRefillReminders`/`refillDueDateIso`/`isRefillDue`; upload/device vocabulary). NEW migration `…270000` (2 tables; `apply_tenant_rls` both, T1/T2 + WITH-CHECK + fail-closed proven in a new `patient-app-isolation` pglite spec — a device/upload NEVER bleeds across tenants). **Flag REUSED (`patient.app` pre-existed in the catalog) — NO catalog change, NO new permission (role-gated).** Web EN+**UR-first** `/patient` PWA (tabbed: Home = greeting + refill nudges + quick actions; Book = family switcher + appointment + home-test; Reports = list + printable viewer, approved-only; Prescriptions = visible list + upload; Family = list + add member; Alerts = notifications; tenant-branded 07, big buttons, `noindex`, manifest shortcut, degrades to a clean sign-in/no-access). Gates green: typecheck 26/26, lint 15/15 (0 warn), build 15/15 (`/patient` 3.62 kB / 142 kB), jest **api 632/632 [+27]** + **db 146/146 [+5 patient-app-isolation]** + i18n 19/19 parity (UR-first). Vertical smoke (via e2e): no token→401; lab tenant (no `patient.app`)→404; staff role→403; self+family resolved by phone; a fresh OTP login (no Patient) sees empty self then a booking auto-provisions it; book appointment→WAITING/BOOKED in the queue; book for a stranger patientId→404; report viewable ONLY when signed (draft & another patient's→404); prescriptions filtered to `sendToPatient`; upload→captured for pharmacy; home test→LabOrder ORDERED + HomeCollection BOOKED (charge from area), no-valid-test→400; notifications scoped to the patient's phone (stranger's not leaked); refill reminder computed; device register idempotent per platform.
- **Prior completed build-step:** **30 — Phase 2: Branch Transfer & Multi-Branch Stock.** ✅ DONE/APPROVED (2026-07-05) — **completes Phase 2** — a tracked, two-sided, audited stock movement between two branches of ONE tenant behind `core.multiBranch` (single-branch/clinic/lab→404); never a silent edit — out-of-source + into-destination, both logged. NEW `TransferModule` (`core.multiBranch` 404 THEN `inventory.manage` REUSED): request (a plan) → dispatch (source batch+aggregate decrement via **TRANSFER − (20)** → in-transit) → receive (`qtyReceived` lands as a NEW mirrored **Batch (28)** at dest via **TRANSFER +**; received≠dispatched → tracked variance MUST carry a reason else 400) → cancel (REQUESTED only); branch-wise stock; idempotent central-warehouse (a special Branch `is_warehouse`). OWNS only `stock_transfers`/`transfer_items`; 28/20/medicines/branches read+moved via its OWN repo, never importing 20/28. Pure `@mp/shared/transfers`. Migration `…260000` (2 tables + `TransferStatus`; additive `ALTER branches ADD is_warehouse`; proven `branch-transfer-isolation`). Flag+perm REUSED. Web EN+UR `/transfer`. Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 605/605 + db 141/141 + i18n 19/19.
- **Current build-step:** **32 — Phase 3: Patient AI Assistant.** Read `specs/32-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/32-patient-ai-assistant.md`
- **Branch state:** `feat/31-patient-app` complete (awaiting controller merge to `staging`), stacked on `feat/30-branch-transfer` → `feat/29-purchase-suppliers` → `feat/28-pharmacy-inventory` → `feat/27-sample-tracking` → `feat/26-phlebotomist-app` → `feat/25-home-collection` → `feat/24-admin-dashboard` → `feat/23-accounts-finance` → `feat/22-doctor-commission` → `feat/21-billing-cashier` → `feat/20-pharmacy-pos` → `feat/19-laboratory` → `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **31 — Patient App (PWA)** ✅ 2026-07-05 — **opens Phase 3** — the patient's OWN phone-facing surface behind `patient.app` (absent→404). A patient = a `User` role PATIENT (03 OTP) linked to a `Patient` by phone; exposes ONLY **self + linked family** (14 guardian dependents + relations) — ownership ABSOLUTE, never widened. NEW `PatientAppModule` (`@RequireFeature('patient.app')` 404 FIRST THEN `@Roles(Role.PATIENT)` — role-gated, NOT permission-gated; staff→403): `/me/profile`(+PATCH), `/me/family`(+add dependent=Patient guardianId+PatientRelation), `/me/doctors`, `/me/appointments`(+book 15→BOOKED in staff queue), `/me/reports`(+`/:id`: visible ONLY once the pathologist signs — APPROVED/DELIVERED; draft or another patient→indistinguishable 404, privacy 18/19), `/me/prescriptions`(sendToPatient-only)+`/upload`(image key→pharmacy from-prescription), `/me/service-areas`+`/me/lab-tests`+`/me/home-collections`(+book 25→LabOrder ORDERED+HomeCollection BOOKED, charge from area), `/me/notifications`(by phone), `/me/refill-reminders`(computed), `POST /me/devices`(idempotent PWA push). OWNS only 2 tables (`patient_devices`/`patient_uploads`); 14/15/19/18/25/09 read+(booking)additively-created via its OWN tenant-scoped repo, never importing the owning module. Pure `@mp/shared/patient-app` (`familyScopeIds`/`isInFamilyScope`; `isReportVisibleToPatient`; `computeRefillReminders`; upload/device vocab). NEW migration `…270000` (2 tables; `apply_tenant_rls` both, proven `patient-app-isolation`). **Flag REUSED (`patient.app` pre-existed) — NO catalog change, NO new permission (role-gated).** Web EN+**UR-first** `/patient` PWA (Home/Book/Reports/Prescriptions/Family/Alerts; tenant-branded, big buttons, `noindex`, manifest shortcut). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/patient` 3.62 kB / 142 kB), jest api 632/632 (+27) + db 146/146 (+5 patient-app-isolation) + i18n 19/19 (UR-first). Smoke (e2e): no token→401, lab tenant→404, staff→403, self+family by phone, fresh login (no Patient)→empty then booking auto-provisions, book→queue, book for stranger→404, report viewable only when signed (draft & other→404), rx filtered to sendToPatient, upload→captured for pharmacy, home test→ORDERED+BOOKED (no-valid-test→400), notifications scoped to phone (stranger not leaked), refill computed, device idempotent.
- **30 — Branch Transfer & Multi-Branch Stock** ✅ 2026-07-05 — **completes Phase 2** — a tracked, two-sided, audited stock move between two branches of ONE tenant behind `core.multiBranch` (single-branch/clinic/lab→404). Never a silent edit: out-of-source + into-destination, both logged. NEW `TransferModule` (`@RequireFeature('core.multiBranch')` 404 THEN `inventory.manage` REUSED; SALESMAN→403): `POST /transfers` (request = plan: ≥2 branches, source **Batch (28)** exists w/ on-hand, no move yet); `/:id/dispatch` (REQUESTED→DISPATCHED: decrement source batch+aggregate via logged **TRANSFER − (20)** → in-transit, re-validates all-or-nothing); `/:id/receive` (DISPATCHED→RECEIVED: `qtyReceived` lands as a NEW mirrored **Batch (28)** at dest via **TRANSFER +**; received≠dispatched → tracked **variance** MUST carry a **reason** else 400, validated per-line before any write); `/:id/cancel` (REQUESTED only); `GET /transfers`/`:id`, `/stock?branchId=` (**branch-wise stock**), `/branches`, **`POST /warehouse`** (idempotent — a **central warehouse = special Branch** `is_warehouse`; hub fulfils via an ordinary transfer). OWNS only 2 tables (`stock_transfers`/`transfer_items`); 28 batches / 20 stock+movements / medicines / branches read+moved via its OWN repo (like 28/29), never importing 20/28. Pure `@mp/shared/transfers` (`TRANSFER_STATUS_FLOW`/`canDispatch/Receive/CancelTransfer`; `transferVariance`/`needsTransferReason`/`transferQty`). NEW migration `…260000` (2 tables + enum `TransferStatus`; additive `ALTER branches ADD is_warehouse`; `apply_tenant_rls` both, proven `branch-transfer-isolation` — cross-branch never cross-tenant). **Flag + perm REUSED — no catalog change.** Web EN+UR `/transfer` (tabbed: New transfer + warehouse, Transfers dispatch/cancel/receive-with-variance+reason, Branch stock). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/transfer` 3.05 kB), jest api 605/605 (+17) + db 141/141 (+4 branch-transfer-isolation) + i18n 19/19. Smoke: lab→404, SALESMAN→403, warehouse idempotent, request 25/40 (no move)→dispatch 40→15+TRANSFER − w/ batch+dest in-transit→receive 25 as mirrored batch+TRANSFER + (15+25=40 conserved, variance 0), receive 18/20 no reason→400 then reason→variance −2+only 18 landed, warehouse→branch 100→70+30, stock A=40/B=0, request 50/10→400, single-branch→400, receive-before-dispatch→400, cancel REQUESTED→dispatch400 (stock unmoved).
- **29 — Purchases & Suppliers** ✅ 2026-07-05 — procurement → stock in → payables OVER the **28 inventory** behind the SAME `pharmacy.inventory` flag (present where a tenant holds purchasable stock; clinic/lab-results-only→404). Stock enters on **PO + credit**; **GRN is the ONE normal way batches enter (28)** — source of truth for cost. NEW `PurchaseModule` (`@RequireFeature('pharmacy.inventory')` 404 THEN `purchase.manage`; ADMIN/MANAGER/OWNER, not PHARMACIST): suppliers CRUD + `GET /suppliers/:id/ledger` (opening + goods − payments + **FIFO aging** reusing accounts `arAging`); PO `GET/POST /orders`/`:id`/`send`/`cancel` (DRAFT→SENT→PARTIAL→RECEIVED/CANCELLED); **`POST /grn`** (each line CREATES a **Batch (28)** + PURCHASE **StockMovement (20)** via the SAME compensating write, fills GRN item `batchId`, accumulates PO `receivedQty`→PARTIAL/RECEIVED via `poStatusAfterReceipt`, posts **Dr Inventory/Cr AP** to 23 via idempotent `accounting_events` source PURCHASE — full mode; **lite records + degrades**); `GET /grns`/`:id`; **`POST /payments`** (settle AP: Dr AP/Cr Cash|Bank + `journalRef`; CREDIT rejected = internal return marker); **`POST /returns`** (against a GRN line → RETURN movement + batch decrement + supplier credit note (reused SupplierPayment method CREDIT) + Dr AP/Cr Inventory; over-return→400); `GET /reorder/suggestions` (**28 on-hand + 30-day velocity** `suggestReorder`); `GET /price-history` (`priceTrend`). OWNS only its 6 tables (`suppliers`/`purchase_orders`/`po_items`/`grns`/`grn_items`/`supplier_payments`); 28 batches / 20 stock+movements / medicines read+moved via its OWN repo (like 28 borrows 20), never importing 20/28. Pure `@mp/shared/purchase` (`PO_STATUS_FLOW`/`canTransitionPO`/`canReceivePO`/`poStatusAfterReceipt`; `lineCost`/`poTotal`; `supplierBalance`/`supplierAging`; `priceTrend`; `suggestReorder` LEAD=7/COVER=30). NEW migration `…250000` (6 tables + enum `POStatus`; `PayMethod` reused; `apply_tenant_rls` all 6, T1/T2+WITH-CHECK+fail-closed proven `purchase-suppliers-isolation`). **Flag + perm REUSED — no catalog change.** Web EN+UR `/purchase` (tabbed: Suppliers + ledger/aging + payment, Orders build→send/cancel, Receive GRN + per-line return, Reorder). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/purchase` 3.53 kB), jest api 588/588 (+25) + db 137/137 (+4 purchase-suppliers-isolation) + i18n 19/19. Smoke: lab→404, PHARMACIST→403, PO 320→send→GRN full→batch 40+aggregate 40+PURCHASE mv+PO RECEIVED+AP posted (one idempotent event), partial→PARTIAL, lite→posted=false/no event, pay 200→Dr AP/Cr Cash+journalRef+balance 320→120, CREDIT pay→400, price history min8/max11, reorder→only low fast-mover, return 10→40→30+Dr AP80/Cr Inventory80+over-return400, cancel→send400 & receive-cancelled400.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
