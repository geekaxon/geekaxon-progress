> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** Launch scope **1–39 COMPLETE** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39) and deployed to staging (login verified end-to-end incl. 2FA enrollment). Now in **PHASE 4 — App Shell, Demo Data, Entrances & Design System (40–44)**: the usability + polish scope that turns the 39 modules into ONE navigable, populated, premium product. Specs `40`–`44` are authored and authoritative; companion code-reference **`specs/40-44-CODEREF.md`** (exact paths/seams/do-not-break — read it each step; re-read live files before editing, its line numbers are a snapshot).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **42 — Branded App Entrances.** ✅ DONE/APPROVED (2026-07-07) — foodpanda-style separate installable "apps" per audience over ONE codebase/backend/auth. `/` is now the **entrance chooser** (server component, product mark + 4 cards Staff/Patient/Rider/Phlebotomist, EN+UR + RTL, brand-tokened, 166 B/106 kB — replaces the step-01 swatch placeholder). **Four scoped PWA manifests** (`manifest-{staff,patient,rider,phlebo}.webmanifest`, own name/id/start_url/scope/icon) linked per audience (`(app)`/`(patient)` layouts + rider/phleb page metadata; root+login keep the flagship) so installing from `/rider` installs "Marham Patti Rider" into the `/rider` scope — verified per-scope `<link rel=manifest>`. Four brand-derived install **icons**. Light **audience chrome** (`EntranceChrome`, path-aware: brand mark + audience pill + language + theme + logout, no staff sidebar) wraps patient/rider/phleb; `poweredBy` honoured. **Dedicated rider/phleb logins** (`(auth)/login/rider|phlebotomist`) reuse 03 staff-auth (email+pw), audience-preset; role/entrance **mismatch → correct role home with a notice** (never blocked/blank). `loginPathForPath` routes field audiences to their own doors. Fixed the `.mp-shell` grid collision on field/patient pages (→ `.mp-entrance-page`). ONE SW (v2, precaches the new manifests+icons). i18n `entrance` block (18 keys ×EN+UR). Gates green: typecheck 42/42 · lint 42/42 · next build 45/45 (URLs unchanged, `/lh-bloated` intact) · db 198/198 · i18n 19/19 (parity @1438) · runtime smoke curl-200 on all new surfaces. Full detail in PROGRESS-HISTORY.md.
- **Current build-step:** **43 — Auth UI Polish.** ⏳ PENDING — spec: `specs/43-auth-ui-polish.md` (+ `specs/40-44-CODEREF.md` §A dark-mode contrast seam). Branch `feature/43-auth-ui-polish`.
- **Next-step pointer:** → **`specs/43-auth-ui-polish.md`** (then 44 design-system; the specced build order currently ends at 44 — beyond that, a missing spec is a hard stop `[HUMAN_REQUIRED]`, never a cue to improvise).
- **Branch state:** launch branches `feat/01`…`feat/39` merged to `staging` (deploy green). Phase 4: `feature/40-app-shell-session` + `feature/41-demo-data-seed` + `feature/42-branded-entrances` COMPLETE → then `feature/43-auth-ui-polish` → `feature/44-design-system`.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **42 — Branded App Entrances** ✅ 2026-07-07 (FEATURE) — foodpanda-style installable "apps" per audience over ONE codebase: `/` entrance chooser (server, 4 cards, EN+UR+RTL, 166 B), four scoped PWA manifests+icons linked per audience (`/rider` install = "Marham Patti Rider" → `/rider` scope), light path-aware `EntranceChrome` (no staff sidebar, poweredBy), dedicated rider/phleb logins reusing 03 staff-auth with role-mismatch→correct-home notice, `.mp-shell` grid-collision fix. Gates: typecheck 42/42, lint 42/42, next build 45/45 (URLs unchanged), db 198/198, i18n 19/19 (parity @1438), curl-200 smoke on all new surfaces.
- **41 — Staging Demo Data Seed** ✅ 2026-07-07 (FEATURE) — hard-gated (`APP_ENV=staging && SEED_DEMO=1`), idempotent `seed:demo` populating Ganatra (Full) with realistic Pakistani demo data via a pure in-memory plan whose invariants (trial balance Σdr==Σcr, stock==Σmovements, FEFO, shift variance) are CI-verified; config seam `SEED_DEMO?/SEED_DEMO_PASSWORD?/MFA_STAGING_RELAX?` + staging-only MFA relaxation (production unchanged, fail-closed). Gates: typecheck 27/27, lint 15/15, db 198/198 (+20), api 874/874 (+5); NEVER in deploy.sh.
- **40 — App Shell + Session Layer** ✅ 2026-07-07 (FEATURE) — turned 39 disconnected islands into ONE navigable app: `<AppShell>` (role+flag-aware nav filtered against `GET /auth/me`, flag-first→permission→role, zero dead links), centralized bearer+tenant+silent-refresh API client, `(app)/(patient)/(field)/(auth)` route groups (URLs unchanged), post-login role-home redirect + route guard + logout, all 34 per-component auth copies removed. Gates: typecheck 27/27, lint 15/15, api 869/869 (+2), db 178/178, ui 38/38, i18n 19/19, next build 43/43.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
