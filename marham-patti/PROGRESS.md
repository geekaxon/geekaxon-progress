> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **36 — AI Suite: Clinical (`ai.clinical`).** ✅ DONE/APPROVED (2026-07-05) — the six **assist-only** clinical AI capabilities, each a 35 task type, each **HUMAN-CONFIRMED**: `rx.read` (prescription image→structured cart/rx lines, salesman/pharmacist verifies), `medicine.search` (intent/generic/alternative over the REAL catalog), `interaction.check` (drug-drug + drug-allergy advisory over the patient's REAL meds 18 + allergies 14; NEVER blocks), `lab.explain` (plain-language EN+UR+Roman report read-out, informational), `result.flag` (abnormal-value flagging — the **pathologist SIGNS** 19; AI never finalises), `note.summarize` (a draft the doctor edits 17/33). NEW **ClinicalAiModule** (`@RequireFeature('ai.clinical')` FIRST → per-task MODULE flag checked in the service [`rx.read`→`pharmacy.aiReader`, `medicine.search`→`pharmacy.pos`, `interaction.check`/`note.summarize`→`clinic.consultation`, `lab.explain`/`result.flag`→`lab.results`] → `@RequirePermission('ai.use')`): `POST /ai/clinical/{rx-read,medicine-search,interaction-check,lab-explain,result-flag,note-summarize}`. Each: gate surface → compute the DETERMINISTIC structured floor (pure `@mp/shared`) from real data/payload → route through **35 `AiGatewayService.runAiTask`** (LOGGED + BUDGETED; new optional `floor` attaches the structured result to the suggestion `output.result`) → return a PENDING suggestion the calling surface renders; the human decides via the **35** `POST /ai/suggestions/:id/decision` (only APPROVED/EDITED applies — `canApplySuggestion`). **OWNS NO tables** — REUSES 35's `AiTask`/`AiSuggestion` + its OWN tenant-scoped read repo (allergies 14 / current-meds 18 / medicine catalog 18/20). NEW pure `@mp/shared/clinical-ai` (`CLINICAL_TASKS` + `surfaceFlagFor`, `smartMedicineSearch`/`explainLabReport`/`flagAbnormalResults` + `intentGenericsFor` + `MEDICINE_SEARCH`/`LAB_EXPLAIN`/`RESULT_FLAG` policies; **rx.read/interaction.check/note.summarize REUSE the 33/34 composers `composePrescriptionRead`/`crossCheckMedications`/`summarizeClinicalNote`**); 6 clinical task types added additively to `AI_TASK_TYPES`+`DEFAULT_TASK_TIER` (quality-first: `interaction.check`/`result.flag`→QUALITY). **NO migration (no new tables/enums). Flags REUSED (`ai.clinical`+`pharmacy.aiReader`+`lab.results`+`clinic.consultation` all pre-existed; `full` preset) — NO catalog change. Permission REUSED — `ai.use` GRANTED additively to PHARMACIST/SALESMAN/PATHOLOGIST/LAB_TECH (clinical staff run the AI).** NO web surface (backend suite; existing POS/lab/consult surfaces call it). Gates green: turbo typecheck+lint **42/42** (0 err; 1 pre-existing doctor-portal warn), test+build **23/23**, jest **api 796/796 [+28]** + **db 167/167** (isolation inherited — 36 owns no tables; reads go through `runWithTenant` RLS) + i18n 19/19 parity. Vertical smoke (e2e over the real graph + REAL 35 mock provider): no token→401; no-`ai.clinical`→404; per-surface module flag (T_PARTIAL `ai.clinical` on but `pharmacy.aiReader`/`lab.results` off → rx-read/lab-explain 404, note-summarize 200); run needs `ai.use`(RECEPTION→403); rx.read→structured must-verify + DONE task + PENDING suggestion carrying the floor + audited; PENDING not applicable→approve→applicable; medicine.search brand+alternative(2-pass generic widen)+intent; interaction.check uses real meds+allergies→DRUG_DRUG HIGH + DRUG_ALLERGY + highRisk, unknown patient→404, empty→400; lab.explain EN/UR/Roman + invalid flag→400; result.flag CRITICAL-first, NORMAL excluded, pathologistSigns; note.summarize draftOnly + empty→400.
- **Prior completed build-step:** **35 — AI Suite: AI Gateway (`@mp/ai`).** ✅ DONE/APPROVED (2026-07-05) — THE provider-agnostic, **assist-only-by-construction** primitive every AI surface routes through, behind `ai.suite` (absent→404). **HITL: `runAiTask` only returns a PENDING `AiSuggestion`; applied ONLY after a human APPROVED/EDITED (`canApplySuggestion`). Per-tenant keys are SECRETS in the env/secret store, NEVER the DB.** `AiModule`: `POST /ai/tasks`(Idempotency-Key→dedupe), `/ai/suggestions/:id/decision`, `GET /ai/suggestions|tasks|budget|config`. `runAiTask`: route (quality-first) → budget BLOCK over cap (FAILED task; spend untouched) → provider → persist `AiTask`(tokens/cost/latency) → accrue `spent` → PENDING `AiSuggestion`. OWNS 4 tables (no key column). Migration `…310000`. Gates: api 768/768 + db 167/167 (+6 isolation) + i18n 19/19.
- **Current build-step:** **37 — AI Suite: Ops (`ai.ops`).** Read `specs/37-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/37-ai-suite-ops.md`
- **Branch state:** `feat/36-ai-suite-clinical` complete (awaiting controller merge to `staging`), stacked on `feat/35-ai-gateway` → `feat/34-online-pharmacy` → `feat/33-doctor-portal-ai` → `feat/32-patient-ai-assistant` → `feat/31-patient-app` → `feat/30-branch-transfer` → `feat/29-purchase-suppliers` → `feat/28-pharmacy-inventory` → `feat/27-sample-tracking` → `feat/26-phlebotomist-app` → `feat/25-home-collection` → `feat/24-admin-dashboard` → `feat/23-accounts-finance` → `feat/22-doctor-commission` → `feat/21-billing-cashier` → `feat/20-pharmacy-pos` → `feat/19-laboratory` → `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **36 — AI Suite: Clinical (`ai.clinical`)** ✅ 2026-07-05 — the six **assist-only** clinical AI capabilities, each a 35 task type + each HUMAN-CONFIRMED: `rx.read`, `medicine.search`, `interaction.check` (advisory over REAL 14/18; never blocks), `lab.explain` (EN+UR+Roman, informational), `result.flag` (pathologist SIGNS 19), `note.summarize` (doctor edits). NEW `ClinicalAiModule` (`@RequireFeature('ai.clinical')` FIRST → per-task MODULE flag in service [`pharmacy.aiReader`/`pharmacy.pos`/`clinic.consultation`/`lab.results`] → `@RequirePermission('ai.use')`): `POST /ai/clinical/{rx-read,medicine-search,interaction-check,lab-explain,result-flag,note-summarize}`, each computing a DETERMINISTIC `@mp/shared` floor then routing through **35 `runAiTask`** (LOGGED+BUDGETED; new opt `floor`→suggestion `output.result`) → PENDING suggestion; human decides via the 35 decision surface. **OWNS NO tables** (reuses 35 `AiTask`/`AiSuggestion` + own read repo for allergies/meds/catalog). NEW pure `@mp/shared/clinical-ai` (`smartMedicineSearch`/`explainLabReport`/`flagAbnormalResults`+`intentGenericsFor`+policies; rx.read/interaction.check/note.summarize REUSE 33/34 composers); 6 task types added additively to routing (quality-first). **NO migration. Flags REUSED (all pre-existed). `ai.use` GRANTED additively to PHARMACIST/SALESMAN/PATHOLOGIST/LAB_TECH.** NO web surface. Gates: turbo 42/42 + 23/23, jest api 796/796 (+28) + db 167/167 + i18n 19/19. Smoke: no token→401, no-`ai.clinical`→404, per-surface module flag (T_PARTIAL rx-read/lab-explain→404, note-summarize→200), `ai.use`(RECEPTION→403), rx.read structured must-verify+PENDING+audited+PENDING-not-applicable→approve→applicable, medicine.search brand+alt+intent, interaction.check real meds/allergies→HIGH+DRUG_ALLERGY (unknown patient→404, empty→400), lab.explain EN/UR/Roman (bad flag→400), result.flag CRITICAL-first+pathologistSigns, note.summarize draftOnly (empty→400).
- **35 — AI Suite: AI Gateway (`@mp/ai`)** ✅ 2026-07-05 — THE provider-agnostic, **assist-only-by-construction** primitive every AI surface (32/33/34/36/37) routes through, behind the `ai.suite` master flag (absent→404). **HITL: `runAiTask` only returns a PENDING `AiSuggestion`; a feature applies one ONLY after a human APPROVED/EDITED (`canApplySuggestion`) — PENDING/REJECTED never apply. Per-tenant keys are SECRETS in the env/secret store, NEVER in the DB or repo.** NEW `AiModule` (`@RequireFeature('ai.suite')` FIRST → `@RequirePermission('ai.use'|'ai.budget.manage')`): `POST /ai/tasks`(run; Idempotency-Key→10 dedupe), `POST /ai/suggestions/:id/decision`, `GET /ai/suggestions/:id`, `GET /ai/tasks`(+`/:id`), `GET/PUT /ai/budget`, `GET/PUT /ai/config`. `runAiTask`: resolve per-task provider/model/**quality-first tier**→check period budget (**BLOCK over cap**, record FAILED task; spend untouched)→provider via `@mp/ai` gateway→persist `AiTask`(tokens/cost/latency)→accumulate `spent`→PENDING `AiSuggestion`. Provider = injected `AiProviderFactory` (config-chosen; safe deterministic `MockAiProvider` until real SDK+key) + `AiSecretStore` (env, never persisted). OWNS 4 tables (`ai_provider_configs`(**no key col**)/`ai_tasks`/`ai_suggestions`/`ai_budgets`). Pure `@mp/shared/ai-gateway` (`canApplySuggestion` HITL guard, `tierForTask`, cost/budget math, `AI_GATEWAY_SYSTEM_POLICY`); `@mp/ai` engine (`AiProvider`+`MockAiProvider`, `createAiGateway` extended — **backward-compat `isConfigured()` preserved so 32/33/34 seams still fall back**). Migration `…310000` (4 tables + enums `AiTier`/`AiTaskStatus`/`SuggestionDecision`; `apply_tenant_rls`×4). **Flags/perms REUSED (all pre-existed) — NO catalog change.** NO web surface (backend primitive; budget UI deferred to 39). Gates: turbo typecheck+lint 42/42, test+build 23/23, jest api 768/768 (+37) + db 167/167 (+6 ai-gateway-isolation) + i18n 19/19. Smoke: no token→401, no-flag→404, run needs `ai.use`(RECEPTION→403), run routes+persists cost/latency+PENDING+audited, unknown task→default tier, no input→400, HITL (PENDING not applicable→approve→applicable; re-decide→409; REJECTED never applies; EDIT needs output), spend accumulates, over-cap→403+FAILED (spend untouched), budget-set needs `ai.budget.manage`(MANAGER→403), same Idempotency-Key replays SAME suggestion (provider once), config rejects `apiKey`→400, secret-store null w/o env.
- **34 — Online Pharmacy (Own Store)** ✅ 2026-07-05 — the tenant's OWN branded online store behind `pharmacy.online` (dependsOn `pharmacy.pos`; absent→404). **RX ASSIST-ONLY: a pharmacist approves every prescription order. NO-OVERSELL: online draws on the SAME stock as the counter (20/28 FEFO) via a GUARDED atomic decrement.** NEW `OnlinePharmacyModule` (flag FIRST; 3 role-scoped controllers): customer `@Roles(PATIENT)` catalog/checkout/track(own→404); pharmacist `@RequirePermission('pharmacy.cashier')` review + assist-only `rx-read` + approve/reject + status + assign + riders; rider `@Roles(RIDER)` own assignments + proof-gated DELIVERED. `requiresRx`→RX_REVIEW until approve→reserve→APPROVED; non-Rx reserves at PLACED (409 rolls back). Payment/COD `BillingService`(21); WhatsApp `NotificationService`(09). Assist-only read via `OnlineRxReadGateway`→`@mp/ai`(35). OWNS 4 tables + additive `medicines.requires_rx`. Migration `…300000` (4 tables + enums; `apply_tenant_rls`×4). **Flag REUSED — added to `pharmacy`+`clinic_pharmacy` presets.** Web UR-first `/store`+`/rider` PWA+`/rx-desk`. Gates: turbo 42/42+23/23, jest api 731/731 (+19) + db 161/161 (+6) + i18n 19/19. Smoke: no-flag→404, non-PATIENT storefront→403, place OTC (10→8), no-oversell (2nd 409), Rx→RX_REVIEW→approve+reserve, reject, pack→assign→deliver+proof→DELIVERED+COD, rider scoping→404.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
