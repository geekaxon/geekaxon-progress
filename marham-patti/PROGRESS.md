> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **20 — Phase 1: Pharmacy POS (Salesman→Cashier + AI Reader).** ✅ DONE/APPROVED (2026-07-04) — the pharmacy counter: extended **medicine master** (+**barcode**/sale/cost price, fuzzy name/generic + **barcode search**) → **multi-salesman cart** (add/remove, hold/resume, **stock-out alternatives**) → **push to one cashier** → **finalize** (offline-idempotent, **compensating** stock decrement + logged `StockMovement`, 06/10) → **80mm thermal slip** (07) → **returns** (reason-required, positive movement, approval→21). **AI prescription reader** (`from-prescription`, flag-gated) seeds an **OPEN DRAFT** cart from a prescription's INTERNAL meds (18) — **never auto-sells** (assist-only; degrades until 35/36). Per-medicine **profit** (sale−cost) + **self-printed barcode labels** (flag). NEW migration `…160000` (Medicine EXTEND additive + 6 tables: `barcode_labels`/`stock`[@@unique tenant,branch,med]/`stock_movements`/`cart_handoffs`/`sales`[`client_action_id @unique`]/`sale_items` + `MovementType`/`CartStatus`/`SaleStatus` enums; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Flags `pharmacy.pos`/`pharmacy.aiReader`/`pharmacy.barcodeLabels` + perms `pharmacy.sell`/`pharmacy.cashier` already in catalogs — **no catalog change**. Pure `@mp/shared/pharmacy` (`computeSaleTotals` = the ONE money math server+client share w/ discount clamp, `lineProfit`/`saleProfit`/`profitMargin`, `canPush`/`canFinalize`/`isStockOut` guards, `summarizeSale`; enum mirrors). API `PharmacyModule` (4 controllers, class `@RequireFeature('pharmacy.pos')`; method flags `pharmacy.aiReader`/`pharmacy.barcodeLabels`) — `/pharmacy/*` prefix (avoids 18's `/medicines`): medicines search/CRUD/**alternatives**/label; carts create/items/±/{push,hold,resume}/**from-prescription**; cashier queue/**finalize**(Idempotency-Key)/**return**; sales list/detail(profit)/slip. `finalize` runs **inside `SyncService.runIdempotent`** (guards + create + decrement + movement + cart flip all inside → replay = same sale, single decrement, no double-sell). Added additive `TimelineRegistry.registerPurchase`/`collectPurchase` → pharmacy `SALE` shows in the **light customer view** too. Web EN+UR `/pharmacy` (Sell/Cashier/Catalog tabs; live discount preview via shared math; 80mm slip + barcode-label print windows; optional controls gated via `/flags/me`). Gates green: typecheck 26/26, lint 15/15 (0 warn), build 15/15 (`/pharmacy` route), jest **api 344/344 [+23]** + **db 98/98 [+4 pharmacy-isolation]** + i18n 19/19 parity. Vertical smoke: present for pharmacy/clinic_pharmacy/full, 404 for lab. Perf ➖ (internal POS tool, `robots:noindex`).
- **Current build-step:** **21 — Phase 2: Billing & Cashier.** Read `specs/21-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/21-billing-cashier.md`
- **Branch state:** `feat/20-pharmacy-pos` complete (awaiting controller merge to `staging`), stacked on `feat/19-laboratory` → `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. Step 21 branch stacks on it if not yet merged. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **20 — Pharmacy POS (Salesman→Cashier + AI Reader)** ✅ 2026-07-04 — the pharmacy counter: extended **medicine master** (+**barcode**/sale/cost price, barcode search) → **multi-salesman cart** (stock-out **alternatives**, hold/resume) → **push to cashier** → **finalize** (offline-idempotent, **compensating** stock decrement + logged `StockMovement`, 06/10) → **80mm slip** (07) → **returns** (reason-required, →21). **AI reader** (`from-prescription`, flag-gated) seeds an **OPEN DRAFT** from a prescription's INTERNAL meds (18) — **never auto-sells** (degrades until 35/36). Per-med **profit** + **barcode labels** (flag). NEW migration `…160000` (Medicine EXTEND additive + 6 tables: `barcode_labels`/`stock`[@@unique tenant,branch,med]/`stock_movements`/`cart_handoffs`/`sales`[`client_action_id @unique`]/`sale_items` + `MovementType`/`CartStatus`/`SaleStatus`; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Flags `pharmacy.pos`/`aiReader`/`barcodeLabels` + perms `pharmacy.sell`/`cashier` already in catalogs (no change). Pure `@mp/shared/pharmacy` (`computeSaleTotals` = ONE money math w/ discount clamp, `lineProfit`/`saleProfit`/`profitMargin`, `canPush`/`canFinalize`/`isStockOut`, `summarizeSale`). API `PharmacyModule` (4 controllers, class `pharmacy.pos`; method flags `aiReader`/`barcodeLabels`) under `/pharmacy/*`: medicines search/CRUD/alternatives/label; carts create/items/±/{push,hold,resume}/from-prescription; cashier queue/finalize(Idempotency-Key)/return; sales list/detail(profit)/slip. `finalize` runs **inside `runIdempotent`** (guards+create+decrement+movement+cart-flip all inside → replay=same sale, single decrement). Added additive `TimelineRegistry.registerPurchase` → `SALE` in the **light customer view**. Web EN+UR `/pharmacy` (Sell/Cashier/Catalog; live discount preview via shared math; 80mm slip + label print; optional controls gated via `/flags/me`). Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 344/344 (+23) + db 98/98 (+4 pharmacy-isolation) + i18n 19/19 parity. Vertical smoke: present pharmacy/clinic_pharmacy/full, 404 lab. Perf ➖ (internal POS tool).
- **19 — Laboratory (Orders + Smart Reports)** ✅ 2026-07-04 — the LIS: catalog (tests+**prep**+packages+**smart result templates** w/ **age+gender ranges**) → order → sample (**barcode**) → result (**auto-flagged** high/low/**critical** by patient age+gender, 14) → **pathologist sign** → **branded report** (07) + WhatsApp (09) + **TAT** + **re-test** + **amend-with-reason** (versioned, audited). NEW migration `…150000` (8 tables + `LabStatus`/`SampleStatus` enums; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Flags `lab.catalog`/`lab.results` + perms `lab.order`/`lab.result.enter`/`lab.result.approve` already in catalogs (no change). Pure `@mp/shared/lab` (`evaluateResults` = ONE evaluator server+client share via `pickRange` age+gender + NORMAL/HIGH/LOW/CRITICAL, `computeTatHours`, `ageYears`, `isDeliverable`; reused additive `TEST` `TimelineKind`). API `LabModule` (4 controllers) **split-gated**: catalog/orders/samples `lab.catalog`+`lab.order`, templates/results/approval method-overridden to `lab.results`+`lab.result.enter`/`lab.result.approve` → **404 vertical smoke** + catalog-only 404s resulting. Web EN+UR `/lab`. Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 321/321 (+25) + db 94/94 (+4 lab-isolation) + i18n 19/19 parity. Perf ➖ (internal LIS tool).
- **18 — E-Prescription (Paper-Friendly + Privacy)** ✅ 2026-07-04 — the prescription pad for paper-first AND digital doctors: per-doctor **privacy** (`sendToPatient=false` → staff/pharmacy only, hidden 404 from non-`rx.viewInternal` incl. patient), **dual prescription** (INTERNAL vs BUY_OUTSIDE), TEST lines → lab, **allergy check** vs 14. NEW migration `…140000` (3 tables + the MINIMAL `medicines` model 20/28 extend additively; `DispenseMode`+`PrescriptionItemKind` enums; DEFENSIVE `pg_trgm` GIN → ILIKE fallback on pglite; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Pure `@mp/shared/prescription` (`medicineLabel`, `partitionPrescription` = ONE routing split, `findAllergyConflicts` = pure 14 matcher, `summarizePrescription`); added `PRESCRIPTION` to additive `TimelineKind`. API `PrescriptionsModule` PRESENT when EITHER `clinic.consultation` OR `pharmacy.pos` (programmatic **404 when neither**) + `rx.write`. Web EN+UR `/prescriptions`. Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 296/296 (+22) + db 90/90 (+4 prescription-isolation) + i18n 19/19 parity. Perf ➖ (internal tool).

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
