> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **17 — Phase 1: Doctor Consultation (Comfort Levels).** ✅ DONE/APPROVED (2026-07-04) — the clinical encounter, built to RESPECT how each doctor works: a per-doctor comfort level (L0 Full Paper … L3 Full Digital, stored on `Doctor.comfortLevel` from 15) drives the UI, but the system always captures the encounter + routes referrals. NEW migration `…130000` (4 tables: `encounters`/`diagnoses`/`consult_templates`/`doctor_favorites` + `EncounterStatus` enum OPEN/CLOSED; all `apply_tenant_rls`, T1/T2 proven). Pure `@mp/shared/consultation` (`COMFORT_LEVELS`+`clampComfortLevel`/`usesPaper` = the ONE L0–L3 mapping server+client agree on, `summarizeEncounter`, `ReferralSeed`/`buildReferralSeed` = the stable lab/pharmacy contract 18/19 consume); added `ENCOUNTER` to the additive `TimelineKind`. API `ConsultationModule` — whole surface (4 controllers) `@RequireFeature('clinic.consultation')` (**404 when off** = vertical smoke) + `@RequirePermission('consult.write')` (DOCTOR); `POST/GET/GET:id/PATCH /encounters` (comfort captured; diagnoses inline or `POST/DELETE :id/diagnoses`); `GET /patients/:id/history` (one-click: vitals 16 + allergy-warnings 14 + prior encounters); `consult-templates` + `doctor-favorites` CRUD; `POST :id/refer/lab|pharmacy` → **SEED** carrying downstream `available` (lab.catalog/pharmacy.pos read at runtime; never commits to not-yet-built 18/19 tables); `POST :id/ai-assist` **assist-only + logged + `ai.clinical`-gated**, degrades cleanly (providerAvailable:false until 33/36, `decisionRequired:true` always); `GET :id/print-header` (L0 paper). Registered as a MEDICAL `TimelineRegistry` contributor → encounter (+FOLLOWUP) on the patient timeline **only in the medical view**. Web EN+UR `/consultation` (comfort selector persists via appointments PATCH, one-click history panel w/ allergy warnings, diagnosis picker, templates/favorites, follow-up, lab/pharmacy referral seeds, AI panel, L0 print-header window). Gates green: typecheck 26/26, lint 15/15, build 15/15, jest **api 274/274 [+27]** + **db 86/86 [+4 consultation-isolation]** + i18n 19/19 parity + ui/import/offline/shared unaffected. Perf ➖ (internal tool, `robots:noindex`, no public surface).
- **Current build-step:** **18 — Phase 1: E-Prescription (Paper-Friendly + Privacy).** Read `specs/18-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/18-eprescription.md`
- **Branch state:** `feat/17-doctor-consultation` complete (awaiting controller merge to `staging`), stacked on `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. Step 18 branch stacks on it if not yet merged. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **17 — Doctor Consultation (Comfort Levels)** ✅ 2026-07-04 — the clinical encounter, built to RESPECT how each doctor works: per-doctor comfort level (L0 Full Paper … L3 Full Digital, on `Doctor.comfortLevel` from 15) drives the UI, system always captures encounter + routes referrals. NEW migration `…130000` (4 tables: encounters/diagnoses/consult_templates/doctor_favorites + `EncounterStatus` enum; all `apply_tenant_rls`, T1/T2 proven). Pure `@mp/shared/consultation` (`clampComfortLevel`/`usesPaper` = ONE L0–L3 mapping, `summarizeEncounter`, `ReferralSeed`/`buildReferralSeed` = stable 18/19 contract); added `ENCOUNTER` to additive `TimelineKind`. API `ConsultationModule` (4 controllers) `@RequireFeature('clinic.consultation')` (**404 when off**) + `@RequirePermission('consult.write')`; encounters CRUD (comfort captured) + diagnoses; `GET /patients/:id/history` (one-click vitals 16 + allergies 14 + prior encounters); templates+favorites CRUD; `refer/lab|pharmacy` → **SEED** w/ downstream `available` (no commit to unbuilt 18/19); `ai-assist` **assist-only+logged+`ai.clinical`-gated**, degrades cleanly (no gateway until 33/36, `decisionRequired` always); L0 `print-header`. MEDICAL `TimelineRegistry` contributor → encounter+FOLLOWUP on timeline (medical view only). Web EN+UR `/consultation`. Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 274/274 (+27) + db 86/86 (+4 consultation-isolation) + i18n 19/19 parity. Perf ➖ (internal tool).
- **16 — Vitals Station (Nurse)** ✅ 2026-07-04 — pre-consultation nurse reading, built around the paper habit: type→print a FILLED A4, or print a BLANK header sheet and write by pen (photo→AI read deferred to 35; manual until then). NEW migration `…120000` (1 table `vitals` + `VitalsSource` enum; all values optional, Decimals, `apply_tenant_rls`+`branch_id`, T1/T2 proven). Pure `@mp/shared/vitals` (`computeBmi` = ONE server+client formula, `summarizeVitals`, `SUGAR_TYPES`); added `VITALS` to additive `TimelineKind`. API `VitalsModule`: whole controller `@RequireFeature('clinic.consultation')` (**404 when off** = vertical smoke) + `@RequirePermission('vitals.enter')`; `POST /vitals` **offline-idempotent** (`runIdempotent`, required `Idempotency-Key`, P2002 backstop) with **BMI always recomputed server-side**; `GET /vitals`; `GET /vitals/sheet?blank=` (filled/blank A4 header from patients 14 + appointments 15); `POST /vitals/from-photo` → DRAFT (aiAvailable:false, no silent commit). NEW decoupled `TimelineRegistry` in PatientsModule (contributors flow IN, no cycle) → vitals on the patient timeline **only in the medical view**. Web EN+UR `/vitals` (offline badge, number grid + live BMI, Save+outbox fallback, Print filled/blank print-window, history). Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 247/247 (+20) + db 82/82 (+3 vitals-isolation) + i18n 19/19 parity.
- **15 — Appointment & Token (paper-first)** ✅ 2026-07-04 — the paper-first front desk: one-tap **offline** tokens, doctor calendar/availability, walk-in queue + display board, book/reschedule/cancel/no-show lifecycle, human-gated register import. NEW migration `…110000` (6 tables: doctors/doctor_availability/doctor_leave/appointments/tokens/register_imports), all `apply_tenant_rls`+`branch_id`, T1/T2 + no-cross-tenant-numbering proven. Token = WALK_IN appointment + **sequential-per-doctor/branch/day** token created atomically; **critical offline path** rides `SyncService.runIdempotent(clientActionId)` → replay returns SAME number (P2002 backstop). Pure `@mp/shared/appointments`. API `AppointmentsModule`: `@RequireFeature('clinic.appointments')` (**404 when off**) + `@RequirePermission('appointments.manage')`; slots; lifecycle PATCH; queue+display; register import UPLOADED→PARSED→VERIFIED→IMPORTED (never auto-commits). Web EN+UR `/appointments`. Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 227/227 (+21) + db 79/79 (+6) + i18n 19/19.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
