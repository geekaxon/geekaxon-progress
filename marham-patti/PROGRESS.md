> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **30 — Phase 2: Branch Transfer & Multi-Branch Stock.** ✅ DONE/APPROVED (2026-07-05) — **completes Phase 2** — a tracked, two-sided, audited stock movement between two branches of ONE tenant, behind the `core.multiBranch` flag (present only for multi-branch tenants; a single-branch / clinic / lab tenant → 404). An untracked transfer would recreate the founding stock-loss problem at a bigger scale, so a transfer is NEVER a silent quantity edit: it is **out-of-source + into-destination, both logged**. NEW **TransferModule** (`@RequireFeature('core.multiBranch')` 404 THEN `@RequirePermission('inventory.manage')` — permission REUSED, a transfer moves stock; SALESMAN etc. w/o it → 403): `GET /transfers` (list, `?status=`), `/:id`; **`POST /transfers`** (request = a plan: validates ≥2 branches, both belong to the tenant + differ, each line's medicine + source **Batch (28)** exists AT the source with enough on-hand; NO stock moves yet); **`POST /transfers/:id/dispatch`** (REQUESTED→DISPATCHED: per line decrement the source batch + aggregate on-hand via a logged **TRANSFER − StockMovement (20)** → in-transit; re-validates on-hand, all-or-nothing); **`POST /transfers/:id/receive`** (DISPATCHED→RECEIVED: each line's actual `qtyReceived` lands at the destination as a **NEW Batch (28)** mirroring the source batch no/expiry/cost via a logged **TRANSFER + movement**; a received qty ≠ dispatched is a tracked **variance = received − dispatched** that MUST carry a **reason** — an in-transit loss, else 400; reasons validated for EVERY line before any write); **`POST /transfers/:id/cancel`** (REQUESTED only — nothing moved); `GET /transfers/stock?branchId=` (**branch-wise stock**: a branch's on-hand medicines + batches, both sides); `GET /transfers/branches` (branch list w/ `isWarehouse`); **`POST /transfers/warehouse`** (idempotent — designate a **central warehouse = a special Branch** flagged `is_warehouse`; hub fulfilment is then an ordinary transfer FROM it). It **OWNS only its 2 new tables** (`stock_transfers`,`transfer_items`); the 28 `batches` / 20 `stock`+`stock_movements` / `medicines` (and `branches` it reads for hubs) are read + moved through its OWN tenant-scoped repo using the SAME compensating write (like 28/29 borrow the 20 tables), **never importing 20/28**. Pure `@mp/shared/transfers` (`TRANSFER_STATUS_FLOW`/`canTransitionTransfer`/`canDispatchTransfer`/`canReceiveTransfer`/`canCancelTransfer`; `transferVariance`/`needsTransferReason`; `transferQty`). NEW migration `…260000` (2 tables + enum `TransferStatus`; **additive `ALTER branches ADD is_warehouse boolean default false`**; `apply_tenant_rls` on both, T1/T2 + WITH-CHECK + fail-closed proven in a new `branch-transfer-isolation` pglite spec — cross-branch NEVER means cross-tenant). **Flag REUSED (`core.multiBranch` pre-existed in the catalog) + permission REUSED (`inventory.manage`) — NO catalog change.** Web EN+UR `/transfer` (tabbed multi-branch desk: New transfer = source/dest + pick batch+qty lines + designate warehouse; Transfers = dispatch/cancel/receive-with-per-line-variance+reason; Branch stock = branch-wise on-hand+batches; `noindex`; degrades to a clean single-branch hint). Gates green: typecheck 26/26, lint 15/15 (0 warn), build 15/15 (`/transfer` 3.05 kB / 140 kB), jest **api 605/605 [+17]** + **db 141/141 [+4 branch-transfer-isolation]** + i18n 19/19 parity. Vertical smoke: lab tenant→404; SALESMAN (flag on, no inventory.manage)→403; warehouse designate (idempotent — 2nd call returns same id); request 25 (from 40 on-hand, no move yet)→dispatch (source batch+aggregate 40→15, TRANSFER − w/ batch, dest uncredited/in-transit)→receive 25 (dest +25 as a NEW mirrored batch, TRANSFER +, source 15 + dest 25 = 40 conserved, variance 0); receive 18 of 20 dispatched w/o reason→400, with reason→variance −2 recorded + only 18 landed; warehouse→branch fulfilment (100→70 + 30); branch-wise stock A=40/B=0; request 50 of 10 on-hand→400; single-branch tenant request→400; receive-before-dispatch→400, cancel a REQUESTED→CANCELLED then dispatch→400, stock never moved.
- **Prior completed build-step:** **29 — Phase 2: Purchases & Suppliers.** ✅ DONE/APPROVED (2026-07-05) — procurement → stock in → payables OVER the **28 inventory**, behind the SAME `pharmacy.inventory` flag (present where a tenant holds purchasable stock; clinic/lab-results-only→404). Stock enters on a **PO + credit**; **GRN is the ONE normal way batches enter (28)** — source of truth for cost. NEW **PurchaseModule** (`@RequireFeature('pharmacy.inventory')` 404 THEN `purchase.manage`; ADMIN/MANAGER/OWNER, not PHARMACIST): suppliers CRUD + `GET /suppliers/:id/ledger` (opening + goods − payments + **FIFO aging** reusing accounts `arAging`); PO `GET/POST /orders`/`:id`/`send`/`cancel` (DRAFT→SENT→PARTIAL→RECEIVED/CANCELLED); **`POST /grn`** (each line CREATES a **Batch (28)** + PURCHASE **StockMovement (20)** via the SAME compensating write, fills GRN item `batchId`, accumulates PO `receivedQty`→PARTIAL/RECEIVED via `poStatusAfterReceipt`, posts **Dr Inventory/Cr AP** to 23 via idempotent `accounting_events` source PURCHASE — full mode; **lite records + degrades**); `GET /grns`/`:id`; **`POST /payments`** (settle AP: Dr AP/Cr Cash|Bank + `journalRef`; CREDIT rejected = internal return marker); **`POST /returns`** (against a GRN line → RETURN movement + batch decrement + supplier credit note (reused SupplierPayment method CREDIT) + Dr AP/Cr Inventory; over-return→400); `GET /reorder/suggestions` (**28 on-hand + 30-day velocity** `suggestReorder`); `GET /price-history` (`priceTrend`). OWNS only its 6 tables (`suppliers`/`purchase_orders`/`po_items`/`grns`/`grn_items`/`supplier_payments`); 28 batches / 20 stock+movements / medicines read+moved via its OWN repo, never importing 20/28. Pure `@mp/shared/purchase` (`PO_STATUS_FLOW`/`poStatusAfterReceipt`; `lineCost`/`poTotal`; `supplierBalance`/`supplierAging`; `priceTrend`; `suggestReorder` LEAD=7/COVER=30). NEW migration `…250000` (6 tables + enum `POStatus`; `PayMethod` reused; `apply_tenant_rls` all 6, proven `purchase-suppliers-isolation`). **Flag + perm REUSED — no catalog change.** Web EN+UR `/purchase` (tabbed: Suppliers+ledger/aging+payment, Orders build→send/cancel, Receive GRN+per-line return, Reorder). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/purchase` 3.53 kB), jest api 588/588 (+25) + db 137/137 (+4) + i18n 19/19. Smoke: lab→404, PHARMACIST→403, PO 320→send→GRN full→batch 40+aggregate 40+PURCHASE mv+PO RECEIVED+AP posted (one idempotent event), partial→PARTIAL, lite→posted=false/no event, pay 200→Dr AP/Cr Cash+journalRef+balance 320→120, CREDIT pay→400, price history min8/max11, reorder→only low fast-mover, return 10→40→30+Dr AP80/Cr Inventory80+over-return400, cancel→send400 & receive-cancelled400.
- **Current build-step:** **31 — Phase 3: Patient App.** Read `specs/31-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/31-patient-app.md`
- **Branch state:** `feat/30-branch-transfer` complete (awaiting controller merge to `staging`), stacked on `feat/29-purchase-suppliers` → `feat/28-pharmacy-inventory` → `feat/27-sample-tracking` → `feat/26-phlebotomist-app` → `feat/25-home-collection` → `feat/24-admin-dashboard` → `feat/23-accounts-finance` → `feat/22-doctor-commission` → `feat/21-billing-cashier` → `feat/20-pharmacy-pos` → `feat/19-laboratory` → `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **30 — Branch Transfer & Multi-Branch Stock** ✅ 2026-07-05 — **completes Phase 2** — a tracked, two-sided, audited stock move between two branches of ONE tenant behind `core.multiBranch` (single-branch/clinic/lab→404). Never a silent edit: out-of-source + into-destination, both logged. NEW `TransferModule` (`@RequireFeature('core.multiBranch')` 404 THEN `inventory.manage` REUSED; SALESMAN→403): `POST /transfers` (request = plan: ≥2 branches, source **Batch (28)** exists w/ on-hand, no move yet); `/:id/dispatch` (REQUESTED→DISPATCHED: decrement source batch+aggregate via logged **TRANSFER − (20)** → in-transit, re-validates all-or-nothing); `/:id/receive` (DISPATCHED→RECEIVED: `qtyReceived` lands as a NEW mirrored **Batch (28)** at dest via **TRANSFER +**; received≠dispatched → tracked **variance** MUST carry a **reason** else 400, validated per-line before any write); `/:id/cancel` (REQUESTED only); `GET /transfers`/`:id`, `/stock?branchId=` (**branch-wise stock**), `/branches`, **`POST /warehouse`** (idempotent — a **central warehouse = special Branch** `is_warehouse`; hub fulfils via an ordinary transfer). OWNS only 2 tables (`stock_transfers`/`transfer_items`); 28 batches / 20 stock+movements / medicines / branches read+moved via its OWN repo (like 28/29), never importing 20/28. Pure `@mp/shared/transfers` (`TRANSFER_STATUS_FLOW`/`canDispatch/Receive/CancelTransfer`; `transferVariance`/`needsTransferReason`/`transferQty`). NEW migration `…260000` (2 tables + enum `TransferStatus`; additive `ALTER branches ADD is_warehouse`; `apply_tenant_rls` both, proven `branch-transfer-isolation` — cross-branch never cross-tenant). **Flag + perm REUSED — no catalog change.** Web EN+UR `/transfer` (tabbed: New transfer + warehouse, Transfers dispatch/cancel/receive-with-variance+reason, Branch stock). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/transfer` 3.05 kB), jest api 605/605 (+17) + db 141/141 (+4 branch-transfer-isolation) + i18n 19/19. Smoke: lab→404, SALESMAN→403, warehouse idempotent, request 25/40 (no move)→dispatch 40→15+TRANSFER − w/ batch+dest in-transit→receive 25 as mirrored batch+TRANSFER + (15+25=40 conserved, variance 0), receive 18/20 no reason→400 then reason→variance −2+only 18 landed, warehouse→branch 100→70+30, stock A=40/B=0, request 50/10→400, single-branch→400, receive-before-dispatch→400, cancel REQUESTED→dispatch400 (stock unmoved).
- **29 — Purchases & Suppliers** ✅ 2026-07-05 — procurement → stock in → payables OVER the **28 inventory** behind the SAME `pharmacy.inventory` flag (present where a tenant holds purchasable stock; clinic/lab-results-only→404). Stock enters on **PO + credit**; **GRN is the ONE normal way batches enter (28)** — source of truth for cost. NEW `PurchaseModule` (`@RequireFeature('pharmacy.inventory')` 404 THEN `purchase.manage`; ADMIN/MANAGER/OWNER, not PHARMACIST): suppliers CRUD + `GET /suppliers/:id/ledger` (opening + goods − payments + **FIFO aging** reusing accounts `arAging`); PO `GET/POST /orders`/`:id`/`send`/`cancel` (DRAFT→SENT→PARTIAL→RECEIVED/CANCELLED); **`POST /grn`** (each line CREATES a **Batch (28)** + PURCHASE **StockMovement (20)** via the SAME compensating write, fills GRN item `batchId`, accumulates PO `receivedQty`→PARTIAL/RECEIVED via `poStatusAfterReceipt`, posts **Dr Inventory/Cr AP** to 23 via idempotent `accounting_events` source PURCHASE — full mode; **lite records + degrades**); `GET /grns`/`:id`; **`POST /payments`** (settle AP: Dr AP/Cr Cash|Bank + `journalRef`; CREDIT rejected = internal return marker); **`POST /returns`** (against a GRN line → RETURN movement + batch decrement + supplier credit note (reused SupplierPayment method CREDIT) + Dr AP/Cr Inventory; over-return→400); `GET /reorder/suggestions` (**28 on-hand + 30-day velocity** `suggestReorder`); `GET /price-history` (`priceTrend`). OWNS only its 6 tables (`suppliers`/`purchase_orders`/`po_items`/`grns`/`grn_items`/`supplier_payments`); 28 batches / 20 stock+movements / medicines read+moved via its OWN repo (like 28 borrows 20), never importing 20/28. Pure `@mp/shared/purchase` (`PO_STATUS_FLOW`/`canTransitionPO`/`canReceivePO`/`poStatusAfterReceipt`; `lineCost`/`poTotal`; `supplierBalance`/`supplierAging`; `priceTrend`; `suggestReorder` LEAD=7/COVER=30). NEW migration `…250000` (6 tables + enum `POStatus`; `PayMethod` reused; `apply_tenant_rls` all 6, T1/T2+WITH-CHECK+fail-closed proven `purchase-suppliers-isolation`). **Flag + perm REUSED — no catalog change.** Web EN+UR `/purchase` (tabbed: Suppliers + ledger/aging + payment, Orders build→send/cancel, Receive GRN + per-line return, Reorder). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/purchase` 3.53 kB), jest api 588/588 (+25) + db 137/137 (+4 purchase-suppliers-isolation) + i18n 19/19. Smoke: lab→404, PHARMACIST→403, PO 320→send→GRN full→batch 40+aggregate 40+PURCHASE mv+PO RECEIVED+AP posted (one idempotent event), partial→PARTIAL, lite→posted=false/no event, pay 200→Dr AP/Cr Cash+journalRef+balance 320→120, CREDIT pay→400, price history min8/max11, reorder→only low fast-mover, return 10→40→30+Dr AP80/Cr Inventory80+over-return400, cancel→send400 & receive-cancelled400.
- **28 — Pharmacy Inventory (Batch / Expiry / FEFO)** ✅ 2026-07-05 — real inventory UNDER the **20 POS** behind `pharmacy.inventory` (dependsOn `pharmacy.pos`); solves the founding hidden stock loss. Extended 20 ADDITIVELY: on-hand split into **batches** (no/expiry/qty/cost); a sale allocates **FEFO** (soonest-expiry first) — the **20 finalize hook now resolves to a specific batch** (per-batch `applyBatchDelta` + SALE movement w/ `batch_id`; aggregate still moves full via the SAME compensating write; shortfall→batchless remainder; return restocks the batch). Flag OFF / no batches → 20 sells simple qty (FEFO empty → additive; **no existing pharmacy test changed**). Pure `@mp/shared/pharmacy-inventory` (`allocateFefo`, `expiryBand`/`isLowStock` NEAR=90/LOW=10, `computeVariance`/`needsAccountability`, `valuation`, `ADJUST_TYPES`/`isLossAdjust`). NEW `InventoryModule` (`@RequireFeature('pharmacy.inventory')` 404 THEN `inventory.manage`; write-off/adjust role-locked to `pharmacy.stock.adjust` method-override): `/inventory/stock`,`/batches`(POST bumps aggregate + PURCHASE movement),`/alerts`(expiry+low-stock→24),`/valuation`(cost+retail),`POST /adjustments`(damage/expire/correction/write-off, reason mandatory), reconciliation `POST /reconciliations`(snapshot expected)/`/:id`/`/:id/counts`/`/:id/close` — **THEFT TRIPWIRE**: every non-zero variance needs **reason + responsible person** before close (else 400); close squares book (CORRECTION adj + ADJUST movement attributed to the person), locks+audits, net **loss best-effort→23** (full: COGS/Inventory via `accounting_events`, source PURCHASE; degrades). OWNS only 4 tables (`batches`/`reconciliations`/`reconciliation_lines`/`stock_adjustments`); 20 stock/movements/medicines read+moved via its OWN repo (like 22/24), never importing 20. FEFO read/decrement on 20 `PharmacyRepo` (`listBatchesForFefo`/`applyBatchDelta`; `SaleItem.batchId` filled on a single-batch line). NEW migration `…240000` (4 tables + enums `ReconStatus`/`AdjustType`; 20 `stock_movements.batch_id`/`sale_items.batch_id` already nullable; `apply_tenant_rls` all 4, T1/T2+WITH-CHECK+fail-closed proven `pharmacy-inventory-isolation`). Flag added `dependsOn['pharmacy.pos']`; perms pre-existed (PHARMACIST both; ADMIN/MANAGER manage-only). Web EN+UR `/inventory` (tabbed: Stock colour-coded batches + receive/write-off forms, Alerts, Valuation, Reconcile count sheet). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/inventory` 3.3 kB), jest api 563/563 (+23) + db 133/133 (+4 pharmacy-inventory-isolation) + i18n 19/19. Smoke: lab→404, SALESMAN→403, batch→+qty+PURCHASE, finalize→FEFO soonest-first, alerts EXPIRED/NEAR+low, close blocked w/o reason+person→400 then squares 100→96 attributed, ADMIN write-off→403, PHARMACIST no-reason→400.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
