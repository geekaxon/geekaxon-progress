> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **28 — Phase 2: Pharmacy Inventory (Batch / Expiry / FEFO).** ✅ DONE/APPROVED (2026-07-05) — real inventory UNDER the **20 POS** behind the `pharmacy.inventory` flag (dependsOn `pharmacy.pos`); where stock theft is caught and the founding ₹1–1.5 lac hidden loss is solved. Extended **20 ADDITIVELY** (never replaced): a medicine's on-hand is now broken into **batches** (batch no, expiry, qty, cost); a sale allocates **FEFO** (soonest-expiry first) — the **20 finalize hook now resolves to a specific batch** (per-batch `applyBatchDelta` + a SALE `StockMovement` carrying `batch_id`; the aggregate `stock.qty` still moves by the full qty via the SAME compensating write; a batch **shortfall** is logged as a batchless remainder; a return restocks the original batch). With the flag **OFF (or a medicine with no batches yet) 20 sells on simple quantity stock** — FEFO returns empty, so it is fully additive and **no existing pharmacy test changed**. Pure `@mp/shared/pharmacy-inventory` (`allocateFefo`; `expiryBand`/`daysUntil`/`isExpired`/`isNearExpiry`/`isLowStock` `NEAR_EXPIRY_DAYS=90`/`DEFAULT_LOW_STOCK=10`; `computeVariance`/`needsAccountability`; `valuation`; `ADJUST_TYPES`/`isAdjustType`/`isLossAdjust`/`RECON_STATUSES`). NEW **InventoryModule** (`@RequireFeature('pharmacy.inventory')` 404 THEN `@RequirePermission('inventory.manage')`; the destructive **write-off/adjust is role-locked to `pharmacy.stock.adjust`** via a method-level override): `GET /inventory/stock` (colour-coded batch list), `/batches`(GET+POST — a new batch bumps the aggregate on-hand via a logged PURCHASE movement), `/alerts` (expiry bands + low-stock, for the 24 dashboard), `/valuation` (cost+retail), `POST /adjustments` (damage/expire/correction/write-off — reason mandatory, attributable), and reconciliation `POST /reconciliations`(snapshot expected — one line per in-stock batch + an aggregate line per batch-less medicine), `/:id`,`/:id/counts` (variance = counted−expected), `/:id/close` — the **MISMATCH/THEFT TRIPWIRE**: every non-zero variance MUST carry a **reason + a responsible person** before close (else 400); close squares the book (CORRECTION adjustment + ADJUST movement, batch+aggregate together, attributed to the responsible person), locks + audits, and a net **loss best-effort posts to 23** (full mode: debit COGS / credit Inventory via the idempotent `accounting_events` outbox, source PURCHASE; degrades). It **OWNS only its 4 new tables** (`batches`,`reconciliations`,`reconciliation_lines`,`stock_adjustments`); the 20 `stock`/`stock_movements`/`medicines` are read + moved through its OWN tenant-scoped repo using the SAME compensating write (like 22/24), **never importing the 20 module**. FEFO batch read/decrement live on the 20 `PharmacyRepo` (`listBatchesForFefo`/`applyBatchDelta`; `SaleItem.batchId` filled when a single batch fully covered a line). NEW migration `…240000` (4 tables + enums `ReconStatus`/`AdjustType`; the 20 `stock_movements.batch_id`/`sale_items.batch_id` already nullable — FEFO just fills them; `apply_tenant_rls` on all 4, T1/T2 + WITH-CHECK + fail-closed proven in a new `pharmacy-inventory-isolation` pglite spec). **Flag pre-seeded — added `dependsOn ['pharmacy.pos']`; perms `inventory.manage`+`pharmacy.stock.adjust` pre-existed** (PHARMACIST holds both; ADMIN/MANAGER hold `inventory.manage` only → can manage, cannot write off). Web EN+UR `/inventory` (tabbed back-office desk: Stock = colour-coded batches + receive-batch + write-off forms; Alerts; Valuation; Reconcile = start → count sheet with per-line reason+person on a variance → close; `noindex`). Gates green: typecheck 26/26, lint 15/15 (0 warn), build 15/15 (`/inventory` 3.3 kB / 150 kB First Load), jest **api 563/563 [+23]** + **db 133/133 [+4 pharmacy-inventory-isolation]** + i18n 19/19 parity. Vertical smoke: lab tenant→404; SALESMAN (flag on, no inventory.manage)→403; batch create→aggregate on-hand +qty + PURCHASE movement w/ batch; **finalize allocates FEFO** (soonest batch exhausted first, per-batch SALE movements, aggregate −full); alerts→EXPIRED/NEAR + low-stock; valuation cost/retail; reconciliation count −4→variance, **close blocked without reason+person→400**, then with reason+person→closed + book squared 100→96 + CORRECTION/ADJUST attributed to the responsible person + loss posted; ADMIN write-off→403, PHARMACIST write-off without reason→400, with reason→−5 + loss posted.
- **Prior completed build-step:** **27 — Phase 2: Sample Tracking & Chain of Custody.** ✅ DONE/APPROVED (2026-07-05) — chain of custody over the **19 sample**, extended ADDITIVELY (never replaced). A physical sample changes many hands; each **scan** records a stage event (WHO/WHEN/WHERE) and the ordered chain is the sample's **custody timeline**. Legal forward transitions (`SAMPLE_STAGE_FLOW`/`canTransitionStage`: COLLECTED→IN_TRANSIT→RECEIVED→ACCESSIONED→ON_BENCH→STORED/DISPOSED; in-lab may skip transit; STORED↔ON_BENCH for a retest) — an **illegal jump is rejected**; REJECTED is NOT a scan target (rides the dedicated reject action, like COLLECTED rode collect in 26). A **reject** (LOST/DAMAGED/REJECTED + reason) sets the sample's `custodyStatus`, records a REJECTED evidence event, marks the sample REJECTED, and **BEST-EFFORT re-opens the home visit as a recollection (25)** via new `HomeCollectionService.recollectForRejectedSample` (walk-in sample → none; degrades). The **MISMATCH GUARD** (patient-safety) gates **19 resulting**: `LabService.enterResults`/`approve` now call `assertResultableSample` — a scanned barcode from ANOTHER order → 400, and a non-ACTIVE (lost/damaged/rejected) sample → 400 (can never be resulted); the guard only ever TIGHTENS 19 (a walk-in order with no sample is untouched). It **OWNS only the 1 new table** `sample_events`; the `samples` row (custody status, container, storage, received) stays owned by 19 and is reached through NEW `LabService` seams (`findSampleWithOrder`, `updateSampleFields`), the recollection through `HomeCollectionService` — never mutating their tables. NEW migration `…230000` (1 table `sample_events` + `Sample` extended: `custodyStatus`/`containerType`/`storageLocation`; enums `SampleStage`/`CustodyStatus`; `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven in a new `sample-tracking-isolation` pglite spec). **Flag+perm REUSED — no catalog change** (PRESENT when `lab.results` **OR** `lab.homeCollection` on — programmatic flag-then-permission like 18; SAME `lab.order` perm the sample collect/receive already ride; LAB_TECH/PATHOLOGIST/DOCTOR/PHLEBOTOMIST hold it; a pharmacy vertical → 404). Pure `@mp/shared/samples` (`SAMPLE_STAGE_FLOW`/`canTransitionStage`/`nextStages`/`currentStageOf`; `isTerminalStage`/`canReject`; `blocksResulting`; `REJECTION_CUSTODY`/`isRejectionCustody`/`isSampleStage`). API `SampleTrackingModule` (controller: assertModulePresent 404 THEN `lab.order` 403): `POST /samples/:barcode/scan`, `POST /samples/:barcode/reject`, `GET /samples/:barcode/custody` (mutations `@Audited` — custody is evidence). Web EN+UR `/samples` (scanner-first bench station: barcode lookup → custody timeline + current stage/custody status → next-stage scan (location/note/container) + lost/damaged/reject with reason; `noindex`). Gates green: typecheck 26/26, lint 15/15 (0 warn), build 15/15 (`/samples` route 2.1 kB / 147 kB First Load), jest **api 540/540 [+25]** + **db 129/129 [+3 sample-tracking-isolation]** + i18n 19/19 parity. Vertical smoke: pharmacy tenant→404; RECEPTION (no lab.order)→403; unknown barcode→404; scan RECEIVED→sample received + event logged; illegal COLLECTED→ACCESSIONED→400; REJECTED/unknown stage on scan route→400; reject damaged→custody DAMAGED + status REJECTED + reason evidence; home-collected sample reject→recollection re-opened + original RECOLLECT; walk-in reject→no recollection; result with another order's barcode→400; result a REJECTED sample→400.
- **Current build-step:** **29 — Phase 2: Purchase & Suppliers.** Read `specs/29-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/29-purchase-suppliers.md`
- **Branch state:** `feat/28-pharmacy-inventory` complete (awaiting controller merge to `staging`), stacked on `feat/27-sample-tracking` → `feat/26-phlebotomist-app` → `feat/25-home-collection` → `feat/24-admin-dashboard` → `feat/23-accounts-finance` → `feat/22-doctor-commission` → `feat/21-billing-cashier` → `feat/20-pharmacy-pos` → `feat/19-laboratory` → `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **28 — Pharmacy Inventory (Batch / Expiry / FEFO)** ✅ 2026-07-05 — real inventory UNDER the **20 POS** behind `pharmacy.inventory` (dependsOn `pharmacy.pos`); solves the founding hidden stock loss. Extended 20 ADDITIVELY: on-hand split into **batches** (no/expiry/qty/cost); a sale allocates **FEFO** (soonest-expiry first) — the **20 finalize hook now resolves to a specific batch** (per-batch `applyBatchDelta` + SALE movement w/ `batch_id`; aggregate still moves full via the SAME compensating write; shortfall→batchless remainder; return restocks the batch). Flag OFF / no batches → 20 sells simple qty (FEFO empty → additive; **no existing pharmacy test changed**). Pure `@mp/shared/pharmacy-inventory` (`allocateFefo`, `expiryBand`/`isLowStock` NEAR=90/LOW=10, `computeVariance`/`needsAccountability`, `valuation`, `ADJUST_TYPES`/`isLossAdjust`). NEW `InventoryModule` (`@RequireFeature('pharmacy.inventory')` 404 THEN `inventory.manage`; write-off/adjust role-locked to `pharmacy.stock.adjust` method-override): `/inventory/stock`,`/batches`(POST bumps aggregate + PURCHASE movement),`/alerts`(expiry+low-stock→24),`/valuation`(cost+retail),`POST /adjustments`(damage/expire/correction/write-off, reason mandatory), reconciliation `POST /reconciliations`(snapshot expected)/`/:id`/`/:id/counts`/`/:id/close` — **THEFT TRIPWIRE**: every non-zero variance needs **reason + responsible person** before close (else 400); close squares book (CORRECTION adj + ADJUST movement attributed to the person), locks+audits, net **loss best-effort→23** (full: COGS/Inventory via `accounting_events`, source PURCHASE; degrades). OWNS only 4 tables (`batches`/`reconciliations`/`reconciliation_lines`/`stock_adjustments`); 20 stock/movements/medicines read+moved via its OWN repo (like 22/24), never importing 20. FEFO read/decrement on 20 `PharmacyRepo` (`listBatchesForFefo`/`applyBatchDelta`; `SaleItem.batchId` filled on a single-batch line). NEW migration `…240000` (4 tables + enums `ReconStatus`/`AdjustType`; 20 `stock_movements.batch_id`/`sale_items.batch_id` already nullable; `apply_tenant_rls` all 4, T1/T2+WITH-CHECK+fail-closed proven `pharmacy-inventory-isolation`). Flag added `dependsOn['pharmacy.pos']`; perms pre-existed (PHARMACIST both; ADMIN/MANAGER manage-only). Web EN+UR `/inventory` (tabbed: Stock colour-coded batches + receive/write-off forms, Alerts, Valuation, Reconcile count sheet). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/inventory` 3.3 kB), jest api 563/563 (+23) + db 133/133 (+4 pharmacy-inventory-isolation) + i18n 19/19. Smoke: lab→404, SALESMAN→403, batch→+qty+PURCHASE, finalize→FEFO soonest-first, alerts EXPIRED/NEAR+low, close blocked w/o reason+person→400 then squares 100→96 attributed, ADMIN write-off→403, PHARMACIST no-reason→400.
- **27 — Sample Tracking & Chain of Custody** ✅ 2026-07-05 — chain of custody over the **19 sample** (extended additively). Each **scan** records a stage event (who/when/where); the ordered chain is the sample's **custody timeline**. Legal forward transitions (`SAMPLE_STAGE_FLOW`/`canTransitionStage`: COLLECTED→IN_TRANSIT→RECEIVED→ACCESSIONED→ON_BENCH→STORED/DISPOSED) — illegal jump→400; REJECTED rides the reject action (not a scan). **Reject** (LOST/DAMAGED/REJECTED+reason) sets `custodyStatus`, logs a REJECTED evidence event, marks sample REJECTED, and **best-effort re-opens the home visit as a recollection (25)** (`HomeCollectionService.recollectForRejectedSample`; walk-in→none). **MISMATCH GUARD** gates **19 resulting** (`enterResults`/`approve`→`assertResultableSample`): a scanned barcode from another order→400, a non-ACTIVE sample→400 (patient-safety); only ever tightens 19. OWNS only `sample_events`; the `samples` row (custody/container/storage) via NEW `LabService` seams (`findSampleWithOrder`/`updateSampleFields`). NEW migration `…230000` (1 table + `Sample` extended custodyStatus/containerType/storageLocation; enums `SampleStage`/`CustodyStatus`; `apply_tenant_rls` T1/T2+WITH-CHECK+fail-closed proven `sample-tracking-isolation`). **Flag+perm REUSED — no catalog change** (PRESENT when `lab.results` OR `lab.homeCollection`, programmatic flag-then-perm like 18; SAME `lab.order`; pharmacy vertical→404). Pure `@mp/shared/samples`. API `SampleTrackingModule`: `POST /samples/:barcode/scan`, `/reject`, `GET /:barcode/custody` (mutations `@Audited`). Web EN+UR `/samples` (scanner-first bench station). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/samples` 2.1 kB), jest api 540/540 (+25) + db 129/129 (+3 sample-tracking-isolation) + i18n 19/19. Smoke: pharmacy→404, RECEPTION→403, unknown barcode→404, illegal transition→400, reject→custody+status+reason, home reject→recollection+original RECOLLECT, walk-in reject→none, mismatch barcode→400, result REJECTED sample→400.
- **26 — Phlebotomist Field App** ✅ 2026-07-05 — the phlebotomist's **offline field PWA** over the SAME home-collection bookings (25). Lists **my visits** (route-ordered by assignment sequence then slot, enriched w/ patient/tests/barcode/proof-flag), drives the doorstep flow **en route → collect (barcode ties the sample to the lab order via `LabService.collectSample` 27; then COLLECTED + patient WhatsApp) → proof (signature+photo) → collected**, flags a **failed draw** (→ FAILED). **Every mutation OFFLINE-IDEMPOTENT** (10, `SyncService.runIdempotent` on the `Idempotency-Key` header; `collection_proofs.clientActionId` unique = 2nd line). OWNS only 1 new table `collection_proofs`; visits/status (HomeCollectionService), sample tie (LabService), patient (PatientService) via services. Narrowed to caller's **own assignments** (not mine→404). NEW migration `…220000` (1 table; `apply_tenant_rls`, T1/T2+WITH-CHECK+fail-closed+global-unique clientActionId proven `phlebotomist-isolation`). **Flag+perm REUSED — no catalog change** (SAME `lab.homeCollection`+`home.collection.manage`; PHLEBOTOMIST/RIDER). Pure `@mp/shared/phlebotomist` (`FIELD_STATUSES`=EN_ROUTE/FAILED, `isFieldStatus`, `routeSequenceOf`/`compareVisits`). API `PhlebotomistModule` (class `@RequireFeature`+`@RequirePermission` → 404 THEN 403): `/phlebotomist/visits`,`/visits/:id/status`,`/visits/:id/collect`,`/visits/:id/proof` (mutations require `Idempotency-Key`). Web EN+UR `/phlebotomist` (large-touch offline PWA: route-ordered list, call/navigate, collect panel = barcode + pointer signature pad + photo; server-then-outbox; manifest shortcut). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/phlebotomist` 3.9 kB), jest api 515/515 (+17) + db 126/126 (+4 phlebotomist-isolation) + i18n 19/19. Smoke: lab-preset→404, LAB_TECH→403, visits=only my assignments route-ordered, COLLECTED-via-status→400, no Idempotency-Key→400, replayed collect/proof→same result+one sample/proof, not-mine→404.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
