> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **23 — Phase 2: Accounts & Finance (Lite / Full).** ✅ DONE/APPROVED (2026-07-05) — the books in two flag-chosen modes, vertical-aware. **LITE** (`accounting.lite`, default-on): a `CashLedger` of simple cash **IN/OUT** with a category → **running balance** (Σin−Σout) + a **by-category period summary** + **udhaar visibility** (outstanding AR read READ-ONLY from invoices), no double-entry. **FULL** (`accounting.full`): a **vertical-aware chart** seeded per enabled module (`chartSeedFor` → base Cash/Bank/AR/AP/Income/Expense/Equity always; pharmacy adds Inventory/COGS/Sales, lab adds Lab Income, clinic adds Consultation Income, commission adds the expense+payable — **a lab NEVER gets a COGS account**, a pharmacy does) → an **IDEMPOTENT poster** that turns the SAME outbox `accounting_events` 21 (BILLING/UDHAAR) + 22 (COMMISSION) already emit into balanced `JournalEntry`+`JournalLine` (dedup on `@@unique(tenant,source,sourceRefId)` + `createMany` P2002-catch so a **re-emit never double-posts**; each event's code→accountId resolved, a missing code lazily materialised as EXPENSE so a posting never fails; `postedAt` marks consumed) → **reports** (pure `@mp/shared` reducers: **trial balance** Σdebit=Σcredit, **P&L** income−expense, **balance sheet** Assets=Liab+Equity+RetainedEarnings, **day book**, **AR aging** from open invoices) run after an auto-post so numbers are always fresh → **manual journal** (rejects unbalanced 400, refuses a CLOSED period 400) + **opening balances** (natural-side lines balanced to Opening Balance Equity, idempotent ref `opening`, the lite→full switch) → **fiscal period** create + **close** which **locks** it (an event dated inside a closed period stays unposted; a manual journal into it is refused). NEW migration `…190000` (5 tables Account/JournalEntry/JournalLine/FiscalPeriod/CashLedger + `AccountType`/`PostingSource`/`PeriodStatus`/`CashDir` enums; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven in a new `accounts-isolation` pglite spec). Perms `accounts.view`/`accounts.post`/`accounts.period.close` + flags `accounting.full`/`accounting.lite` already in catalogs — **no catalog change**. Pure `@mp/shared/accounts` (`chartSeedFor`/`accountTypeForCode`, `trialBalance`/`profitAndLoss`/`balanceSheet`/`generalLedger`/`arAging`, `cashBalance`/`cashSummarize`, enum mirrors). API `AccountsModule` (**programmatic mode-flag 404 FIRST then perm**; full-only handlers additionally 404 when `accounting.full` off): `/accounts/mode`,`/cash`(lite),`/chart`,`/seed`,`/journal`,`/opening`,`/reports`,`/ledger`,`/periods`(+`/:id/close`). Web EN+UR `/accounts` (mode auto-detected by probing `/accounts/mode`: LITE = one cash screen; FULL = Reports/Chart/Journal/Periods tabs w/ **animated P&L bar chart**; optional/`noindex`). Gates green: typecheck 26/26, lint 15/15 (0 warn), build 15/15 (`/accounts` route), jest **api 448/448 [+33]** + **db 114/114 [+6 accounts-isolation]** + i18n 19/19 parity. Vertical smoke: lab tenant chart has LAB_INCOME but no COGS/commission accounts; pharmacy tenant→lite mode; full-suite→full mode w/ COGS; both-flags-off tenant→404; ADMIN (no accounts perm)→403; a re-emitted event posts ONE journal.
- **Prior completed build-step:** **22 — Phase 1: Doctor Commission & Payouts.** ✅ DONE/APPROVED (2026-07-05) — per-doctor, per-tenant-configurable earnings across the whole suite: a **CommissionConfig** (SALARY / PER_PATIENT / PERCENT(consult|lab|pharmacy) / HYBRID / REFERRAL) with **effective-date rate history** (a new config deactivates the doctor's prior active one; the latest active config `effectiveFrom ≤` an activity's date applies, so a rate change never rewrites history) → a **sweep** that assembles the period's real activity READ-ONLY from encounters (17), lab orders (19), pharmacy sales (20 via sale→cart→prescription→doctor) and **billing revenue** (21, PAID/PARTIAL invoices keyed by source+refId) and turns each doctor's rates into **idempotent `CommissionEntry` credits** — each `sourceRefId` embeds its BASIS (`salary:<period>`/`pp:`/`consult:`/`lab:`/`pharmacy:`/`referral:`) so the `@@unique(tenant,doctor,sourceType,sourceRefId)` makes a **recompute never double-credit** → a **period report** grouped by source (recompute-then-breakdown) + a **doctor self-view** (`/commission/my`, flag-only, no manage perm, matches admin to the paisa) → a **payout** (`CommissionPayout` DRAFT→APPROVED→PAID; a DRAFT refreshes to the live total, APPROVED/PAID is locked) that on **PAID posts a balanced expense `AccountingEvent`** to 23 (Dr `COMMISSION_EXPENSE` / Cr `COMMISSION_PAYABLE`, Σdebit=Σcredit, idempotent outbox `commission:<payoutId>` so a re-pay never double-posts; `journalRef` recorded). **Per-tenant allowed bases** are DERIVED from enabled verticals (no extra table): salary/per-patient always; consult/lab/pharmacy per flag; referral when lab OR pharmacy — a disallowed rate is **rejected 403**. NEW migration `…180000` (3 tables + `CommissionType`/`CommSource`/`PayoutStatus` enums; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven in a new `commission-isolation` pglite spec). Perm `commission.manage` + flag `clinic.commission` already in catalogs — **no catalog change**. Pure `@mp/shared/commission` (`commissionEntriesFor` = the ONE engine, `periodKeyOf`/`periodRange`, `allowedBasesFor`/`RATE_BASIS`, `buildCommissionLines`/`COMMISSION_ACCOUNT_CODES`, enum mirrors). API `CommissionModule` (**programmatic flag 404 FIRST then `commission.manage`** like 18/21; `/my` needs flag + linked doctor only): `/commission/configs`,`/doctors`,`/recompute`,`/report`,`/my`,`/payouts`(+`/:id/approve`,`/:id/pay`). Reuses the same idempotent outbox 21 emits to. Web EN+UR `/commission` (admin Config/Payouts/Report tabs w/ **animated breakdown bar chart**; doctor-only My-Earnings view; role auto-detected by probing the manage-gated overview; optional/`noindex`). Gates green: typecheck 26/26, lint 15/15 (0 warn), build 15/15 (`/commission` route), jest **api 415/415 [+28]** + **db 108/108 [+5 commission-isolation]** + i18n 19/19 parity. Vertical smoke: Lab-only tenant→404, clinic tenant rejects a lab rate (403) & allows consult, DOCTOR w/o manage→403 on admin but 200 on `/my`.
- **Current build-step:** **24 — Phase 2: Admin Dashboard.** Read `specs/24-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/24-admin-dashboard.md`
- **Branch state:** `feat/23-accounts-finance` complete (awaiting controller merge to `staging`), stacked on `feat/22-doctor-commission` → `feat/21-billing-cashier` → `feat/20-pharmacy-pos` → `feat/19-laboratory` → `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. Step 22 branch stacks on it if not yet merged. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **23 — Accounts & Finance (Lite / Full)** ✅ 2026-07-05 — the books in two flag-chosen, vertical-aware modes. **LITE** (`accounting.lite`, default-on): `CashLedger` cash IN/OUT + running balance + by-category period summary + udhaar visibility (read-only from invoices), no double-entry. **FULL** (`accounting.full`): a **vertical-aware chart** (`chartSeedFor` — base always; pharmacy Inventory/COGS/Sales, lab Lab Income, clinic Consult Income, commission expense/payable; **a lab never gets COGS**) → an **IDEMPOTENT poster** turning the SAME outbox 21/22 emit into balanced `JournalEntry`/`JournalLine` (`@@unique(tenant,source,sourceRefId)`+P2002-catch → **re-emit never double-posts**; missing code lazily materialised; `postedAt` marks consumed) → **reports** (pure `@mp/shared`: trial balance Σd=Σc, P&L, balance sheet Assets=Liab+Equity+RetEarn, day book, AR aging) fresh after auto-post → **manual journal** (unbalanced→400, closed period→400) + **opening balances** (→ Opening Equity, idempotent) → **fiscal period** create+**close** locks it. NEW migration `…190000` (5 tables + 4 enums; all `apply_tenant_rls`, T1/T2+WITH-CHECK+fail-closed proven). Perms/flags already in catalogs (no change). Pure `@mp/shared/accounts`. API `AccountsModule` (**mode-flag 404 FIRST then perm**; full-only→404 when full off): `/accounts/mode`,`/cash`,`/chart`,`/seed`,`/journal`,`/opening`,`/reports`,`/ledger`,`/periods`(+close). Web EN+UR `/accounts` (mode auto-detected: LITE one screen; FULL Reports/Chart/Journal/Periods + **animated P&L chart**). Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 448/448 (+33) + db 114/114 (+6 accounts-isolation) + i18n 19/19. Vertical smoke: lab chart no COGS, pharmacy→lite, full→full w/ COGS, both-flags-off→404, ADMIN (no perm)→403, re-emit posts ONE journal.
- **22 — Doctor Commission & Payouts** ✅ 2026-07-05 — per-doctor, per-tenant-configurable earnings: `CommissionConfig` (SALARY/PER_PATIENT/PERCENT(consult|lab|pharmacy)/HYBRID/REFERRAL) w/ **effective-date rate history** → a **sweep** assembling the period's real activity READ-ONLY from encounters(17)/lab(19)/pharmacy(20)/**billing revenue**(21 PAID+PARTIAL) into **idempotent `CommissionEntry`** (each `sourceRefId` embeds its basis; `@@unique(tenant,doctor,sourceType,sourceRefId)` → recompute never double-credits) → **report** grouped by source + **doctor self-view** `/commission/my` (flag-only, matches admin) → **payout** (DRAFT→APPROVED→PAID; on PAID posts a **balanced expense** `AccountingEvent` to 23, idempotent `commission:<id>` outbox, `journalRef` set). **Allowed bases DERIVED from enabled verticals** (no extra table; disallowed rate→403). NEW migration `…180000` (3 tables + `CommissionType`/`CommSource`/`PayoutStatus`; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Perm `commission.manage` + flag `clinic.commission` already in catalogs (no change). Pure `@mp/shared/commission` (`commissionEntriesFor`, `periodKeyOf`/`periodRange`, `allowedBasesFor`, `buildCommissionLines`). API `CommissionModule` (**flag 404 FIRST then perm**): `/commission/configs`,`/doctors`,`/recompute`,`/report`,`/my`,`/payouts`(+approve/pay). Web EN+UR `/commission` (admin Config/Payouts/Report + **animated breakdown chart**; doctor My-Earnings; role auto-detected). Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 415/415 (+28) + db 108/108 (+5 commission-isolation) + i18n 19/19 parity. Vertical smoke: Lab-only→404, clinic rejects lab rate/allows consult, DOCTOR→403 admin but 200 `/my`.
- **21 — Billing & Cashier** ✅ 2026-07-04 — ONE money surface across OPD/lab/pharmacy: **unified invoice** from an **enabled source** (CONSULT/LAB/PHARMACY/PACKAGE/OTHER; disabled source→400; CONSULT keys the fee since 17 stores none) → **payments** (cash/card/online/**udhaar**; partial→PARTIAL, **CREDIT→PatientCredit 14 + AR event**) → **refunds** (reason-required; **over-threshold→approval** `billing.refund.approve`; refundable subtracts pending so paid **never over-refunded**; partial reopens balance, full→REFUNDED) → **cashier shift** (float→close **denomination counter→variance**, the theft tripwire; cash-out charged to the requesting cashier) → **end-of-day per-person** (byMethod/refunds/variance; `collected` excludes udhaar) → **receipts** (80mm+A4, 07). Each settle/refund writes an **idempotent balanced `AccountingEvent`** (@@unique tenant,source,sourceRefId) for 23; `pay`/`refund`/`close` ride `runIdempotent` (Idempotency-Key) → no double-charge/refund/post. NEW migration `…170000` (7 tables + 5 enums; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Perms/flags already in catalogs (no change). Pure `@mp/shared/billing` (`computeInvoiceTotals`, `invoiceStatusFor`/`balanceDue`, `refundNeedsApproval`, `shiftExpectedCash`/`shiftVariance`/`denominationTotal`, `buildPaymentLines`/`buildRefundLines`/`assertBalanced`). API `BillingModule` (3 controllers, **programmatic OR-flag 404 then permission**): `/invoices`,`/shifts`,`/billing/reports`. Reuses `PatientService.addCredit`. Web EN+UR `/billing` (Bill/Shift/Report; denomination counter + thermal/A4 receipt). Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 387/387 (+43) + db 103/103 (+5 billing-isolation) + i18n 19/19 parity. Vertical smoke: lab-only bills lab & rejects pharmacy, no-billable→404. Perf ➖ (internal cashier tool).

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
