> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **21 — Phase 1: Billing & Cashier.** ✅ DONE/APPROVED (2026-07-04) — ONE money surface across OPD (17), lab (19) and pharmacy (20): **unified invoice** built from an **enabled source** (`clinic.consultation`→CONSULT / `lab.results`→LAB+PACKAGE / `pharmacy.pos`→PHARMACY / OTHER=manual; a **disabled source is rejected 400**; lines auto-pulled from the source doc read-only, CONSULT keys the fee since 17 stores none) → **payments** (cash/card/online/**credit=udhaar**; partial→PARTIAL+balance, `paid`≥total→PAID; **CREDIT posts a PatientCredit entry 14 + an AR accounting event**) → **refunds** (reason-required; **over-threshold→REQUESTED until a `billing.refund.approve` holder approves**, at/under clears at once; refundable subtracts pending/approved refunds so paid is **never over-refunded**; partial refund reopens the balance, only a full refund→REFUNDED) → **cashier shift** (open float → collect → **close count w/ denomination counter → variance = counted − (float + cash in − cash out)**, the theft tripwire; cash-out charged to the **requesting** cashier's shift) → **end-of-day per-person report** (collections by method, refunds, variance; `collected` excludes udhaar) → **receipts** (thermal 80mm + A4, 07 letterhead). Each settle/refund writes an **idempotent double-entry `AccountingEvent`** (Σdebit=Σcredit, `@@unique(tenant,source,sourceRefId)` outbox) 23 consumes; `pay`/`refund`/shift-`close` all ride **`SyncService.runIdempotent`** (Idempotency-Key) so a replay never double-charges/double-refunds/double-posts. NEW migration `…170000` (7 tables: `invoices`/`invoice_items`/`payments`[`client_action_id @unique`]/`refunds`[+`shift_id`]/`cashier_shifts`/`shift_counts`/`accounting_events`[@@unique tenant,source,sourceRefId] + `InvoiceSource`/`InvoiceStatus`/`PayMethod`/`RefundStatus`/`ShiftStatus` enums; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Perms `billing.manage`/`billing.refund.approve` + flags already in catalogs — **no catalog change**. Pure `@mp/shared/billing` (`computeInvoiceTotals` w/ discount clamp+tax, `invoiceStatusFor`/`balanceDue`, `refundNeedsApproval`, `denominationTotal`/`shiftExpectedCash`/`shiftVariance` = the exact tripwire math, `buildPaymentLines`/`buildRefundLines`/`assertBalanced` = balanced double-entry; enum mirrors). API `BillingModule` (3 controllers, **programmatic OR-flag presence 404 FIRST then permission** like 18 — present when ANY billable flag on): `/invoices` create/list/detail/pay/refund/refund-decision/receipt; `/shifts` open/close/current/:id/report; `/billing/reports` end-of-day/refunds-pending. Reuses `PatientService.addCredit` (14). Web EN+UR `/billing` (Bill/Shift/Report tabs; live invoice preview + denomination counter via shared math; thermal+A4 receipt print; refund approvals; optional via `/flags/me`). Gates green: typecheck 26/26, lint 15/15 (0 warn), build 15/15 (`/billing` route), jest **api 387/387 [+43]** + **db 103/103 [+5 billing-isolation]** + i18n 19/19 parity. Vertical smoke: lab-only bills lab & rejects pharmacy, no-billable→404. Perf ➖ (internal cashier tool, `robots:noindex`).
- **Current build-step:** **22 — Phase 2: Doctor Commission.** Read `specs/22-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/22-doctor-commission.md`
- **Branch state:** `feat/21-billing-cashier` complete (awaiting controller merge to `staging`), stacked on `feat/20-pharmacy-pos` → `feat/19-laboratory` → `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. Step 22 branch stacks on it if not yet merged. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **21 — Billing & Cashier** ✅ 2026-07-04 — ONE money surface across OPD/lab/pharmacy: **unified invoice** from an **enabled source** (CONSULT/LAB/PHARMACY/PACKAGE/OTHER; disabled source→400; CONSULT keys the fee since 17 stores none) → **payments** (cash/card/online/**udhaar**; partial→PARTIAL, **CREDIT→PatientCredit 14 + AR event**) → **refunds** (reason-required; **over-threshold→approval** `billing.refund.approve`; refundable subtracts pending so paid **never over-refunded**; partial reopens balance, full→REFUNDED) → **cashier shift** (float→close **denomination counter→variance**, the theft tripwire; cash-out charged to the requesting cashier) → **end-of-day per-person** (byMethod/refunds/variance; `collected` excludes udhaar) → **receipts** (80mm+A4, 07). Each settle/refund writes an **idempotent balanced `AccountingEvent`** (@@unique tenant,source,sourceRefId) for 23; `pay`/`refund`/`close` ride `runIdempotent` (Idempotency-Key) → no double-charge/refund/post. NEW migration `…170000` (7 tables + 5 enums; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Perms/flags already in catalogs (no change). Pure `@mp/shared/billing` (`computeInvoiceTotals`, `invoiceStatusFor`/`balanceDue`, `refundNeedsApproval`, `shiftExpectedCash`/`shiftVariance`/`denominationTotal`, `buildPaymentLines`/`buildRefundLines`/`assertBalanced`). API `BillingModule` (3 controllers, **programmatic OR-flag 404 then permission**): `/invoices`,`/shifts`,`/billing/reports`. Reuses `PatientService.addCredit`. Web EN+UR `/billing` (Bill/Shift/Report; denomination counter + thermal/A4 receipt). Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 387/387 (+43) + db 103/103 (+5 billing-isolation) + i18n 19/19 parity. Vertical smoke: lab-only bills lab & rejects pharmacy, no-billable→404. Perf ➖ (internal cashier tool).
- **20 — Pharmacy POS (Salesman→Cashier + AI Reader)** ✅ 2026-07-04 — the pharmacy counter: extended **medicine master** (+**barcode**/sale/cost price, barcode search) → **multi-salesman cart** (stock-out **alternatives**, hold/resume) → **push to cashier** → **finalize** (offline-idempotent, **compensating** stock decrement + logged `StockMovement`, 06/10) → **80mm slip** (07) → **returns** (reason-required, →21). **AI reader** (`from-prescription`, flag-gated) seeds an **OPEN DRAFT** from a prescription's INTERNAL meds (18) — **never auto-sells** (degrades until 35/36). Per-med **profit** + **barcode labels** (flag). NEW migration `…160000` (Medicine EXTEND additive + 6 tables + `MovementType`/`CartStatus`/`SaleStatus`; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Pure `@mp/shared/pharmacy` (`computeSaleTotals`, profit helpers, lifecycle guards). API `PharmacyModule` (4 controllers) under `/pharmacy/*`; `finalize` inside `runIdempotent`. Web EN+UR `/pharmacy`. Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 344/344 (+23) + db 98/98 (+4) + i18n 19/19. Vertical smoke: present pharmacy/clinic_pharmacy/full, 404 lab.
- **19 — Laboratory (Orders + Smart Reports)** ✅ 2026-07-04 — the LIS: catalog (tests+**prep**+packages+**smart result templates** w/ **age+gender ranges**) → order → sample (**barcode**) → result (**auto-flagged** high/low/**critical** by patient age+gender, 14) → **pathologist sign** → **branded report** (07) + WhatsApp (09) + **TAT** + **re-test** + **amend-with-reason** (versioned, audited). NEW migration `…150000` (8 tables + `LabStatus`/`SampleStatus`; all `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven). Pure `@mp/shared/lab` (`evaluateResults`, `computeTatHours`, `ageYears`, `isDeliverable`). API `LabModule` (4 controllers) **split-gated** `lab.catalog`/`lab.results`. Web EN+UR `/lab`. Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 321/321 (+25) + db 94/94 (+4) + i18n 19/19.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
