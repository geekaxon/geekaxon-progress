> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **10 — Foundation: Offline-First Core.** ✅ DONE/APPROVED (2026-07-03) — the always-on local-first sync seam every critical-path module (15 token, 16 vitals, 18 capture, 20 POS) builds on. NEW pure package **`@mp/offline`** (framework-free, reused by web + API): outbox model + queue helpers (`enqueue` idempotent, `dedupeActions`, `drainableActions`, `canRunWhileOffline`), idempotency contract (`SYNC_IDEMPOTENCY_HEADER`, `isValidClientActionId`), `syncBackoffMs`, pull cursor (`advanceCursor`/`parseCursor`), `deriveSyncStatus`, and the **conflict policy** — `resolveServerLww` (DEFAULT server-authoritative LWW + log) + `applyCompensatingDelta`/`resolveCompensatingDelta` (additive stock write, never LWW) + per-entity `ConflictPolicy` override registry. DB: `SyncIdempotency` (client_action_id UNIQUE, result JSON — dedupe ledger) + `SyncDemoEntity` (`rev` = tenant-monotonic revision doubling as change-cursor + optimistic-lock token; stock_qty) + `SyncConflict` (append-only conflict log) — NEW migration `20260703170000`, all three canonical `apply_tenant_rls`, T1/T2 proven in pglite. API **`SyncModule`**: `SyncService.runIdempotent()` (at-most-once, no double-post) + `getChanges()` (tenant deltas since cursor) + `upsertDemo()` (LWW conflict detect+log) + `applyStockDelta()` (compensating); repo seam+fake; NEW `@IdempotencyKey()` param decorator (the header seam all mutations ride). Endpoints `/sync`: `GET changes?since=` (any authed, tenant-scoped), `POST demo/upsert` + `POST demo/stock` (require Idempotency-Key, gated by NEW `sync.inspect` perm, `@Audited`), `GET conflicts`. Web: `lib/offline-db.ts` (IndexedDB outbox) + `OfflineProvider`/`useSyncStatus` (local-first `enqueue`, online/offline events, reconnect drain w/ backoff, AI/network-only "will run when online") in `layout.tsx`, always-visible `OfflineIndicator` (offline→syncing→synced), EN+UR `/admin/sync` inspector (create offline, sell stock, queue AI, watch synced items + conflicts). **Launch note:** in-process outbox dispatch today; Redis/BullMQ worker-backed server dispatch + richer delta feed + per-module conflict rules (token/vitals/POS) = `[DECIDE AT BUILD]`. Gates green (typecheck 22/22, lint 13/13, build 13/13, jest: db 55/55 incl. offline-isolation + api 154/154 incl. 10 sync e2e + i18n 19/19 incl. parity + notifications 18/18 + offline 29/29).
- **Current build-step:** **11 — (next module in build order).** Read `specs/11-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/11-consent-terms.md`
- **Branch state:** `feat/10-offline-core` complete (awaiting controller merge to `staging`), stacked on `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. Step 11 branch stacks on it if not yet merged. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **10 — Offline-First Core** ✅ 2026-07-03 — NEW pure `@mp/offline` (outbox model + `enqueue`/`dedupeActions`/`drainableActions`, `SYNC_IDEMPOTENCY_HEADER` idempotency contract, `syncBackoffMs`, pull cursor `advanceCursor`, `deriveSyncStatus`, conflict policy: `resolveServerLww` DEFAULT + `applyCompensatingDelta` stock + `ConflictPolicy` registry). DB `SyncIdempotency`+`SyncDemoEntity`(`rev`=cursor+lock token)+`SyncConflict` (NEW migration `…170000`, all three `apply_tenant_rls`, T1/T2 proven). API `SyncModule`: `runIdempotent()` (no double-post) + `getChanges()` + `upsertDemo()` (LWW conflict+log) + `applyStockDelta()` (compensating); `@IdempotencyKey()` decorator; `/sync` routes (`GET changes` any-authed tenant-scoped, `POST demo/upsert`+`demo/stock` gated by NEW `sync.inspect`, `@Audited`, `GET conflicts`). Web IndexedDB outbox + `OfflineProvider`/`useSyncStatus` (reconnect drain+backoff, AI queues "will run when online") in layout + always-visible `OfflineIndicator`, EN+UR `/admin/sync` inspector. Launch note: in-process dispatch today; Redis worker + per-module conflict rules = `[DECIDE AT BUILD]`. Gates: typecheck 22/22, lint 13/13, build 13/13, jest db 55/55 + api 154/154 (10 sync e2e) + i18n 19/19 + notifications 18/18 + offline 29/29.
- **09 — Notifications Engine (provider-agnostic messaging)** ✅ 2026-07-03 — NEW pure `@mp/notifications` (provider contract, `renderTemplate`/`{var}`, WhatsApp→SMS `resolveChannelChain`, retry policy, guarded `webhookNextStatus`, `auth.otp` system templates EN+UR). DB `NotificationTemplate`+`NotificationLog` + enums `Channel`/`TemplateStatus`/`DeliveryStatus` (NEW migration `…160000`, both tables `apply_tenant_rls`, T1/T2 proven). API `NotificationsModule`: `notify()` (consent→sender identity (07)→resolve/render→channel-chain+per-channel retry→log) + `sendOtp()` + `handleWebhook()` + template CRUD/approve; 2 config-swappable mock WA providers (no secret in repo) + SMS/in-app; `AllowAllConsentProvider` seam (14 swaps real); controller gated by NEW `notifications.manage` perm, `@Audited`. **03 OTP stub REPLACED** (`OTP_TRANSPORT`→`NotificationOtpTransport`, auth API unchanged). Web EN+UR `/admin/notifications` (editor+live preview RTL-UR+var chips, approve, status-filtered log). Launch note: WA Cloud API provider + verified number + Redis worker dispatch = `[DECIDE AT BUILD]`. Gates: typecheck 20/20, lint 12/12, build 12/12, jest db 48/48 + api 144/144 (13 notif e2e) + i18n 19/19 + notifications 18/18.
- **08 — i18n Base (EN+UR UI, EN+UR+Roman AI)** ✅ 2026-07-03 — NEW `@mp/i18n` (EN+UR catalogs = single source of truth; `translate` dot-path+`{var}`, `formatLabel` enum→label, **parity gate** fails on any EN↔UR key gap both ways). `@mp/shared` **AI-lang contract** (`AiLang` en/ur/roman-ur, `detectAiLang`/`normalizeForAi`) for all AI surfaces. DB `enum Lang` + nullable `User.language` (NEW migration `…150000`, inherits users RLS). API `PreferencesModule` (`GET`/`PUT /me/preferences`, authed+self-scoped, `@Audited`; role default patient→UR/staff→EN) w/ repo seam+fake. Web `I18nProvider`/`useT` (cookie+localStorage+account sync, no-flash), `serverLocale`→`<html lang/dir>`, `LanguageSwitcher`, `/settings`, `auth-i18n` re-based as adapter (pages untouched), all shells honour cookie. **Urdu font strategy** (`@font-face` swap + `unicode-range` subset + `local()` fallback, offline-safe). **ESLint i18n guard** = no hardcoded UI string. Gates: typecheck 18/18, lint 11/11, build 11/11, jest db 42/42 + api 131/131 + i18n 19/19 (incl. parity).

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
