> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **33 — Phase 3: Doctor Portal + AI Decision Support.** ✅ DONE/APPROVED (2026-07-05) — the doctor's remote workspace + **assist-only CDS** behind `clinic.doctorPortal` (dependsOn `clinic.consultation`; absent→404); the AI panel additionally needs `ai.clinical`. **CDS ASSIST-ONLY: the doctor always decides + signs; nothing auto-writes.** NEW **DoctorPortalModule** (`@RequireFeature('clinic.doctorPortal')` 404 FIRST → `@Roles(Role.DOCTOR)`; other staff→403): every path resolves the caller's OWN `Doctor` (`principal.userId → Doctor.userId`) + scopes to it — sees ONLY own patients/appointments/reports/suggestions, and any write's `doctorId` is ALWAYS the caller (never a body field). Patient scope HARD (`assertLinked` via a shared appointment/encounter → stranger 404). `GET /doctor/me`, `/doctor/appointments`, `/doctor/patients`(+`/:id`: profile+allergies+current meds+lab reports+prior encounters), `POST /doctor/encounters`(+`PATCH`) (17 shape), `POST /doctor/prescriptions` (18 shape; mirrors the **18 allergy contract** — proposed meds cross-checked vs REAL allergies (14) → conflict w/o `allergyOverrideReason`→**409**, resubmit w/ reason), `GET /doctor/earnings` (REUSES `CommissionService.myEarnings` → **matches 22**), self-scheduling `GET/POST/DELETE /doctor/availability`+`/leave`+`GET /doctor/slots` (SAME pure `buildDaySlots` as 15), **AI** `POST /doctor/cds/suggest` (needs `ai.clinical`→404 if off), `/cds/:id/decision`, `GET /doctor/cds`(+`/:id` own→404). **CDS** SUGGESTS (differentials, drug-drug + drug-allergy flags from the patient's REAL meds (18)+allergies (14), note summary) → every suggestion LOGGED (`cds_suggestions`, `PENDING`); doctor's **accept/edit/reject** a SEPARATE logged act. Generation via injected `DoctorCdsGateway` seam through **`@mp/ai` (35)** under exported `CDS_SYSTEM_POLICY` → deterministic `composeCdsSuggestions` safety FLOOR until 35 wired. **OWNS only 1 table** (`cds_suggestions`); all clinical data REUSES existing tables via its OWN repo (31 precedent), never importing 14/15/17/18/19. Pure `@mp/shared/doctor-cds` (`suggestDifferentials`/`crossCheckMedications`/`summarizeClinicalNote`/`composeCdsSuggestions`/`cdsDisclaimer`/`CDS_SYSTEM_POLICY`/`drugClassesOf`; `DoctorCdsKind`/`CdsSuggestionStatus`/`CdsSeverity`). NEW migration `…290000` (1 table + enum `CdsStatus`; `apply_tenant_rls`, T1/T2 + WITH-CHECK + fail-closed proven in a new `doctor-portal-isolation` pglite spec over 01→33). **Flags REUSED (`clinic.doctorPortal`+`ai.clinical` pre-existed) — NO catalog change, NO new permission (role-gated).** Web EN-first (staff), UR-ready `/doctor` lean island (tabs Today/Patients/Earnings/Schedule; patient view + write-note/e-prescription 409→override + **AI suggestion panel** "suggestions — you decide" w/ differentials/interactions/high-risk + Accept/Edit/Reject; earnings; availability editor + slots; panel degrades to 404). Gates green: typecheck **27/27**, lint **15/15** (0 warn), build **15/15** (`/doctor` 3.48 kB / 144 kB), jest **api 712/712 [+35]** + **db 155/155 [+4 doctor-portal-isolation]** + i18n 19/19 parity; turbo test 20/20. Vertical smoke (via e2e over the real graph + REAL gateway seam): no token→401; no-flag tenant→404; non-DOCTOR→403; DOCTOR w/o linked `Doctor`→404; own profile; lists ONLY my appts+patients (colleague leak blocked); patient view scoped + stranger→404; encounter written in OWN name (spoofed body doctorId ignored)+audited; encounter for stranger→404; e-prescription allergy→409 then override→created; earnings via commission (own data); self-schedule availability + slots mark booked 10:00 unavailable + delete; CDS assist-only differentials + drug-allergy flag from REAL records, logged PENDING (no auto-write)+audited then ACCEPTED; CDS ABSENT when `ai.clinical` off (404) while portal works; another doctor's suggestion→404; empty CDS kinds→400.
- **Prior completed build-step:** **32 — Phase 3: Patient AI Assistant (triage-only).** ✅ DONE/APPROVED (2026-07-05) — the patient's conversational health-info + **TRIAGE** surface behind `patient.aiAssistant` (dependsOn `patient.app`+`ai.suite`; absent→404). **ASSIST-ONLY: NEVER diagnosis/prescription/dosage.** NEW `AiAssistantModule` (`patient.aiAssistant` 404 → `@Roles(PATIENT)` → `@RequiresConsent('AI_ASSISTANT_DISCLAIMER')`; staff→403): `POST /me/assistant/message`(+`conversationId`+`action`), `GET /me/assistant/conversations`(+`/:id`). Each turn: `self()`(31)→normalize(08)→`classifyRisk`(EN+Roman+Urdu→EMERGENCY/ADVISE)→`classifyRequest`(refuse DIAGNOSIS/PRESCRIPTION/DOSAGE)→`classifyIntent`; red flag raises `riskFlag`(monotonic)+banner+suppresses booking. Book via chat + explain OWN report/rx (sign-gated+scoped→404). Every message logged+audited. OWNS `ai_conversations`/`ai_messages`; REUSES `PatientAppService`(31). Via `AiAssistantGateway`→`@mp/ai`(35) under `TRIAGE_SYSTEM_POLICY`→`composeAssistantReply` floor. Pure `@mp/shared/patient-ai`. Migration `…280000` (proven `patient-ai-isolation`). Flag REUSED. Web UR-first `/patient/assistant`. Gates: typecheck 27/27, lint 15/15, build 15/15, jest api 677/677 (+45) + db 151/151 (+5) + i18n 19/19.
- **Current build-step:** **34 — Phase 3: Online Pharmacy.** Read `specs/34-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/34-online-pharmacy.md`
- **Branch state:** `feat/33-doctor-portal-ai` complete (awaiting controller merge to `staging`), stacked on `feat/32-patient-ai-assistant` → `feat/31-patient-app` → `feat/30-branch-transfer` → `feat/29-purchase-suppliers` → `feat/28-pharmacy-inventory` → `feat/27-sample-tracking` → `feat/26-phlebotomist-app` → `feat/25-home-collection` → `feat/24-admin-dashboard` → `feat/23-accounts-finance` → `feat/22-doctor-commission` → `feat/21-billing-cashier` → `feat/20-pharmacy-pos` → `feat/19-laboratory` → `feat/18-eprescription` → `feat/17-doctor-consultation` → `feat/16-vitals-station` → `feat/15-appointment-token` → `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **33 — Doctor Portal + AI Decision Support** ✅ 2026-07-05 — the doctor's remote workspace + **assist-only CDS** behind `clinic.doctorPortal` (dependsOn `clinic.consultation`; absent→404); AI panel needs `ai.clinical`. **CDS ASSIST-ONLY: the doctor decides + signs; nothing auto-writes.** NEW `DoctorPortalModule` (`clinic.doctorPortal` 404→`@Roles(DOCTOR)`; other staff→403): resolves the caller's OWN `Doctor` (`userId→Doctor.userId`)+scopes to it — sees ONLY own patients/appts/reports/suggestions; any write's `doctorId` is ALWAYS the caller; patient scope HARD (`assertLinked`→stranger 404). `GET /doctor/me|appointments|patients(+/:id: profile+allergies+meds+reports+encounters)`, `POST /doctor/encounters`(+PATCH, 17), `POST /doctor/prescriptions` (18; allergy conflict vs REAL allergies→**409** unless `allergyOverrideReason`), `GET /doctor/earnings` (REUSES `CommissionService.myEarnings`→matches 22), self-scheduling availability/leave/slots (SAME `buildDaySlots` as 15), **AI** `POST /doctor/cds/suggest`(needs `ai.clinical`→404 off)+`/cds/:id/decision`+`GET /doctor/cds`(+`/:id` own→404). CDS SUGGESTS (differentials, drug-drug+drug-allergy flags from REAL meds(18)+allergies(14), note summary)→LOGGED `PENDING`; doctor accept/edit/reject a SEPARATE logged act; via `DoctorCdsGateway`→`@mp/ai`(35) under `CDS_SYSTEM_POLICY`→`composeCdsSuggestions` floor. OWNS only `cds_suggestions`; all clinical data via OWN repo (31 precedent), never importing 14/15/17/18/19. Pure `@mp/shared/doctor-cds`. Migration `…290000` (1 table + enum `CdsStatus`; proven `doctor-portal-isolation` 01→33). **Flags REUSED — NO catalog change, NO new perm (role-gated).** Web EN-first `/doctor` lean island (Today/Patients/Earnings/Schedule; patient view + note/e-rx 409→override + **AI panel** "suggestions — you decide" Accept/Edit/Reject; availability+slots; panel→404 degrade). Gates: typecheck 27/27, lint 15/15, build 15/15 (`/doctor` 3.48 kB/144 kB), jest api 712/712 (+35) + db 155/155 (+4 doctor-portal-isolation) + i18n 19/19; turbo test 20/20. Smoke: no token→401, no-flag→404, non-DOCTOR→403, no linked Doctor→404, own profile, ONLY my appts+patients (leak blocked), patient view scoped+stranger→404, encounter forced identity (spoofed doctorId ignored)+audited, encounter-for-stranger→404, e-rx allergy→409 then override→created, earnings via commission, availability+slots (booked 10:00 unavailable)+delete, CDS assist-only differentials+drug-allergy flag from REAL records logged PENDING (no auto-write)+audited then ACCEPTED, CDS absent when `ai.clinical` off→404 while portal works, another doctor's suggestion→404, empty CDS kinds→400.
- **32 — Patient AI Assistant (triage-only)** ✅ 2026-07-05 — the patient's conversational health-info + **TRIAGE** surface behind `patient.aiAssistant` (dependsOn `patient.app`+`ai.suite`; absent→404). **ASSIST-ONLY: NEVER diagnosis/prescription/dosage.** NEW `AiAssistantModule` (`patient.aiAssistant` 404 → `@Roles(PATIENT)` → `@RequiresConsent('AI_ASSISTANT_DISCLAIMER')`; staff→403): `POST /me/assistant/message`(+`conversationId`+`action`), `GET /me/assistant/conversations`(+`/:id` own-scoped→404). Each turn: `self()`(reused 31)→normalize(08)→`classifyRisk`(EN+Roman+Urdu red flags→EMERGENCY/ADVISE, escalation-biased)→`classifyRequest`(refuse DIAGNOSIS/PRESCRIPTION/DOSAGE)→`classifyIntent`; red flag raises `riskFlag`(`escalateRisk` monotonic)+banner+**suppresses booking**; disclaimer always appended. **Book via chat**(action reuses 31 DTO→real Appointment/HomeCollection) + **explain OWN report/rx**(via 31, sign-gated+scoped→stranger 404; neutral name-only). **Every message logged**+audited. OWNS only `ai_conversations`/`ai_messages`; all patient data REUSES `PatientAppService`(31). Generation via `AiAssistantGateway` seam through **`@mp/ai`(35)** under `TRIAGE_SYSTEM_POLICY` → deterministic `composeAssistantReply` safety FLOOR until 35 wired. Pure `@mp/shared/patient-ai`. Migration `…280000`(2 tables + `RiskFlag`/`MsgRole`; proven `patient-ai-isolation` 01→32). **Flag REUSED — NO catalog change, NO new perm (role+consent-gated).** Web UR-first `/patient/assistant` lean chat (not-a-doctor banner, voice, emergency CTA, consent gate; linked from 31 Home). Gates: typecheck 27/27, lint 15/15, build 15/15 (`/patient/assistant` 1.82 kB), jest api 677/677 (+45) + db 151/151 (+5) + i18n 19/19; turbo test 20/20. Smoke: no token→401, no-flag tenant→404, staff→403, consent forced then allowed, in-language NONE+disclaimer, red flag→EMERGENCY+banner+no-diagnosis, Roman red flag→EMERGENCY, refuses diagnose/prescribe, risk monotonic, OWN report explained + stranger→404, real appt booked via chat, red flag suppresses booking, messages logged+audited, no message→400.
- **31 — Patient App (PWA)** ✅ 2026-07-05 — **opens Phase 3** — the patient's OWN phone-facing surface behind `patient.app` (absent→404). A patient = a `User` role PATIENT (03 OTP) linked to a `Patient` by phone; exposes ONLY **self + linked family** (14 guardian dependents + relations) — ownership ABSOLUTE, never widened. NEW `PatientAppModule` (`@RequireFeature('patient.app')` 404 FIRST THEN `@Roles(Role.PATIENT)` — role-gated, NOT permission-gated; staff→403): `/me/profile`(+PATCH), `/me/family`(+add dependent=Patient guardianId+PatientRelation), `/me/doctors`, `/me/appointments`(+book 15→BOOKED in staff queue), `/me/reports`(+`/:id`: visible ONLY once the pathologist signs — APPROVED/DELIVERED; draft or another patient→indistinguishable 404, privacy 18/19), `/me/prescriptions`(sendToPatient-only)+`/upload`(image key→pharmacy from-prescription), `/me/service-areas`+`/me/lab-tests`+`/me/home-collections`(+book 25→LabOrder ORDERED+HomeCollection BOOKED, charge from area), `/me/notifications`(by phone), `/me/refill-reminders`(computed), `POST /me/devices`(idempotent PWA push). OWNS only 2 tables (`patient_devices`/`patient_uploads`); 14/15/19/18/25/09 read+(booking)additively-created via its OWN tenant-scoped repo, never importing the owning module. Pure `@mp/shared/patient-app` (`familyScopeIds`/`isInFamilyScope`; `isReportVisibleToPatient`; `computeRefillReminders`; upload/device vocab). NEW migration `…270000` (2 tables; `apply_tenant_rls` both, proven `patient-app-isolation`). **Flag REUSED (`patient.app` pre-existed) — NO catalog change, NO new permission (role-gated).** Web EN+**UR-first** `/patient` PWA (Home/Book/Reports/Prescriptions/Family/Alerts; tenant-branded, big buttons, `noindex`, manifest shortcut). Gates: typecheck 26/26, lint 15/15, build 15/15 (`/patient` 3.62 kB / 142 kB), jest api 632/632 (+27) + db 146/146 (+5 patient-app-isolation) + i18n 19/19 (UR-first). Smoke (e2e): no token→401, lab tenant→404, staff→403, self+family by phone, fresh login (no Patient)→empty then booking auto-provisions, book→queue, book for stranger→404, report viewable only when signed (draft & other→404), rx filtered to sendToPatient, upload→captured for pharmacy, home test→ORDERED+BOOKED (no-valid-test→400), notifications scoped to phone (stranger not leaked), refill computed, device idempotent.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
