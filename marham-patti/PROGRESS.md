> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker.** It holds only: current state, the next-step pointer, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only — write via redirection, never read it back).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md** (including the reasoning behind any judgement call you made). Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (fully autonomous):** every step ends with `[CHECKPOINT]` on its own final line — a **marker, not a gate**. The controller records progress, runs the gates (`lint` → `typecheck` → `test:unit` → `build`) in Node itself, and on green auto-merges to staging and continues. There is no operator approval step. The agent **writes** the step's tests but does **not run** them. The ONLY stop is `[HUMAN_REQUIRED]`, reserved strictly for a **missing/empty spec** or **infra the agent physically cannot do** (DNS, SSL, firewall/ports, hosting-panel config, a missing credential, a non-code-fixable deploy failure) — never for an approval, design review, or security/money sign-off: the agent decides, logs the reasoning in PROGRESS-HISTORY.md, and continues.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS **becoming a healthcare PLATFORM/marketplace** · first tenant Ganatra Clinic = Full suite; 4–5 tenants incl. a standalone pharmacy + standalone lab at launch · built by AboveNext)
- **Phase:** 1–39 launch scope COMPLETE · **Phase 4 (40–44) COMPLETE** (AppShell + session, staging demo seed, branded entrances, auth/dashboard polish, design-system consistency). **PHASE 5 — PLATFORM & MARKETPLACE (45–53) IN PROGRESS:** 45 design-v2 ✅ · 46 platform-identity ✅ · 47 marketplace-discovery ✅ · 48 platform-settlement ✅ · 49 field-pools ✅ · 50 saas-console ✅ · 51 subscriptions-ai-modes ✅ → **now 52 mobile-app-experience** → 53 tenant-addressing. Specs 45–53 + companion **`specs/45-53-CODEREF.md`** (read the CODEREF each step; re-read live files — its structure notes are a snapshot).
- **SaaS model:** every capability is a feature flag bundled into vertical presets with per-tenant overrides; **51 adds subscription-package CEILINGS above presets** (ceilings only turn flags OFF). Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag. **RLS is sacred: cross-tenant assembly happens ONLY via per-tenant scoped reads under platform scope — the "Ganatra test" (no tenant ever sees another tenant's records/relationships) is a HARD GATE on every remaining step.**
- **Last completed build-step:** **51 — Subscriptions & AI Commercial Modes (`feature/51-subscriptions-ai-modes`).** ✅ DONE (2026-07-12) — commercial packaging ABOVE the flag engine. `SubscriptionPackage` = flag CEILING (`["*"]`=unrestricted) + branch/user limits + marketplace participation + AI entitlement (NONE|MANAGED|BYOK|BOTH) + managed rate card; `TenantSubscription` assigns one; `TenantAiCredential` holds a BYOK key. **Ceiling** intersects at the 05 `getEffectiveFlagsFor` choke point (only turns flags OFF; override-enable outside the ceiling rejected; upgrade widens live). **AI two ways** at the single 35 `runAiTask` bite-point: MANAGED ⇒ balanced idempotent 48 ledger entry (`TENANT_RECEIVABLE`/`AI_REVENUE`) beside the 39 SpendLog; BYOK ⇒ tenant key (encrypted via the EXACT CNIC AES-GCM seam, never stored/returned/logged, masked last-4), spend logged but never billed; NONE ⇒ 403, BYOK-no-key ⇒ clean 503 (assist-only degrade, never blocks care). Self-contained `apps/api/src/subscriptions/` + `ai.commercial.ts` port (`@Optional` Noop preserves pre-51 behaviour). Migration seeds 4 packages and **grandfathers every existing tenant** onto unrestricted@rate-0 (deploy byte-for-byte). Vendor console gained package CRUD (VENDOR_OWNER, audited) + assign-subscription; web tenant Settings→Subscription + `/vendor/packages` (EN+UR). Isolation floor extended (subscriptions-isolation, real 01→51: BYOK keys tenant-RLS-owned, packages platform-only). Full detail in PROGRESS-HISTORY.md.
- **Current build-step:** **52 — Mobile App Experience.** Spec: `specs/52-mobile-app-experience.md` (+ `specs/45-53-CODEREF.md`). Branch `feature/52-mobile-app-experience`. Native-app feel per audience (bottom tab bar fed from the ONE `nav.ts` registry — no forked registry, view transitions, safe areas, pull-to-refresh, ≥44px targets) + **QR install** on every home/dashboard (local SVG QR to that entrance URL, no external service) + install prompts/iOS instructions (EN+UR). Presentation-only: must not widen any read. Public routes add ≤15 KB; budgets never loosen.
- **Next-step pointer:** → **`specs/52-mobile-app-experience.md`** (then 53 tenant-addressing; the specced build order ends at 53 — beyond that, a missing spec is a hard stop, never a cue to guess). **The Ganatra test remains a HARD GATE: no tenant may ever see another tenant's records or relationships; the 46–51 isolation suites must stay green and be EXTENDED by each new step (52 → presentation-only, must not widen any read; 53 → host resolution must not create a new bypass).**
- **Branch state:** `feat/01`…`feat/39` + `feature/40`…`feature/44` + `feature/45-design-system-v2` → `feature/46-platform-identity` → `feature/47-marketplace-discovery` → `feature/48-platform-settlement` → `feature/49-field-pools` merged to `staging`; `feature/50-saas-console` and `feature/51-subscriptions-ai-modes` complete, awaiting the controller's staging merge. Phase 5 continues stacking `feature/52-mobile-app-experience` onward.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **51 — Subscriptions & AI Commercial Modes** ✅ (Phase 5, 2026-07-12) — commercial packaging above the flag engine. `SubscriptionPackage` = flag CEILING + branch/user limits + marketplace participation + AI entitlement (NONE|MANAGED|BYOK|BOTH) + managed rate card; `TenantSubscription` + BYOK `TenantAiCredential`. Ceiling intersects at the 05 `getEffectiveFlagsFor` choke point (only turns flags OFF; override-outside-ceiling rejected; upgrade widens live). AI two ways at the single 35 `runAiTask` bite-point: MANAGED ⇒ balanced idempotent 48 ledger entry beside the 39 SpendLog; BYOK ⇒ tenant key (CNIC AES-GCM seam, never logged, masked last-4), spend logged not billed; NONE ⇒ 403, BYOK-no-key ⇒ clean 503 (degrade, never block care). Migration seeds 4 packages + **grandfathers every existing tenant** (deploy byte-for-byte). Vendor package CRUD + assign-subscription; web Settings→Subscription + `/vendor/packages` (EN+UR). Isolation floor extended (real 01→51).
- **50 — Super-Admin SaaS Console** ✅ (Phase 5, 2026-07-12) — the vendor's own portal to onboard + run every tenant with ZERO SQL. Platform-level `VendorAdmin` ABOVE tenants (email+pw + **mandatory TOTP 2FA, never staging-relaxed**; VENDOR_OWNER|VENDOR_STAFF; own token audience `typ:'vendor'` rejected by staff + 46 guards). Self-contained `apps/api/src/vendor/` — create-tenant wizard (provisions tenant+branch+preset+owner via **extracted seed logic**, branding/commission/marketplace starters, 3-day single-use owner-invite), per-tenant flags/preset/branding/commission edits **delegated to the 05/45/48 services under `runWithTenant`** (no raw cross-tenant SQL), suspend/archive fail-closed, **listing-approval queue**, oversight dashboard (tenant counts/preset-mix + 48 balances/runs + 49 pending pool workers + recent audit) — **operational/financial METADATA only, ZERO medical data**. Schema additive platform section + `marketplace_listings.approved_at/by` (existing + seed-demo listings seed-approved → staging discovery byte-for-byte). Web `(vendor)` entrance (own `mp.vendor` session/client/guard/chrome, EN+UR). Isolation floor extended to the console (real 01→50).
- **49 — Field Worker Pools: Riders & Phlebotomists** ✅ (Phase 5, 2026-07-12) — tenant staff unchanged (additive) + a shared PLATFORM POOL (46 identities) serving many tenants: own-staff-first dispatch → one-at-a-time offers (accept/decline/lazy expiry) → visible escalation; **job-scoped DTOs ONLY** (no patient history). Self-contained `apps/api/src/dispatch/` (ZERO edits to 25/26/34) — `DispatchService` + `PoolEarningsService` posting balanced **POOL_JOB** entries via the SAME `PlatformLedgerService` writer (tenant owes fee = receivable, platform owes worker = payable, margin = revenue). New `PoolWorkerProfile`/`JobOffer`/`WorkerEarning`/`PoolJobFeeConfig`; assignment rows gained `workerKind`(OWN default)+`poolWorkerId`. Flags `platform.pool.riders|phlebotomists` (off ⇒ OWN_ONLY), perm `dispatch.manage`. Web shared `(field)/PoolPanel` on both entrances (EN/UR). Seed +2 pool riders +1 phleb (Karachi). Isolation floor extended to dispatch (real 01→49) + DTO negatives.

---

> Older steps are archived in **PROGRESS-HISTORY.md** — append-only; never read it back.
