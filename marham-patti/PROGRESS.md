> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** Launch scope **1–39 COMPLETE** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39) and deployed to staging (login verified end-to-end incl. 2FA enrollment). Now in **PHASE 4 — App Shell, Demo Data, Entrances & Design System (40–44)**: the usability + polish scope that turns the 39 modules into ONE navigable, populated, premium product. Specs `40`–`44` are authored and authoritative; companion code-reference **`specs/40-44-CODEREF.md`** (exact paths/seams/do-not-break — read it each step; re-read live files before editing, its line numbers are a snapshot).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **40 — App Shell + Session Layer.** ✅ DONE/APPROVED (2026-07-07) — `<AppShell>` (persistent role+flag-aware sidebar + top bar, EN+UR/RTL, light/dark, mobile drawer, logout, active highlight) wrapping the new `(app)/` route group; `GET /auth/me` (`SessionModule`, composes 04 perms + 05 flags); centralized `lib/api.ts` (auto bearer + tenant header + single-flight 401 refresh + `apiFetch`); route groups `(app)/(patient)/(field)/(auth)` (URLs UNCHANGED); post-login role-home redirect (+`?next`); route guard; ALL 34 per-component auth copies removed (grep-clean) + infra providers migrated. Gates green (typecheck 27/27 · lint 15/15 · api 869/869 · db 178/178 · ui 38/38 · i18n 19/19 parity · next build 43/43, URLs unchanged). Full detail in PROGRESS-HISTORY.md.
- **Current build-step:** **41 — Demo Data Seed.** ⏳ PENDING — spec: `specs/41-demo-data-seed.md` (+ `specs/40-44-CODEREF.md` §C). Builds `packages/db/prisma/seed-demo.ts` + `seed:demo` script (hard-gated `APP_ENV==='staging' && SEED_DEMO==='1'`, NEVER in deploy.sh), deterministic tenant-scoped demo data via the real service-layer write-sets (double-entry, FEFO, shift-close) so invariants hold; config seam adds `SEED_DEMO?/SEED_DEMO_PASSWORD?/MFA_STAGING_RELAX?`. Branch `feature/41-demo-data-seed`.
- **Next-step pointer:** → **`specs/41-demo-data-seed.md`** (then 42 branded-entrances → 43 auth-ui-polish → 44 design-system; the specced build order currently ends at 44 — beyond that, a missing spec is a hard stop `[HUMAN_REQUIRED]`, never a cue to improvise).
- **Branch state:** launch branches `feat/01`…`feat/39` merged to `staging` (deploy green). Phase 4: `feature/40-app-shell-session` COMPLETE (this step) → then `feature/41-demo-data-seed` → `feature/42-branded-entrances` → `feature/43-auth-ui-polish` → `feature/44-design-system`.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **40 — App Shell + Session Layer** ✅ 2026-07-07 (FEATURE) — turned 39 disconnected islands into ONE navigable app: `<AppShell>` (role+flag-aware nav filtered against `GET /auth/me`, flag-first→permission→role, zero dead links), centralized bearer+tenant+silent-refresh API client, `(app)/(patient)/(field)/(auth)` route groups (URLs unchanged), post-login role-home redirect + route guard + logout, all 34 per-component auth copies removed. Gates: typecheck 27/27, lint 15/15, api 869/869 (+2), db 178/178, ui 38/38, i18n 19/19, next build 43/43.
- **Launch hotfix — Lighthouse public-route JS budget (recharts out of first-load)** ✅ 2026-07-07 (stabilization) — the Lighthouse gate caught `script.size` over the 250 KB budget on all 4 public routes because recharts leaked into the shared first-load graph via the `@mp/ui` barrel; fix severed it into an `@mp/ui/chart` subpath (+ `WhenVisible` below-the-fold gate on `/ui`). Budget NOT raised — all 4 routes PASS (206–212 KB), `/lh-bloated` negative canary still fails as designed.
- **Launch hotfix — CRLF-safe env sourcing (+ ports/health verified)** ✅ 2026-07-06 (stabilization) — staging deploy died at `source ./.env` (CRLF, exit 127); `deploy.sh` §2 now sources via CR-normalized process substitution, works for LF or CRLF. Verified with env-driven ports: API binds `API_PORT` (staging 4001), `GET /health` `@Public()` → 200 unauth.

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
