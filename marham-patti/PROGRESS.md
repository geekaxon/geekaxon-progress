> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty, an infra problem is not code-fixable, or a spec explicitly mandates a halt. **⚠️ The step-48 §6 halt has been RELEASED by the owner — 48 is DONE and signed off for continuation; do NOT re-halt at 48. Proceed per the next-step pointer below. No remaining spec (49–53) mandates a halt.**

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS **becoming a healthcare PLATFORM/marketplace** · first tenant Ganatra Clinic = Full suite; 4–5 tenants incl. a standalone pharmacy + standalone lab at launch · built by AboveNext)
- **Phase:** 1–39 launch scope COMPLETE · **Phase 4 (40–44) COMPLETE** (AppShell + session, staging demo seed, branded entrances, auth/dashboard polish, design-system consistency). **PHASE 5 — PLATFORM & MARKETPLACE (45–53) IN PROGRESS:** 45 design-v2 ✅ · 46 platform-identity ✅ · 47 marketplace-discovery ✅ · 48 platform-settlement ✅ *(owner-verification halt RELEASED)* · 49 field-pools ✅ → **now 50 saas-console** → 51 subscriptions-ai-modes → 52 mobile-app-experience → 53 tenant-addressing. Specs 45–53 authored + companion **`specs/45-53-CODEREF.md`** (read it each step; re-read live files — its structure notes are a snapshot).
- **SaaS model:** every capability is a feature flag bundled into vertical presets with per-tenant overrides; **51 adds subscription-package CEILINGS above presets.** Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag. **RLS is sacred: cross-tenant assembly happens ONLY via per-tenant scoped reads under platform scope — the "Ganatra test" (no tenant ever sees another tenant's records/relationships) is a HARD GATE on every remaining step.**
- **Last completed build-step:** **49 — Field Worker Pools: Riders & Phlebotomists (`feature/49-field-pools`).** ✅ DONE (2026-07-12) — tenant-employed field staff keep working EXACTLY as before (purely additive); ON TOP, a SHARED PLATFORM POOL (foodpanda-style) lets one 46 identity (RIDER/PHLEBOTOMIST) serve many tenants: own-staff-first dispatch → one-at-a-time offers (accept/decline/**lazy** expiry — no queue infra) → visible escalation, **job-scoped DTOs ONLY** (a pool worker never sees patient history), and per-job earnings posting into the 48 ledger. New self-contained `apps/api/src/dispatch/` (ZERO edits to 25/26/34) — `DispatchService` (own-first, eligibility, offer chain, drive-status over the tenant job rows, COD via the tenant's OWN billing with 34's idempotency key) + `PoolEarningsService` (fee resolve → balanced POOL_JOB entry via the SAME `PlatformLedgerService` writer → `WorkerEarning`). Schema additive: `PoolWorkerProfile` (platform-only), `JobOffer`/`WorkerEarning`/`PoolJobFeeConfig` (link tables, 46 RLS helpers) + `PlatformAccount+=WORKER_PAYABLE`, `LedgerSourceType+=POOL_JOB`; assignment rows gained `workerKind`(default OWN)+`poolWorkerId` (staff FK relaxed nullable). Flags `platform.pool.riders`/`platform.pool.phlebotomists` (off ⇒ OWN_ONLY, identical to today); perm `dispatch.manage`. Web: shared `(field)/PoolPanel` on both entrances (platform session, duty/offers/jobs/earnings, skeletons+empties, EN/UR). Seed +2 pool riders +1 pool phleb (Karachi). Gates green: typecheck 29/29 · lint clean · build 16/16 (48 pages) · api unit 470 (+14 dispatch,+7 helpers,+2 pool-math) · db isolation +9 (field-pools, real 01→49; Ganatra floor for dispatch) · i18n parity (+33). Full detail in PROGRESS-HISTORY.md.
- **Current build-step:** **50 — SaaS Console (VendorAdmin portal).** ⏳ PENDING — spec: `specs/50-saas-console.md` (+ `specs/45-53-CODEREF.md`). Branch `feature/50-saas-console`. Vendor portal (VendorAdmin, mandatory 2FA): create-tenant wizard (preset/branding/commission/owner invite), suspend/archive (fail-closed), per-tenant flags/branding/commission, listing approval, cross-tenant oversight (operational/financial metadata ONLY — **console must reach ZERO medical data**), platform audit.
- **Next-step pointer:** → **`specs/50-saas-console.md`** (then 51 subscriptions-ai-modes → 52 mobile-app-experience → 53 tenant-addressing; the specced build order ends at 53 — beyond that, a missing spec is a hard stop, never a cue to guess). **The Ganatra test remains a HARD GATE on every remaining step: no tenant may ever see another tenant's records or relationships; the 46/47/48/49 isolation suites must stay green and be EXTENDED by each new step (50 → console must reach zero medical data; 51 → BYOK keys encrypted/never logged; 53 → host resolution must not create a new bypass).**
- **Branch state:** `feat/01`…`feat/39` + `feature/40`…`feature/44` + `feature/45-design-system-v2` → `feature/46-platform-identity` → `feature/47-marketplace-discovery` → `feature/48-platform-settlement` complete and merged to `staging`; `feature/49-field-pools` complete (this step) awaiting the controller's staging merge (post git-filter-repo history cleanup; remote re-added; `*.zip` gitignored; staging deploy green). Phase 5 continues stacking `feature/50-saas-console` onward.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **49 — Field Worker Pools: Riders & Phlebotomists** ✅ (Phase 5, 2026-07-12) — tenant staff unchanged (additive) + a shared PLATFORM POOL (46 identities) serving many tenants: own-staff-first dispatch → one-at-a-time offers (accept/decline/**lazy** expiry, no queue infra) → visible escalation; **job-scoped DTOs ONLY** (no patient history). Self-contained `apps/api/src/dispatch/` (ZERO edits to 25/26/34) — `DispatchService` + `PoolEarningsService` posting balanced **POOL_JOB** entries via the SAME `PlatformLedgerService` writer (`PlatformAccount+=WORKER_PAYABLE`, `LedgerSourceType+=POOL_JOB`; tenant owes fee = receivable, platform owes worker = payable, margin = revenue). New tables `PoolWorkerProfile`(platform-only)/`JobOffer`/`WorkerEarning`/`PoolJobFeeConfig` (46 RLS helpers); assignment rows gained `workerKind`(OWN default)+`poolWorkerId` (staff FK relaxed nullable). Flags `platform.pool.riders|phlebotomists` (off ⇒ OWN_ONLY), perm `dispatch.manage`. Web shared `(field)/PoolPanel` on both entrances (platform session, duty/offers/jobs/earnings, skeletons+empties, EN/UR). Seed +2 pool riders +1 phleb (Karachi). Ganatra floor extended to dispatch (db field-pools +9, real 01→49) + service/DTO negatives (api +23). Gates green (typecheck 29/29 · lint · build 16/16 · api unit 470 · parity +33). *(e2e needs the deploy DB — runs in the pipeline like 48.)*
- **48 — Platform Settlement & Commission** ✅ (Phase 5, 2026-07-11) — the PLATFORM's money layer over the marketplace, separate from tenant accounting. Provider-AGNOSTIC `@mp/payments` (port + deterministic MockProvider, `mock|mock-alt` config swap). Append-only double-entry platform books: `PlatformLedgerEntry`+balanced postings, `PlatformCommissionConfig` (tenant/cap/default precedence), `PaymentIntent`, `SettlementRun`+lines — RLS via `apply_platform_link_rls` (tenant reads OWN; platform sole writer) + APPEND-ONLY trigger. `PayMethod+=PLATFORM_ONLINE`. Online-collect (confirm/webhook → tenant paid via its OWN billing + platform payable = gross − commission, idempotent, refund reverses) + cash accrual (tenant receivable); 47 booking gained `paymentMode`. Web Settings→Settlement (RLS-own) + SUPER_ADMIN cross-tenant API (for 50). Ganatra floor extended to money (db +10) + settlement invariants/idempotency/provider-swap (api +16). Gates green (db 231 · api 920 · parity · `/settings` 204 KB). *(spec-48 §6 owner-verification halt RELEASED.)*
- **47 — Marketplace Discovery & Booking** ✅ (Phase 5) — foodpanda storefront over the platform: published-copy `MarketplaceListing` + doctor/test/product projections (discovery reads ONLY these) + `apply_marketplace_rls` (tenant R/W own; platform reads all); `ApptSource += MARKETPLACE`, new `OrderSource` on lab/online orders. `marketplace` module: tenant publication (Settings→Marketplace), `@Public` discovery reads, and the booking orchestrator (`/platform/marketplace/book/*`) that link-if-firsts then calls the SAME 15/19/25/34 services under `runWithTenant` stamped `source:MARKETPLACE` (forks nothing). Web: patient Browse tab + public `/discover(/[slug])` + Settings→Marketplace (EN/UR). Seed +2 standalone tenants (pharmacy + lab). Ganatra floor extended to discovery (db +9) + booking negatives/link-reuse (api +10); gates green (db 221 · api 904 · parity 19 · `/discover` 202 KB).

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
