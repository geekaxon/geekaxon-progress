> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker.** It holds only: current state, the next-step pointer, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only — write via redirection, never read it back).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md** (including the reasoning behind any judgement call you made). Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (fully autonomous):** every step ends with `[CHECKPOINT]` on its own final line — a **marker, not a gate**. The controller records progress, runs the gates (`lint` → `typecheck` → `test:unit` → `build`) in Node itself, and on green auto-merges to staging and continues. There is no operator approval step. The agent **writes** the step's tests but does **not run** them. The ONLY stop is `[HUMAN_REQUIRED]`, reserved strictly for a **missing/empty spec** or **infra the agent physically cannot do** (DNS, SSL, firewall/ports, hosting-panel config, a missing credential, a non-code-fixable deploy failure) — never for an approval, design review, or security/money sign-off: the agent decides, logs the reasoning in PROGRESS-HISTORY.md, and continues.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS **becoming a healthcare PLATFORM/marketplace** · first tenant Ganatra Clinic = Full suite; 4–5 tenants incl. a standalone pharmacy + standalone lab at launch · built by AboveNext)
- **Phase:** 1–39 launch scope COMPLETE · **Phase 4 (40–44) COMPLETE** (AppShell + session, staging demo seed, branded entrances, auth/dashboard polish, design-system consistency). **PHASE 5 — PLATFORM & MARKETPLACE (45–53) COMPLETE:** 45 design-v2 ✅ · 46 platform-identity ✅ · 47 marketplace-discovery ✅ · 48 platform-settlement ✅ · 49 field-pools ✅ · 50 saas-console ✅ · 51 subscriptions-ai-modes ✅ · 52 mobile-app-experience ✅ · **53 tenant-addressing ✅ (LAST specced step — build order ends here).** Specs 45–53 + companion **`specs/45-53-CODEREF.md`** (read the CODEREF each step; re-read live files — its structure notes are a snapshot).
- **SaaS model:** every capability is a feature flag bundled into vertical presets with per-tenant overrides; **51 adds subscription-package CEILINGS above presets** (ceilings only turn flags OFF). Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag. **RLS is sacred: cross-tenant assembly happens ONLY via per-tenant scoped reads under platform scope — the "Ganatra test" (no tenant ever sees another tenant's records/relationships) is a HARD GATE on every remaining step.**
- **Last completed build-step:** **53 — Tenant Addressing (`feature/53-tenant-addressing`).** ✅ DONE (2026-07-12) — every tenant gets its OWN address, DOMAIN-AGNOSTIC until the product domain is purchased. EXTENDED the existing 02/03 resolver with a precedence chain as pure, unit-testable host→resolution functions (`classifyHost`/`extractSubdomainSlug`/`resolveHost`): (1) ACTIVE custom domain, (2) `HOSTNAME_MAP` exact, (3) `<slug>.PLATFORM_BASE_DOMAIN`, (4) shared-entrance fallback — NEVER a second resolver. `PLATFORM_BASE_DOMAIN` + `HOSTNAME_MAP` (JSON) added to `packages/config` zod + `.env.example`; buying the real domain later = env + ops DNS/TLS, ZERO code edits (grep-guard test proves no real hostname is hardcoded in app code). `TenantDomain` table (PENDING→VERIFIED→ACTIVE|DISABLED, tenant-owned RLS + platform read/write, only ACTIVE resolves) + `apply_tenant_domain_rls`. `HostResolutionService` (per-host 60s cache, platform-scope ACTIVE-domain lookup + global-registry slug lookup) + `HostResolutionMiddleware` (stamps `req.tenantId`/`hostTenantId` for tenant hosts; shared host stamps nothing → today's behaviour byte-for-byte). Host hygiene in `JwtAuthGuard`: a token used on another tenant's host is cleanly rejected (re-auth). Tenant `Settings→Domains` (subdomain address always shown; custom-domain add/verify via DNS-TXT / HTTP well-known, package-gated `platform.customDomain` premium-only); `(vendor)` console Domains list + activate/disable (audited, cache-invalidating). Public `GET /host-context` drives the web: tenant host → branded chooser + login with NO clinic field; shared host keeps the tenant field + slug fallback. Ops runbook `docs/addressing-runbook.md` (wildcard base cert + per-custom-domain cert steps for Nginx/aaPanel). New isolation suite proves the Ganatra floor holds for `tenant_domains`. Full detail in PROGRESS-HISTORY.md.
- **Current build-step:** **NONE — the specced build order (1→53) is COMPLETE.** There is no `specs/54-*.md`. Per AGENT.md §2 a MISSING/empty spec is a hard stop: any request to build "the next step" beyond 53 must emit `[HUMAN_REQUIRED]` (missing spec) rather than guess. New work needs a new spec first.
- **Next-step pointer:** → **(none — build order ends at 53).** A future step requires the owner to add `specs/54-<slug>.md` (+ extend the CODEREF); until then, building further is a missing-spec `[HUMAN_REQUIRED]`, never a cue to invent scope. **The Ganatra test remains a HARD GATE forever: no tenant may ever see another tenant's records/relationships; the 46–53 isolation suites (now incl. `tenant_domains`) must stay green.**
- **Branch state:** `feat/01`…`feat/39` + `feature/40`…`feature/44` + `feature/45-design-system-v2` → `feature/46-platform-identity` → `feature/47-marketplace-discovery` → `feature/48-platform-settlement` → `feature/49-field-pools` merged to `staging`; `feature/50-saas-console`, `feature/51-subscriptions-ai-modes`, `feature/52-mobile-app-experience` complete, awaiting the controller's staging merge. **Phase 5 completes stacking `feature/53-tenant-addressing` (this step, stacked on 52).**

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **fix — demo seed account idempotency** ✅ (2026-07-13, `fix/demo-seed-account-idempotency`) — `persist()` keyed every table by `id`, but Account's real key is `(tenantId, code)` and the app's `ensureAccounts` seeds cuid-id accounts → P2002 on re-run/live DB. New `persistAccounts` upserts by `(tenantId, code)` and remaps FK-less journal-line accountIds onto the winning rows. @mp/db lint+typecheck clean.
- **fix — `@mp/api` `test:unit` DB-free gate green** ✅ (2026-07-13, `fix/prisma-generate-root-gate`) — 36 e2e wiring suites 500'd at `loadConfig()` `DATABASE_URL: Required` because `PlatformSettlementModule`'s `PAYMENT_PROVIDER` factory validates the env when building the real `AppModule` graph, and the local gate (unlike CI, which sets a placeholder `DATABASE_URL`) exports no env. Fix: added `src/jest.setup.ts` (jest `setupFiles`) that sets a NON-CONNECTING placeholder `DATABASE_URL` only when unset; excluded from build emit. Test-infra only — no product/schema/module change. lint+typecheck clean.
- **fix — `pnpm prisma generate` gate green on new server** ✅ (2026-07-13, `fix/prisma-generate-root-gate`) — the documented verify command (CLAUDE.md §6) died with `ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL: Command "prisma" not found` because `prisma` is only a `@mp/db` devDep and isn't hoisted to the repo-root `.bin` on this fresh install (NOT a schema/migration conflict — generate is clean). Fix: added root `"prisma": "pnpm --filter @mp/db exec prisma"` script so the bare `pnpm prisma generate` (and `migrate` etc.) resolves from root and targets the `@mp/db` schema. Verified exit 0; arg-forwarding intact. package.json only, no feature/schema change.

---

> Older steps are archived in **PROGRESS-HISTORY.md** — append-only; never read it back.
