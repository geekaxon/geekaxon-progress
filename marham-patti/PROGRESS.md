> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo (`projects-abovenext/marham-patti-progress`). **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — Marham Patti Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, branch state, and the last 3 steps as one-liners. The COMPLETE detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred):** (1) mark the finished step DONE in **Current Status**, (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th one-liner (keep exactly 3), and (4) append the FULL entry to **PROGRESS-HISTORY.md**. Tied to the SPEC being done, NOT to any deploy/merge. Never let this file grow long.
>
> **Numbering:** canonical per ARCHITECTURE.md §14 (continuous 1→N; phase = label).
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in background; check with `DEPLOY STATUS`) and continues. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth / flags / accounts / payments changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** Marham Patti (AI-powered, multi-tenant, white-label, vertical-configurable healthcare OS · first tenant Ganatra Clinic = Full suite · built by AboveNext)
- **Phase:** **NOT STARTED.** Launch scope = build-steps **1–39** (Foundation 1–13, Phase 1 core 14–24, Phase 2 ops 25–30, Phase 3 apps 31–34, AI Suite 35–39). Deferred (stub specs only): 40–45 (white-label admin UI, SaaS admin console, subscription billing, CRM, telemedicine, marketplace — marketplace 45 & billing 42 depend on the SaaS console 41).
- **SaaS model:** every capability is a feature flag (sub-feature granularity); flags bundle into **vertical presets** (Pharmacy / Lab / Clinic / Clinic+Pharmacy / Full) with per-tenant overrides. Always-on infra (auth, RLS isolation, audit, offline, flag engine, branding, i18n) is never a flag.
- **Last completed build-step:** **15 — Phase 1: Appointment & Token (paper-first).** ✅ DONE/APPROVED (2026-07-04) — the paper-first front desk: one-tap **offline** tokens, doctor calendar/availability, walk-in queue + display board, book/reschedule/cancel/no-show lifecycle, and a **human-gated** end-of-day register import. NEW migration `…110000` (6 tables: `doctors`/`doctor_availability`/`doctor_leave`/`appointments`/`tokens`/`register_imports`), all `apply_tenant_rls` + `branch_id`, T1/T2 + no-cross-tenant-numbering proven. Token = a WALK_IN appointment + a **sequential-per-doctor/branch/day** token (`day_key` + tenant-scoped unique index) created atomically; **critical offline path** rides `SyncService.runIdempotent(clientActionId)` (required `Idempotency-Key` header) so a replay returns the SAME number (P2002 backstop on the unique `client_action_id`). Pure `@mp/shared/appointments` (`dayKeyOf`/`nextTokenNumber`/`buildDaySlots`=avail−leave−booked/lifecycle state machine/register-draft contract). API `AppointmentsModule` — whole controller `@RequireFeature('clinic.appointments')` (already in catalog/presets; **404 when off** = vertical smoke) + `@RequirePermission('appointments.manage')` (already in catalog); slots endpoint; lifecycle PATCH with state-machine 400s; queue + display; register import UPLOADED→PARSED→VERIFIED→IMPORTED (never auto-commits, incomplete rows skipped). Web EN+UR `/appointments` (offline badge, giant one-tap New Token w/ outbox fallback, Queue taps, Doctors availability/leave, Display board, register wizard). Gates green: typecheck 26/26, lint 15/15, build 15/15, jest **api 227/227 [+21]** + **db 79/79 [+6 appointment-isolation]** + i18n 19/19 parity + ui/import/offline/consent/notifications/patients unaffected.
- **Current build-step:** **16 — Phase 1: Vitals Station (Nurse).** Read `specs/16-*.md` (authoritative). If missing/empty, STOP with `[HUMAN_REQUIRED]`.
- **Next-step pointer:** → `specs/16-vitals-station.md`
- **Branch state:** `feat/15-appointment-token` complete (awaiting controller merge to `staging`), stacked on `feat/14-patient-management` → `feat/13-ui-foundation` → `feat/12-import-export` → `feat/11-consent-terms` → `feat/10-offline-core` → `feat/09-notifications-engine` → `feat/08-i18n-base` → `feat/07-branding-whitelabel` → `feat/06-audit-log` → `feat/05-capabilities-flags` → `feat/04-rbac-permissions` → `feat/03-auth` → `feat/02-tenancy-rls` → `feat/01-foundation-monorepo`. Step 16 branch stacks on it if not yet merged. (Repo must be cloned onto the server ONCE before the first deploy — see ARCHITECTURE.md §15.)

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **15 — Appointment & Token (paper-first)** ✅ 2026-07-04 — the paper-first front desk: one-tap **offline** tokens, doctor calendar/availability, walk-in queue + display board, book/reschedule/cancel/no-show lifecycle, human-gated register import. NEW migration `…110000` (6 tables: doctors/doctor_availability/doctor_leave/appointments/tokens/register_imports), all `apply_tenant_rls`+`branch_id`, T1/T2 + no-cross-tenant-numbering proven. Token = WALK_IN appointment + **sequential-per-doctor/branch/day** token (`day_key` + tenant-scoped unique index) created atomically; **critical offline path** rides `SyncService.runIdempotent(clientActionId)` (required `Idempotency-Key`) → replay returns SAME number (P2002 backstop). Pure `@mp/shared/appointments` (dayKeyOf/nextTokenNumber/buildDaySlots=avail−leave−booked/lifecycle machine/register-draft). API `AppointmentsModule`: whole controller `@RequireFeature('clinic.appointments')` (**404 when off** = vertical smoke) + `@RequirePermission('appointments.manage')`; slots; lifecycle PATCH w/ state-machine 400s; queue+display; register import UPLOADED→PARSED→VERIFIED→IMPORTED (never auto-commits, skips incomplete). Web EN+UR `/appointments` (offline badge, giant one-tap New Token + outbox fallback, Queue taps, Doctors availability/leave, Display board, register wizard). Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 227/227 (+21) + db 79/79 (+6 appointment-isolation) + i18n 19/19 parity.
- **14 — Patient / Customer Management** ✅ 2026-07-04 — the central person record, ONE model + flag-driven view (`customers.medicalRecord` OFF→light customer / ON→full medical record, same data, no migration). NEW migration `…100000` (7 tables: `patients` phone-primary/nullable, CNIC **encrypted** at rest + plaintext `cnic_last4` for search, guardian link, status; relations/tags/allergies/credits/notes/notification_prefs), all `apply_tenant_rls` T1/T2 proven; **pg_trgm** GIN indexes created DEFENSIVELY (DO/EXCEPTION → pglite-safe, ILIKE fallback). Pure `@mp/shared/patients` (maskCnic/cnicLast4/normalizePhone/creditBalance/timeline). API-only `CnicCipher` (AES-256-GCM, NEW `CNIC_ENCRYPTION_KEY`) — reveal via `patients.cnic.reveal` `@Audited` (masked in log). `PatientsModule`: fuzzy search, CRUD, family/guardian, tags, **allergies `@RequireFeature`→404 when off**, credit add/settle, notes, notification prefs (honoured by 09), merge (moves children→source INACTIVE, audited), flag-shaped timeline. Registers a patient importer (12, idempotent by phone). Web EN+UR `/patients` flag-aware ModuleTabs + masked/audited CNIC. Gates: typecheck 26/26, lint 15/15, build 15/15, jest api 206/206 (+31) + db 73/73 (+6 patient-isolation) + i18n 19/19 parity.
- **13 — UI Design System + Perf Foundation** ✅ 2026-07-04 — `@mp/ui` scaffold → full React library on Tailwind + shadcn/ui-style Radix primitives, themed by `--brand-*` (reskin, no code change), light+dark `ThemeProvider`, RTL, large-touch/keyboard/voice. Components: Button/Input/Label/Card/Badge/Skeleton/Spinner/Field/EmptyState, Dialog(+Modal)/Toast(`useToast`)/SearchSelect+MultiSearchSelect/DateField/Tabs, `ModuleTabs` (active-only mount→lazy render), `DataList` (search+paginate+`onExport`→12), animated recharts Chart wrappers (dynamic-import), `AppShell` (flag+permission-aware nav via `visibleNav`/`hasFlag`/`can` — disabled module→no entry), `Feature`, `ShortcutScope`, `VoiceInput`. Shared Tailwind preset (`@mp/ui/tailwind-preset`) + `styles.css` (`--mp-*` light/dark + reduced-motion). Pure seams: `screenshot*` (lean `@mp/ui/screenshot` subpath) + `evaluateBudgets` perf model. Web: tailwind+postcss+globals `@tailwind` (coexist w/ legacy `.mp-*`), `ThemeProvider` in layout, EN+UR `/ui` showcase (only @mp/ui, AppShell frame, dynamic chart) proven via `next start`, `middleware.ts` screenshot token (non-staging IGNORES param; staging+valid→200+`x-mp-screenshot`; staging+wrong→403 — all proven), `/lh-bloated` canary (431kB vs 279kB). CI: real Lighthouse job (≥90 mobile, `lighthouserc.json`+`budgets.json`) + negative "gate bites" check. Launch note: local-font Urdu today, self-hosted subset woff2 = `[DECIDE AT BUILD]`. Gates: typecheck 26/26, lint 15/15, build 15/15, jest ui 38/38 + api 175/175 + import 18/18 + db 67/67 + i18n 19/19 (parity).

---

> Older steps are archived in **PROGRESS-HISTORY.md**. Read that file only when recovering detail about an older step — never cover-to-cover each step.
