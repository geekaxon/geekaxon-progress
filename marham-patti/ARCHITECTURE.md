# ARCHITECTURE.md — Phase 8 additions (append to §14 BUILD ORDER, after Phase 7 / step 69)

> Paste this block into `ARCHITECTURE.md` §14 immediately after the Phase 7 (64–69) entries, and update the closing scope line (bottom of §14) to the new one given here. Do not duplicate the Phase 6/7 lines already present.

**PHASE 8 — VENDOR PLATFORM COMPLETION & GANATRA LAUNCH READINESS (70–84)**

> Owner testing of the redesigned vendor console produced a full punch-list; the driving deadline is **Ganatra Clinic go-live, 1 August 2026** (clinic + pharmacy + lab, single branch). Two platforms must ship: the vendor console (onboard + bill Ganatra) and the tenant/clinic-staff app (Ganatra's operations — a **separate spec block after** vendor is complete + owner-tested). Phase 8 is the vendor-completion + shared-foundation + cross-cutting-features work, each step tagged **[LAUNCH-CRITICAL]** or **[POST-LAUNCH]**. Companion: `specs/70-84-CODEREF.md` (read each step). Settled product decisions live in the CODEREF §C (AI two-state, platform rider/phleb pools, patient out-of-scope, per-booking commission, custom RBAC both sides, 3–7d trial + shared demo, per-package whitelabel, Trial→Active→Suspended→Cancelled lifecycle, Grandfathered legacy).

70. `70-global-foundation` **[LAUNCH-CRITICAL]** — the shared UX layer every screen (vendor now, all surfaces later) inherits: searchable Select2-grade dropdowns (grouped), top-right toast system, human date formatter ("Friday, July 17, 2026"), SweetAlert2-grade confirm + prompt dialogs, visible toggle off-state, legible headings/breadcrumbs (light+dark), table kit rows-per-page + column show/hide, and a `logEvent` logging base. Build FIRST.
71. `71-vendor-bugs` **[LAUNCH-CRITICAL]** — the confirmed vendor defects: remove EN/UR switcher (vendor EN-only), real logo, billing "Wrong token type" + dashboard-load fix, absolute invite URL, consumed/expired/invalid invite states, remove tab vertical-scroll, remove AI-mode/BYOK picker (two-state), capability-ceiling as toggle switches, AI-key-rotate themed dialog, subscription→listing consistency.
72. `72-subdomain-surface-routing` **[LAUNCH-CRITICAL]** — BUG-B: host→surface routing so `vendor.<base>`→console, `<slug>.<base>`→that tenant's app (scoped), unknown→clean not-found, apex→public; reserved subdomains; all base-derived. (TLS/DNS nested-wildcard is INFRA-1, handled at infra, not code.)
73. `73-rbac-core` **[LAUNCH-CRITICAL]** — permission catalog + custom roles engine, both scopes (platform + tenant), schema (permissions/roles/role_permissions/user_roles) with fail-closed RLS, `can()`/`<Can>`/`usePermission`, seeded system roles (vendor + tenant: Owner/Manager/Doctor/Receptionist/Pharmacist/LabTech/Cashier).
74. `74-rbac-users-management` **[LAUNCH-CRITICAL]** — role editor + user manager UI: vendor "Team & Roles", and each tenant's users/roles from tenant detail (Ganatra's staff), tenant-scoped; invites absolute + branded.
75. `75-subscription-module` **[LAUNCH-CRITICAL]** — lifecycle state machine Trial→Active→Suspended→Cancelled (+resume), access gate + billing tie-in (61) + commission accrual (per-booking), per-tenant subscription view + transition history; assign excludes Grandfathered; ceiling via switches.
76. `76-trial-and-demo` **[POST-LAUNCH]** — 3–7 day free trial with auto-suspend at expiry; separate shared demo session (no-signup, dummy data, RLS-isolated, auto-reset), flag-gated.
77. `77-whitelabel-core` **[LAUNCH-CRITICAL]** — per-package whitelabel capability; branding editor (app name, custom colours, logo + app-icon upload, live preview) in Create-Tenant + Branding; tenant app renders its branding (scoped, no bleed); non-whitelabel → read-only + upsell.
78. `78-whitelabel-invite-and-domain` — branded invite/password screen in the tenant's branding **[LAUNCH-CRITICAL]**; custom-domain add→DNS→verify→activate completing the 53/59 loop **[POST-LAUNCH]**.
79. `79-tenant-detail-enhancements` — Overview mini-dashboard (stat cards + trend + subscription + quick actions) **[LAUNCH-CRITICAL]**; Flags core/toggleable labels + upfront dependencies + ceiling **[LAUNCH-CRITICAL]**; Owners→partner shares (sum-to-100) **[POST-LAUNCH]**.
80. `80-tenants-list-enhancements` — logo-or-initials, commission column, column show/hide, province-grouped Pakistan city picker **[LAUNCH-CRITICAL]**; advanced/specific search, whitelabel indicator, card-view polish **[POST-LAUNCH]**.
81. `81-login-as-tenant` **[POST-LAUNCH]** — enhanced impersonation from tenant detail with complete on-behalf-of logging (dispute-ready), on 55's safety model.
82. `82-analytics-consistency` **[POST-LAUNCH]** — per-tenant table gets the full 70 kit (list/card toggle etc.); cards + donut consistent; "—" for empty.
83. `83-vendor-design-pass` **[POST-LAUNCH]** — premium presentation pass (login redesign, advanced cards/charts/donut, card views), against mockups if provided; zero behaviour change.
84. `84-detailed-logging` **[POST-LAUNCH]** — rich audit surfaces over `logEvent`: vendor audit search-by-tenant-name + filters + before→after + export; tenant-facing audit; clinical payloads redacted in vendor views.

> **Launch scope = 1–39 (COMPLETE). Phase 4 (40–44) usability (COMPLETE). Phase 5 (45–53) platform/marketplace (COMPLETE). Phase 6 (54–63) control/commerce/design (COMPLETE). Phase 7 (64–69) vendor-console professional finish (COMPLETE). Phase 8 (70–84) is vendor-platform completion + Ganatra launch readiness; the specced build order currently ends at 84.** The build runs fully autonomously end-to-end — every step ends `[CHECKPOINT]`; [LAUNCH-CRITICAL] steps precede [POST-LAUNCH]. Any further capability is a new, fully-authored `specs/NN-slug.md` (85+) only when defined. The tenant/clinic-staff **operations** platform (Ganatra's pharmacy/clinic/lab screens) is the next spec block, authored after vendor is complete + owner-tested. A missing spec is a hard stop (`[HUMAN_REQUIRED]`), never a cue to guess.

---

## Also update §15 (Deployment) — the domain reality changed

> Add this note to §15 so the deploy record matches reality:

**Environments (as of Phase 8):** staging = `staging.marhampatti.com` (own folder, `marham_staging`, ports 3001/4001, `APP_ENV=staging`, `PLATFORM_BASE_DOMAIN=staging.marhampatti.com`); production = `marhampatti.com` (own folder, `marham_prod`, ports 3002/4002, `APP_ENV=production`, `PLATFORM_BASE_DOMAIN=marhampatti.com`, fresh secrets, no MFA_STAGING_RELAX / no SEED_DEMO). The old `qa-marhampatti.abovenext.com` is retired. Production is human-gated (`DEPLOY PRODUCTION`, strict fast-forward). Subdomains derive from `PLATFORM_BASE_DOMAIN`; `vendor.<base>`→console, `<slug>.<base>`→tenant (spec 72). INFRA-1: nested `*.staging.marhampatti.com` isn't proxied on Cloudflare free tier (grey-cloud / production single-level wildcard resolves it) — an infra matter, not code.
