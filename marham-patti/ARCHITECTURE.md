# ARCHITECTURE.md — Phase 9 block

> **PASTE this block into `ARCHITECTURE.md` §14 (BUILD ORDER) immediately after the Phase 8 (70–84) entries, then replace the closing scope paragraph at the end of §14 with the one given at the bottom of this file. Do not delete the existing Phase 1–8 entries.**

**PHASE 9 — VENDOR CONSOLE DESIGN IMPLEMENTATION (85–89)**

> Phase 8 delivered the behaviour; Phase 9 delivers the **look**, against an owner-approved visual target: a **Component Library (v2.4)** plus four screens (login, dashboard, analytics, tenants) built from it, in the real MP palette, light + dark, at `specs/mockups/`. Presentation-only — no service, RLS, policy, permission or migration change. Precedence: the **component library is authoritative for component anatomy**; a screen mockup is authoritative for that page's composition; the spec/CODEREF wins on behaviour, data and paths. Companion: `specs/85-89-CODEREF.md` (read each step). Driving deadline: **Ganatra go-live 1 Aug 2026** — [LAUNCH-CRITICAL] steps ship first.

85. `85-brand-assets-and-shell` **[LAUNCH-CRITICAL]** — the real brand SVGs rendered through the `@mp/brand` resolver (sidebar = horizontal-full, login = vertical-full, icon = insignia; never a hardcoded path); **sidebar header and top bar aligned to exactly `--topbar-h`** so the top horizontal line is unbroken; sidebar (grouped labels, tinted active pill, bottom user block, collapsed rail) and slim top bar (⌘K affordance, bell, theme toggle, avatar) brought to the library.
86. `86-login-redesign` **[LAUNCH-CRITICAL]** — the owner-rejected vendor login rebuilt to `login.png`: centred card on a neutral page, vertical logo lockup, title + subtitle, icon-led inputs, password show/hide, forgot-password link, full-width primary button; all four states (default / focused / submitting / error) in both themes, with the **error as a top-right toast**, not an inline banner; EN-only (no switcher).
87. `87-table-and-card-kit` **[LAUNCH-CRITICAL]** — `data-list` gains **inline row actions** (View / Edit / Delete icon buttons, destructive tone on delete, `⋯` only for extras, emphasised on row hover and focus-within) replacing the overflow menu, and the **refined record card** (logo tile + name + slug → divider → meta pills → metric, hover lift, actions revealed at the base); toolbar/footer composed as `tenants.png` (search · Filter · Columns · view toggle; rows-per-page · range · pager). Applied to every vendor list. Additive props only.
88. `88-dashboard-and-analytics` **[POST-LAUNCH]** — Dashboard and Analytics re-skinned to their mockups: premium stat-card anatomy (tinted icon square, uppercase label, oversized tabular value, coloured trend + sparkline, graceful "—" no-data variant with caption), chart cards with header rows, gradient-area lines, value-labelled bars, and the **donut with centre total + count/percentage legend** sized to content; animated, theme-aware, reduced-motion honoured, with a chart empty state.
89. `89-vendor-screens-consistency` **[POST-LAUNCH]** — every remaining vendor screen (billing, audit, packages, AI, status, listings, approvals, domains, admins, tenant detail's 8 tabs, create-tenant) brought to the same bar: PageHeader + breadcrumbs legible in both themes, lists on the 87 kit, forms in grouped section cards with the library field anatomy and a sticky footer, metric cards on the 88 anatomy, tenant-detail tabs with **no vertical scrollbar**, and library dialogs/toasts/skeletons/empty states throughout.

---

## Replace the closing scope paragraph of §14 with this:

> **Launch scope = 1–39 (COMPLETE). Phase 4 (40–44) usability (COMPLETE). Phase 5 (45–53) platform/marketplace (COMPLETE). Phase 6 (54–63) control/commerce/design (COMPLETE). Phase 7 (64–69) vendor-console professional finish (COMPLETE). Phase 8 (70–84) vendor completion + launch readiness (COMPLETE). Phase 9 (85–89) is the vendor-console design implementation against approved mockups; the specced build order currently ends at 89.** The build runs fully autonomously end-to-end — every step ends `[CHECKPOINT]`; [LAUNCH-CRITICAL] steps precede [POST-LAUNCH]. Any further capability is a new, fully-authored `specs/NN-slug.md` (90+) only when its requirements are defined — never improvised. The tenant/clinic-staff **operations** platform (Ganatra's pharmacy / clinic / lab screens) is the next spec block, authored after the vendor console is owner-tested. A missing spec is always a hard stop (`[HUMAN_REQUIRED]`), never a cue to guess.
