# PROGRESS.md — SpotEdge Trading Bot

**Status:** Module 24 (Strategy D Revisions — risk-consistent + January-faithful) DONE/APPROVED (2026-07-08) — [CHECKPOINT] (changes D money-sizing + stop behavior; continuous-dev so [CHECKPOINT] not [FIXED_CHECKPOINT]). Amendment to module 23 per the operator's own diagnosis ("the January *method* was sound — patient limit-buys of dips, small limit-sells, NO tight stop; my losses were EMOTIONAL discipline failures, not the method"). Two design corrections + reaffirmations: **(Change 1 — SIZE)** DELETED the "fixed 5%-of-capital bag"; D is now **risk-sized like A/B** via the SAME `sizePosition` formula off the catastrophe-stop distance (`bagUsd = capital×risk_pct/100 ÷ stopPct`, tier-cap bound, LOT_SIZE floored). `risk_pct` = the global setting (default 1%, hard-bounded 0.25–2%); D never exceeds the 2% ceiling. `BAG_FRACTION` removed entirely (0 refs). **(Change 2 — STOP)** DELETED the tight `entry×(1−1.2%)`; REPLACED with a **wide CATASTROPHE stop** `CATASTROPHE_STOP_PCT=0.04` (4%, config; ≥3× the 0.8% target margin so normal dip-and-recover never trips it — only a genuine breakdown does). `STOP_PCT` kept as a `@deprecated` alias pointing at the same 0.04. Wider stop → smaller bag so dollar-risk stays ≤ risk_pct (property-tested). **(Change 3 — ENTRY)** dip-reclaim entry unchanged (discount ≥0.8% below 24-period 15m EMA + 15m close back above prior 15m high; fee-aware target unchanged); reframed as the automated form of the operator's patient limit-buy; between target and stop D simply WAITS (no mid-cycle tightening). **(Change 4 — RAILS)** daily D cap (−1.5%/UTC-day), 8×15m post-catastrophe cooldown, MAX_D_CYCLES 3, vol gate (≥2.5%), one-owner, −3% breaker, STOP/PAUSE/drain — ALL KEPT. **Explicitly REJECTED:** no LLM/AI in any trade-decision path (grep-clean; only a comment mentions it); no change to the global 1%/2% risk system. **Plumbing:** `LevelingEntrySnapshot` gained `riskPct`; `RiskEngine.approveLevelingEntry` now fetches `risk_pct` via `settings.getNumber` and passes it to the pure `approveLevelingEntry`, which calls `sizePosition` and reports real `riskUsd`/`effectiveRiskUsd`/`tierCapBound`. Driver (`apps/bot/src/engine.ts`) unchanged — already delegates to `RiskEngine.approveLevelingEntry` (never computed a bag itself). `PNL D`/dashboard per-strategy split unaffected (Trade-based, from module 23). Gates: core typecheck+build, bot typecheck+build, db typecheck all green; **379 core tests** (+4 vs 375: strategyD +2 = catastrophe-stop-is-4%/≥3×-margin + normal-−2%-dip-holds; risk.engine +2 = risk-sized-not-flat + worst-case-loss≤risk_pct property + wider-stop-smaller-bag). NEXT → **module 24 is the LAST spec present** (`specs/` ends at 24); no module 25 exists yet — build halts here pending a new spec from the controller/operator. New registry-based strategy plugin modeled on the operator's PROVEN Jan-2026 manual leveling style (buy a bag, sell +~0.8%, rebuy, many cycles/day) but WITH the discipline that sheet lacked: a −1.2% hard stop, a fee-aware target (`entry×(1+max(0.8%, 4×round-trip fee))`), a 24h (96×15m) time stop, an 8×15m post-stop-out cooldown, and a D-specific −1.5%/day loss cap. Ships **DISABLED by default** (`strategy_D_enabled:"false"`); enabled per-mode via ENABLE D; validated in PAPER before any LIVE use. Per §Plumbing, D flows through the **existing Position/Trade tables** (strategy="D") so PNL D / dashboard splits / Trades page work automatically — NO new cycle table. Delivered: (1) **PURE `strategies/strategyD.ts`** (25 fixture tests) — volatility gate (24h range ≥2.5% from the last 96×15m bars), 15m dip-reclaim trigger (low ≥0.8% below the 24-period 15m EMA within 8 bars AND the just-closed candle closes above the prior 15m high — never catch the knife), fee-aware `dTargetPrice`/`dStopPrice`, `dTimeStopReached`, `dCooldownActive`, `checkDEntry`, whole-bag `manageExitD` (SL/TARGET/TIME_STOP), code-level hard-exclude (BTC/ETH + all T3, non-overridable), `strategyD` plugin (inert `checkEntry`; registered so `registry.get("D").manageExit` routes). (2) **Risk `approveLevelingEntry`** (11 tests) — fixed 5%-of-capital bag, ordered gate chain state→breaker→data→pair-removing→one-owner(A/B/D + driver-side C-cycle pre-check)→D_MAX_CYCLES(3)→D_DAILY_CAP(−1.5%)→fee-target sanity→minNotional(bag+at-stop sell)→free-USDT; new `RiskLedgerReader.realizedNetTodayForStrategy`; SymbolOwner widened to include "D". (3) **15m timeframe** — `Timeframe`/`TF_MS` + `close_15m` scheduler event; engine maintains 15m (`MAINTAINED_TFS`) but does NOT gate on it (A/B/C eligibility untouched). (4) **Driver** `apps/bot/src/engine.ts` — `onClose15m`→`runDEntries` (allowlist `strategy_D_pairs` default 9 pairs, 15m readiness, one-owner + C-cycle + cooldown pre-checks, `checkDEntry`→`approveLevelingEntry`→`openPosition`); D exits via the same manageExit path on ticks/closes (paper engine forwards `elapsedMs` for the time stop). (5) **Companion: settings-backed strategy-tier matrix** — `allowedTiers` moved to `strategy_tiers_A/B/C/D` (seeded to current values), registry gates on an injected resolver (falls back to plugin default), `getStrategies` loops A/B/C/D; D's hard exclusions stay code-level. (6) **Plumbing** — Prisma `Strategy` enum +D (isolated `ADD VALUE` migration `20260707000000_strategy_d`), `StrategyId`/ledger-row/reports/ports/telegram-parse/dashboard-badge+filter all widened; `pnlByStrategy` D bucket (Trade-based). SKIPPED (deferred, non-gating): live FilterState `strategyD` node persistence — the builder+type (`buildStrategyDNode`/`StrategyDNode`) are shipped+tested, but merging into the pipeline-owned FilterState JSON risks clobbering the A/B verdict (sole writer, no read seam); D is NOT backtestable (separate 15m path, `parseBacktestArgs` still A/B only). typecheck/build green (core/db/bot tsc + core & dashboard build), **518 tests** (core 375 **+38**, bot 99, db 14, dashboard 30); Prisma schema valid. Deploy-time [HUMAN_REQUIRED]: `migrate deploy`+`seed`, then `ENABLE D` on the VPS and confirm ≥1 full D cycle (entry→target/stop) in logs/ledger with net-of-fees P&L to the cent; D's T2 paper results carry ±per-tier-slippage uncertainty — judge with margin. NEXT → **module 24** (`specs/24-strategy-d-revisions.md`, appeared this session).
**Recent (one line each):**
- Module 23 (Strategy D — Active Leveling, intraday) DONE/APPROVED 2026-07-07 [FIXED_CHECKPOINT] — new registry plugin: 15m dip-reclaim on 24-EMA, vol gate ≥2.5%, BTC/ETH+T3 code-excluded, flows through existing Position/Trade tables (strategy="D"), ships DISABLED. (Sizing/stop since AMENDED by module 24.)
- Module 22 (Wire EventLog Sink + Engine Heartbeat, runtime wiring gap) DONE/APPROVED 2026-07-07 [FIXED_CHECKPOINT] — `main.ts` never attached the event sink → EventLog empty → dashboard permanently "offline"; fix attaches the sink + 30s engine heartbeat + nightly heartbeat prune; 480 tests.
- Module 21 (Dashboard Status/Equity Export Fix, corr. 14/20) DONE/APPROVED 2026-07-07 [CHECKPOINT] — partial-200 degradation (`lib/degrade.ts`) on `/api/status,equity,overview` so one bad field can't 500/blank; `/api/status` reads state/heartbeat exchange-free → bar flips online; `data-routes.smoke.test.ts` (+10).
**Mode policy:** LIVE permitted ONLY after owner `APPROVE LIVE GATE` + a PASSING pre-flight + typed `CONFIRM LIVE`. Real Binance key + testnet dress-rehearsal + runtime-engine wiring = deploy-time [HUMAN_REQUIRED].
**Branch:** staging | **Server:** Oracle VPS (GarageBrainPro host) | **Repo:** PRIVATE
**HARD checkpoints ([FIXED_CHECKPOINT]):** modules 01, 02, 03, 06, 11, 15, 16
**Build-order note:** Module 16 (env-loading hotfix, foundational) is built **immediately after module 11** — before 12 — because the bot cannot stay alive under PM2 until root-`.env` loading is fixed, and 11 is where a live bot is first needed for command testing.
**[HUMAN_REQUIRED] hand-offs expected:** 11 (BotFather), 14 (DNS/SSL/noindex), 15 (Binance API key + LIVE pre-flight)

## Build Order Checklist

- [x] 01 Foundation — DONE/APPROVED 2026-07-06 — monorepo + config/logging/scheduler/state shell; typecheck/build/28 tests green; boots + graceful shutdown verified
- [x] 02 Database — DONE/APPROVED 2026-07-06 — Prisma schema (10 models/7 enums), init migration, seed (settings + 15 pairs), pnl.ts, in-bot 03:30-UTC backup; typecheck/build green, 46 tests (db 14 + bot 32), boot+shutdown verified
- [x] 03 Exchange connector — DONE/APPROVED 2026-07-06 — BinanceConnector (ccxt+ws) in @spotedge/core: filter compliance (roundQty/roundPrice/minNotional/TP1-split), clientOrderId idempotency, market fills w/ USD fees, stop-limit+watchdog seam, WS resilience+backoff, clock sync, BNB reserve, reconcile (EXTERNAL_CLOSE), PAPER/LIVE ExecutionRouter; typecheck/build green, 88 tests (core 42 + bot 32 + db 14), boot+shutdown verified; testnet integration deferred to server deploy
- [x] 04 Indicator engine — DONE/APPROVED 2026-07-06 — @spotedge/core indicators/: math (SMA-seeded EMA, Wilder RSI/ATR/ADX, VolumeAvg20+ratio, emaRising), structure (no-repaint pivots, body-based S/R zones + srFlip, swings/fibZone, range detector w/ 3×ATR + invalidation, bullish engulfing, higher-low+minor-high break, H4 bearish rejection), CandleStore (600-window ×TF, 500-kline warm-up gate on 1h/4h, onClose append + gap-detect/refetch, snapshot → FilterState sink); typecheck/build green, 127 tests (core 81 = +39 indicator, bot 32, db 14); 15-pair snapshot smoke green
- [x] 05 Filter pipeline — DONE/APPROVED 2026-07-06 — @spotedge/core filters/pipeline.ts: ordered veto chain L0 system gates → L1 BTC market (skips BTCUSDT) → L2 trend bias → L3 ADX regime (routes A/B) → L4 S/R 1:2 geometry → L5 discount (WAITING/IN_ZONE/OVERSHOT); first hard-veto short-circuits + named in `blocking`; single FilterState JSON for both renderers; runFilterCycle persists per-pair, exceptions → DATA veto, never throws; typecheck/build green, 157 tests (core 111 = +30 pipeline, bot 32, db 14)
- [x] 06 Risk engine — DONE/APPROVED 2026-07-06 — @spotedge/core risk/: PURE sizing.ts (PDF formula, tier caps T1 15/T2 10/T3 5%, floored qty so effective risk ≤ intent, computeNetRR w/ round-trip fees, clamp 0.25–2%, SETRISK/SETCAPITAL guards) + engine.ts (ordered 7-check approveEntry veto chain, approveLevelingSell ≤30% + one-owner rule, RiskEngine orchestrator: compounding capital, breaker trip/reset, reporting-only streak, hourly equity + LEDGER_DRIFT); 193 tests
- [x] 07 Strategy A (Trend-Pullback) — DONE/APPROVED 2026-07-06 — @spotedge/core strategies/: StrategyPlugin interface + StrategyRegistry (central ENABLE/DISABLE + allowedTiers gate before checkEntry) + strategyA.ts (PURE checkEntry — 1H engulfing[vol≥1.3]/higher-low-break trigger inside discount zone, structural stop floored at 1×ATR(H4), TP1 2×/A+ 3× risk, A+ H4-support proximity; PURE manageExit — TP1 50%+breakeven, higher-low trail −0.25×ATR(1H), SL, 24-H4 time stop); Position.trailHistory Json migration; typecheck/build green, 214 tests (core 168 = +21 strategy-A, bot 32, db 14)
- [x] 08 Strategy B (Range Reclaim) — DONE/APPROVED 2026-07-06 — @spotedge/core strategies/strategyB.ts: PURE checkEntry (resolveRange → floor/ceiling zones from H4 pivots, ≥2 touches, height≥3×ATR via detectRange, freshness gate; armed floor TOUCH RSI(14,H4)<35 within 12 H4; fresh 1H RECLAIM above floor-zone top; two-layer stop min(floorBottom−0.25×ATR, entry−1×ATR), TP1=range midpoint, TP2=ceiling-zone bottom) + PURE manageExit (SL / RANGE_TP+breakout / TP2 RANGE_TP / REGIME_FLIP kill-switch ADX≥20 on H4 close / TP1 50%+breakeven / TIME_STOP 24-H4); allowedTiers=[T1,T2] (registry-enforced); EntryProposal.tp2Price/ceilingTop + Position.tp2Price/ceilingTop migration; typecheck/build green, 241 tests (core 195 = +27 strategy-B, bot 32, db 14)
- [x] 09 Strategy C (Core Leveling) — DONE/APPROVED 2026-07-06 — @spotedge/core strategies/strategyC.ts (T1-only LevelingPlugin, distinct from A/B): PURE checkLevelingSell (bearish H4 rejection inside a resistance zone → sell 30% of core bag; RUNAWAY-BULL BLOCK when this/prev H4 body-closed above the zone) + PURE checkRebuy (FORCED insurance ≥sellPrice×1.05 on any TICK / STRUCTURAL 61.8%-or-support discount + fresh 1H reclaim on close / TIMEOUT 90-H4 → CYCLE_TIMEOUT) + chunkPnl (proceeds−cost−fees, decimal-safe); forcedRebuyTrigger/inRebuyDiscount/reclaimedC helpers; risk engine's approveLevelingSell (module 06) sizes the chunk + one-owner/breaker/minNotional; CycleStatus+EXTERNAL & LevelingCycle.exitReason migration (20260706000003); typecheck/build green, 268 tests (core 222 = +27 strategy-C, bot 32, db 14)
- [x] 10 Paper-trading engine — DONE/APPROVED 2026-07-06 — @spotedge/core paper/: PaperExecutor (virtual ExecutionRouter — last-print fills, per-tier adverse slippage T1 0.02/T2 0.05/T3 0.10%, live-identical 0.1%/0.075%-BNB fees in feeUsd, REAL cached filter LOT_SIZE/minNotional rounding, mode-scoped virtual balances + SETCAPITAL rebase, idempotent virtual stop registry) + PaperTradingEngine (same strategy/risk code path; A/B open→TP1→trail→exit + Strategy-C sell→rebuy lifecycles through an injected LedgerRepository writing Position/Fill/Trade/LevelingCycle mode=PAPER; rebuildStops on restart) + THE FENCE (BinanceConnector.assertLive on every live* endpoint → PAPER can't reach a real order method); typecheck/build green, 291 tests (core 245 = +23 paper: executor/fence 19 + engine 4 incl. round-trip P&L-to-the-cent, stop blow-through, leveling chunk P&L; bot 32, db 14). 48h VPS continuous-PAPER smoke = deploy-time gate (reviewed via logs by controller before module 11).
- [x] 11 Telegram control panel — DONE/APPROVED 2026-07-06 — dedicated grammY bot + the shared @spotedge/core service layer (services/control.ts + reports.ts + ports.ts + engineState.ts) that module 14's dashboard will also call (NO business logic in handlers). Owner-only gate (foreign ids silently ignored + logged); full command set (STATUS/POSITIONS/PNL/TRADES/BALANCE/FEES/FILTERS/LOGS/RECONCILE • STOP/START/PAUSE/RESUME/MODE/CLOSE/CLOSEALL • PAIRS/ADDPAIR/REMOVEPAIR/SETTIER • STRATEGIES/ENABLE/DISABLE/SETCAPITAL/RISK/SETRISK/SETCORE/SETSUMMARY/RESETPAPER • REPORT/BACKTEST/HELP) routed to services; registry-driven HELP (index/card/ALL can't drift); sendLongMessage (≤3900, line-boundary split, 📄(i/n) labels, 350ms gap) + per-chat 1msg/s OutboundQueue; confirm flow (typed CONFIRM LIVE/CLOSEALL/RESETPAPER + button CLOSE/SETCAPITAL/SETRISK/SETCORE, 60s TTL + timeout); RESETPAPER wipes ONLY PAPER rows (mode-scoped, test-covered); automatic-push formatters + error-class rate limiter (1/15min) + daily-heartbeat latch; DB-backed StateStore so STOP/PAUSE/MODE persist; engine-only fallback on invalid token (verified). typecheck/build green, 333 tests (core 255 = +10 services; bot 64 = +32 telegram; db 14). BotFather token + real-bot live-command smoke = deploy-time hand-offs.
- [x] 12 Backtest module — DONE/APPROVED 2026-07-06 — @spotedge/core backtest/: HistoricalFeed (getKlinesSince pagination + warm-up/TF + disk-cache seam + always-BTC L1 feed), runBacktest replay over the SAME pipeline/strategy/risk/paper-engine code (next-1H-open entries, intracandle low→high→close SL-wins-ties, H4 boundary regime/time-stop, breaker latch + UTC rollover), InMemoryLedger isolation (never live/paper), pure metrics (win%/net $&%/PF/maxDD/avgR/avgHold/fees&%gross/per-strategy+symbol/equity curve/veto histogram); BacktestPort + parseBacktestArgs/runBacktestCommand single-flight → Telegram BACKTEST async ack+renderBacktest; backtest_runs migration; typecheck/build green, 350 tests (core 272 = +13; bot 64; db 14) incl. determinism + golden P&L-to-the-cent + isolation. Real 180d run + live TG smoke = deploy-time gates
- [x] 13 Reporting engine — DONE/APPROVED 2026-07-06 — @spotedge/core services/reports.ts: one mode-scoped div-by-zero-proof query layer (pnlSummary/bySymbol/byStrategy incl. C forced-rebuy count, equityCurve gap-fill, drawdownSeries, feeReport BNB/quote split+projection, dailyAudit veto/breaker/heartbeat from EventLog window, tradeHistory paging, streaming exportCsv PKT+UTC 17-col BOM, buildWeeklyDigest); fixed-+5 utils/time.ts (core tz-db-free); new LedgerPort/EventsPort/SchedulePort ports + Prisma adapters; ReportPusher (daily + Monday weekly on scheduler summary_time) + SETSUMMARY live-reschedule; Telegram REPORT→daily-audit, REPORT CSV→document. typecheck/build green, 375 tests (core 287 +15, bot 74 +10, db 14)
- [x] 14 Web dashboard — DONE/APPROVED 2026-07-06 — Next.js 15 `apps/dashboard` on the shared `@spotedge/core` services (DB-backed ServiceContext + StatePort; 5s DB-poll live updates; public-API marks/candles; in-process backtest port); jose session + bcrypt + edge middleware + login rate-limit + fail-closed staging screenshot token + idempotent control POSTs + typed confirms; four-layer noindex (curl-verified); 8 pages + login + control bar, shadcn/ui dark+light, animated Recharts, SVG candlesticks, full control parity; typecheck/build green, 395 tests (dashboard +20), live-smoke passed. DNS/SSL/Nginx + Lighthouse = deploy-time
- [x] 15 Live-trading enablement — DONE/APPROVED 2026-07-07 — [FIXED_CHECKPOINT] — mode state machine finalized (both DRAINING paths + restart/STOP-during-drain + gate-closed init fail-safe); `services/liveEnable.ts` (preflightLive 6-check card · approveLiveGate via new StatePort.setLiveGate · preflightForLive→commitLive · requestPaper MANAGE/CLOSEALL · settleDrain); `exchange/routing.ts` mode×state×executor truth table + fence; connector `fetchApiPermissions`; Telegram `APPROVE LIVE GATE` + preflight-gated `MODE LIVE` + `MODE PAPER MANAGE|CLOSEALL`; bot preflight adapter wired; dashboard liveGateOpen + drain-choice dialog. typecheck/build green, 458 tests (core 333 +46, bot 91 +17, db 14, dashboard 20). Real key + testnet dress-rehearsal + runtime-engine wiring = deploy-time [HUMAN_REQUIRED]; activation is the operator's act (no mainnet order placed by the agent)
- [x] 17 Dashboard typecheck-ordering hotfix (correction to module 14) — DONE/APPROVED 2026-07-07 — [CHECKPOINT] — Approach B: dashboard `typecheck` defers to `next build` (real TS checking stays IN build, `ignoreBuildErrors` OFF — deliberate TS error → build FAILS, proven); tsconfig excludes `.next`/`out`/`dist`; deploy.sh builds before typecheck; turbo `typecheck.dependsOn=["^build","build"]`. Fresh-clean build+typecheck green, 458 tests green, `.next` gitignored
- [x] 18 Runtime Trading Engine (composition root) — DONE/APPROVED 2026-07-07 — [FIXED_CHECKPOINT] — new `apps/bot/src/engine.ts` (`TradingEngine`) drives the built pipeline/strategy/risk/paper code at runtime (init+warm-up, boot reconcile+rebuildStops, close_1h/4h/rollover loop, ticker watchdog+forced-rebuy, PAUSED/STOPPED/DRAINING state-awareness, two mode-scoped engines route by booked mode; live engine only when cfg.MODE===LIVE); built the 3 missing pieces (Prisma `ledger.ts`, `risk.ts` adapters+createRiskEngine, PaperExecutor wiring in main.ts); preflight runtime handles + STATUS engine-health wired; fence intact; typecheck/build green, 463 tests (+5 engine). 48h VPS PAPER smoke + real key/testnet = deploy-time [HUMAN_REQUIRED]
- [x] 19 Dashboard root-env loading hotfix (correction to module 14) — DONE/APPROVED 2026-07-07 — [CHECKPOINT] — dashboard loads the monorepo-root `.env` in code (like the bot, module 16) so login works with NO `apps/dashboard/.env.local` symlink. `instrumentation.ts` `register()`→`loadRootEnv` (nodejs-guarded) already existed (module 14); added the acceptance masked startup log + `maskPath()`; rewrote ecosystem dashboard ENV comment. Edge-runtime concern resolved by inspecting compiled `middleware.js` (runtime `process.env` read, NOT build-inlined; Next awaits `register()` before serving → edge sandbox sees the loaded secret). typecheck/build green, 463 tests unchanged
- [x] 20 Dashboard ccxt import-chain fix (correction to module 14) — DONE/APPROVED 2026-07-07 — [CHECKPOINT] — dashboard routes crashed with `Cannot find module 'ccxt'` because the main `@spotedge/core` barrel `export *`s `BinanceConnector` (the sole ccxt importer). New connector-free `packages/core/src/dashboard.ts` barrel + `@spotedge/core/dashboard` subpath; 17 dashboard imports repointed (bot keeps the full barrel); `dashboardBarrel.test.ts` asserts the graph never reaches ccxt/BinanceConnector. typecheck/build green, 467 tests (core 337 +4). Deploy-time: rebuild+`pm2 restart trading-dashboard` → dashboard "offline"→"online"
- [x] 23 Strategy D — Active Leveling (intraday) — DONE/APPROVED 2026-07-07 — [FIXED_CHECKPOINT] — new registry plugin (Jan-2026 manual-leveling style + hard stop / fee-aware target / 24h time-stop / 8×15m cooldown / −1.5%/day cap); PURE `strategyD.ts` (15m dip-reclaim on 24-EMA, vol gate ≥2.5%, BTC/ETH+T3 code-excluded) + risk `approveLevelingEntry` (fixed 5% bag, D_MAX_CYCLES 3, D_DAILY_CAP, one-owner); 15m TF + `close_15m` (NOT a warm gate → A/B/C untouched); flows through existing Position/Trade tables (strategy="D"); companion settings-backed `strategy_tiers_*` matrix; enum migration; ships DISABLED. typecheck/build green, 518 tests (core 375 +38 = strategyD 25 + risk 11 + control 2). Deferred (non-gating): live FilterState D-node persistence (builder shipped). Deploy-time: `ENABLE D` + ≥1 full paper cycle on VPS. **(Sizing/stop AMENDED by module 24.)**
- [x] 24 Strategy D Revisions (risk-consistent + January-faithful, amends 23) — DONE/APPROVED 2026-07-08 — [CHECKPOINT] — per operator's "method was sound, emotions failed" diagnosis: (1) DELETE fixed-5% bag → **risk-size like A/B** via `sizePosition` off the catastrophe stop (never > risk_pct of capital, tier-cap bound); `BAG_FRACTION` removed (0 refs); `LevelingEntrySnapshot.riskPct` + `RiskEngine.approveLevelingEntry` fetches `risk_pct`. (2) DELETE tight 1.2% stop → **wide CATASTROPHE stop `CATASTROPHE_STOP_PCT=0.04`** (≥3× the 0.8% target margin; `STOP_PCT` kept as `@deprecated` alias). (3) entry/target unchanged; between them D just WAITS. (4) all discipline rails (daily cap/cooldown/3-cycle/vol-gate/one-owner/breaker) KEPT. Explicitly rejected: no LLM/AI in trade decisions (grep-clean), no change to global 1%/2% risk. core/bot/db typecheck+build green, 379 core tests (+4: risk-sized-not-flat + worst-case-loss≤risk_pct property + wider-stop-smaller-bag + catastrophe-stop-4%/≥3×-margin + normal-−2%-dip-holds). **LAST spec present** (`specs/` ends at 24). Deploy-time (carried from 23): `ENABLE D` + ≥1 full paper D cycle on VPS with net-of-fees P&L to the cent
- [x] 22 Wire EventLog sink + engine heartbeat (runtime wiring gap) — DONE/APPROVED 2026-07-07 — [FIXED_CHECKPOINT] — ROOT CAUSE: `main.ts` never called `attachEventSink(makeEventSink(prisma))` → `logEvent()` no-oped every DB write → `EventLog` empty → dashboard `isBotOnline()` found no heartbeat → permanent "offline" despite a live engine. Fix: (1) main.ts attaches the sink (shared prisma singleton) before the first persisted event → boot/state/live-gate/backup events now persist; (2) engine.ts 30s heartbeat timer (immediate emit on start, ref'd, cleared in `stop()`) emitting `heartbeat` with message "engine heartbeat" + `data.type:"heartbeat"` (both dashboard matchers hit — verified vs `makeEventSink` shape), continues in PAUSED/STOPPED; (3) backup.ts nightly prune of `data.type=="heartbeat"` rows >7d only (injectable `pruneRunner`, best-effort). SKIPPED spec §3 (Setting `engine_heartbeat_at` stamp) — `setSetting` audits every change → 2,880 audit rows/day for an unread optional signal (spec permits skipping). typecheck/build green (core/db/bot tsc + dashboard next build), 480 tests (bot 99 +3: heartbeat-shape + prune-cutoff + prune-failure-isolation). Deploy-time: `pm2 restart trading-bot` → EventLog rising, `/api/status` botOnline:true, dashboard online + controls enabled + Recent events populate
- [x] 21 Dashboard status/equity export fix (correction to modules 14/20) — DONE/APPROVED 2026-07-07 — [CHECKPOINT] — server `/api/status,equity,overview` threw `TypeError: … is not a function` (ccxt crash gone). Investigation found all barrel exports present + callable (not the spec's type-only theory) → stale-build symptom; durable fix = partial-200 degradation (`lib/degrade.ts` `degradeTracker().safe()` + typed empty fallbacks) on all three routes so one bad field can't 500/blank the page, and `/api/status` reads state/mode/heartbeat exchange-free so the bar flips online regardless. `data-routes.smoke.test.ts` (seeded in-memory ctx) locks in export-presence + guard behavior. ccxt boundary preserved. typecheck/build green, 477 tests (dashboard 30 +10). Deploy-time: rebuild+`pm2 restart trading-dashboard`
- [x] 16 Environment loading hotfix (foundational correction to module 01) — DONE/APPROVED 2026-07-06 — shared `loadRootEnv()` (@spotedge/core, subpath export `@spotedge/core/env/loadRootEnv`, ESM-safe import.meta.url, anchors on pnpm-workspace.yaml+.env, dotenv no-override) called first in bot bootstrap (index.ts→main.ts dynamic import); config.ts CWD-dotenv removed; FATAL prints resolved .env path; ecosystem.config.js clean form (absolute cwd, no env_file/node_args/secrets); deploy.sh DATABASE_URL="$DATABASE_URL" prefix (no dotenv-cli); typecheck/build green, 337 tests (+4 loadRootEnv); 3-CWD same-root-.env + FATAL-path verified via compiled dist ESM

## Decisions / Assumptions Log

- Stack: Node 20 + TS, pnpm/Turborepo, ccxt, PostgreSQL + Prisma, Next.js 15 dashboard, dedicated Telegram bot.
- Capital: $11,000 paper / $11,000 live (allocated-capital model; wallet remainder untouched for manual trading).
- Risk: 1% per trade default (hard bounds 0.25–2%), tier caps 15/10/5%, net R:R ≥1.5 after fees, −3% daily circuit breaker, max 3 open positions.
- Tiers: T1 BTC/ETH/BNB/SOL • T2 DOT/LINK/AVAX/NEAR/TON/SUI/FIL/ATOM • T3 XLM/DASH/ALGO. GRAM + CVX excluded.
- Timezone: UTC engine, PKT display; daily summary default 21:00 PKT (operator-editable setting).
- Dashboard: trade.geekaxon.com, noindex (header+robots+meta+login wall), port [DECIDE AT BUILD].
- Strategy D/E: designed stubs only (ARCHITECTURE.md Appendix A), [DECIDE AT BUILD] after paper data.
- LIVE capital source & Binance sub-account: [DECIDE AT BUILD] (operator guide covers optional sub-account).
- **Env loading (module 16, discovered in staging):** bot booted under PM2 with `cwd: apps/bot` and saw all env vars empty despite a correct root `.env`; root causes were cwd-relative dotenv resolution + unreliable PM2 `env_file` on the server's PM2 version. Fix = shared `loadRootEnv()` (walks up to the dir with pnpm-workspace.yaml + .env) called first in every entrypoint; ecosystem.config.js restored to clean form (absolute repo-root cwd, no env_file/node_args, no secrets). deploy.sh keeps the working `DATABASE_URL="$DATABASE_URL"` prefix on Prisma commands. Temporary server stopgap until 16 ships: `node_args: "-r dotenv/config"` + `DOTENV_CONFIG_PATH` absolute path.
