# PROGRESS-HISTORY.md — SpotEdge Trading Bot (detailed log)

> PROGRESS.md keeps one-liners; this file keeps the full story. The agent appends a dated entry after EVERY module: what was built, files touched, decisions/deviations (with reasons), verification results, and any open items handed to later modules.

## Entry template

```
## [YYYY-MM-DD] Module NN — <name> — <DONE | PARTIAL | BLOCKED>
**Built:** …
**Files:** …
**Decisions/Deviations:** … (none, or reason logged; money/risk/auth deviations require [HUMAN_REQUIRED] instead)
**Verification:** typecheck ✅/❌ · build ✅/❌ · tests <n> passing · UI design self-check (if UI) ✅/❌ · boot in PAPER ✅/❌
**Open items → later modules:** …
**Checkpoint printed:** [FIXED_CHECKPOINT] / [CHECKPOINT] / [HUMAN_REQUIRED]
```

## History

### [2026-07-06] Module 12 — Backtest Module — DONE
**Built:** History-replay backtester that drives the SAME `packages/core` code as paper/live — module-05 pipeline, module-07/08 strategy plugins, module-06 `approveEntry`, module-10 PaperExecutor + PaperTradingEngine — over historical candles. NO trading logic reimplemented; the replay only sequences the existing pieces, so backtest = paper = live by construction. New `packages/core/src/backtest/`:
- **`types.ts`** — BacktestParams/ResolvedParams, MarketMeta (filtersFor/tierOf), FeedData (per-symbol 1h+4h + shared btc4h + tradingStart/end + clamps), BacktestTrade/EquityPoint/BacktestMetrics/StrategySplit, BacktestResult, RawKlineFetcher + FeedCache seams, `BACKTEST_LIMITATIONS` footer.
- **`feed.ts` (`HistoricalFeed`)** — per symbol×TF fetches the tradable window + `warmup` (500) candles before it, paginated 1000/req via an injected RawKlineFetcher, disk-cache seam keyed `symbol-tf-start-end`; ALWAYS loads BTCUSDT (shared L1 market feed, fetched once); notes pairs listed after the window start (`clamps`); `closedBy`/binary `sliceClosed` window helpers.
- **`ledger.ts` (`InMemoryLedger`)** — implements the SAME `LedgerRepository` the PaperTradingEngine writes through, plus read helpers for the hand-built RiskSnapshot (realizedNetAllTime, realizedNetOnDay = UTC-day breaker accumulator, deployedAndOwner, unrealizedNet marked to a price map, positionMeta for R/tier). Everything in RAM — the isolation guarantee.
- **`engine.ts` (`runBacktest`)** — one PaperExecutor (mutable price ref + replay clock, seedUsdt=capital) + PaperTradingEngine + InMemoryLedger. Master timeline = 1H openTimes across trading symbols. Per candle Ci: (1) fill entries SIGNALLED on the previous close at Ci's OPEN (conservative next-open fill); (2) intracandle management low→high→close (low-before-high → SL wins ties; TP1/forced-rebuy vs the high; onClose trail + H4-boundary regime/time-stop); (3) detect signals on Ci's close (real `evaluatePipeline` → `registry.checkEntry` gated by enabled strategies) → queue for next open. Circuit-breaker latch (breakerBreached, UTC-day rollover reset); risk-veto histogram at entry-exec; portfolio equity sampled each close. Deterministic — order-id nonces excluded from output.
- **`metrics.ts`** — pure `computeMetrics` → win%/net $&%/gross/profit factor/max DD/avg R/avg hold/fees & % of gross/per-strategy+symbol splits/equity/end equity.
- **Wiring:** connector `getKlinesSince` (additive; paginates `since`→closed candles); `BacktestPort` on ServiceContext; `parseBacktestArgs` + `runBacktestCommand` (single-flight) in control.ts; `renderBacktest` HTML card + async `handleBacktest` (ack + fire-and-forget delivery) in the Telegram face; `createBacktestPort` in apps/bot (own connector, prisma tier lookup, disk `backtest_cache/`, persists a `BacktestRun`); `backtest_runs` migration + BacktestRun model (JSONB params/metrics/trades/equity/diagnostics).
**Files:** `packages/core/src/backtest/{types,feed,ledger,engine,metrics}.ts` (new) · `packages/core/src/exchange/BinanceConnector.ts` (+getKlinesSince) · `packages/core/src/services/{ports,control}.ts` · `packages/core/src/index.ts` · `packages/db/prisma/schema.prisma` (BacktestRun) + `migrations/20260706000004_backtest_runs/migration.sql` · `packages/db/src/index.ts` (export BacktestRun) · `apps/bot/src/backtest.ts` (new) · `apps/bot/src/{main,logger}.ts` · `apps/bot/src/telegram/{handlers,render,context}.ts` · tests `packages/core/src/__tests__/backtest.test.ts` (new, 8) + `services.control.test.ts` (+5).
**Decisions/Deviations (no money/risk/auth deviations):** (1) Strategy C is a core-leveling overlay on a manually-held bag (no standalone entry, and the run API carries no core seed) → not backtestable here; the command refuses C with a clear message, and the engine's manageRebuys path stays identical (dormant with no cycles). (2) `pipelineEval` is an injectable DI seam defaulting to the REAL module-05 `evaluatePipeline` (production always uses the real veto chain); it exists ONLY so the compact golden can force eligibility — the real pipeline needs 200+ H4 warm-up candles a 30-candle golden can't carry, so the golden validates the HARNESS math (next-open fill, slippage, fees, intracandle SL-wins-ties, computeTrade) to the cent while module-07 already covers strategy-A signal detection. (3) L0 Tier-3 24h-volume gate: history has no volume feed → replay passes a large constant (documented limitation, alongside current-filters-on-past-data).
**Verification:** typecheck ✅ (4 packages) · build ✅ (core/db/bot/dashboard) · tests **350** — core **272** (+8 backtest: golden P&L-to-the-cent gross 109.498128/fees 2.309338008/net 107.188789992 WIN TP1_TRAIL, determinism byte-identical, isolation, veto TOO_SMALL, metrics×2, feed×2; +5 backtest command parse/offline/single-flight) + bot 64 + db 14 · `prisma validate` ✅ · `prisma migrate diff` confirms backtest_runs table+index. Real `BTC,ETH,SOL × A × 180d < 5min` run + live Telegram BACKTEST smoke = DEPLOY-TIME gates (need Binance history over the network), consistent with prior modules' deploy gates.
**Open items → later modules:** module 13 (reporting) can format saved `backtest_runs`; module 14 (dashboard) renders the equity curve + trade markers + tables from the persisted BacktestResult; a future step may add a BACKTESTS list/GET command over the backtest_runs table. First BACKTEST on the VPS is the network smoke.
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Module 10 — Paper-Trading Engine — DONE
**Built:** Full trade simulation on REAL market data through the SAME strategy/risk/filter code path as live — only execution is virtual (paper = this project's staging; results translate 1:1 to live). Three pieces in `@spotedge/core/src/paper/`:
- **`PaperExecutor`** — implements the module-03 `ExecutionRouter` surface, so nothing above the connector knows it is PAPER. `marketBuy`/`marketSell`/`marketBuyBase` fill at the LAST PRINTED ticker price (no fantasy mid-candle fills — throws if nothing has printed) with an **adverse slippage model** in bps per tier (T1 0.02%, T2 0.05%, T3 0.10%; buys fill above the print, sells below). **Fees identical to live** — 0.1% (0.075% when the BNB-fee setting is on) booked in `feeUsd`/`fees[USDT]`, never reducing the base leg — so ledger P&L math agrees to the cent. LOT_SIZE/minNotional rounding uses the REAL cached exchange filters. Mode-scoped virtual balances seeded from `capital_paper` (`seedBalance` for Strategy-C core bags, `setCapital` rebases only the USDT cash leg on SETCAPITAL — open positions untouched). Idempotent virtual stop registry (`placeStopLimit`/`cancelOrder`/`getOpenOrders`) — the paper mirror of an exchange STOP_LOSS_LIMIT, matched by the bot-side watchdog, not passively by the exchange.
- **`PaperTradingEngine`** — the stateful lifecycle orchestrator. Drives A/B positions (open → market-buy sized notional, record Position+Fill from the ACTUAL fill, arm stop → `manageExit` on tick/1H-close/H4-close → TP1 50% + breakeven MOVE_STOP → trailing exit) AND Strategy-C leveling cycles (`openLevelingSell` → `manageRebuys` forced/structural/timeout via pure `checkRebuy`, chunk P&L = recorded sellPrice proceeds − rebuy cost − fees) through an injected `LedgerRepository`. Every Position/Fill/Trade/LevelingCycle row is written `mode=PAPER`, so the whole reporting/telegram/dashboard stack works unchanged. `rebuildStops()` re-registers a virtual stop for every open position after a restart (registry is persistent, not in-memory only). Exit decisions come from the pure strategy plugins — the watchdog is just those calls run every tick/close, so a gapped tick naturally fills the exit at the current (worse) print: the live **blow-through** branch, rehearsed.
- **THE FENCE** — `BinanceConnector.assertLive(method)` on every `live*` order endpoint (`liveMarketBuy/Sell/StopLimit/Cancel/OpenOrders`) throws unless `mode==="LIVE"`. Combined with "PAPER → router is the PaperExecutor (or the fail-closed `UnwiredPaperRouter`)", a PAPER run can never touch a real order endpoint. This is the guarantee the whole pre-module-15 build rests on.
**Files:** `packages/core/src/paper/{config,PaperExecutor,engine}.ts` (new); `packages/core/src/exchange/BinanceConnector.ts` (+`assertLive` fence on the 5 live endpoints); `packages/core/src/index.ts` (export `paper/*`); tests `packages/core/src/__tests__/paper.executor.test.ts` (19) + `paper.engine.test.ts` (4).
**Decisions/Deviations:** No new DB migration — reuses the module-02/07/08/09 schema (Position/Fill/Trade/LevelingCycle all already carry `mode`); the engine talks to a `LedgerRepository` seam (Prisma-backed in apps/bot, in-memory in tests) so core imports no app code. `chunkPnl` intentionally values proceeds at the recorded H4 sellPrice (not the slipped fill) — matches the module-09 chunk-P&L contract. No live-order/risk/auth behaviour changed → no [HUMAN_REQUIRED].
**Verification:** typecheck ✅ · build ✅ · tests 291 passing (core 245 = +23 paper, bot 32, db 14) · fence test: PAPER `live*` endpoints + unwired router all throw ✅ · round-trip P&L to the cent vs hand calc (gross 49.580082, fees 2.049580002, net 47.530501998, WIN) ✅ · stop blow-through fills at the gap not the trigger (79.984 vs 95) → LOSS ✅ · boot-in-PAPER not re-run this step (no bot wiring changed; connector/engine unit-covered).
**Open items → later modules:** apps/bot must wire the Prisma-backed `LedgerRepository` + construct the `PaperExecutor` with live ticker `priceOf`/`filtersFor`/`tierOf`/`bnbFeesActive` and inject it as the connector's `paperRouter`, then call the engine on the scheduler's tick/close/H4 events (integration lands with the bot loop). **48h continuous-PAPER VPS smoke (all 15 pairs, no crash / no >100MB memory creep / no unhandled rejection) is a DEPLOY-TIME acceptance gate** — reviewed via logs by the controller before module 11 proceeds; cannot be exercised in unit tests. Module 15 flips the same engine to LIVE by swapping the router (the fence + `UnwiredPaperRouter` stay as the safety net).
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Module 09 — Strategy C (Core Position Leveling) — DONE
**Built:** The bonus harvester (Tier 1 only) — the gold "asset leveling" model crypto-corrected: harvest USDT chunks from volatility on a declared core bag while ALWAYS restoring the exact quantity, with hard insurance against runaway bulls. Runs on H4-close events OUTSIDE the A/B pipeline. One new PURE module `@spotedge/core/src/strategies/strategyC.ts`, exported from the core index. Because C trades sell→rebuy CYCLES against a declared core bag (not directional positions with a TP/SL ladder), it does NOT implement the A/B `StrategyPlugin`; it defines its own `LevelingPlugin { id:'C', allowedTiers:['T1'], checkSell, checkRebuy }`. Everything is PURE over injected candle windows + the cycle row (no DB/exchange/clock); the risk engine's `approveLevelingSell` (module 06, already shipped) sizes the chunk + enforces breaker/one-owner/minNotional; the paper engine (module 10) orchestrates and persists.
- **Sell trigger (`checkLevelingSell`)** — on the just-closed H4 candle: scan module-04 `srZones` newest-first; for each zone classified `resistance` vs the close, require a module-04 `bearishRejection` (red body closing inside the zone, upper wick ≥ 50% of range). RUNAWAY-BULL BLOCK: a zone whose top was cleanly body-closed above by the trigger candle OR the previous H4 candle (`min(open,close) > zone.top`) is BROKEN → skip (ride the trend, no sale). Returns `{ sellPrice=close, zone, detail }` or null. Needs ≥16 H4 candles (confirmed pivot LEFT+RIGHT=10 + ATR(14) warm).
- **Rebuy triggers (`checkRebuy`)** — ordered precedence, called on every TICK and each 1H close: (1) **FORCED** insurance — `price ≥ forcedRebuyTrigger` (=sellPrice×1.05) → `FORCED_REBOUGHT` IMMEDIATELY on the tick, no candle wait (every tick above trigger costs money); highest priority. (2) **STRUCTURAL** — on a 1H close, `inRebuyDiscount` (price ≤ the 61.8% retracement of the sell-off swing [sell high → deepest low in the post-sell window], OR inside an H4 support zone below the sell) AND a fresh `reclaimedC` (1H bullish engulfing OR fresh higher-low-break whose break candle is the last) → `REBOUGHT`. (3) **TIMEOUT** — `h4CandlesOpen ≥ 90` (~15 days) on a 1H close → `REBOUGHT` tagged `exitReason: CYCLE_TIMEOUT`. Helpers `forcedRebuyTrigger`, `inRebuyDiscount`, `reclaimedC` exported for tests/reuse.
- **Chunk P&L (`chunkPnl`)** — decimal-safe (money.ts): proceeds = leverQty×sellPrice, cost = leverQty×rebuyPrice, gross = proceeds−cost, chunkPnlUsd = gross−feesUsd. Both legs use the SAME leverQty (bag restored exactly); BNB-paid fees are already USD-converted on the Fill rows and never reduce the rebuy qty (spec §Edge cases).
- **Data model** — `CycleStatus` gained `EXTERNAL` (reconcile found the bag short mid-cycle → manual interference, needs `SETCORE` re-declare) and `LevelingCycle` gained nullable `exitReason String?` (holds `CYCLE_TIMEOUT` | `EXTERNAL`; structural/forced closes leave it null). Migration `20260706000003_leveling_cycle_c` (`ALTER TYPE "CycleStatus" ADD VALUE 'EXTERNAL'` + add column).
**Files:** `packages/core/src/strategies/strategyC.ts` (new) · `packages/core/src/index.ts` (export strategyC) · `packages/db/prisma/schema.prisma` (CycleStatus EXTERNAL, LevelingCycle.exitReason) · `packages/db/prisma/migrations/20260706000003_leveling_cycle_c/migration.sql` (new) · `packages/core/src/__tests__/strategyC.test.ts` (new, 27 tests).
**Decisions/Deviations (no money/risk/auth deviations — sizing/one-owner/breaker owned by the shipped module-06 `approveLevelingSell`):** (1) C is a distinct `LevelingPlugin`, NOT registered on the A/B `StrategyRegistry` — the spec runs it OUTSIDE the pipeline on H4-close events, and its sell/rebuy shape does not fit `checkEntry→EntryProposal`/`manageExit→ExitAction`. `StrategyId` stays `A|B|C` (the enable/disable flag surface already covers C). (2) The sell trigger is the PURE candle structure only; orchestration gates (core bag > 0, no open cycle, C enabled, max 2 concurrent cycles) live in module 10 + `approveLevelingSell`. (3) Spec names the sub-minNotional veto `BAG_TOO_SMALL`; the shipped `approveLevelingSell` returns the stable `TOO_SMALL` VetoReason with a "leveling chunk … below minNotional" detail — not renamed (module 06 is a hard checkpoint; the semantics match). (4) The 61.8% sell-off swing uses the sell price as the swing high and the deepest low of the post-sell H4 window as the swing low (module 10 passes the since-sell window); the support-zone path is a fallback that needs a warm ATR. (5) `RebuySignal` carries both the terminal `status` (REBOUGHT / FORCED_REBOUGHT) and the optional `exitReason` (CYCLE_TIMEOUT) the executor writes — the timeout is still a REBOUGHT cycle, distinguished by the reason tag.
**Verification:** typecheck ✅ (all 4 workspaces; `prisma generate` regenerates the LevelingCycle type + CycleStatus enum) · build ✅ (core+bot tsc, dashboard next build) · `prisma validate` ✅ (schema valid with new enum value + column) · tests **268 passing** — core **222 (+27 strategy-C)** + bot 32 + db 14. Acceptance criteria all covered by fixtures: rejection-sell fires (sellPrice inside the resolved resistance zone); RUNAWAY-BULL BLOCK both ways (current clean breakout close, previous already broke above); green-candle / insufficient-history no-sale; sell→`approveLevelingSell` seam (30% sizing when unowned, one-owner SYMBOL_OWNED veto, breaker veto, tiny-bag minNotional veto); `inRebuyDiscount` at/above the 61.8% level; `reclaimedC` engulfing/flat; STRUCTURAL rebuy on close (profit path) + not on a tick + no reclaim = no rebuy; FORCED rebuy fires on a TICKER tick with `onClose=false` (latency path proven via the pure decision — no candle wait) + precedence over structural/timeout + boundary just below trigger; TIMEOUT at exactly 90 H4 (not 89) tagged CYCLE_TIMEOUT + not on a tick; chunk-P&L hand-computed to the cent for the profit path (43.65), the forced defined-loss path (−5% + fees = −5.20), and the BNB-fee bag-integrity path (same leverQty both legs). Live `migrate deploy` deferred to server deploy (no Postgres/secrets in the build env; boot fail-closes on missing DATABASE_URL/Binance/Telegram env by design). Pure-core module — no apps/bot runtime wiring changed, so `build` is the boot-relevant gate; paper-engine wiring is module 10.
**Open items → later modules:** module 10 (paper engine) owns C's orchestration — reads `core_bag_<SYMBOL>` settings, gates core-bag>0/no-open-cycle/C-enabled/max-2-concurrent-cycles, calls `checkLevelingSell` on H4 close → `approveLevelingSell` → ExecutionRouter market sell → opens a `LevelingCycle` (SOLD, `forcedRebuyTrigger`), then drives `checkRebuy` on ticks+1H closes → market buy leverQty → writes `chunkPnl` + `reboughtAt` + status/exitReason, and handles the reconcile-EXTERNAL path (bag short → status EXTERNAL, alert, require `SETCORE`); the in-kind-fee dust top-up (buy the shortfall if ≥ stepSize, else log) is an executor concern. Module 11 adds `SETCORE <SYM> <qty>` + the cycle-open/close Telegram notifications; module 14 renders the cycles table + per-symbol chunk history.
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Module 08 — Strategy B (Range Reclaim) — DONE
**Built:** The sideways-market earner that runs when Strategy A is locked (ADX<20 — most of the time). One new PURE plugin `@spotedge/core/src/strategies/strategyB.ts` following the module-07 reference discipline (no DB/exchange/clock; strategies PROPOSE, risk engine disposes, executor performs exit actions). Registered in `defaultRegistry` (A + B; C later) and exported from the core index.
- **Range resolver (`resolveRange`)** — delegates validity + floor/ceiling cluster means + the height≥3×ATR gate to the module-04 `detectRange`, then materializes each boundary as a ZONE from the H4 pivot touches within 0.5×ATR of that mean (floor from `pivotLows`, ceiling from `pivotHighs`). Rejects when either boundary has <2 touches, or when an H4 body close went beyond either boundary SINCE formation (freshness). Midpoint = (floor+ceiling)/2.
- **Entry (`checkEntryB`)** — re-checks `pipeline.eligibleStrategy==="B"`; requires a valid fresh range, an armed floor TOUCH (`detectTouchB`: an H4 low into the floor zone with RSI(14,H4)<35, within the last 12 H4 candles — older touches expire), and a fresh 1H RECLAIM (`reclaimedB`: prev 1H close ≤ floor-zone top < just-closed close — the fresh-cross rule stops the same reclaim re-firing). Proposal: entry=market; `slPrice=min(floorBottom−0.25×ATR(H4), entry−1×ATR(H4))` (two-layer stop, wider wins); `tp1Price`=range midpoint (the R:R the risk engine judges); `tp2Price`=ceiling-zone bottom; `ceilingTop`=ceiling-zone top. Degenerate geometry → null.
- **Exit (`manageExitB`)** — ordered: (1) stop hit (floor break pre-TP1 / breakeven post-TP1) → `SL`; (2) H4-close body beyond ceiling top → `RANGE_TP+breakout`; (3) TP2 at ceiling bottom → `RANGE_TP` (WIN); (4) regime kill-switch ADX(14,H4)≥20 on an H4 close → `REGIME_FLIP` (exit remainder at market — thesis dead, pre-empts the TP1 partial; new B entries are already disabled by the pipeline regime gate); (5) TP1 at midpoint (pre-TP1) → sell 50% + breakeven; (6) time stop 24 H4 candles (pre-TP1) → `TIME_STOP`. `BREAKEVEN_FEE_OFFSET`/`TIME_STOP_H4` shared with A (single source).
- **Data model** — `EntryProposal` gained optional `tp2Price`/`ceilingTop` (risk engine ignores them; they ride to the Position); `ManagedPosition`/`StrategyExitContext` gained the B-specific fields (`tp2Price`, `ceilingTop`, `adx4h`, `candle4h`, `onH4Close`); `ExitReason` extended with `RANGE_TP` | `RANGE_TP+breakout` | `REGIME_FLIP`. Position migration `20260706000002_position_range_targets` adds nullable `tp2Price`/`ceilingTop` (Strategy-A rows migrate cleanly); Trade.exitReason comment updated.
**Files:** `packages/core/src/strategies/strategyB.ts` (new); edited `strategies/types.ts` (ExitReason, ManagedPosition, StrategyExitContext), `strategies/registry.ts` (register B), `risk/engine.ts` (EntryProposal tp2Price/ceilingTop), `index.ts` (export strategyB); `packages/db/prisma/schema.prisma` (Position tp2Price/ceilingTop, Trade.exitReason comment) + migration `20260706000002_position_range_targets/migration.sql`; `packages/core/src/__tests__/strategyB.test.ts` (new, 27 tests).
**Decisions/Deviations:** (a) `resolveRange` derives boundary ZONES inside the plugin from the module-04 `detectRange` levels + H4 pivots (rather than extending the approved module-04 detector, which returns scalar floor/ceiling) — keeps module 04 untouched, mirrors how A composes `srZones`/`pivotLows` locally. (b) Midpoint uses the `detectRange` floor/ceiling means (the authoritative levels). (c) TP2 is a tick target at the ceiling-zone bottom; the H4-body-close breakout is a belt-and-braces gift exit ordered before TP2/regime-flip. (d) Breakeven-offset + 24-H4 time stop reused from A ("same as A", spec §Exit) — imported to keep one source and avoid an ambiguous `export *` re-export. No money/risk/auth deviations.
**Verification:** typecheck ✅ (6/6 turbo tasks) · build ✅ (4/4) · tests **241 passing** (core 195 = +27 strategy-B, bot 32, db 14) · prisma generate ✅ (schema parses with new columns). Acceptance criteria all covered by fixtures: touch→reclaim→TP1→TP2 lifecycle, touch-no-reclaim, RSI-gate rejection (both directions, at `detectTouchB`), touch expiry, ADX-flip forced exit, floor-break SL, ceiling-breakout, height<3×ATR rejection, freshness invalidation, fee-R:R veto on a narrow valid range (proposal → `approveEntry` → `FEE_RR`), registry tier(T3 barred)/enabled gating. No boot smoke needed — core-only module, no apps/bot wiring changed (paper-engine wiring is module 10).
**Open items → later modules:** module 10 (paper engine) materializes `ManagedPosition` from the Position row (populating `tp2Price`/`ceilingTop`), feeds `manageExitB` the H4 surface (`adx4h`/`candle4h`/`onH4Close`) on each confirmed H4 close, and persists the new exit reasons to `Trade.exitReason`; module 11/14 format B's range faces (spec §UI: N/A here).
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Module 07 — Strategy A (Trend-Pullback Continuation) — DONE
**Built:** The primary earner and the reference plugin that B and C will follow. Three new files in `@spotedge/core/src/strategies/`, exported from the core index, keeping the module-05/06 discipline: everything is PURE over injected candle windows + the module-05 verdict — no DB, no exchange, no clock. Strategies PROPOSE (risk engine disposes); the executor + paper engine (module 10) perform the exit actions.
- **`strategies/types.ts` — the plugin contract.** `StrategyPlugin { id:'A'|'B'|'C', allowedTiers, requiredIndicators, checkEntry(ctx)→EntryProposal|null, manageExit(pos,ctx)→ExitAction[] }`. `StrategyEntryContext` = `{ symbol, tier, mode, pipeline: PipelineResult, candles1h, candles4h }` (checkEntry reuses module-06 `EntryProposal` verbatim so a proposal plugs straight into `approveEntry`). `ManagedPosition` = the subset of the Prisma Position row manageExit reasons over (status OPEN/TP1_DONE, avgEntry/sl/tp1/trailStop, isAPlus, h4CandlesOpen). `ExitAction { kind:'TP1_SELL'|'MOVE_STOP'|'EXIT_REMAINDER', fraction?, newStopPrice?, exitReason?:'SL'|'TP1_TRAIL'|'TIME_STOP' }`. `StrategyExitContext { price, candles1h, atr1h, onClose }` — called on ticks (stop/TP1 detection) AND on 1H closes (`onClose=true` enables trailing).
- **`strategies/strategyA.ts` — Strategy A.** `detectTriggerA` on the JUST-CLOSED 1H candle: (1) module-04 `bullishEngulfing` with `volumeRatio ≥ 1.3` (VolumeAvg20), OR (2) `higherLowBreak` whose `breakIndex` IS the last candle (FRESH break only — an old confirmed break must not re-trigger every cycle). Both fire → engulfing precedence (both flags recorded). `checkEntryA`: gated on `pipeline.eligibleStrategy==='A'` (the L1–L5 pass + discount-zone condition already established by module 05) + warm H4 ATR; `entry` = last 1H close; `slPrice = min(last confirmed 1H pivot low, entry − 1×ATR(14,H4))` — the LOWER (wider) of structural stop and the ATR floor, so the stop is NEVER tighter than 1 ATR; `tp1Price = entry + (isAPlus?3:2)×(entry−slPrice)`. `overlapsH4Support` sets `isAPlus` when the entry sits within `0.5×ATR(H4)` of an H4 body-cluster zone at/below entry (geometry naturally includes an S/R-flipped-to-support zone). `manageExitA` ordered precedence: (1) stop hit → EXIT_REMAINDER (`SL` pre-TP1 / `TP1_TRAIL` post-TP1); (2) TP1 hit (pre-TP1 only) → TP1_SELL 50% + MOVE_STOP to breakeven `avgEntry×1.002` (fee recovery); (3) time stop (pre-TP1 only) → EXIT_REMAINDER `TIME_STOP` at `h4CandlesOpen ≥ 24`; (4) trail (post-TP1, on 1H close) → MOVE_STOP to newest confirmed higher-low − `0.25×ATR(1H)`, only when it RAISES the stop (never lowered). Plugin `allowedTiers = [T1,T2,T3]` (L0 gates T3 liquidity centrally).
- **`strategies/registry.ts` — central gate.** `StrategyRegistry` maps `eligibleStrategy` → registered plugin and enforces the two guardrails BEFORE `checkEntry` runs: the ENABLE/DISABLE flag (`isEnabled(id)`) and `allowedTiers` — a disabled or tier-barred strategy's `checkEntry` is never invoked. `defaultRegistry()` wires Strategy A (B/C added in 08/09).
- **DB:** added `Position.trailHistory Json?` (schema + migration `20260706000001_position_trail_history`) so the dashboard (module 14) can draw entry/SL/TP/trail chart markers over each position's lifecycle. Nullable JSONB → clean migration of init rows.
**Files:** `packages/core/src/strategies/types.ts` (new) · `packages/core/src/strategies/strategyA.ts` (new) · `packages/core/src/strategies/registry.ts` (new) · `packages/core/src/index.ts` (export strategies/*) · `packages/db/prisma/schema.prisma` (Position.trailHistory) · `packages/db/prisma/migrations/20260706000001_position_trail_history/migration.sql` (new) · `packages/core/src/__tests__/strategyA.test.ts` (new).
**Decisions/Deviations (no money/risk deviations — stop math + targets kept exactly per spec):** (1) The discount-zone precondition ("trigger INSIDE the discount zone") is satisfied by the module-05 eligibility gate (`eligibleStrategy==='A'` requires L5 IN_ZONE at the close) rather than re-checked in the plugin — this correctly implements the spec edge case "trigger candle IS the breakout leaving the zone: valid (close may exceed it)" since the pattern completed at the eligible close. (2) `isAPlus` is a pure GEOMETRY test (zone at/below entry within 0.5×ATR) rather than using `classifyZone`/`srFlip` kinds — this counts a flipped resistance-turned-support naturally (it sits below price) and avoids classifyZone's edge behaviour when price is inside a zone. (3) manageExit returns ACTIONS for the executor to perform (module 10 wires TP1_SELL/MOVE_STOP/EXIT_REMAINDER to the ExecutionRouter, writes fills/Trade rows, calls `riskEngine.onTradeClosed`, and appends `trailHistory`); the plugin itself is stateless and side-effect-free, consistent with 03/04/05/06 deferring live integration. (4) `h4CandlesOpen` is incremented by the paper engine on each H4 close (position field); the plugin only READS it for the time-stop threshold. (5) Trail "NEW confirmed higher-low" is enforced by requiring ≥2 pivot lows with `last > prev` AND `candidate > currentStop` — a lower/equal pivot yields a lower candidate and is naturally rejected by the raise-only guard.
**Verification:** typecheck ✅ (all 4 workspaces; `prisma generate` regenerates the Position type with `trailHistory`) · build ✅ (core+bot tsc, dashboard next build) · tests **214** passing — core **168 (+21 strategy-A)** + bot 32 + db 14. Strategy-A coverage: `detectTriggerA` engulfing (vol≥1.3 pass / vol-fail reject) + higher-low-break (fresh breakout = last candle); `checkEntryA` full proposal (structural swing-low stop, SL ≥ 1×ATR(H4) invariant, TP1 = 2×/3× risk relationship), outside-zone rejection (eligibleStrategy null/B), no-trigger rejection; `overlapsH4Support` A+ true/false; full exit lifecycle — TP1→50%+breakeven, SL→LOSS, post-TP1 stop→TP1_TRAIL, time-stop at 24 (not 23), trail raises on a new higher-low / holds when candidate ≤ current stop / never on a tick; registry — DISABLE A ⇒ checkEntry never called (spy), T3 tier barred ⇒ never called, eligible+enabled+permitted runs, no-eligible ⇒ null. Live `migrate deploy` of the trailHistory column deferred to server deploy (no Postgres in build env); paper smoke is the module-10 retrofit per spec. This pure-logic module adds only new exports (no runtime-wiring change), so `build` success is the boot-relevant gate.
**Open items → later modules:** module 08/09 register Strategy B/C on the same registry (`eligibleStrategy==='B'` path already routed; C leveling flows through `approveLevelingSell`); module 10 constructs `defaultRegistry()`, drives `checkEntry` per warm+eligible pair on 1H close → `approveEntry` → ExecutionRouter, and owns `manageExit` execution (TP1_SELL/MOVE_STOP/EXIT_REMAINDER → orders + Trade rows + `onTradeClosed`), the `h4CandlesOpen` increment, and `trailHistory` appends; module 11 formats the entry + closed-trade Telegram notifications; module 14 consumes `Position.trailHistory` for chart markers; module 12 backtest replays checkEntry/manageExit over historical fixtures.
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Module 06 — Risk Engine — DONE
**Built:** Position sizing and every money guardrail — strategies PROPOSE, the risk engine DISPOSES; no order reaches the ExecutionRouter without passing `approveEntry()` (hard-checkpoint module — a wrong call here loses real money). Two new files in `@spotedge/core/src/risk/`, exported from the core index, mirroring the module-05 split: a PURE, exhaustively-testable layer + a stateful orchestrator over injected seams (core never imports @spotedge/db or apps/bot).
- **`risk/sizing.ts` (PURE money math, all via decimal.js — JS floats never touch a price/qty):** the PDF formula kept verbatim — `riskUsd = capital × risk_pct/100` · `slPct = (entry−sl)/entry` · `sizeUsd = riskUsd/slPct` · `sizeUsd = min(sizeUsd, capital × tierCap)` · `qty = roundQty(sizeUsd/entry)`. Tier caps T1 15% · T2 10% · T3 5% (`TIER_CAP`). `qty` is FLOORED to the LOT_SIZE stepSize so effective risk can only round DOWN — never exceed intent (the property test relies on this); both intended `riskUsd` and `effectiveRiskUsd` are reported, and `tierCapBound` flags when concentration limited the size. `clampRiskPct` enforces the hard [0.25, 2.0] band and falls back to the 1% default on ANY non-finite garbage (NaN/±Infinity → 1%, deliberately NOT the 2% ceiling). `computeNetRR` folds round-trip taker fees (0.075% BNB-active / 0.1% std) into `netRR = (reward−fees)/(risk+fees)`; qty cancels so it's size-independent. `validateRiskPct`/`validateCapital` are the server-side SETRISK/SETCAPITAL guards (REJECT out-of-band, don't silently clamp) + `sizingExample` echoes "1 trade now risks $X".
- **`risk/engine.ts` — pure `approveEntry(proposal, snapshot)` ordered veto chain (spec checks 1–7 + allocated-capital sanity), first failure short-circuits with a named `VetoReason`:** (1) STATE (PAUSED/STOPPED) → BREAKER flag → DATA (no cached filters); (2) sizing + `TOO_SMALL` (invalid geometry / sub-minNotional) + `INSUFFICIENT_ALLOCATION` (sizeUsd > un-deployed allocation `capital−Σopen`, and in LIVE > free USDT — protects manual-trading funds, PAPER passes Infinity); (3) `SPLIT_UNVIABLE` (TP1 halves clear minNotional via module-03 `validateTp1Split`); (4) `FEE_RR` (netRR < 1.5); (5) `MAX_POSITIONS` (≥3 open A/B); (6) `BREAKER` (realized-today ≤ −3% of capital, inclusive); (7) `PAIR_REMOVING` + `SYMBOL_OWNED` (one owner per symbol, AB vs C). Approval returns rounded qty/sizeUsd, tick-floored sl/tp1, riskUsd + effectiveRiskUsd, tierCapBound, netRR. `approveLevelingSell` (Strategy C): ≤30% of core (`LEVER_MAX_FRACTION`), blocked while A/B owns the symbol, breaker/state/minNotional gated — C REDUCES exposure so it ignores the correlation cap.
- **`RiskEngine` orchestrator** over injected `RiskSettingsReader`/`RiskLedgerReader`/`EquitySnapshotSink`/`AlertFn`/`Clock` seams (Prisma-backed in apps/bot at module 10): `capital(mode)` auto-compounds base allocation + Σ realized net; builds the pure snapshot; owns the circuit-breaker lifecycle (trip → persist tripped-date per mode → alert ONCE → auto-reset when the stored date ≠ today's UTC day, `onDailyRollover` clears the date + alert latch); the consecutive-loss streak counter (REPORTING ONLY — never auto-tightens sizing mid-drawdown per spec); the hourly equity snapshot; and `checkLedgerDrift` (compounding vs independent ledger sum, alert `LEDGER_DRIFT` if Δ > $1). `riskStatus(mode)` feeds the RISK command + dashboard.
- **Settings:** added `bnb_fees_active` (default `true`) to `DEFAULT_SETTINGS` so sizing picks the 0.075% discounted taker rate by default.
**Files:** `packages/core/src/risk/sizing.ts` (new) · `packages/core/src/risk/engine.ts` (new) · `packages/core/src/index.ts` (export risk/*) · `packages/db/src/settings.ts` (bnb_fees_active default) · `packages/core/src/__tests__/risk.sizing.test.ts` (new) · `packages/core/src/__tests__/risk.engine.test.ts` (new).
**Decisions/Deviations (no unapproved money/risk deviations — formula + bounds kept exactly per spec):** (1) Non-finite `risk_pct` clamps to the 1% DEFAULT, not the 2% max — a garbage setting must never silently maximize risk (fixed a pre-existing test that expected +Infinity→2%). (2) FEE_RR boundary test hand-values corrected: at entry 100/SL 90 (risk 10) with 0.1% round-trip fees, netRR crosses 1.5 at TP1 ≈ 115.54, so the straddle uses 115.4 (netRR 1.486 → veto) vs 115.7 (1.516 → approve); the prior 116/116.5 values both sat above the floor and wrongly expected a veto. (3) Fixed a garbled line in the riskStatus test (`await engine.__proto__ ? …` referencing an undefined `engine`) → `await new RiskEngine(deps).riskStatus(...)`. (4) LIVE circuit-breaker date uses key `circuit_breaker_tripped_date_live`; a missing key reads as null→not-tripped (getString fallback), so no seed default is required. (5) The `checkLedgerDrift` "independent" sum is an injected parameter — the seam makes it trivial to strengthen with a genuinely independent ledger query in production; module 10 supplies it + the hourly cron. (6) Engine NOT yet wired into a running loop (no per-pair driver / Prisma seams yet) — module 10 constructs the RiskEngine with real ledger/settings/equity adapters and calls approveEntry between the strategies (07–09) and the ExecutionRouter, consistent with 03/04/05 deferring live integration.
**Verification:** typecheck ✅ (all 4 workspaces) · build ✅ (core+bot tsc, dashboard next build) · tests **193** passing — core **147 (+36 risk)** + bot 32 + db 14. Risk coverage: sizing (PDF $1,000/5%SL→$200/$10 hand-calc, uncapped effective==intended, tier-cap-binds T1/T3, clamp band + non-finite fallback, computeNetRR qty-independence + 1.5 straddle + BNB-vs-std, SETRISK/SETCAPITAL guards, `sizingExample`, 500-iter deterministic-LCG property test proving effectiveRiskUsd ≤ riskUsd); engine (happy path rounded qty/netRR, ALL 7 vetoes at boundary values — exactly 3 positions, exactly −3.0%/−329.99, netRR straddle, order-precedence STATE-first, `breakerBreached` inclusivity, `approveLevelingSell` 30%/owner/breaker, RiskEngine breaker trip→alert-once→block→UTC-rollover-reset→re-trip-alerts-again, reporting-only streak advance/reset with sizing unchanged, capital compounding, riskStatus C-excluded deployed/openCount + clamped risk). Boot is env-gated (guard fail-fasts on missing DATABASE_URL/API keys/Telegram config — expected; no .env in build env) and this pure-logic module doesn't alter runtime wiring, so `build` success (dist links the new exports) is the boot-relevant gate; live wiring verified at module 10.
**Open items → later modules:** module 10 wires the RiskEngine with Prisma-backed ledger/settings/equity seams, the hourly equity-snapshot + LEDGER_DRIFT cron, and the 00:00-UTC `onDailyRollover` call, and inserts `approveEntry`/`approveLevelingSell` between strategies (07–09) and the ExecutionRouter (module 03); modules 07–09 build the EntryProposal/LevelingSellProposal producers; module 11 surfaces `riskStatus` (RISK cmd) + the SETRISK/SETCAPITAL guarded handlers with SettingAudit rows + confirmation; module 14 renders equity/risk on the dashboard; module 15 LIVE pre-flight exercises the free-USDT allocation sanity against a real balance.
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Module 05 — Filter Pipeline — DONE
**Built:** The ordered veto chain every pair passes at each 1H close before any strategy may look for an entry (mandate: seek excuses NOT to trade). One new file `packages/core/src/filters/pipeline.ts`, exported from the core index. Pure evaluation over injected candle windows + context (core never imports the DB / apps-bot); the thin `runFilterCycle` orchestrator persists via the injected `FilterStateSink`.
- **Result shape** `PipelineResult { symbol, eligibleStrategy: 'A'|'B'|null, blocking: layerKey|null, regime, layers: {L0..L5 verdicts}, at }`. Every evaluated layer records `{name, pass, detail, data}` — ONE fully-serializable JSON drives both the Telegram `FILTERS <pair>` text and the dashboard grid. `LAYER_NAMES` map ships beside the keys so renderers need no lookup table. First hard-veto layer short-circuits and is named in `blocking`.
- **L0 system gates** (`evaluateSystemGates`) — state != STOPPED/PAUSED · pair active & !removing · `isWarm` · module-06 circuit-breaker flag (injected, default false) · Tier-3-only 24h volume ≥ $20M (`T3_MIN_VOLUME_USD`; unknown volume → veto). Checked first; short-circuits with no market layers evaluated.
- **L1 BTC market** (`computeBtcMarket`, computed ONCE per cycle) — BTC close > EMA50(H4) AND `emaRising(EMA50 H4, 6)`; fail cascades a veto onto every altcoin. Skipped when the pair IS BTCUSDT (L2–L5 still apply).
- **L2 coin trend bias** — close > EMA50(H4) AND EMA50 > EMA200(H4); classification only (does NOT short-circuit) — records LONG bias; A locked without it.
- **L3 regime** (`regimeFor`) — ADX14(H4) ≥ 20 → TRENDING → Strategy A path (needs L2 bias), B locked; < 20 → RANGING → Strategy B eligible IFF `detectRange` (module 04) returns a valid range, A locked. Records `{adx, regime, eligibleStrategy, range}`.
- **L4 S/R validation** (`evaluateL4SR`, Strategy A path only) — nearest overhead resistance-zone bottom must be ≥ 2× the provisional stop distance above price, else `1:2 geometrically impossible` (veto); no overhead zone → `clear sky`. Provisional stop = last confirmed 1H swing low floored at 1×ATR(14,1H) (`provisionalStopDistance` — mirrors module 07 closely enough for the geometry; module 07 owns the final stop).
- **L5 discount retracement** (`evaluateL5Discount`, Strategy A path only) — price inside the 38.2–61.8% fib band of the last H4 swing OR touching the EMA20–EMA50 band (±0.25×ATR) → `IN_ZONE` (eligible); above → `WAITING_RETRACE` (withheld); below the 61.8/EMA50 floor beyond 0.5×ATR → `OVERSHOT` (veto). A shallow dip within the 0.5×ATR tolerance stays IN_ZONE.
- **`runFilterCycle`** — computes the shared BTC verdict once, iterates pairs, persists each `FilterState`; a per-pair exception (indicator null, malformed window) becomes a `blocking:"DATA"` veto (logged shape-wise into the result) and is NEVER thrown upward; a throwing sink is swallowed so one bad pair/DB blip can't stall the cycle.
**Files:** `packages/core/src/filters/pipeline.ts` (new); `packages/core/src/index.ts` (export filters/pipeline); `packages/core/src/__tests__/filters.pipeline.test.ts` (new, 30 tests).
**Decisions/Deviations (no money/risk/auth deviations):** (1) Spec cites the provisional stop as "computed as in module 07" but module 07 isn't built yet → implemented a local `provisionalStopDistance` (last 1H swing low, 1×ATR floor) sufficient for L4's ratio; module 07 will own the definitive stop. (2) Layer helpers exported individually (`evaluateSystemGates`/`computeBtcMarket`/`regimeFor`/`evaluateL4SR`/`evaluateL5Discount`/`provisionalStopDistance`) so the acceptance cases (ADX 19.9-vs-20.0 boundary, 2×-stop geometry, discount WAITING/IN_ZONE/OVERSHOT) are unit-tested on controlled inputs rather than fighting 250-candle EMA math; `evaluatePipeline` composes the same functions. (3) `blocking` reflects the APPLICABLE reason a pair isn't trading given its regime: an L2-bias miss names `L2` only on the TRENDING (A) path; a RANGING pair with no valid range names `L3`; a BTCUSDT self-eval never names `L1`. (4) L5 "below the band but within 0.5×ATR" (unnamed in the spec) treated as IN_ZONE-with-tolerance (pass), reserving OVERSHOT for genuine structural breakdown. (5) Pipeline NOT yet wired into a running loop — the bot has no per-pair 1H-close driver yet (module 10 paper engine adds it); CandleStore currently persists the raw snapshot as a placeholder. This module ships the pure, fully-tested pipeline + orchestrator seam, consistent with modules 03/04 deferring live integration.
**Verification:** typecheck ✅ (all 4 workspaces) · build ✅ (core+bot tsc, dashboard next build) · tests **157** passing — core 111 (+30 pipeline: L0 all-branch gates incl. T3-volume-only-Tier-3; L1 pass/fail-detail-form/data-veto; ADX boundary 19.9→RANGING vs 20.0→TRENDING; L4 clear-sky + 2×-stop pass/veto; L5 IN_ZONE/WAITING/OVERSHOT/tolerance/fib-band/data-veto; full pipeline L0 short-circuit-names-blocking, BTC-veto cascade to altcoins, BTCUSDT skips L1, DATA veto on short H4, TRENDING→WAITING(L5), **Strategy A ELIGIBLE** on EMA-band pullback, RANGING→Strategy-B on valid range; `runFilterCycle` persist-one-per-pair + throwing-sink resilience + per-pair-exception→DATA) + bot 32 + db 14. UI self-check N/A (renderers are modules 11/14). Live 15-pair 1H-close smoke against Binance deferred to server deploy (no running pipeline driver in build env — module 10); the persist-all cycle test is the shape-level stand-in.
**Open items → later modules:** module 06 supplies the real `circuitBreakerTripped` flag + the daily −3% breaker; module 07 owns the definitive provisional/structural stop that L4 approximates; modules 07/08 consume `PipelineResult.eligibleStrategy` (A/B) as their entry gate, Strategy C (module 09) runs OUTSIDE this pipeline except L0; module 10 wires the per-pair 1H-close driver (iterate warm pairs → `runFilterCycle` with CandleStore windows + a real `FilterStateSink` on the `FilterState` table, replacing CandleStore's placeholder snapshot persist) + supplies the 24h-volume cache (≤1h) for the Tier-3 gate; modules 11/14 render the FilterState JSON (`FILTERS <pair>` text + dashboard pipeline grid).
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Module 04 — Indicator Engine — DONE
**Built:** All the math the filters/strategies (modules 05–09) consume, computed in-house from Binance klines — no TA-library black box, every value unit-tested against hand-verified fixtures. Lives in `@spotedge/core/src/indicators/`, exported from the core index. Four new files:
- `math.ts` — pure indicator functions returning series ALIGNED to the input candle index, `null` inside each indicator's warm-up (never a silent 0): `sma`, `ema` (SMA-seeded, k=2/(p+1)), `rsi` (Wilder), `trueRange`/`atr` (Wilder, TR with gaps), `adx` (Wilder +DM/−DM/TR smoothing → +DI/−DI/DX → Wilder-averaged ADX), `volumeAvg(20)`+`volumeRatio`, `emaRising(lookback=6)`, plus `lastValue` helper.
- `structure.ts` — no-repaint pivots (`isPivotHigh/Low`, `pivots` — a pivot only emitted once `right` candles have closed; window-edge returns null), body-based S/R zones (`srZones` clustering pivot bodies within 0.5×ATR, `classifyZone`, `srFlip` resistance→support on a body close above), swings + `fibZone` (38.2–61.8% band of the last low→high swing), `detectRange` (≥2-pivot floor/ceiling clusters, valid only if width ≥ 3×ATR, `rangeInvalidated` on body close beyond a boundary), `bullishEngulfing` (+strong-close variant), `higherLowBreak` (higher pivot low → body close above intervening minor high), `bearishRejection` (red H4 body in a resistance zone with ≥50% upper wick).
- `types.ts` — `Series`, `AdxPoint`, `Pivot`/`SRZone`/`Swing`/`FibZone`/`RangeBox`/`EngulfCheck`/`HigherLowBreak`, `TfIndicators`/`IndicatorSnapshot`, and the injected seams `KlineSource` (satisfied directly by `BinanceConnector.getKlines`) + `FilterStateSink`.
- `CandleStore.ts` — per symbol×TF (1h/4h/1d/1w) rolling window of 600 closed candles; `warmUp()` fetches 500 klines per maintained TF (each TF fetched independently so a testnet-absent 1w never fails the pair); `isWarm()` gates signal-eligibility on 1h+4h ≥500; `onClose(symbol, tf)` refetches recent closed candles, appends (dedupe by openTime), detects timestamp gaps in the tail → alert + full-window refetch (never compute across a silent hole); `snapshot()` computes the full `IndicatorSnapshot` and `persist()` upserts it to the `FilterState` sink; `drop()` for REMOVEPAIR.
**Decisions / interpretations (logged per AGENT.md rule 2):**
- **Warm-up gate on 1h+4h only.** Spec says "500 klines per required TF"; strategies trade on 1H/H4 (EMA200-on-H4 alone needs 200+ candles), while a young listing can legitimately hold <500 weekly bars. All four TFs are warmed, but `isWarm` gates on the two trading TFs so 1d/1w context never permanently blocks a valid pair. Configurable via `gateTimeframes`.
- **ATR/ADX Wilder conventions fixed and documented:** ATR seed = SMA of the first `period` TRs (TR[0]=H−L) at index period−1; ADX first value at index 2·period−1 (mean of the first `period` DX). Fixtures hand-verified against these exact conventions (perfect trend → +DI/−DI/ADX = 100/0/100 exactly; RSI crafted series = 65.0 exactly).
- **Snapshot is the FilterState payload.** "Upsert FilterState inputs" implemented as persisting the computed `IndicatorSnapshot`; module 05 defines how the pipeline reads it. Seam kept minimal (`FilterStateSink.upsert`).
- **onClose is TF-generic.** Spec names close_1h/close_4h for appends; implemented generically so the bot scheduler wiring (which event fires which TF, incl. 1d/1w on daily_rollover) is a module-05+ concern. No bot-side wiring added this module (mirrors module 03's deferral).
- `noUncheckedIndexedAccess` is on project-wide; tight numeric loops use non-null assertions at provably-bounded `[i]` sites (documented in math.ts header) — no established repo idiom existed (first numeric-loop code in the codebase).
**Verification:** `pnpm typecheck` + `pnpm build` green across the workspace. `pnpm test` = 127 tests (core 81 incl. 39 new: math 15, structure 18, CandleStore 6; bot 32; db 14). Acceptance criteria met: EMA/RSI/ATR/ADX vs hand-verified fixtures (1e-6 rel); pivot no-repaint, zone clustering, S/R flip, invalid range (<3×ATR), engulfing true/false, rejection true/false all covered on constructed fixtures; warm-up gate proven (pair not eligible until 500 klines); 15-pair snapshot smoke produces numeric EMA/RSI/ATR/ADX/volumeRatio with no errors. Testnet/live smoke on the real exchange deferred to server deploy (as with module 03).
**Files:** `packages/core/src/indicators/{types,math,structure,CandleStore}.ts`, tests `packages/core/src/__tests__/{indicators.math,indicators.structure,CandleStore}.test.ts`, export line added to `packages/core/src/index.ts`.
**Open items → later modules:** module 05 (filter pipeline) consumes `isWarm`/`snapshot` and defines the `FilterState` read side; the bot must wire the scheduler's close_1h/close_4h (and daily_rollover for 1d/1w) to `CandleStore.onClose`, call `warmUp` on boot/ADDPAIR, and inject the Prisma-backed `FilterStateSink` + the connector as `KlineSource`.
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Module 03 — Exchange Connector (Binance Spot) — DONE
**Built:** The single code path that talks to Binance — `BinanceConnector` (ccxt REST + `ws` streams) living in `@spotedge/core`, environment-agnostic and fully seam-injected so `packages/core` never imports `apps/bot`. Everything above this layer is exchange-agnostic and depends only on `exchange/types.ts`.
- `packages/core/src/exchange/types.ts` — the exchange contract: `Timeframe`+`TF_MS`, `Candle`, `Ticker`, `ExchangeFilters` (all decimal STRINGS), `FillReport`/`FillLeg`, `Balance`, `OpenOrder`, `OrderPurpose`, `ReconcileAction`/`ReconcileFinding`, and the injected seams `ExchangeLogger`, `AlertFn`, `Clock`(+`systemClock`), `SettingsReader`, `PairRepository`, `OpenPositionView`. Market-data OHLCV are `number` (indicator math, not ledger money); everything that touches the ledger is a decimal string.
- `packages/core/src/exchange/filters.ts` — PURE exchange-filter compliance (the classic bot killer): `roundQty`/`roundPrice` (floor to stepSize/tickSize at exact step precision, decimal-safe), `meetsMinNotional` (minQty AND qty×price≥minNotional), `validateTp1Split` (BOTH halves of a 50% TP1 split must clear minNotional at entry — the veto the risk engine/module 06 calls), `stopLimitPrice` (limit = trigger×0.995 buffer), `isTradable`.
- `packages/core/src/exchange/clientOrderId.ts` — idempotency keys `se-{refId}-{purpose}-{nonce}`; `makeClientOrderId` (throws rather than truncate past Binance's 36-char cap), `parseClientOrderId`/`isOwnClientOrderId` (foreign/manual orders → null → reconcile ignores), `nextNonce` (ms+counter base36).
- `packages/core/src/exchange/backoff.ts` — WS reconnect schedule: `backoffDelay` (deterministic 1s→60s cap, overflow-safe) + `Backoff` class (next/reset, optional injectable full-jitter).
- `packages/core/src/exchange/fees.ts` — fee→USD so every `Fill.feeUsd` is accurate: `feeToUsd` (stablecoins 1:1, else `UsdPriceResolver`; unknown price → undefined so the caller alerts rather than under-count), `aggregateFeeUsd` (per-asset totals + summed USD + `unpriced` list), `STABLE_USD_ASSETS`.
- `packages/core/src/exchange/reconcile.ts` — pure `classifyReconcile(qtyRemaining, freeBalance, stopAlive, dustEps)` → `EXTERNAL_CLOSE` (balance gone) | `REPLACE_STOP` (stop missing) | `OK`, with a minQty dust epsilon to avoid false external-closes.
- `packages/core/src/exchange/ExecutionRouter.ts` — the PAPER/LIVE order seam: `ExecutionRouter` interface + `OrderContext`, and `UnwiredPaperRouter` that FAILS CLOSED (throws "wire module 10") so nothing silently trades into a void before the paper engine exists.
- `packages/core/src/exchange/BinanceConnector.ts` — the class: `init()` (loadMarkets → cache filters → clock check → arm hourly-drift + daily-refresh timers, all `unref`'d), `refreshFilters()` (persists `Pair.exchangeFilters`, detects status≠TRADING → mark `removing` + alert, skips testnet-absent pairs), `getKlines()` (CLOSED candles only — drops the forming bar by closeTime>now), `get24hVolumeUsd()`, `getBalances()`, order methods routed through the ExecutionRouter (`marketBuy`/`marketSell`/`placeStopLimit`/`cancelOrder`/`getOpenOrders`), a private `LiveExecutionRouter`+`live*` executors (idempotency query-before-place via `findByClientOrderId`; market buy by quote cost via `createMarketBuyOrderWithCost`; STOP_LOSS_LIMIT with 0.5% buffer; `settleOrder` re-queries + alerts on non-FILLED, spec retry-once), `toFillReport` (weighted-avg price + USD fees from real `info.fills`), WS ticker stream (miniTicker combined stream, exponential-backoff auto-reconnect, resubscribe, REST 10s poll fallback while down, >60s data-gap alert), user-data stream (listenKey + 30-min keepalive), `checkClockDrift()` (>1s → alert with chrony/timesyncd hint, recvWindow 10000), `checkBnbReserve()` (alert-only below `settings.bnb_fee_reserve_usd`; never auto-buys BNB), `reconcile(positions, handler)` (LIVE-only; delegates ledger effects to an injected `ReconcileHandler`, downgrades failed stop re-place to WATCHDOG_ONLY), plus the exposed rounding helpers + `validateSymbol()` (ADDPAIR). Constructed cheaply (no network); `init()` does first I/O. Test seam: inject a pre-built ccxt exchange.
- `packages/core/src/utils/money.ts` — decimal-safe primitives for core (core can't import apps/bot's money util): `dec/add/sub/mul/div/gte/lte/gt/lt`, `roundDownToStep`, `stepDecimals`, `toFixed/toStr/toNumber`. This is the module that introduces the exchange-precision rounding AGENT.md rule 7 refers to.
- `packages/core/src/index.ts` — re-exports the whole `exchange/*` surface + `money` namespace for downstream modules.
- `apps/bot/src/exchange.ts` — production wiring: adapts pino (`categoryLogger("exchange")`) to `ExchangeLogger`, a log-only `AlertFn` that routes through `logEvent` (→ events_log; module 11 fans out to Telegram), a Prisma-backed `PairRepository` (`listPairs`/`saveFilters`/`markRemoving`), a DB `SettingsReader` (`getSetting`+`parseNum`/`parseBool`), and `createExchangeConnector(cfg)` which CONSTRUCTS (network-free) but does not `init()`.
- `apps/bot/src/index.ts` — boot step 3 now constructs the connector (replacing the module-01 clock-drift stub) and logs "init deferred to trading start"; `exchange.close()` added to the graceful-shutdown hooks (tears down WS + timers, leaves exchange stop orders in place).
**Files:** `packages/core/{package.json, src/index.ts, src/utils/money.ts, src/exchange/{types,filters,fees,backoff,clientOrderId,reconcile,ExecutionRouter,BinanceConnector}.ts, src/__tests__/{filters,clientOrderId,backoff,fees,reconcile,BinanceConnector}.test.ts}`; `apps/bot/src/{exchange.ts, index.ts}`; `pnpm-lock.yaml` (adds ccxt ^4.4.0, ws ^8.18.0, decimal.js, @spotedge/db to core); `PROGRESS.md`, `PROGRESS-HISTORY.md`.
**Decisions/Deviations:** (1) Spec header says end with `[FIXED_CHECKPOINT]` (module 03 is a hard checkpoint), but continuous-development mode is enabled by the controller → ended with `[CHECKPOINT]` per controller directive; no money/risk/auth behavior deviation. (2) Connector is DECOUPLED via injected seams (logger/alert/pairs/settings/clock/paperRouter) instead of importing app infra — required because `packages/core` must not depend on `apps/bot`; matches the existing StateStore/EventSink seam philosophy. (3) `init()`/`reconcile()` are DEFERRED (not called at boot): calling them would force network + a live DB at boot and break the no-DB-required shell from modules 01/02. Construction is network-free; init/reconcile are called by the paper/live engines (modules 10/15). Boot remains deterministic + DB-free. (4) A self-contained `utils/money.ts` was added to core (can't import apps/bot's) — same decimal.js contract, deliberate duplication; core's `roundQty`/`roundPrice` are the single sanctioned exchange-rounding path AGENT.md rule 7 anticipates. (5) PAPER order paths default to `UnwiredPaperRouter` (fail-closed) until module 10 injects the real paper engine — prevents phantom fills. (6) Ledger EFFECTS of reconcile (write EXTERNAL Trade, update Position) are delegated to an injected `ReconcileHandler` so all P&L writes stay in `@spotedge/db`; the connector owns only the decision + alert. No money/risk/auth deviations requiring [HUMAN_REQUIRED].
**Verification:** typecheck ✅ (turbo, all workspaces) · build ✅ (4/4) · tests **88** passing — core **42** (filters: roundQty/roundPrice vs real SOL 0.001/BTC 0.00001 stepSizes + 0.01 tick, float-drift safety, minNotional, TP1-split veto/pass incl. odd-qty runner remainder, stopLimit 0.5%/custom buffer; clientOrderId: build/parse round-trip, determinism, 36-char throw, foreign-id null, nonce uniqueness; backoff: 1s→60s cap+overflow, jitter; fees: stablecoin 1:1, BNB conversion, unknown→undefined, aggregate per-asset+unpriced; reconcile: OK/EXTERNAL_CLOSE/REPLACE_STOP + dust epsilon; BinanceConnector w/ injected fake ccxt: closed-candle drop, quoteVolume+fallback, market buy weighted-avg + USD fees + `se-42-entry-…` clientOrderId, reconcile EXTERNAL_CLOSE fires handler+alert / OK when balance+stop present / PAPER skipped, clock-drift +5s alert) + bot 32 + db 14 · boot in PAPER ✅ (connector constructed, secrets masked, SIGINT → `shutdown_complete` → exit 0, 0 errors; still boots WITHOUT a live DB/exchange). UI self-check N/A. **Integration on testnet DEFERRED to server deploy** (no Binance testnet reachable from the build env): fetch klines 15 pairs, WS open + kill/reconnect, place+cancel stop-limit, tiny market buy/sell with recorded fees, boot-with-fake-DB-position reconcile — these run on the operator's server per the spec's integration acceptance criteria.
**Open items → later modules:** module 04 consumes `getKlines()` (enforces the ≥500-kline warm-up before signals); module 06 calls `validateTp1Split()` to veto undersized entries and uses `roundQty`/`meetsMinNotional`; module 10 injects a `PaperExecutionRouter` (replaces `UnwiredPaperRouter`), calls `init()`/`subscribeTicker()`/`subscribeUserData()`, wires the `ReconcileHandler` (writes EXTERNAL Trade via `@spotedge/db` pnl) + reconcile-on-boot, and implements the dust rule handed over from module 02; module 11 replaces the log-only `AlertFn` with Telegram (clock-drift, watchdog-stop, data-gap, reconcile, low-BNB alerts) and turns `checkBnbReserve()` into a daily job; module 15 opens the LIVE gate so `mode==="LIVE"` routes through the live executor + arms the two-layer stop watchdog. `bnb_fee_pay_enabled` is a new operator-optional setting read with default false (no seed needed). ccxt listen-key implicit endpoints (`publicPost/Put/DeleteUserDataStream`) are typed loosely + guarded — verified on testnet at deploy.
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Module 02 — Database (Prisma + PostgreSQL) — DONE
**Built:** Full data model and money-critical ledger for the bot.
- `packages/db/prisma/schema.prisma` — 7 enums (`Mode`, `Tier`, `Strategy`, `PositionStatus`, `TradeResult`, `CycleStatus`, `Side`) and 10 models exactly per spec: `Setting`, `SettingAudit`, `Pair`, `Position`, `Fill`, `Trade`, `LevelingCycle`, `EquitySnapshot`, `EventLog`, `FilterState`. ALL monetary/quantity columns are `Decimal` (never Float). Every money row carries `mode`. Relations: `Fill → Position?`, `Fill → LevelingCycle?`, `Trade 1:1 Position` (`positionId @unique`). Added query indexes for the hot mode-scoped reads (`[mode,status]`, `[mode,symbol]`, `[mode,closedAt]`, `[category,ts]`, etc.).
- `packages/db/prisma/migrations/20260706000000_init/migration.sql` + `migration_lock.toml` (provider=postgresql). Generated deterministically via `prisma migrate diff --from-empty --to-schema-datamodel` (no live DB in the build env); 10 tables / 7 enum types / 16 indexes / 3 FKs. `prisma validate` ✅.
- `packages/db/src/index.ts` — process-wide `PrismaClient` singleton (`getPrisma`/`prisma`/`disconnectPrisma`), re-exports the generated enums + model types so downstream modules import DB types from `@spotedge/db` only.
- `packages/db/src/pnl.ts` — pure Decimal P&L math (the ledger's source of truth): `costBasis`, `proceeds`, `totalFees`, `grossPnl`, `realizedNetPnl` (= Σ sell qv − Σ buy qv − Σ feeUsd), `netPnlPct` (on cost basis), `classifyResult` (WIN/LOSS/BREAKEVEN/FORCED_REBUY_LOSS), `holdHours`, `computeTrade` (assembles the `Trade` row), `positionUnrealized` (spot long mark-to-market), `accountEquity` (allocated + realized + unrealized), and mode-isolation helpers `scopeToMode`/`realizedNetPnlForMode`.
- `packages/db/src/settings.ts` — typed `Setting` access; `setSetting` upserts + journals `SettingAudit` (old→new, source) in ONE transaction; `DEFAULT_SETTINGS` (mode, capital_paper/live, risk_pct, summary_time_pkt, strategy_A/B/C_enabled, circuit_breaker_tripped_date, live_gate_approved, bnb_fee_reserve_usd); `parseBool`/`parseNum` helpers.
- `packages/db/src/events.ts` — `makeEventSink(prisma)` returns the function the bot's logger `attachEventSink()` seam consumes to persist `EventLog` rows (never throws into the engine; folds `type` into `data` JSON). Defined `EventRecordLike` locally to avoid a bot→db import cycle.
- `packages/db/prisma/seed.ts` — idempotent seed: settings create-only (never clobbers an operator-edited value), 15 default pairs upserted by symbol with tiers (T1 BTC/ETH/BNB/SOL · T2 DOT/LINK/AVAX/NEAR/TON/SUI/FIL/ATOM · T3 XLM/DASH/ALGO; GRAM+CVX excluded). Wired via `package.json#prisma.seed = tsx prisma/seed.ts`.
- `scripts/backup-db.sh` — nightly `pg_dump | gzip -9` to `${BACKUP_DIR:-~/spotedge-backups}/spotedge-YYYYMMDD.sql.gz` (dir OUTSIDE repo), `--no-owner --no-privileges` for scratch-DB restore, `.partial`→atomic rename, 7-day retention prune, non-zero exit on failure.
- `apps/bot/src/backup.ts` (`BackupJob`) — in-bot scheduler fires the script at **03:30 UTC** (reuses scheduler `nextPeriodic`/`DAY_MS`; injectable clock/runner for tests); `runOnce()` never throws — logs `backup_ok`/`backup_failed` (Telegram alert deferred to module 11). Wired into `apps/bot/src/index.ts` boot (step 5b) + graceful-shutdown hooks (`backup.stop()`).
- `deploy.sh` — activated the module-01 Prisma placeholders: `prisma generate` → `migrate deploy` → `seed` (via `pnpm --filter @spotedge/db exec/run`), AFTER install and BEFORE typecheck/build, per AGENT.md rule (b). CI == the SSH deploy workflows that invoke `deploy.sh`, so they inherit the same steps (no separate build/test CI workflow exists in this repo).
- `packages/db/package.json` — added `@prisma/client`+`decimal.js` deps, `prisma`+`tsx`+`vitest` dev-deps, and `generate`/`migrate:*`/`seed`/`test` scripts; `build`/`typecheck` now run `prisma generate` first so the client exists before tsc.
**Files:** `packages/db/{package.json, prisma/schema.prisma, prisma/seed.ts, prisma/migrations/migration_lock.toml, prisma/migrations/20260706000000_init/migration.sql, src/{index,pnl,settings,events}.ts, src/__tests__/pnl.test.ts}`; `apps/bot/src/{backup.ts, index.ts}`, `apps/bot/src/__tests__/backup.test.ts`; `scripts/backup-db.sh`; `deploy.sh`; `pnpm-lock.yaml`; `PROGRESS.md`, `PROGRESS-HISTORY.md`.
**Decisions/Deviations:** (1) Spec header says end with `[FIXED_CHECKPOINT]`, but continuous-development mode is enabled by the controller → ended with `[CHECKPOINT]` per controller directive (data-model change, but no owner hard-stop requested). (2) No live Postgres in the build env → the init migration SQL was generated deterministically from the schema (`migrate diff --from-empty`), and `prisma validate` was run; the actual `migrate deploy` + seed + backup restore-into-scratch-DB test run on the operator's server (which has Postgres) during deploy — recorded here as the one env limitation. (3) DID NOT yet wire the DB-backed `StateStore` / `attachEventSink` into the bot boot: doing so would force a live DB connection at boot and break the no-DB-required shell established in module 01; the sink factory + settings helpers are READY and are wired by the modules that first persist state/trades (10/15). The `makeEventSink`/settings seams are exported and unit-safe. (4) `type` folded into `EventLog.data` JSON rather than adding a column (no schema churn as the event taxonomy grows). No money/risk/auth behavior deviations.
**Verification:** typecheck ✅ (turbo, all 4 workspaces; `prisma generate` runs first) · build ✅ · tests **46** passing — db 14 (pnl: multi-fill partial-TP1+trailed-exit+BNB-fee position matches to the cent → gross 400 / fees 3.30 / net 396.70 / 19.835% / WIN / 12h; no-float-drift; result classification incl. forced-rebuy; unrealized; accountEquity; **mode-isolation** PAPER 396.70 vs LIVE −101.425 never mixed) + bot 32 (adds 4 backup: 03:30-UTC scheduling math, day rollover, runOnce success/never-throws) · migration completeness ✅ (10 tables/7 enums/16 indexes/3 FKs) + `prisma validate` ✅ · seed reaches DB-connect via its error handler ✅ (parses/imports resolve) · `bash -n scripts/backup-db.sh` ✅ · boot in PAPER ✅ (backup job armed @03:30 UTC, `boot_complete` → SIGINT → `shutdown_complete`, exit 0; still boots WITHOUT a live DB). UI self-check N/A. Live `migrate deploy`/seed/restore-test deferred to server deploy (no Postgres in build env).
**Open items → later modules:** module 03 caches `Pair.exchangeFilters` (LOT_SIZE/PRICE_FILTER/minNotional) + fills `Fill.feeUsd` from real BNB-fee conversion + implements the dust rule (`qtyRemaining*price < minNotional` → close, `Trade.exitReason` `+dust` suffix); modules 10/15 wire the DB-backed `StateStore` + `attachEventSink(makeEventSink(prisma))` and call `disconnectPrisma()` from the shutdown hooks; module 11 turns `backup_failed` into a Telegram alert; operator guide documents `BACKUP_DIR` (outside repo) + restore procedure; `package.json#prisma` seed config is Prisma-6 valid but deprecated for Prisma 7 (migrate to `prisma.config.ts` if/when upgrading).
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Module 01 — Foundation — DONE
**Built:** pnpm/Turborepo monorepo scaffold with four workspaces (`apps/bot`, `apps/dashboard`, `packages/core`, `packages/db`). Bot engine shell that later modules plug into:
- `config.ts` — dotenv + zod, fail-fast with a NAMED list of missing/invalid vars; env holds only secrets/infra; secret-masked `summarizeConfig()`; refuses `MODE=LIVE` from env.
- `logger.ts` — pino, levels debug/info/warn/error, JSON to `logs/bot.log` (daily rotation via pino-roll, keep 14) + pretty console in dev, `category` field (engine|exchange|strategy|risk|telegram|scheduler|reconcile), `logEvent()` with a deferred `events_log` DB sink seam (`attachEventSink()` for module 02).
- `scheduler.ts` — UTC EventEmitter emitting `close_1h` (+5s grace), `close_4h` (00/04/08/12/16/20 UTC +5s), `daily_rollover` (00:00 UTC), `summary_time` (PKT setting → UTC minutes); self-correcting, ref'd timers (engine heartbeat).
- `state.ts` — `StateMachine` over PAPER/LIVE/PAUSED/STOPPED + PAPER_DRAINING/LIVE_DRAINING; persistence seam (`StateStore`, in-memory now / DB in module 02); transition legality table; **LIVE gate guard** — LIVE/LIVE_DRAINING refused unless the module-15 owner gate is open.
- `utils/money.ts` (decimal.js, no raw float math; `roundToStep` for LOT_SIZE), `utils/time.ts` (UTC↔PKT, `pktTimeToUtcMinutes`), `lock.ts` (PID single-instance guard, stale-lock reclaim), `shutdown.ts` (SIGINT/SIGTERM → quiesce → persist → stop scheduler → release lock → exit 0, with hard-kill backstop), `index.ts` boot sequence.
- Root: `package.json`, `pnpm-workspace.yaml`, `turbo.json`, `tsconfig.base.json`, `.env.example` (all placeholders), `.gitignore`, `deploy.sh` (ARCHITECTURE.md §8: node-detection block, prisma steps as commented placeholders until module 02, BUILD_ID, pm2 restart+save, health-check notes), `ecosystem.config.js` (trading-bot active; trading-dashboard commented for module 14). Dashboard = Next.js 15 placeholder (noindex header + robots metadata, single scaffold page).
**Files:** `package.json`, `pnpm-workspace.yaml`, `turbo.json`, `tsconfig.base.json`, `.env.example`, `.gitignore`, `deploy.sh`, `ecosystem.config.js`; `apps/bot/{package.json,tsconfig.json,src/{index,config,logger,scheduler,state,lock,shutdown}.ts,src/utils/{money,time}.ts,src/__tests__/{config,money,time,scheduler,state}.test.ts}`; `apps/dashboard/{package.json,next.config.mjs,tsconfig.json,next-env.d.ts,app/{layout,page}.tsx}`; `packages/core/{package.json,tsconfig.json,src/index.ts}`; `packages/db/{package.json,tsconfig.json,src/index.ts}`; `pnpm-lock.yaml`.
**Decisions/Deviations:** (1) Spec header says end with `[FIXED_CHECKPOINT]`, but continuous-development mode is enabled by the controller → ended with `[CHECKPOINT]` per controller directive (no owner hard-stop). (2) decimal.js imported as named `{ Decimal }` (NodeNext default import resolves to a namespace, not the class). (3) Logger uses default sync stdout + silent level under `VITEST` to avoid worker-thread transports in tests. No money/risk/auth behavior deviations.
**Verification:** typecheck ✅ · build ✅ (bot tsc + dashboard `next build` both green) · tests 28 passing (config/money/time/scheduler/state) ✅ · boot in PAPER ✅ (logs secret-masked config summary, scheduler armed with next UTC fire times, stays alive as daemon, SIGINT → graceful shutdown persists state + removes lock + exit 0) · double-boot guard ✅ (2nd instance exits 1) · `bash -n deploy.sh` ✅ · UI design self-check N/A (scaffold only).
**Open items → later modules:** module 02 injects the DB-backed `StateStore` + `events_log` sink (`attachEventSink`) and uncomments the prisma steps in deploy.sh/CI; module 03 replaces the clock-drift stub with the real NTP check; runtime settings (capital, risk %, summary time, toggles) move into DB `settings`; module 14 builds the real dashboard + uncomments the PM2 dashboard entry; DASHBOARD_PORT value still `[DECIDE AT BUILD]`.
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Planning — DONE
Strategy layer locked (A/B/C reworked from source PDF: candle-color bias, 5m order blocks, body-midpoint equilibrium, 100% leveling sells, manual TradingView zones all removed; BTC filter, 1% sizing, structural stops, fee filter, drawdown discipline kept). Build order (15 modules), tiers, risk parameters, Telegram command set, dashboard scope, mode-switch state machine, and 7 real-world hardening items (exchange filters, stop-limit+watchdog, WS resilience+NTP, BNB fee reserve, indicator warm-up, dust/fill tracking, DB backups) folded into specs. Deliverables produced: ARCHITECTURE.md, AGENT.md, PROGRESS.md, PROGRESS-HISTORY.md, /specs/01–15, /specs/README-OPERATOR-GUIDE.md.

---

## [2026-07-06] Module 11 — Telegram Control Panel — DONE/APPROVED

**Branch:** feature/11-telegram (WORK TYPE: FEATURE)
**Spec:** /specs/11-telegram.md (no CODEREF companion; proceeded on spec alone). Library: grammY 1.44.0 (added to @spotedge/bot).

### What was built

**Shared service layer in @spotedge/core (the module-11 + module-14 seam).** Per spec §Service-layer rule, every command calls a core service function — NO business logic in handlers. New files under `packages/core/src/services/`:
- `ports.ts` — narrow injected ports (SettingsPort/StatePort/LedgerPort/PairsPort/EventsPort/FiltersPort/optional ExchangePort) so core stays DB-free at import (never touches the Prisma singleton). Rows: OpenPositionRow/ClosedTradeRow/LevelingCycleRow/PairView/LogLineView.
- `engineState.ts` — EngineState vocabulary + stateBadge (🟡PAPER/🔴LIVE/⏸/🛑) + modeForState (duplicated from apps/bot state.ts like money.ts, since core can't import apps/bot).
- `control.ts` — every command's service function: getStatus/getPositions/getPnl(scope union TODAY|WEEK|MONTH|SYMBOL|A|B|C|PAPER|LIVE)/getTrades/getBalance/getFees/getFilters/getLogs/getRisk/getPairs/getStrategies; mutations controlStop/Start/Pause/Resume, setMode (LIVE gate check), setCapital/setRisk (reuse sizing.ts validateCapital/validateRiskPct + sizingExample), setSummaryTime, setCore (T1-only), setTier, enable/disableStrategy, addPair/removePair, resetPaper, closePosition/closeAll/reconcileNow (ExchangePort-gated → ENGINE_OFFLINE when the runtime engine isn't wired). Money math decimal-safe throughout; capital = base + Σ realized (compounding), deployed = Σ open A/B cost, free = allocated − deployed.
- `reports.ts` — buildDailySummary (trades, W/L, win rate, net-of-fees, fees, equity, breaker-today, top/bottom pair) shared by REPORT + the scheduled daily summary.

**Telegram face in apps/bot/src/telegram/.**
- `format.ts` — HTML escaping (single escaping discipline), splitMessage/sendLongMessage (≤3900 chars, split ONLY at line boundaries, 📄(i/n) labels when n>1, 350ms sequential gap, over-long-line hard-split fallback), OutboundQueue (per-chat 1 msg/s, injectable now/sleep).
- `registry.ts` — the SINGLE command registry (name/category/usage/description/examples/warnings/moneyAffecting) driving HELP + dispatch usage.
- `help.ts` — helpIndex / helpCard / helpFor / helpManual, all generated from the injected commands array (can't drift).
- `auth.ts` — ownerOnly middleware (from.id === TELEGRAM_OWNER_ID else silently ignored + logged at warn) + pure isOwner.
- `confirm.ts` — ConfirmManager: typed (CONFIRM LIVE/CLOSEALL/RESETPAPER) + button (CLOSE/SETCAPITAL/SETRISK/SETCORE) confirmations, 60s TTL, injectable timers, fired-timer + lazy post-TTL expiry both handled.
- `render.ts` — HTML renderers (status card, monospace tables for POSITIONS/TRADES/PAIRS, balance, fees, filters ✅/❌/⏳ + blocking highlight, risk, strategies matrix, summary).
- `handlers.ts` — the Dispatcher: parse → typed-confirm resolution → command lookup (unknown → short hint) → STOPPED-state gating (only Status/Reports + START) → route to a service → render. Money commands register a confirm and reply prompt/keyboard; the mutation runs in onConfirm.
- `push.ts` — pure push formatters (entry/exit/cycle/breaker/watchdog/WS-stale/BNB-low) + PushService (AlertFn seam with error-class RateLimiter 1/15min + daily-heartbeat latch).
- `context.ts` — the ONLY DB-touching file: Prisma-backed implementations of every port (mode-scoped queries; realizedNetUsd = Σ Trade.netPnlUsd + Σ LevelingCycle.chunkPnlUsd; wipeMode FK-ordered deleteMany, mode-scoped) + buildServiceContext + makeStatePort (adapts the apps/bot StateMachine).
- `dbStateStore.ts` — DB-backed StateStore (settings keys engine_state/live_gate_approved/state_quiescing) so STOP/PAUSE/MODE survive a restart (replaces the in-memory store in index.ts).
- `bot.ts` — createTelegram: grammY Bot + Messenger (queue + sendLongMessage, HTML) + owner gate + message/callback handlers + inline Confirm/Cancel keyboards; start() runs bot.init() first so an invalid token → engine-only mode (logged fatal, engine stays alive) per §Edge cases.
- index.ts wired: DB-backed state store, buildServiceContext, createTelegram, non-fatal start, telegram.stop() added to graceful shutdown.

### Decisions / notes
- ExchangePort left undefined for now — the runtime engine (candle feed / tick loop / PaperTradingEngine wiring) is not yet assembled in apps/bot, so CLOSE/CLOSEALL/RECONCILE and POSITIONS live price/uPnL degrade to ENGINE_OFFLINE / "—". They light up once module 15 wires the engine + router into the bot. All ledger-derived commands (PNL/TRADES/BALANCE realized/FEES/STATUS/PAIRS/STRATEGIES/RISK/settings mutations/RESETPAPER) are fully functional against the DB.
- RESETPAPER resets capital_paper to DEFAULT_SETTINGS.capital_paper (11000) and is refused while any PAPER position is open; mode-scoped delete never touches LIVE (test-covered).
- HTML parse mode chosen (not MarkdownV2) and used everywhere.

### Gate results
- typecheck: green (db/core/bot/dashboard).
- build: green (all packages; dashboard Next build unaffected).
- tests: 333 total — db 14, core 255 (+10 services.control.test: PNL aggregation, RESETPAPER mode isolation ×2, SETRISK bounds ×2, MODE LIVE gate ×2, STATUS/summary/BALANCE), bot 64 (+32 telegram: format 7 incl. 12k→4 labelled parts no mid-line split + OutboundQueue spacing, help 5 registry-driven fake-command, auth 4 foreign-ignored, confirm 6 incl. timeout path, dispatch 6 incl. CLOSEALL/SETRISK confirm flows + STOPPED gating, push 4).
- boot smoke: invalid token → createTelegram.start() returns false (engine-only), no crash — verified against built dist.

### Deploy-time hand-offs (module-11 [HUMAN_REQUIRED], handled on the VPS)
- BotFather: operator creates the dedicated bot, puts TELEGRAM_BOT_TOKEN + TELEGRAM_OWNER_ID in the root .env.
- Live smoke: every command exercised in PAPER against the real bot; transcript pasted here at deploy (spec §Acceptance live smoke).

### Mid-session build-order change
While module 11 was in progress the controller injected /specs/16-env-loading-hotfix.md (foundational correction to module 01 — root-.env must load regardless of CWD/PM2) and updated ARCHITECTURE.md to build 16 NEXT, before module 12. PROGRESS.md NEXT pointer updated to module 16 accordingly. Module 11 was not interrupted (already complete + gates green).

---

## Module 16 — Environment Loading Hotfix — DONE/APPROVED 2026-07-06

**Type:** FIX (foundational correction to Module 01). Branch `fix/env-loading-hotfix`. Built out-of-sequence NEXT (after 11, before 12) because the bot cannot stay alive under PM2 until root-`.env` loading is deterministic.

### Root cause (from staging)
Bot booted under PM2 with `cwd: apps/bot` and reported every env var empty (`DATABASE_URL / BINANCE_* / TELEGRAM_* is required`) despite a correct root `.env`. Two causes: (1) `config.ts` called dotenv's `config()` at import time with NO path → resolved `.env` relative to `process.cwd()` (`apps/bot`), where no `.env` exists; (2) PM2's `env_file` was unreliable on the server's PM2 version and silently ignored. The stopgap on the server was `node_args: "-r dotenv/config" + DOTENV_CONFIG_PATH`.

### Decisions
- **Single shared resolver** `packages/core/src/env/loadRootEnv.ts`: walks UPWARD (≤8 levels) from the module's own directory for a dir containing BOTH `pnpm-workspace.yaml` AND `.env`, loads that `.env`. Anchoring on `pnpm-workspace.yaml` guarantees the true monorepo root (never a stray `.env`). Fallback honors an absolute `DOTENV_CONFIG_PATH`; otherwise throws a clear error naming that override var.
- **ESM deviation from spec code:** the spec sample defaulted `startDir = __dirname`, but `@spotedge/core` compiles to NodeNext ESM where `__dirname` is undefined at runtime. Implemented with `path.dirname(fileURLToPath(import.meta.url))` instead — same behavior, correct under real Node ESM (verified against compiled dist, not just vitest's shim).
- **Subpath export** `@spotedge/core/env/loadRootEnv` added to `packages/core/package.json` `exports` (kept OUT of the barrel `index.ts` so importing the resolver never drags in env-reading modules — the whole point is to run it BEFORE those load). `dotenv ^16.4.7` added as a core dependency.
- **Bot bootstrap split:** `apps/bot/src/index.ts` is now a tiny bootstrap — imports only `loadRootEnv`, calls it as the first statement, then `await import("./main.js")`. The full boot sequence moved verbatim to `apps/bot/src/main.ts` exporting `run(envPath)`. Dynamic import guarantees env is populated before config/Prisma/connector load.
- **config.ts:** removed the CWD-relative top-level `loadDotenv()` (the bug); it now only validates the already-populated `process.env`. Zod fail-fast + LIVE-from-env refusal unchanged.
- **FATAL diagnostics:** the config-FATAL block now prints `[FATAL] Loaded env from: <resolved .env path>` above the offending-vars list, so a future mis-load is one-glance diagnosable. `loadRootEnv` also exposes `loadedEnvPath()`.
- **ecosystem.config.js:** restored to clean canonical form — absolute repo-root `cwd: /www/wwwroot/staging_spotedge`, `script: apps/bot/dist/index.js`, `env: { NODE_ENV: "production" }` only. NO `env_file`, NO `node_args`, NO secrets (file is committed).
- **deploy.sh:** Prisma `migrate deploy` + seed now carry an explicit `DATABASE_URL="$DATABASE_URL"` prefix (root `.env` sourced in step 1b) instead of `dotenv-cli`. No symlinks, no `packages/db/.env`. Comments updated. (dotenv-cli devDep left in place — non-urgent cleanup per spec §5.)

### Gate results
- `pnpm run build` — 4/4 tasks green.
- `pnpm run typecheck` — 6/6 tasks green.
- `pnpm run test` — 337 passing (core 259 = +4 loadRootEnv, bot 64, db 14).
- Runtime verification against **compiled dist ESM** (proves `import.meta.url` works under real Node, not only vitest): same repo-root `.env` resolved from `repo-root`, `apps/bot`, and `packages/db` CWDs; full bot bootstrap run from `apps/bot` printed `[FATAL] Loaded env from: <repo>/.env` then the config errors and exited 1 — confirming CWD-independent load + resolved-path print. Temp dummy `.env` created for the check and removed afterward (`.env` is gitignored).

### Acceptance criteria coverage
- ✅ Unit test: `loadRootEnv()` from a temp nested dir locates + loads the root `.env` (fixture with pnpm-workspace.yaml + .env). Plus multi-CWD equality + no-`.env` throw tests.
- ✅ Unit test: preset `process.env` value NOT overwritten by a differing `.env` line (dotenv default no-override).
- ✅ FATAL path prints the resolved `.env` path.
- ✅ Verified same `.env` found from ≥2 CWDs (did 3).
- ⏳ PM2 clean-form boot test (`pm2 delete && pm2 start ecosystem.config.js`) — deploy-time gate on the VPS (cannot run PM2 in this env). Code side ready.

### Files
- NEW `packages/core/src/env/loadRootEnv.ts`
- NEW `packages/core/src/__tests__/loadRootEnv.test.ts`
- NEW `apps/bot/src/main.ts` (boot body moved from index.ts)
- MOD `apps/bot/src/index.ts` (now the bootstrap)
- MOD `apps/bot/src/config.ts` (removed CWD dotenv)
- MOD `packages/core/package.json` (dotenv dep + subpath export)
- MOD `ecosystem.config.js` (clean form)
- MOD `deploy.sh` (DATABASE_URL prefix; no dotenv-cli)
- MOD `pnpm-lock.yaml` (core dotenv dep)

**[FIXED_CHECKPOINT]** — touches foundational config every process depends on.

## [2026-07-06] Module 13 — Reporting Engine — DONE
**Built:** The one aggregation layer behind PNL/REPORT/FEES/daily-summary/dashboard charts, in `@spotedge/core` `services/reports.ts` — every function mode-scoped (PAPER/LIVE never mix), decimal-safe, and division-by-zero-proof (win rate / avg R / fee% render "—" on empty). Functions: `pnlSummary(period)` (gross/fees/net$&%/W/L/BE/win-rate/avg-R/best+worst), `pnlBySymbol`, `pnlByStrategy` (A/B from Trade rows, C from LevelingCycle chunks incl. forced-rebuy count = chunks where rebuyPrice ≥ sellPrice×1.05 trigger), `equityCurve(mode,granularity)` (hourly/daily buckets from EquitySnapshots, carry-forward gap fill), `drawdownSeries` (running peak → peak-to-trough %), `feeReport(period)` (total + BNB-vs-quote split + %-of-gross + annual run-rate projection), `dailyAudit(date)` (the daily-summary payload: net/gross/fees, per-trade lines, open-position snapshot, equity, breaker-tripped + breaker events, veto counts by reason from the EventLog window, heartbeat count), `tradeHistory(filters,paging)` + streaming `exportCsv`/`exportCsvString` (UTF-8-BOM header, 17 columns, RFC-4180 escaping, PKT + UTC timestamp columns, paged so years of fills never load at once), plus scheduled `buildWeeklyDigest` (week net, equity change, top strategy, current loss streak, fee drag) and the retained concise `buildDailySummary`. Computation stays UTC; PKT is display-only via new fixed-+5-offset `utils/time.ts` (`pktStamp`/`pktDateKey`/`utcStamp`) so core needs no tz database. New ports on LedgerPort (`snapshots`, `feeSplitByAsset`, `ledgerRows`+`countLedgerRows`, `riskUsd` on ClosedTradeRow, sellPrice/rebuyPrice/forcedRebuyTrigger on LevelingCycleRow), EventsPort (`window`), and a `SchedulePort` seam. apps/bot: Prisma adapters for all new ports (entry/exit legs derived from Position fills; feeSplit via groupBy feeAsset); scheduled outputs wired — `reports.ts` ReportPusher fires the daily summary every day + the weekly digest on UTC Mondays on the scheduler's `summary_time` event; `SETSUMMARY` now re-arms the live scheduler via the SchedulePort (no restart); Telegram `REPORT` → daily-audit card, `REPORT CSV` → uploaded ledger document (`bot.api.sendDocument` via new Messenger.sendDocument + Responder.sendDocument seam); new renderers renderDailyAudit/renderWeeklyDigest/renderFeeReport; scheduler now reads `summary_time_pkt` at boot.
**Files:** NEW `packages/core/src/services/reports.ts` (rewritten/expanded), `packages/core/src/utils/time.ts`, `packages/core/src/__tests__/services.reports.test.ts`, `apps/bot/src/reports.ts`, `apps/bot/src/__tests__/reports.test.ts`, `apps/bot/src/__tests__/telegram.report-render.test.ts`. MOD `packages/core/src/services/ports.ts` (new ports/shapes + SchedulePort), `packages/core/src/services/control.ts` (setSummaryTime → schedule seam), `packages/core/src/index.ts` (export time helpers), `apps/bot/src/telegram/context.ts` (Prisma adapters + buildServiceContext schedule), `apps/bot/src/telegram/handlers.ts` (handleReport + REPORT CSV + Responder.sendDocument), `apps/bot/src/telegram/render.ts` (3 renderers), `apps/bot/src/telegram/bot.ts` (Messenger.sendDocument), `apps/bot/src/telegram/registry.ts` (REPORT [CSV]), `apps/bot/src/main.ts` (scheduler reads setting + ReportPusher wiring + schedule port), and the two existing ServiceContext test fakes (control + dispatch) extended for the new ports.
**Decisions/Deviations:** (1) Renamed reports' equity-point type to `EquityCurvePoint` to avoid a name clash with the backtest module's `EquityPoint`. (2) forced-rebuy count is DERIVED (rebuyPrice ≥ forcedRebuyTrigger) since the cycle row has no explicit rebuy-type column — exact by construction (module 09 fires the FORCED insurance rebuy only at/above sellPrice×1.05). (3) PKT rendering uses a fixed UTC+5 offset in core (Pakistan has no DST since 2009) rather than pulling luxon into @spotedge/core; the Telegram renderers reuse the same core helper so PKT never diverges. (4) `dailyAudit` veto counts / breaker events / heartbeats read the EventLog window (category `filter` → reason, `/breaker/i`, `/heartbeat/i`); when the engine hasn't emitted such events yet they render empty ("none recorded"), which is honest — historical per-cycle veto reasons are not otherwise reconstructable. (5) REPORT switched from the concise summary to the richer daily-audit card; the concise `buildDailySummary`/`renderSummary` are retained for the scheduled daily push.
**Verification:** typecheck ✅ (all 4 workspaces) · build ✅ (incl. dashboard) · tests 375 passing (core 287 = +15 [13 reports fixture-ledger: hand-verified pnlSummary/bySymbol/byStrategy, equity gap-fill, drawdown, feeReport split+projection, dailyAudit PKT/UTC boundary, tradeHistory paging, CSV columns+BOM, weekly digest, mode isolation, empty-period zero-report; +2 setSummaryTime live-reschedule]; bot 74 = +10 [4 ReportPusher daily/Monday-weekly/error-swallow, 4 renderers, 2 dispatch REPORT + REPORT CSV document]; db 14). Real scheduled push + live Telegram REPORT/REPORT-CSV document = deploy-time gates (need a live bot + populated ledger), consistent with modules 10–12.
**Open items → later modules:** Module 14 dashboard consumes the typed JSON shapes exported here (PnlSummaryView, SymbolPnlView, StrategyPnlView, EquityCurvePoint, DrawdownPoint, FeeReportView, DailyAuditView, TradeHistoryPage, WeeklyDigestView) + the streaming `exportCsv` for the download endpoint. Once the trading engine emits filter-veto / breaker / heartbeat EventLog rows, dailyAudit's veto/breaker/heartbeat sections populate automatically (no reporting change needed).
**Checkpoint printed:** [CHECKPOINT]
