# PROGRESS-HISTORY.md — SpotEdge Trading Bot (detailed log)

> PROGRESS.md keeps one-liners; this file keeps the full story. The agent appends a dated entry after EVERY module: what was built, files touched, decisions/deviations (with reasons), verification results, and any open items handed to later modules.

## Entry template

```
## [YYYY-MM-DD] Module NN — <name> — <DONE | PARTIAL | BLOCKED>
**Built:** …
**Files:** …
**Decisions/Deviations:** … (none, or reason logged; money/risk/auth deviations require [HUMAN_REQUIRED] instead)
**Verification:** typecheck ✅/❌ · build ✅/❌ · tests <n> passing · UI design self-check (if UI) ✅/❌ · boot in PAPER ✅/❌
**Open items → later modules:** …
**Checkpoint printed:** [FIXED_CHECKPOINT] / [CHECKPOINT] / [HUMAN_REQUIRED]
```

## History

### [2026-07-06] Module 04 — Indicator Engine — DONE
**Built:** All the math the filters/strategies (modules 05–09) consume, computed in-house from Binance klines — no TA-library black box, every value unit-tested against hand-verified fixtures. Lives in `@spotedge/core/src/indicators/`, exported from the core index. Four new files:
- `math.ts` — pure indicator functions returning series ALIGNED to the input candle index, `null` inside each indicator's warm-up (never a silent 0): `sma`, `ema` (SMA-seeded, k=2/(p+1)), `rsi` (Wilder), `trueRange`/`atr` (Wilder, TR with gaps), `adx` (Wilder +DM/−DM/TR smoothing → +DI/−DI/DX → Wilder-averaged ADX), `volumeAvg(20)`+`volumeRatio`, `emaRising(lookback=6)`, plus `lastValue` helper.
- `structure.ts` — no-repaint pivots (`isPivotHigh/Low`, `pivots` — a pivot only emitted once `right` candles have closed; window-edge returns null), body-based S/R zones (`srZones` clustering pivot bodies within 0.5×ATR, `classifyZone`, `srFlip` resistance→support on a body close above), swings + `fibZone` (38.2–61.8% band of the last low→high swing), `detectRange` (≥2-pivot floor/ceiling clusters, valid only if width ≥ 3×ATR, `rangeInvalidated` on body close beyond a boundary), `bullishEngulfing` (+strong-close variant), `higherLowBreak` (higher pivot low → body close above intervening minor high), `bearishRejection` (red H4 body in a resistance zone with ≥50% upper wick).
- `types.ts` — `Series`, `AdxPoint`, `Pivot`/`SRZone`/`Swing`/`FibZone`/`RangeBox`/`EngulfCheck`/`HigherLowBreak`, `TfIndicators`/`IndicatorSnapshot`, and the injected seams `KlineSource` (satisfied directly by `BinanceConnector.getKlines`) + `FilterStateSink`.
- `CandleStore.ts` — per symbol×TF (1h/4h/1d/1w) rolling window of 600 closed candles; `warmUp()` fetches 500 klines per maintained TF (each TF fetched independently so a testnet-absent 1w never fails the pair); `isWarm()` gates signal-eligibility on 1h+4h ≥500; `onClose(symbol, tf)` refetches recent closed candles, appends (dedupe by openTime), detects timestamp gaps in the tail → alert + full-window refetch (never compute across a silent hole); `snapshot()` computes the full `IndicatorSnapshot` and `persist()` upserts it to the `FilterState` sink; `drop()` for REMOVEPAIR.
**Decisions / interpretations (logged per AGENT.md rule 2):**
- **Warm-up gate on 1h+4h only.** Spec says "500 klines per required TF"; strategies trade on 1H/H4 (EMA200-on-H4 alone needs 200+ candles), while a young listing can legitimately hold <500 weekly bars. All four TFs are warmed, but `isWarm` gates on the two trading TFs so 1d/1w context never permanently blocks a valid pair. Configurable via `gateTimeframes`.
- **ATR/ADX Wilder conventions fixed and documented:** ATR seed = SMA of the first `period` TRs (TR[0]=H−L) at index period−1; ADX first value at index 2·period−1 (mean of the first `period` DX). Fixtures hand-verified against these exact conventions (perfect trend → +DI/−DI/ADX = 100/0/100 exactly; RSI crafted series = 65.0 exactly).
- **Snapshot is the FilterState payload.** "Upsert FilterState inputs" implemented as persisting the computed `IndicatorSnapshot`; module 05 defines how the pipeline reads it. Seam kept minimal (`FilterStateSink.upsert`).
- **onClose is TF-generic.** Spec names close_1h/close_4h for appends; implemented generically so the bot scheduler wiring (which event fires which TF, incl. 1d/1w on daily_rollover) is a module-05+ concern. No bot-side wiring added this module (mirrors module 03's deferral).
- `noUncheckedIndexedAccess` is on project-wide; tight numeric loops use non-null assertions at provably-bounded `[i]` sites (documented in math.ts header) — no established repo idiom existed (first numeric-loop code in the codebase).
**Verification:** `pnpm typecheck` + `pnpm build` green across the workspace. `pnpm test` = 127 tests (core 81 incl. 39 new: math 15, structure 18, CandleStore 6; bot 32; db 14). Acceptance criteria met: EMA/RSI/ATR/ADX vs hand-verified fixtures (1e-6 rel); pivot no-repaint, zone clustering, S/R flip, invalid range (<3×ATR), engulfing true/false, rejection true/false all covered on constructed fixtures; warm-up gate proven (pair not eligible until 500 klines); 15-pair snapshot smoke produces numeric EMA/RSI/ATR/ADX/volumeRatio with no errors. Testnet/live smoke on the real exchange deferred to server deploy (as with module 03).
**Files:** `packages/core/src/indicators/{types,math,structure,CandleStore}.ts`, tests `packages/core/src/__tests__/{indicators.math,indicators.structure,CandleStore}.test.ts`, export line added to `packages/core/src/index.ts`.
**Open items → later modules:** module 05 (filter pipeline) consumes `isWarm`/`snapshot` and defines the `FilterState` read side; the bot must wire the scheduler's close_1h/close_4h (and daily_rollover for 1d/1w) to `CandleStore.onClose`, call `warmUp` on boot/ADDPAIR, and inject the Prisma-backed `FilterStateSink` + the connector as `KlineSource`.
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Module 03 — Exchange Connector (Binance Spot) — DONE
**Built:** The single code path that talks to Binance — `BinanceConnector` (ccxt REST + `ws` streams) living in `@spotedge/core`, environment-agnostic and fully seam-injected so `packages/core` never imports `apps/bot`. Everything above this layer is exchange-agnostic and depends only on `exchange/types.ts`.
- `packages/core/src/exchange/types.ts` — the exchange contract: `Timeframe`+`TF_MS`, `Candle`, `Ticker`, `ExchangeFilters` (all decimal STRINGS), `FillReport`/`FillLeg`, `Balance`, `OpenOrder`, `OrderPurpose`, `ReconcileAction`/`ReconcileFinding`, and the injected seams `ExchangeLogger`, `AlertFn`, `Clock`(+`systemClock`), `SettingsReader`, `PairRepository`, `OpenPositionView`. Market-data OHLCV are `number` (indicator math, not ledger money); everything that touches the ledger is a decimal string.
- `packages/core/src/exchange/filters.ts` — PURE exchange-filter compliance (the classic bot killer): `roundQty`/`roundPrice` (floor to stepSize/tickSize at exact step precision, decimal-safe), `meetsMinNotional` (minQty AND qty×price≥minNotional), `validateTp1Split` (BOTH halves of a 50% TP1 split must clear minNotional at entry — the veto the risk engine/module 06 calls), `stopLimitPrice` (limit = trigger×0.995 buffer), `isTradable`.
- `packages/core/src/exchange/clientOrderId.ts` — idempotency keys `se-{refId}-{purpose}-{nonce}`; `makeClientOrderId` (throws rather than truncate past Binance's 36-char cap), `parseClientOrderId`/`isOwnClientOrderId` (foreign/manual orders → null → reconcile ignores), `nextNonce` (ms+counter base36).
- `packages/core/src/exchange/backoff.ts` — WS reconnect schedule: `backoffDelay` (deterministic 1s→60s cap, overflow-safe) + `Backoff` class (next/reset, optional injectable full-jitter).
- `packages/core/src/exchange/fees.ts` — fee→USD so every `Fill.feeUsd` is accurate: `feeToUsd` (stablecoins 1:1, else `UsdPriceResolver`; unknown price → undefined so the caller alerts rather than under-count), `aggregateFeeUsd` (per-asset totals + summed USD + `unpriced` list), `STABLE_USD_ASSETS`.
- `packages/core/src/exchange/reconcile.ts` — pure `classifyReconcile(qtyRemaining, freeBalance, stopAlive, dustEps)` → `EXTERNAL_CLOSE` (balance gone) | `REPLACE_STOP` (stop missing) | `OK`, with a minQty dust epsilon to avoid false external-closes.
- `packages/core/src/exchange/ExecutionRouter.ts` — the PAPER/LIVE order seam: `ExecutionRouter` interface + `OrderContext`, and `UnwiredPaperRouter` that FAILS CLOSED (throws "wire module 10") so nothing silently trades into a void before the paper engine exists.
- `packages/core/src/exchange/BinanceConnector.ts` — the class: `init()` (loadMarkets → cache filters → clock check → arm hourly-drift + daily-refresh timers, all `unref`'d), `refreshFilters()` (persists `Pair.exchangeFilters`, detects status≠TRADING → mark `removing` + alert, skips testnet-absent pairs), `getKlines()` (CLOSED candles only — drops the forming bar by closeTime>now), `get24hVolumeUsd()`, `getBalances()`, order methods routed through the ExecutionRouter (`marketBuy`/`marketSell`/`placeStopLimit`/`cancelOrder`/`getOpenOrders`), a private `LiveExecutionRouter`+`live*` executors (idempotency query-before-place via `findByClientOrderId`; market buy by quote cost via `createMarketBuyOrderWithCost`; STOP_LOSS_LIMIT with 0.5% buffer; `settleOrder` re-queries + alerts on non-FILLED, spec retry-once), `toFillReport` (weighted-avg price + USD fees from real `info.fills`), WS ticker stream (miniTicker combined stream, exponential-backoff auto-reconnect, resubscribe, REST 10s poll fallback while down, >60s data-gap alert), user-data stream (listenKey + 30-min keepalive), `checkClockDrift()` (>1s → alert with chrony/timesyncd hint, recvWindow 10000), `checkBnbReserve()` (alert-only below `settings.bnb_fee_reserve_usd`; never auto-buys BNB), `reconcile(positions, handler)` (LIVE-only; delegates ledger effects to an injected `ReconcileHandler`, downgrades failed stop re-place to WATCHDOG_ONLY), plus the exposed rounding helpers + `validateSymbol()` (ADDPAIR). Constructed cheaply (no network); `init()` does first I/O. Test seam: inject a pre-built ccxt exchange.
- `packages/core/src/utils/money.ts` — decimal-safe primitives for core (core can't import apps/bot's money util): `dec/add/sub/mul/div/gte/lte/gt/lt`, `roundDownToStep`, `stepDecimals`, `toFixed/toStr/toNumber`. This is the module that introduces the exchange-precision rounding AGENT.md rule 7 refers to.
- `packages/core/src/index.ts` — re-exports the whole `exchange/*` surface + `money` namespace for downstream modules.
- `apps/bot/src/exchange.ts` — production wiring: adapts pino (`categoryLogger("exchange")`) to `ExchangeLogger`, a log-only `AlertFn` that routes through `logEvent` (→ events_log; module 11 fans out to Telegram), a Prisma-backed `PairRepository` (`listPairs`/`saveFilters`/`markRemoving`), a DB `SettingsReader` (`getSetting`+`parseNum`/`parseBool`), and `createExchangeConnector(cfg)` which CONSTRUCTS (network-free) but does not `init()`.
- `apps/bot/src/index.ts` — boot step 3 now constructs the connector (replacing the module-01 clock-drift stub) and logs "init deferred to trading start"; `exchange.close()` added to the graceful-shutdown hooks (tears down WS + timers, leaves exchange stop orders in place).
**Files:** `packages/core/{package.json, src/index.ts, src/utils/money.ts, src/exchange/{types,filters,fees,backoff,clientOrderId,reconcile,ExecutionRouter,BinanceConnector}.ts, src/__tests__/{filters,clientOrderId,backoff,fees,reconcile,BinanceConnector}.test.ts}`; `apps/bot/src/{exchange.ts, index.ts}`; `pnpm-lock.yaml` (adds ccxt ^4.4.0, ws ^8.18.0, decimal.js, @spotedge/db to core); `PROGRESS.md`, `PROGRESS-HISTORY.md`.
**Decisions/Deviations:** (1) Spec header says end with `[FIXED_CHECKPOINT]` (module 03 is a hard checkpoint), but continuous-development mode is enabled by the controller → ended with `[CHECKPOINT]` per controller directive; no money/risk/auth behavior deviation. (2) Connector is DECOUPLED via injected seams (logger/alert/pairs/settings/clock/paperRouter) instead of importing app infra — required because `packages/core` must not depend on `apps/bot`; matches the existing StateStore/EventSink seam philosophy. (3) `init()`/`reconcile()` are DEFERRED (not called at boot): calling them would force network + a live DB at boot and break the no-DB-required shell from modules 01/02. Construction is network-free; init/reconcile are called by the paper/live engines (modules 10/15). Boot remains deterministic + DB-free. (4) A self-contained `utils/money.ts` was added to core (can't import apps/bot's) — same decimal.js contract, deliberate duplication; core's `roundQty`/`roundPrice` are the single sanctioned exchange-rounding path AGENT.md rule 7 anticipates. (5) PAPER order paths default to `UnwiredPaperRouter` (fail-closed) until module 10 injects the real paper engine — prevents phantom fills. (6) Ledger EFFECTS of reconcile (write EXTERNAL Trade, update Position) are delegated to an injected `ReconcileHandler` so all P&L writes stay in `@spotedge/db`; the connector owns only the decision + alert. No money/risk/auth deviations requiring [HUMAN_REQUIRED].
**Verification:** typecheck ✅ (turbo, all workspaces) · build ✅ (4/4) · tests **88** passing — core **42** (filters: roundQty/roundPrice vs real SOL 0.001/BTC 0.00001 stepSizes + 0.01 tick, float-drift safety, minNotional, TP1-split veto/pass incl. odd-qty runner remainder, stopLimit 0.5%/custom buffer; clientOrderId: build/parse round-trip, determinism, 36-char throw, foreign-id null, nonce uniqueness; backoff: 1s→60s cap+overflow, jitter; fees: stablecoin 1:1, BNB conversion, unknown→undefined, aggregate per-asset+unpriced; reconcile: OK/EXTERNAL_CLOSE/REPLACE_STOP + dust epsilon; BinanceConnector w/ injected fake ccxt: closed-candle drop, quoteVolume+fallback, market buy weighted-avg + USD fees + `se-42-entry-…` clientOrderId, reconcile EXTERNAL_CLOSE fires handler+alert / OK when balance+stop present / PAPER skipped, clock-drift +5s alert) + bot 32 + db 14 · boot in PAPER ✅ (connector constructed, secrets masked, SIGINT → `shutdown_complete` → exit 0, 0 errors; still boots WITHOUT a live DB/exchange). UI self-check N/A. **Integration on testnet DEFERRED to server deploy** (no Binance testnet reachable from the build env): fetch klines 15 pairs, WS open + kill/reconnect, place+cancel stop-limit, tiny market buy/sell with recorded fees, boot-with-fake-DB-position reconcile — these run on the operator's server per the spec's integration acceptance criteria.
**Open items → later modules:** module 04 consumes `getKlines()` (enforces the ≥500-kline warm-up before signals); module 06 calls `validateTp1Split()` to veto undersized entries and uses `roundQty`/`meetsMinNotional`; module 10 injects a `PaperExecutionRouter` (replaces `UnwiredPaperRouter`), calls `init()`/`subscribeTicker()`/`subscribeUserData()`, wires the `ReconcileHandler` (writes EXTERNAL Trade via `@spotedge/db` pnl) + reconcile-on-boot, and implements the dust rule handed over from module 02; module 11 replaces the log-only `AlertFn` with Telegram (clock-drift, watchdog-stop, data-gap, reconcile, low-BNB alerts) and turns `checkBnbReserve()` into a daily job; module 15 opens the LIVE gate so `mode==="LIVE"` routes through the live executor + arms the two-layer stop watchdog. `bnb_fee_pay_enabled` is a new operator-optional setting read with default false (no seed needed). ccxt listen-key implicit endpoints (`publicPost/Put/DeleteUserDataStream`) are typed loosely + guarded — verified on testnet at deploy.
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Module 02 — Database (Prisma + PostgreSQL) — DONE
**Built:** Full data model and money-critical ledger for the bot.
- `packages/db/prisma/schema.prisma` — 7 enums (`Mode`, `Tier`, `Strategy`, `PositionStatus`, `TradeResult`, `CycleStatus`, `Side`) and 10 models exactly per spec: `Setting`, `SettingAudit`, `Pair`, `Position`, `Fill`, `Trade`, `LevelingCycle`, `EquitySnapshot`, `EventLog`, `FilterState`. ALL monetary/quantity columns are `Decimal` (never Float). Every money row carries `mode`. Relations: `Fill → Position?`, `Fill → LevelingCycle?`, `Trade 1:1 Position` (`positionId @unique`). Added query indexes for the hot mode-scoped reads (`[mode,status]`, `[mode,symbol]`, `[mode,closedAt]`, `[category,ts]`, etc.).
- `packages/db/prisma/migrations/20260706000000_init/migration.sql` + `migration_lock.toml` (provider=postgresql). Generated deterministically via `prisma migrate diff --from-empty --to-schema-datamodel` (no live DB in the build env); 10 tables / 7 enum types / 16 indexes / 3 FKs. `prisma validate` ✅.
- `packages/db/src/index.ts` — process-wide `PrismaClient` singleton (`getPrisma`/`prisma`/`disconnectPrisma`), re-exports the generated enums + model types so downstream modules import DB types from `@spotedge/db` only.
- `packages/db/src/pnl.ts` — pure Decimal P&L math (the ledger's source of truth): `costBasis`, `proceeds`, `totalFees`, `grossPnl`, `realizedNetPnl` (= Σ sell qv − Σ buy qv − Σ feeUsd), `netPnlPct` (on cost basis), `classifyResult` (WIN/LOSS/BREAKEVEN/FORCED_REBUY_LOSS), `holdHours`, `computeTrade` (assembles the `Trade` row), `positionUnrealized` (spot long mark-to-market), `accountEquity` (allocated + realized + unrealized), and mode-isolation helpers `scopeToMode`/`realizedNetPnlForMode`.
- `packages/db/src/settings.ts` — typed `Setting` access; `setSetting` upserts + journals `SettingAudit` (old→new, source) in ONE transaction; `DEFAULT_SETTINGS` (mode, capital_paper/live, risk_pct, summary_time_pkt, strategy_A/B/C_enabled, circuit_breaker_tripped_date, live_gate_approved, bnb_fee_reserve_usd); `parseBool`/`parseNum` helpers.
- `packages/db/src/events.ts` — `makeEventSink(prisma)` returns the function the bot's logger `attachEventSink()` seam consumes to persist `EventLog` rows (never throws into the engine; folds `type` into `data` JSON). Defined `EventRecordLike` locally to avoid a bot→db import cycle.
- `packages/db/prisma/seed.ts` — idempotent seed: settings create-only (never clobbers an operator-edited value), 15 default pairs upserted by symbol with tiers (T1 BTC/ETH/BNB/SOL · T2 DOT/LINK/AVAX/NEAR/TON/SUI/FIL/ATOM · T3 XLM/DASH/ALGO; GRAM+CVX excluded). Wired via `package.json#prisma.seed = tsx prisma/seed.ts`.
- `scripts/backup-db.sh` — nightly `pg_dump | gzip -9` to `${BACKUP_DIR:-~/spotedge-backups}/spotedge-YYYYMMDD.sql.gz` (dir OUTSIDE repo), `--no-owner --no-privileges` for scratch-DB restore, `.partial`→atomic rename, 7-day retention prune, non-zero exit on failure.
- `apps/bot/src/backup.ts` (`BackupJob`) — in-bot scheduler fires the script at **03:30 UTC** (reuses scheduler `nextPeriodic`/`DAY_MS`; injectable clock/runner for tests); `runOnce()` never throws — logs `backup_ok`/`backup_failed` (Telegram alert deferred to module 11). Wired into `apps/bot/src/index.ts` boot (step 5b) + graceful-shutdown hooks (`backup.stop()`).
- `deploy.sh` — activated the module-01 Prisma placeholders: `prisma generate` → `migrate deploy` → `seed` (via `pnpm --filter @spotedge/db exec/run`), AFTER install and BEFORE typecheck/build, per AGENT.md rule (b). CI == the SSH deploy workflows that invoke `deploy.sh`, so they inherit the same steps (no separate build/test CI workflow exists in this repo).
- `packages/db/package.json` — added `@prisma/client`+`decimal.js` deps, `prisma`+`tsx`+`vitest` dev-deps, and `generate`/`migrate:*`/`seed`/`test` scripts; `build`/`typecheck` now run `prisma generate` first so the client exists before tsc.
**Files:** `packages/db/{package.json, prisma/schema.prisma, prisma/seed.ts, prisma/migrations/migration_lock.toml, prisma/migrations/20260706000000_init/migration.sql, src/{index,pnl,settings,events}.ts, src/__tests__/pnl.test.ts}`; `apps/bot/src/{backup.ts, index.ts}`, `apps/bot/src/__tests__/backup.test.ts`; `scripts/backup-db.sh`; `deploy.sh`; `pnpm-lock.yaml`; `PROGRESS.md`, `PROGRESS-HISTORY.md`.
**Decisions/Deviations:** (1) Spec header says end with `[FIXED_CHECKPOINT]`, but continuous-development mode is enabled by the controller → ended with `[CHECKPOINT]` per controller directive (data-model change, but no owner hard-stop requested). (2) No live Postgres in the build env → the init migration SQL was generated deterministically from the schema (`migrate diff --from-empty`), and `prisma validate` was run; the actual `migrate deploy` + seed + backup restore-into-scratch-DB test run on the operator's server (which has Postgres) during deploy — recorded here as the one env limitation. (3) DID NOT yet wire the DB-backed `StateStore` / `attachEventSink` into the bot boot: doing so would force a live DB connection at boot and break the no-DB-required shell established in module 01; the sink factory + settings helpers are READY and are wired by the modules that first persist state/trades (10/15). The `makeEventSink`/settings seams are exported and unit-safe. (4) `type` folded into `EventLog.data` JSON rather than adding a column (no schema churn as the event taxonomy grows). No money/risk/auth behavior deviations.
**Verification:** typecheck ✅ (turbo, all 4 workspaces; `prisma generate` runs first) · build ✅ · tests **46** passing — db 14 (pnl: multi-fill partial-TP1+trailed-exit+BNB-fee position matches to the cent → gross 400 / fees 3.30 / net 396.70 / 19.835% / WIN / 12h; no-float-drift; result classification incl. forced-rebuy; unrealized; accountEquity; **mode-isolation** PAPER 396.70 vs LIVE −101.425 never mixed) + bot 32 (adds 4 backup: 03:30-UTC scheduling math, day rollover, runOnce success/never-throws) · migration completeness ✅ (10 tables/7 enums/16 indexes/3 FKs) + `prisma validate` ✅ · seed reaches DB-connect via its error handler ✅ (parses/imports resolve) · `bash -n scripts/backup-db.sh` ✅ · boot in PAPER ✅ (backup job armed @03:30 UTC, `boot_complete` → SIGINT → `shutdown_complete`, exit 0; still boots WITHOUT a live DB). UI self-check N/A. Live `migrate deploy`/seed/restore-test deferred to server deploy (no Postgres in build env).
**Open items → later modules:** module 03 caches `Pair.exchangeFilters` (LOT_SIZE/PRICE_FILTER/minNotional) + fills `Fill.feeUsd` from real BNB-fee conversion + implements the dust rule (`qtyRemaining*price < minNotional` → close, `Trade.exitReason` `+dust` suffix); modules 10/15 wire the DB-backed `StateStore` + `attachEventSink(makeEventSink(prisma))` and call `disconnectPrisma()` from the shutdown hooks; module 11 turns `backup_failed` into a Telegram alert; operator guide documents `BACKUP_DIR` (outside repo) + restore procedure; `package.json#prisma` seed config is Prisma-6 valid but deprecated for Prisma 7 (migrate to `prisma.config.ts` if/when upgrading).
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Module 01 — Foundation — DONE
**Built:** pnpm/Turborepo monorepo scaffold with four workspaces (`apps/bot`, `apps/dashboard`, `packages/core`, `packages/db`). Bot engine shell that later modules plug into:
- `config.ts` — dotenv + zod, fail-fast with a NAMED list of missing/invalid vars; env holds only secrets/infra; secret-masked `summarizeConfig()`; refuses `MODE=LIVE` from env.
- `logger.ts` — pino, levels debug/info/warn/error, JSON to `logs/bot.log` (daily rotation via pino-roll, keep 14) + pretty console in dev, `category` field (engine|exchange|strategy|risk|telegram|scheduler|reconcile), `logEvent()` with a deferred `events_log` DB sink seam (`attachEventSink()` for module 02).
- `scheduler.ts` — UTC EventEmitter emitting `close_1h` (+5s grace), `close_4h` (00/04/08/12/16/20 UTC +5s), `daily_rollover` (00:00 UTC), `summary_time` (PKT setting → UTC minutes); self-correcting, ref'd timers (engine heartbeat).
- `state.ts` — `StateMachine` over PAPER/LIVE/PAUSED/STOPPED + PAPER_DRAINING/LIVE_DRAINING; persistence seam (`StateStore`, in-memory now / DB in module 02); transition legality table; **LIVE gate guard** — LIVE/LIVE_DRAINING refused unless the module-15 owner gate is open.
- `utils/money.ts` (decimal.js, no raw float math; `roundToStep` for LOT_SIZE), `utils/time.ts` (UTC↔PKT, `pktTimeToUtcMinutes`), `lock.ts` (PID single-instance guard, stale-lock reclaim), `shutdown.ts` (SIGINT/SIGTERM → quiesce → persist → stop scheduler → release lock → exit 0, with hard-kill backstop), `index.ts` boot sequence.
- Root: `package.json`, `pnpm-workspace.yaml`, `turbo.json`, `tsconfig.base.json`, `.env.example` (all placeholders), `.gitignore`, `deploy.sh` (ARCHITECTURE.md §8: node-detection block, prisma steps as commented placeholders until module 02, BUILD_ID, pm2 restart+save, health-check notes), `ecosystem.config.js` (trading-bot active; trading-dashboard commented for module 14). Dashboard = Next.js 15 placeholder (noindex header + robots metadata, single scaffold page).
**Files:** `package.json`, `pnpm-workspace.yaml`, `turbo.json`, `tsconfig.base.json`, `.env.example`, `.gitignore`, `deploy.sh`, `ecosystem.config.js`; `apps/bot/{package.json,tsconfig.json,src/{index,config,logger,scheduler,state,lock,shutdown}.ts,src/utils/{money,time}.ts,src/__tests__/{config,money,time,scheduler,state}.test.ts}`; `apps/dashboard/{package.json,next.config.mjs,tsconfig.json,next-env.d.ts,app/{layout,page}.tsx}`; `packages/core/{package.json,tsconfig.json,src/index.ts}`; `packages/db/{package.json,tsconfig.json,src/index.ts}`; `pnpm-lock.yaml`.
**Decisions/Deviations:** (1) Spec header says end with `[FIXED_CHECKPOINT]`, but continuous-development mode is enabled by the controller → ended with `[CHECKPOINT]` per controller directive (no owner hard-stop). (2) decimal.js imported as named `{ Decimal }` (NodeNext default import resolves to a namespace, not the class). (3) Logger uses default sync stdout + silent level under `VITEST` to avoid worker-thread transports in tests. No money/risk/auth behavior deviations.
**Verification:** typecheck ✅ · build ✅ (bot tsc + dashboard `next build` both green) · tests 28 passing (config/money/time/scheduler/state) ✅ · boot in PAPER ✅ (logs secret-masked config summary, scheduler armed with next UTC fire times, stays alive as daemon, SIGINT → graceful shutdown persists state + removes lock + exit 0) · double-boot guard ✅ (2nd instance exits 1) · `bash -n deploy.sh` ✅ · UI design self-check N/A (scaffold only).
**Open items → later modules:** module 02 injects the DB-backed `StateStore` + `events_log` sink (`attachEventSink`) and uncomments the prisma steps in deploy.sh/CI; module 03 replaces the clock-drift stub with the real NTP check; runtime settings (capital, risk %, summary time, toggles) move into DB `settings`; module 14 builds the real dashboard + uncomments the PM2 dashboard entry; DASHBOARD_PORT value still `[DECIDE AT BUILD]`.
**Checkpoint printed:** [CHECKPOINT]

### [2026-07-06] Planning — DONE
Strategy layer locked (A/B/C reworked from source PDF: candle-color bias, 5m order blocks, body-midpoint equilibrium, 100% leveling sells, manual TradingView zones all removed; BTC filter, 1% sizing, structural stops, fee filter, drawdown discipline kept). Build order (15 modules), tiers, risk parameters, Telegram command set, dashboard scope, mode-switch state machine, and 7 real-world hardening items (exchange filters, stop-limit+watchdog, WS resilience+NTP, BNB fee reserve, indicator warm-up, dust/fill tracking, DB backups) folded into specs. Deliverables produced: ARCHITECTURE.md, AGENT.md, PROGRESS.md, PROGRESS-HISTORY.md, /specs/01–15, /specs/README-OPERATOR-GUIDE.md.
