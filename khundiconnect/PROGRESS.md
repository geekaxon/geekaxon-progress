> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 program underway (build-steps 54+, see `specs/README-v3.1.md`).** v3 (38–53) + v2 (19–37) complete. **Wave 1 (54–60) COMPLETE.** **Wave 2 (61–64) COMPLETE.** **Wave 3 — Form engine & census (65–69) COMPLETE.** **Wave 4 — Finance & community modules (70–75) UNDERWAY: 70, 71, 72, 73, 74 DONE.** Next is **build-step 75 (Accounting/reconciliation UI cleanup: tabbed IA + reconciliation polish, no API/schema change)** — consult `specs/README-v3.1.md` for the Wave-4 build order. The cross-org welfare ledger stays deferred to v4; the data model is built forward-compatible.
- **Last completed build-step:** **74 — Graveyard registry: OMJ-standard + Khundi-local + merge-on-remove** ✅ **DONE** (built 2026-06-27) · **FEATURE** · branch `feat/74-graveyard-registry-v31` · **SCHEMA + ISOLATION · Isolation ✅ (NEW global `graveyard_standard` = OMJ reference, NO tenant_id/NO RLS like `taxonomy_standard`/`permissions`; NEW `graveyard_local` = TENANT DATA, RLS ENABLE+FORCE `graveyard_local_isolation` + tenant FK Cascade; OMJ-scope = `runAsPlatform` only, gated `graveyard.govern` @ ALL_TENANTS)**. Replaces free-text graveyard with a managed registry (build-step-66 "OMJ governs / Khundi consumes" pattern). Schema: `GraveyardStandard` (id,name,location,active,createdBy,ts; @@unique name), `GraveyardLocal` (+tenantId, @@unique[tenant,name]); `DeathRecord.graveyardId` SOFT ref (no FK — spans both tables); `graveyardName`/`graveyardSource`/`graveNumber` RETAINED for back-compat + one-off externals. Migration backfills existing `external` free-text into each Khundi's local registry + re-points the record (no loss; OMJ-source stays free-text until OMJ governs). Shared pure: `GraveyardSource`/`GraveyardScope`/`graveyardScopeSource`, `GraveyardView`/`GraveyardRegistryView`/`GraveyardOption`+`buildGraveyardOptions` (active-only, standard-first); `DeathRecordView/DeathRecordInput += graveyardId`. API `graveyard` module: Khundi controller (`/graveyard` GET list = active standard + own locals, gated `death.view`; `POST/PATCH/POST :id/merge-remove local` gated `death.record`, OWN_TENANT/RLS) + OMJ platform controller (`/platform/graveyard` CRUD + merge-remove, gated `graveyard.govern` @ ALL_TENANTS, `runAsPlatform`). MERGE-ON-REMOVE re-points `DeathRecord.graveyardId`+denormalised name/source onto a chosen target then deletes — never orphaned, audited (Khundi re-points own records; OMJ re-points across all Khundis). Bereavement create/update DENORMALISE the picked registry graveyard (name+source kept in step); free-text one-off still allowed. Web: bereavement form graveyard `<Select>` (OMJ optgroup + "your external" optgroup + "Other/free text" fallback + save-to-my-list); NEW `/platform/graveyard` OMJ management screen (create/edit/merge-remove) gated `graveyard.govern`. `death.record` reused for Khundi graveyard mgmt (no `bereavement.manage` key exists — same call as build-step-73's `prize.approve`). Tests **1458/1458** (177 suites; +25: graveyard service/access/isolation/migration specs). typecheck (api+web+shared)/lint (api+web)/web-build/prisma-validate/i18n parity+web-coverage green; zero physical-direction CSS; 41 new `t()` keys both locales (+ delta `graveyard-registry.json`).
- **Current build-step:** **Wave 4 build-step 74 COMPLETE — next is build-step 75 (Accounting/reconciliation UI cleanup).** Read `specs/75-*-v31.md` (+ matching `70-75-CODEREF.md`) before building. If that spec file is missing/empty, end with `[HUMAN_REQUIRED]`.
- **Branch state:** build-steps 54–61 merged to staging; 62/63/64 on the 64 branch; 65–73 each on their `feat/<NN-*>` branch; **74 built on `feat/74-graveyard-registry-v31`** (off the staging head after 73) — the controller merges it to staging at the checkpoint. Start build-step 75 on a fresh `feat/75-*` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **74 — Graveyard registry: OMJ-standard + Khundi-local + merge-on-remove** ✅ DONE · **FEATURE** · branch `feat/74-graveyard-registry-v31` · tests 1458 (177 suites; +25) · **SCHEMA + ISOLATION ✅ (NEW global `graveyard_standard` = OMJ reference, NO tenant_id/NO RLS like `taxonomy_standard`; NEW `graveyard_local` = TENANT DATA RLS ENABLE+FORCE + tenant FK Cascade; OMJ-scope = `runAsPlatform`, gated `graveyard.govern` @ ALL_TENANTS)**. Replaces free-text graveyard with a managed registry (build-step-66 "OMJ governs / Khundi consumes"). `DeathRecord.graveyardId` SOFT ref (spans both tables); name/source/graveNumber RETAINED for back-compat. Migration backfills `external` free-text → each Khundi's local + re-points (no loss). Shared: `GraveyardView`/`GraveyardRegistryView`/`buildGraveyardOptions`. API `graveyard` module: Khundi list/add/edit/merge-remove local (`death.view`/`death.record`) + OMJ platform CRUD+merge-remove (`graveyard.govern`). MERGE-ON-REMOVE re-points records+denormalised label then deletes — never orphaned, audited. Bereavement create/update denormalise the picked registry graveyard. Web: bereavement graveyard `<Select>` (OMJ + own externals + free-text fallback + save-to-list) + NEW `/platform/graveyard` OMJ screen. 41 new `t()` keys both locales (+ delta `graveyard-registry.json`).
- **73 — Prize fixes: controlled reopen + ceremony-scoped apply/list** ✅ DONE · **FIX** · branch `feat/73-prize-fixes-v31` · tests 1433 (173 suites; +5) · **NO-SCHEMA · Isolation ➖→light (existing tenant reads only — rides `runWithTenant`/RLS; no new table/column, no cross-tenant read)**. API: NEW `prize.service.reopen` (closed→open only, audited, optional `closesAt`) + `POST /prize/ceremonies/:id/reopen` (gated `prize.approve`; no `prize.manage` key exists) + `ReopenCeremonyDto`; refused unless status=closed. Web: apply flow defaults ceremony to the **active instance** (build-step-69 semantics, client-computed); applications list **ceremony-scoped** via toolbar `<Select>` (defaults active, "All ceremonies"); reopen button shows only on `closed` → dedicated audited endpoint (was generic status-setter); RowAction intents aligned (apply=primary/approve=success/reject=danger/assign=primary); apply category picker = active application categories only (recognition separate). 5 new `t()` keys both locales (+ delta `prize-fixes.json`).
- **72 — Events multi-type + agenda + fast attendance** ✅ DONE · FEATURE · branch `feat/72-events-attendance-v31` · tests 1428 (173 suites; +18) · **SCHEMA-LIGHT · Isolation ✅ (no NEW table — `events.agenda` ADD COLUMN + bulk attendance over existing `event_registrations`; RLS unchanged)**. Shared pure: `EventType += ceremony/janaza`, `AgendaItem`+`normalizeAgenda`, `EventView.agenda`, `eventReminderAt`, `BulkAttendanceResult`+`AnnounceEventView`. API: `PUT /events/:id/agenda`, `POST /events/:id/attendance/bulk` (each explicit+stamped, de-duped, foreign skipped), `POST /events/:id/announce` (DRAFT message + optional QUEUED soft reminder — never auto-sent); `GET /members/search` extended with gated partial-CNIC (`members.reveal_cnic` else 403; decrypts at-rest, never returns CNIC). Web: Roll-call modal (debounced name/card+CNIC, Enter-toggle hotkey, bulk present/absent + Save-as-bulk-diff, optional QR, inline Collect-dues→lawazim), agenda editor+panel, Announce action + announce-on-create prompt. 36 new `t()` keys both locales (+ delta `events-rollcall.json`).
> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
