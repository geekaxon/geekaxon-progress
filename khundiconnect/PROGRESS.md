> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 program underway (build-steps 54+, see `specs/README-v3.1.md`).** v3 (38–53) + v2 (19–37) complete. **Wave 1 (54–60) COMPLETE.** **Wave 2 (61–64) COMPLETE.** **Wave 3 — Form engine & census (65–69) UNDERWAY: 65 done.** Next is build-step 66 (taxonomy governance) — consult `specs/README-v3.1.md` for the Wave-3 build order. The cross-org welfare ledger stays deferred to v4; the data model is built forward-compatible.
- **Last completed build-step:** **65 — Advanced form builder (inline options · auto-slug · new field types · conditional logic)** ✅ **DONE** (built 2026-06-27) · FEATURE · branch `feat/65-form-builder-advanced-v31` · **Isolation ✅** (additive `FormFieldType` enum extension + `form_fields.visibility` ADD COLUMN; migration `20260713000000_form_builder_advanced`; RLS ENABLE+FORCE preserved). Shared: `FormFieldType`+`FORM_FIELD_TYPES` += `radio|checkbox|member|searchable` (+`fieldTypeUsesOptions`/`fieldTypeIsMulti`); NEW pure `slugifyFieldKey`, `evaluateFieldVisibility`/`visibleFields` (cascade fixpoint), `validateVisibilityRefs` (cycle/ref guard); `FormFieldView.visibility`; `validateSubmission` shares the visibility evaluator + validates new types (hidden field not required/ignored). API `replaceFields` derives keys (label→slug, locked-core stable), upserts inline OptionSets, persists/validates visibility (rejects cycles pre-write). Web: label-first `<FieldEditor>` (no keys, inline choices, Allow-Other, Pro conditional-logic editor) + `census-form.tsx` renders new types (`<MemberPicker>`/`<SearchSelect>`/buttons/checklist) and hides fields live. Tests **1285/1285** (158 suites; +23). typecheck/lint/web-build/i18n parity+web-coverage green; zero physical-direction CSS; no new i18n `t()` keys (new `form_field_type`+`visibility_op` label rows, both locales).
- **Current build-step:** **Wave 3 (65–69) UNDERWAY — 65 done; next is build-step 66 (taxonomy governance).** Read `specs/66-taxonomy-governance-v31.md` (+ CODEREF `specs/65-69-CODEREF.md`) before building. If that spec file is missing/empty, end with `[HUMAN_REQUIRED]`.
- **Branch state:** build-steps 54–61 merged to staging; 62 (`ea5b831`)/63/64 on the 64 branch; **65 built on `feat/65-form-builder-advanced-v31`** (off the 64 head) — the controller merges them to staging at the checkpoint. Start the next build-step on a fresh `feat/<NN-*>` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **65 — Advanced form builder (inline options · auto-slug · new field types · conditional logic)** ✅ DONE · FEATURE · branch `feat/65-form-builder-advanced-v31` · tests 1285 (158 suites; +23) · **Isolation ✅** (additive enum + `form_fields.visibility` ADD COLUMN; migration `20260713000000_form_builder_advanced`; RLS preserved). Shared: 4 new field types + `slugifyFieldKey` + `evaluateFieldVisibility`/`visibleFields` (cascade) + `validateVisibilityRefs` (cycle guard) + `FormFieldView.visibility`; `validateSubmission` shares the evaluator (hidden field not required/ignored) + validates radio/checkbox/member/searchable. API `replaceFields`: label→key auto-slug (locked-core stable), inline OptionSet upsert, visibility persist + pre-write cycle reject. Web label-first `<FieldEditor>` (no keys, inline choices, Pro conditional-logic editor) + `census-form.tsx` renders new types + hides fields live. New `form_field_type`(×4)+`visibility_op` label rows (EN/UR); no new `t()` keys.
- **64 — Family-tree fix (marriage lens + two-lineage no-doubling)** ✅ DONE · FIX · branch `feat/64-family-tree-fix-v31` · tests 1262 · NO schema/RLS/permission change (read-path/render fix). NEW shared pure `dedupeFamilyTree(view)` + `childVisibleUnderLens(lens, hasSpouseLink)`. API `getFamily` returns `dedupeFamilyTree(view)`. Web `family-tree.tsx` By-Marriage shows focal+spouse(s)+children as a unit. +6 family-tree specs; **isolation ➖** (read-path only).
- **63 — Card system (OMJ/Khundi/OMYS) + certificate-card canvas** ✅ DONE · FEATURE · branch `feat/63-member-cards-canvas-v31` · tests 1256 · NEW `members.khundiCardNumber`+`omysNumber` columns (migration `20260712`, nullable ADD COLUMN, members RLS untouched; OMJ canonical+unique, new ones NOT unique); styling extends JSON (`CertificatePlaceholder.color?` + `CardLayoutField.font?/size?/color?` + `CardLayout.backgroundRef`); **QR unchanged (no CNIC)**; web member-form Khundi#/OMYS# + certificate/card canvas styling. NEW migration-regression + canvas/styling specs; isolation gate ✅.

> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
