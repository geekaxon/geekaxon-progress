> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 program underway (build-steps 54+, see `specs/README-v3.1.md`).** v3 (38–53) + v2 (19–37) complete. **Wave 1 (54–60) COMPLETE.** **Wave 2 (61–64) COMPLETE.** **Wave 3 — Form engine & census (65–69) COMPLETE.** **Wave 4 — Finance & community modules (70–75) COMPLETE.** **Wave 5 — Communication, access & polling (76–82) UNDERWAY: 76–79 DONE; 80–82 remain.** Consult `specs/README-v3.1.md` for the next build-step. The cross-org welfare ledger stays deferred to v4; the data model is built forward-compatible.
- **Last completed build-step:** **79 — Access & credentials (onboarding wizard)** ✅ **DONE** (built 2026-06-28) · **FEATURE** (auth-sensitive) · branch `feat/79-access-credentials-v31` · **SCHEMA ✅ (migration `20260722000000_must_change_password`: ADDITIVE `users.must_change_password BOOLEAN NOT NULL DEFAULT false`; `users` is GLOBAL identity — no RLS touched; backfills to false so existing logins unchanged)**. **Password-primary credentials:** onboarding auto-generates a strong readable password (`generatePassword()` — unambiguous alphabet, guaranteed upper/lower/digit, ≥12 chars), hashes it (scrypt), returns the plaintext **ONCE** with a **`wa.me` deep link** (no provider needed) + best-effort SMS where configured; `mustChangePassword` forces a first-login change; the plaintext is never stored/returned-twice/logged (§17). NEW `PermissionService.provisionCredentials` (the single user-provisioning seam: validate member → create/load user → gen+hash → set flag → assign roles). NEW **leaf** `OnboardingModule` (`POST /onboarding`, gated `roles.manage` @ OWN_TENANT) composes PermissionService + NotificationsService **without a cycle**; NEW `NotificationsService.deliverCredentialSms` dispatches SMS directly (NO `Notification` row — body never persisted). NEW pure `@kc/shared` `buildWhatsappHandoffUrl` + `buildCredentialMessage` (localized). **OTP optional:** NEW unauth `GET /auth/methods` (tenant-from-host) returns `{password, otp}` — otp only where the Khundi has an enabled SMS/WhatsApp provider; login UI now hides OTP per-Khundi (anti-enumeration intact — tenant-level fact only). NEW `POST /auth/change-password` (authed). **My-Access readability:** web `/access` now renders grants grouped by module with human capability text (`perm.*`) + plain scopes — no raw keys (shared `lib/perm-labels` extracted, role editor refactored onto it). Web: `/change-password` (forced first-login), onboarding wizard modal in Settings → Roles. Tests **1556/1556** (190 suites; +24 — crypto/generatePassword, changePassword+loginMethods+§17, migration regression, provisionCredentials+onboarding+§17, shared handoff helpers). typecheck (shared+api+web)/lint (web)/api+web-build/prisma-validate/i18n parity+web-coverage green; §17 secret discipline tested; zero physical-direction CSS; 40 new `t()` keys both locales (+ delta `access-onboarding.json`).
- **Current build-step:** **Wave 5 build-step 79 COMPLETE.** Next: build-step **80 — CSV member import** (`specs/80-*-v31.md`, CODEREF `specs/76-82-CODEREF.md`). Read the spec (+ CODEREF) before building; if that spec file is missing/empty, end with `[HUMAN_REQUIRED]`.
- **Branch state:** build-steps 54–61 merged to staging; 62/63/64 on the 64 branch; 65–78 each on their `feat/<NN-*>` branch; **79 built on `feat/79-access-credentials-v31`** (off the staging head after 78) — the controller merges it to staging at the checkpoint. Start the next build-step on a fresh `feat/<NN>-*` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **79 — Access & credentials (onboarding wizard)** ✅ DONE · **FEATURE** (auth-sensitive) · branch `feat/79-access-credentials-v31` · tests 1556 (190 suites; +24) · **SCHEMA ✅ (migration `20260722000000_must_change_password`: ADDITIVE `users.must_change_password` BOOLEAN NOT NULL DEFAULT false; GLOBAL `users`, no RLS touched)**. Password-primary: onboarding auto-generates a readable password (`generatePassword()`), hashes it (scrypt), returns plaintext **ONCE** + a **`wa.me` handoff** (no provider) + best-effort SMS where set up; `mustChangePassword` forces first-login change; §17 (never stored/returned-twice/logged). NEW `PermissionService.provisionCredentials` (single provisioning seam) + **leaf** `OnboardingModule` (`POST /onboarding`, `roles.manage`) composing Permissions+Notifications without a cycle + `NotificationsService.deliverCredentialSms` (no `Notification` row). NEW pure shared `buildWhatsappHandoffUrl`/`buildCredentialMessage`. OTP optional: unauth `GET /auth/methods` (tenant-from-host) gates OTP per-Khundi (anti-enumeration intact). NEW `POST /auth/change-password`. My-Access `/access` now groups grants by module with human capability text + plain scopes (shared `lib/perm-labels`). Web `/change-password` + onboarding wizard modal. 40 new `t()` keys both locales (+ delta `access-onboarding.json`).
- **78 — Custom audience builder (saved audiences)** ✅ DONE · **FEATURE** · branch `feat/78-audience-builder-v31` · tests 1532 (187 suites; +25) · **SCHEMA + ISOLATION ✅ (migration `20260721000000_saved_audience`: NEW tenant-DATA `saved_audiences`, RLS ENABLE+FORCE + tenant FK cascade, NULLABLE `tenant_id` = OMJ-level; ADDITIVE)**. Named reusable member segment whose `definition` IS a build-step-56 `FilterState` (one filter language) resolved **LIVE** against the roster (shared pure `resolveAudienceDefinition`; active-only default unless `status` is explicit; `votingEligible` = derived build-step-61 fact). NEW `AudiencesService` owns the single roster-resolution path; the build-step-76 `collectCustomMemberIds` now resolves `audienceId` + an inline `filters` definition (NEW `NotificationAudienceSelector.filters`) and unions/dedupes. **OMJ** `/platform/audiences` CRUD (`tenant_id=null`, `announcements.broadcast` @ ALL_TENANTS) + the build-step-77 broadcast gains a `saved` audience type that reads the OMJ definition ONCE then fans it DOWN per Khundi as `{type:'custom',filters}` (resolved in-context, skip-not-abort). Khundi `/audiences` reuses `notifications.send`/`.config` (NO new key). Web `/audiences` + `/platform/audiences` + saved-audience pickers in both composers. 41 new `t()` keys both locales (+ delta `audience-builder.json`).
- **77 — Announcement landing pages + OMJ cross-Khundi** ✅ DONE · **FEATURE** · branch `feat/77-announcement-pages-v31` · tests 1507 (184 suites; +22) · **SCHEMA + ISOLATION ✅ (migration `20260720000000_announcement_pages`: NEW tenant-DATA `announcement_pages`, RLS ENABLE+FORCE + tenant FK cascade, NULLABLE `tenant_id` = OMJ-level at OMJ altitude, GLOBAL-unique `slug`; ADDITIVE)**. Richer landing pages via short `/a/<slug>`; **PUBLIC** unauth+rate-limited read returns ONLY sanitized fields + a **content guard** (`findRestrictedPublicContent`/`isPublicContentSafe`) that rejects a public page embedding CNIC/`{{welfare:…}}`-refs/restricted-store media (authoring + read boundary); **LOGIN-GATED** member view (RLS + audience match); **OMJ cross-Khundi** `announcements.broadcast` (NEW ALL_TENANTS key) authors OMJ-level pages + fans a broadcast out per Khundi through the build-step-76 engine (isolation absolute, broadcast-down, skip-not-abort). Web `/announcements` + `/a/[slug]` + `/platform/announcements`. 49 new `t()` keys both locales (+ delta `announcement-pages.json`).
> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
