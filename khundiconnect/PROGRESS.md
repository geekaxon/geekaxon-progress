> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 COMPLETE (build-steps 54–84).** v3 (38–53) + v2 (19–37) complete. **NEW PROGRAM: v3.2 — concern-resolution (build-steps 86–93, README-v3.2.md), Wave 1 = permissions + cross-cutting roll-out (86–89), Wave 2 = notifications + bug (90–91), Wave 3 = member UX (92–93).** **Build-step 86 — Permissions & roles correctness ✅ DONE.** **Build-step 87 — Dynamic-filter rollout ✅ DONE** (v3.2 Wave 1 / item 2 — adopt the build-step-56 filter engine on every remaining list).
- **Last completed build-step:** **87 — Dynamic-filter rollout to every list** ✅ **DONE** (built 2026-06-28) · **FIX** (completion/roll-out: the build-step-56 `<FilterBar>`/`applyFilters` engine had been adopted on ONLY `members`; rolled it out to the other ~18 lists) · branch `feat/87-filter-rollout-v32` · **NO schema/RLS/auth/permission** (web client + per-list configs; isolation ➖ — client filter over already-tenant-scoped, fully-loaded data). **One tiny PURE shared helper** `labelDomainOptions(domain)` (enumerates a `LABEL_CATALOG` domain → `{value,label}[]`; keeps each list's select filters in lock-step with the column registry's `labelDomain`, so NO raw enum reaches the screen) — configuration, not new engine. **DataList lists given `filterConfig`+`getFilterValue` (16):** census, welfare, lawazim, prize_applications, omj_sync, notifications, deaths(graveyard), accounting_journal, accounting_ledger, donations, documents, cards, events, event_registrations, billing, audit (+ platform_audit free). **Non-DataList lists given a standalone `<FilterBar>`+`applyFilters` (2):** polls, taxonomy. **Audit viewer migrated** from its bespoke toolbar Selects to the standard collapsible accordion + chips. **Documents** dropped its redundant category-toolbar Select in favour of the accordion (removed dead `query`/`catFilter` state + `filterDocuments` import). All lists load whole → client-side (`clientRowCap: 100_000`); `members` keeps its server-side path. **i18n:** reused existing `col.*` keys for every field label (zero new keys) EXCEPT two new `filters.origin`/`filters.status` (EN+UR in messages-extra + filters.json delta) for taxonomy. **Out of scope (documented):** `events_attendance` (owned by step 91), `audiences` (the page IS the FilterBar builder; its saved-segment list has no enum dimension), platform billing catalogs (Vendor console). Gates: typecheck/lint/web-build (44 routes)/i18n parity+coverage green; filter-engine spec extended with `labelDomainOptions` cases.
- **Current build-step:** **Build-step 87 COMPLETE.** Next: the controller advances to spec **88** (v3.2 Wave 1 — Real international phone input via libphonenumber-js) — STOPS with `[HUMAN_REQUIRED]` if `specs/88-*.md` is missing/empty. **Follow-up owed: 86b — fork-on-edit (§2.2)** for non-protected seeded roles.
- **Branch state:** build-steps 54–61 merged to staging; 62/63/64 on the 64 branch; 65–86 each on their `feat/<NN-*>` branch; **87 built on `feat/87-filter-rollout-v32`** (off the staging head after 86) — the controller merges it to staging at the checkpoint. v3.2's remaining steps (88–93) each start on a fresh `feat/<NN>-*` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **87 — Dynamic-filter rollout to every list** ✅ DONE · **FIX** (completion/roll-out; v3.2 Wave 1 / item 2) · branch `feat/87-filter-rollout-v32` · **NO schema/RLS/auth/permission** (web client + per-list configs). The build-step-56 `<FilterBar>`/`applyFilters` engine was adopted on ONLY `members`; rolled it to the other ~18 lists. One tiny PURE shared helper `labelDomainOptions(domain)` keeps each select filter in lock-step with the column registry's `labelDomain` (no raw enum). **16 DataList lists** got `filterConfig`+`getFilterValue` (census, welfare, lawazim, prize, omj_sync, notifications, deaths, accounting_journal/ledger, donations, documents, cards, events, event_registrations, billing, audit[+platform_audit]); **2 non-DataList** (polls, taxonomy) got a standalone `<FilterBar>`+`applyFilters`. Audit viewer migrated bespoke toolbar→accordion; documents dropped its redundant category Select. All client-side (`clientRowCap` high — lists load whole); `members` keeps server-side. i18n: reused existing `col.*` (zero new) except 2 new `filters.origin/status` for taxonomy. Out of scope (documented): events_attendance (step 91), audiences (IS the FilterBar builder), platform billing catalogs. Gates: typecheck/lint/web-build/i18n green; filter-engine spec extended.
- **86 — Permissions & roles correctness** ✅ DONE · **FIX** (PERMISSION-SENSITIVE; v3.2 Wave 1 / item 1 — unblocks Reporting/Polls testing) · branch `feat/86-permissions-fix-v32` · tests 1649 (201 suites; +11, +2 suites) · **NO SCHEMA** (data-only migration). Root cause: `SUPER_ADMIN_KEYS` is dynamic but materialised into `role_permissions` only at provisioning, so later-added perms (reports/polls) never reached already-provisioned tenants' protected Super Admin. **Shipped §2.1 reconcile (the blocker); SPLIT §2.2 fork-on-edit → 86b** (spec-sanctioned: fork model needs a schema/identity decision). Reconcile = (a) idempotent one-shot data migration `20260724000000_superadmin_perm_reconcile` (pure SQL on the GLOBAL/no-RLS authz backbone) + (b) boot-time safety net `PermissionService.reconcileSuperAdminGrants()` on `onApplicationBootstrap` (dynamic to `SUPER_ADMIN_KEYS`; never crashes boot). Preserved all 3 already-correct guards + ≥1-Super-Admin lock-out.
- **85 — Brand/logo deployment** ✅ DONE · **FIX** (real KhundiConnect brand across favicon/PWA/maskable/iOS/login/header/push, replacing the placeholder `icon.svg`) · branch `feat/85-brand-logo-deploy-v31` · **NO schema/auth/permission** (pure web client + public static assets; isolation ➖). `layout.tsx` icons → favicon.svg/.ico + apple-touch-icon; **DEFAULT manifest icons** → four `icon-default-*` PNGs in `@kc/shared` `DEFAULT_PWA_MANIFEST`; `sw.js` CACHE v3→v4 + real precache/push icons; login lockup + `sr-only` h1; header mark; deleted `public/icon.svg`. No new `t()` keys. Gates: typecheck/lint/web-build (44 routes)/i18n parity+coverage/pwa-manifest spec green.
> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
