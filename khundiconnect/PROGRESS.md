> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 COMPLETE (build-steps 54–84).** v3 (38–53) + v2 (19–37) complete. **PROGRAM v3.2 — concern-resolution (build-steps 86–94, README-v3.2.md), Wave 1 = permissions + cross-cutting roll-out (86–89, + the 1b root-cause follow-up 92), Wave 2 = notifications + bug (90–91), Wave 3 = member UX (93–94).** **Wave 1 COMPLETE (86–89, 92). Wave 2 COMPLETE (90–91). Wave 3 COMPLETE (93–94). 🎉 v3.2 build-out COMPLETE (86–94).** **Build-step 94 — Unified "View member" tabbed modal ✅ DONE** (v3.2 Wave 3 / item 2; FEATURE, WEB tabbed modal + 2 additive read endpoints; NO schema/migration/RLS-policy/auth/permission change).
- **Last completed build-step:** **94 — Unified "View member" tabbed modal** ✅ **DONE** (built 2026-06-28) · **FEATURE** (v3.2 Wave 3 / item 2) · branch `feat/94-view-member-modal-v32` · **WEB tabbed modal + 2 small additive read endpoints (census-by-member, welfare-by-member); NO schema/migration/RLS-policy/auth/permission change.** Problem: the per-row "Family tree" action opened a cramped single-scroll modal (tree + a short info block + household + timeline) and Notes was missing entirely. Fix: relabelled the action `Family tree` → **"View member"** and replaced the old modal with a NEW unified `ViewMemberModal` (tab shell over the build-step-38 `<ModuleTabs>`, which mounts only the active tab → lazy per-tab fetch). Tabs: **Basic Info** (fuller member detail incl. card #s, CNIC masked + build-step-48 reveal-audit, derived markers), **Family Tree** (the build-step-93 convergent flow-chart moved in UNCHANGED via a `familyTreeSlot` — page still owns the tree/household/divorce state), **Census** (`census.view`), **Lawazim** (`lawazim.view`, payment history + total), **Welfare** (`welfare.view`, confidential notice), **Notes** (`members.notes` — closes the missing-notes gap, read+add), **Timeline** (`members.view`). Each tab is hidden when the user lacks the source permission AND enforced server-side. API: NEW `GET /census/members/:memberId/entries` (`census.view`) → `CensusService.listEntriesForMember`, and `GET /welfare/members/:memberId/cases` (`welfare.view`, confidential) → `WelfareService.listCasesForMember`; both tenant-scoped via `runWithTenant` (RLS) — only this Khundi's data for this member. Reused: members detail/CNIC-reveal/notes/timeline (build-steps 48/62) + lawazim `memberPayments` (already existed). 30 new i18n keys (dual-write EN+UR in messages-extra). Gates: typecheck(6/6)/lint(clean)/full turbo build ✅; **full API jest 1689/1689 (205 suites; +4 tests)** incl. new spec-94 `listEntriesForMember` (member+tenant scoped, empty) + `listCasesForMember` (subject-filtered, totals, empty) + i18n parity/coverage ✅.
- **Current build-step:** **Build-step 94 COMPLETE — v3.2 build-out (86–94) COMPLETE.** No further numbered spec queued; the controller STOPS with `[HUMAN_REQUIRED]` if `specs/95-*.md` is missing/empty. **Follow-up owed: 86b — fork-on-edit (§2.2)** for non-protected seeded roles.
- **Branch state:** build-steps 54–61 merged to staging; 62/63/64 on the 64 branch; 65–89 each on their `feat/<NN-*>` branch; 90 on `feat/90-notification-preferences-v32`; 91 on `feat/91-attendance-list-fix-v32`; 92 on `feat/92-permission-catalog-sync-v32`; 93 on `feat/93-family-tree-flowchart-v32`; **94 built on `feat/94-view-member-modal-v32`** (stacked on the 93 branch) — the controller merges it to staging at the checkpoint.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **94 — Unified "View member" tabbed modal** ✅ DONE · **FEATURE** (v3.2 Wave 3 / item 2 — v3.2 build-out COMPLETE) · branch `feat/94-view-member-modal-v32` · **WEB tabbed modal + 2 additive read endpoints; NO schema/migration/RLS-policy/auth/permission.** Replaced the cramped single-scroll family-tree modal (Notes was missing) with a NEW `ViewMemberModal` — a tab shell over build-step-38 `<ModuleTabs>` (mounts only the active tab → lazy per-tab fetch). Per-row action relabelled `Family tree` → **"View member"**. Tabs (each hidden without the source permission + enforced server-side): **Basic Info** (fuller detail, CNIC masked + build-step-48 reveal-audit, derived markers), **Family Tree** (build-step-93 chart moved in UNCHANGED via `familyTreeSlot`; page keeps tree/household/divorce state), **Census** (`census.view`), **Lawazim** (`lawazim.view`, history+total), **Welfare** (`welfare.view`, confidential), **Notes** (`members.notes` — closes the gap, read+add), **Timeline** (`members.view`). API: NEW `GET /census/members/:memberId/entries` → `listEntriesForMember`, `GET /welfare/members/:memberId/cases` → `listCasesForMember`; both `runWithTenant` RLS-scoped (only this Khundi's member). Reused members detail/notes/timeline + lawazim `memberPayments`. 30 new i18n keys (dual-write). Gates: typecheck(6/6)/lint/turbo build ✅; full API jest **1689/1689 (205 suites; +4)** ✅.
- **93 — Family-tree convergent flow-chart** ✅ DONE · **FIX (redesign)** (v3.2 Wave 3 / item 1) · branch `feat/93-family-tree-flowchart-v32` · **WEB visualization redesign + additive in-law payload; NO schema/migration/RLS/auth/permission.** The v3.1 tree diverged from the operator's model: a "Father's/Mother's-side" toggle forced flipping (the "doubling" objection) and the convergent flow-chart was never built. REMOVED the side toggle (`side`/`setSide` + segmented control gone); rebuilt the band layout into a CONVERGENT FLOW-CHART of lineage towers — focal's parents(+grandparents)→focal, each spouse's parents (in-laws)→spouse, couple join at a "Married" connector, children descend from the marriage. Lens kept (Both=full convergent / By birth=own ancestry+siblings+children / By marriage=couple+children, v3.1 fix kept). Printable via a build-step-68 `window.open` print view (RTL-correct). API `getFamily` now resolves each open spouse's parents (tenant-scoped `CHILD_OF` inside `runWithTenant`, gender-split) → new `FamilyTreeView.spouseParents`/`SpouseParents`; `dedupeFamilyTree` de-dupes in-laws LAST (cousin-marriage in-law renders once). 4 new i18n keys (dual-write) + father/mother-in-law labels. Gates: typecheck(6/6)/lint/turbo build ✅; full API jest **1685/1685 (205 suites; +4)** ✅.
- **92 — Permission catalog sync to DB** ✅ DONE · **FIX** (v3.2 Wave 1 / item 1b — root-cause follow-up to 86, the testing blocker) · branch `feat/92-permission-catalog-sync-v32` · **GLOBAL `permissions` table + re-runs the 86 reconcile; NEW migration + boot hook + 2 API specs; NO web/i18n/RLS-policy/auth.** The `permissions` catalog is seeded ONCE, so keys added to `@kc/shared` `PERMISSIONS` after a long-lived DB was seeded (`reports.view`, `polls.*`) never reached it → live Super Admin showed 57 not 66 (CI missed it: fresh seed). Fix (sync THEN reconcile): NEW migration `20260726000000_permission_catalog_sync` UPSERTs all 74 catalog rows (`ON CONFLICT DO UPDATE`, add/update only) then re-runs the 86 reconcile (excl. the 8 `NON_TENANT` keys → 66); NEW boot hook `PermissionService.onApplicationBootstrap` runs `syncPermissionCatalog()`→`reconcileSuperAdminGrants()` every boot for durability (never crashes boot). 2 new suites prove it on a NON-fresh DB (missing keys + an out-of-catalog row): inserts missing keys idempotently, Super Admin reconciles to full assignable set incl. Reporting/Polls, no platform/OMJ key granted, stray row never deleted, President untouched, T2 isolated, backbone RLS-OFF / tenants RLS-ON. Gates: typecheck(6/6)/lint/turbo build ✅; full API jest **1681/1681 (205 suites; +2)** ✅.
> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
