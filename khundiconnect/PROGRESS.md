> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 program underway (build-steps 54+, see `specs/README-v3.1.md`).** v3 (38–53) + v2 (19–37) complete. **Wave 1 (54–60) COMPLETE.** **Wave 2 (61–64) COMPLETE.** **Wave 3 — Form engine & census (65–69) COMPLETE.** **Wave 4 — Finance & community modules (70–75) UNDERWAY: 70 DONE.** Next is **build-step 71 (Bereavement expense-lines)** — consult `specs/README-v3.1.md` for the Wave-4 build order. The cross-org welfare ledger stays deferred to v4; the data model is built forward-compatible.
- **Last completed build-step:** **70 — Accounting COA surfacing + Funds & Donations** ✅ **DONE** (built 2026-06-27) · FEATURE · branch `feat/70-accounting-coa-funds-v31` · **MONEY + SCHEMA · Isolation ✅ (NEW tenant tables `funds`/`donors`/`donations` — ENABLE+FORCE RLS; no cross-org seam)**. Shared (pure, tested): `STANDARD_FUNDS` (OMJ core set, Zakat-flagged) + `FundView`/`DonorView`/`DonationView` + `fundBalance`(income−expense, never stored) + `summarizeFunds`(Zakat ISOLATED) + `buildAccountTree`(nest + roll balances up to parents; orphan/self-parent safe); NEW LABEL_CATALOG `fund_kind`+`donation_method`; NEW perms `funds.view`/`funds.manage`/`donations.record` (CORE, OWN_TENANT) → Accountant/President/GenSec defaults; `JournalSourceModule += donation`. API: NEW `funds` module (`GET/POST /funds`, `PUT /funds/:id`, `GET/POST /funds/donors`, `GET/POST /funds/donations` — donation posts a balanced income entry atomically via `postModuleIncomeInTx`, sets `journal_entry_id`; standard funds lazy-seeded); accounting `GET /accounting/accounts/tree` (`listAccountTree`). Web: accounting **Funds & Donations** tab (fund cards w/ Zakat distinct + totals, record-donation form, donor quick-add, donations DataList) + **Tree/Flat** COA toggle (rolled-up tree). Tests **1396/1396** (170 suites; +36 — NEW `funds-shared`/`funds.service`/`funds.isolation`/`funds.migration`/`funds.access` specs). typecheck/lint/web-build/prisma-validate/i18n parity+web-coverage green; zero physical-direction CSS; 50 new `t()` keys both locales (+ delta `funds-donations.json`).
- **Current build-step:** **Wave 4 build-step 70 COMPLETE — next is build-step 71 (Bereavement expense-lines + member-expired + janaza).** Read `specs/71-*-v31.md` (+ matching `70-75-CODEREF.md`) before building. If that spec file is missing/empty, end with `[HUMAN_REQUIRED]`.
- **Branch state:** build-steps 54–61 merged to staging; 62/63/64 on the 64 branch; 65–69 each on their `feat/<NN-*>` branch; **70 built on `feat/70-accounting-coa-funds-v31`** (off the staging head after 69) — the controller merges it to staging at the checkpoint. Start build-step 71 on a fresh `feat/71-*` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **70 — Accounting COA surfacing + Funds & Donations** ✅ DONE · FEATURE · branch `feat/70-accounting-coa-funds-v31` · tests 1396 (170 suites; +36) · **MONEY + SCHEMA · Isolation ✅ (NEW tenant tables `funds`/`donors`/`donations` — ENABLE+FORCE RLS)**. Shared pure: `STANDARD_FUNDS`(Zakat-flagged)+`FundView`/`DonorView`/`DonationView`+`fundBalance`(income−expense, never stored)+`summarizeFunds`(Zakat ISOLATED)+`buildAccountTree`(roll balances up to parents); NEW `fund_kind`/`donation_method` LABEL_CATALOG; NEW perms `funds.view`/`funds.manage`/`donations.record` (CORE); `JournalSourceModule += donation`. API: NEW `funds` module (donation posts a balanced income entry atomically via `postModuleIncomeInTx`, sets `journal_entry_id`; standard funds lazy-seeded) + accounting `GET /accounting/accounts/tree`. Web: accounting **Funds & Donations** tab (fund cards w/ Zakat distinct, record-donation form, donor add, donations DataList) + Tree/Flat COA toggle. 50 new `t()` keys both locales (+ delta `funds-donations.json`).
- **69 — Instance / season pattern (active-latest + archived-viewable + cross-instance comparison · Census rounds + Prize ceremonies)** ✅ DONE · FEATURE · branch `feat/69-instance-season-v31` · tests 1360 (165 suites; +15) · **Isolation ✅ (read/UX-only; cross-instance = multiple rounds/ceremonies of the SAME tenant via RLS — NO schema change)**. Shared pure: `InstanceSummary`+`pickActiveInstance`+`censusSnapshotToInstance`/`prizeCeremonyToInstance` mappers + `buildCensusComparison`(reuses `buildCensusReport` per round → aligned zero-filled trend series)/`buildPrizeComparison`(participation/winners/cash totals); NEW `instance_status` LABEL_CATALOG domain. API: census+prize `listInstances`/`compare*` + `GET /{census,prize}/instances` & `/compare?ids=` (`*.view`, tenant-scoped). Web: census+prize **Compare** tab (recharts + RTL tables) + explicit active/archived badge; census lands on active-latest via `pickActiveInstance`. 25 new `t()` keys both locales (+ delta `instance-season.json`).
- **68 — Polished census PDF report (branded cover · inline-SVG charts · Gatta-G sections · RTL · presentation view)** ✅ DONE · FEATURE · branch `feat/68-census-report-pdf-v31` · tests 1345 (164 suites; +11) · **Isolation ✅ (read-path; tenant-scoped report+branding via RLS — NO schema change)**. **Shipped: server-rendered self-contained HTML deck → browser print-to-PDF** (RTL/Urdu correct via browser shaping); binary-PDF lib deferred (decision in history). Shared pure: NEW `svgBarChart`(RTL-mirroring, `safeColor`-guarded)/`svgDonutChart`(center total+legend); `renderCensusReportHtml` rewritten → branded `CensusReportBranding` cover + per-section inline-SVG charts + Gatta-G narrative + AGGREGATE-ONLY vulnerability COUNT + privacy footer; `@page` A4 + logical-property CSS. Reuses `buildCensusReport`/`deriveVulnerability` (no recompute). API `exportReport` loads `loadReportBranding`. Web: NEW "Present" button opens the deck in its own window; Export downloads it. 3 new `t()` keys both locales (+ delta `census-pdf.json`).
> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
