> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 program underway (build-steps 54+, see `specs/README-v3.1.md`).** v3 (38–53) + v2 (19–37) complete. **Wave 1 (54–60) COMPLETE.** **Wave 2 (61–64) COMPLETE.** **Wave 3 — Form engine & census (65–69) COMPLETE.** **Wave 4 — Finance & community modules (70–75) COMPLETE.** **Wave 5 — Communication, access & polling (76–82) UNDERWAY: 76–77 DONE; 78–82 remain.** Consult `specs/README-v3.1.md` for the next build-step. The cross-org welfare ledger stays deferred to v4; the data model is built forward-compatible.
- **Last completed build-step:** **77 — Announcement landing pages + OMJ cross-Khundi** ✅ **DONE** (built 2026-06-28) · **FEATURE** · branch `feat/77-announcement-pages-v31` · **SCHEMA + ISOLATION ✅ (migration `20260720000000_announcement_pages`: NEW tenant-DATA `announcement_pages` with RLS ENABLE+FORCE + tenant FK cascade; `tenant_id` NULLABLE so OMJ-level rows live at OMJ altitude — a NULL tenant_id matches no `app_current_tenant_id()` so a Khundi can neither read nor INSERT one; GLOBAL-unique `slug`; ADDITIVE — no existing table's RLS touched)**. A richer-than-a-notification page (title/body/media) reached by a short `/a/<slug>` link in two visibilities. **PUBLIC path:** NEW unauthenticated + rate-limited `GET /public/announcements/:slug` returns ONLY sanitized fields (no member/welfare/financial join); the build-step-77 **content guard** (`findRestrictedPublicContent`/`isPublicContentSafe` in shared) REJECTS at authoring time + re-asserts at the read boundary any public page embedding a CNIC / `{{welfare:…}}`-style restricted ref / restricted-store media key. **LOGIN-GATED:** `GET /announcements/view/:slug` serves a published, unexpired, audience-matching member only (RLS + build-step-76 `resolveAudienceMemberIds`). **OMJ cross-Khundi:** `announcements.broadcast` (NEW OMJ-altitude key, ALL_TENANTS, NON_TENANT + OMJ_ADMIN seed) authors OMJ-level pages + `POST /platform/announcements/broadcast` fans OUT per Khundi via the build-step-76 `send` — each per-Khundi audience resolved INSIDE that Khundi's context (isolation absolute, broadcast-down; a Khundi w/o a channel is `skipped`, never aborts). Web: `/announcements` (Khundi manager), `/a/[slug]` (public), `/platform/announcements` (OMJ broadcast + OMJ pages). Tests **1507/1507** (184 suites; +22 — content-guard ×6, service ×7, isolation ×9). typecheck (shared+api+web)/lint (web)/api+web-build/prisma-validate/i18n parity+web-coverage green; isolation REQUIRED+delivered (incl. nullable-tenant OMJ rows + public path can't cross-read); zero physical-direction CSS; 49 new `t()` keys both locales (+ delta `announcement-pages.json`).
- **Current build-step:** **Wave 5 build-step 77 COMPLETE.** Next: build-step **78 — Audience builder (saved audiences)** (`specs/78-*-v31.md`, CODEREF `specs/76-82-CODEREF.md`). Read the spec (+ CODEREF) before building; if that spec file is missing/empty, end with `[HUMAN_REQUIRED]`.
- **Branch state:** build-steps 54–61 merged to staging; 62/63/64 on the 64 branch; 65–76 each on their `feat/<NN-*>` branch; **77 built on `feat/77-announcement-pages-v31`** (off the staging head after 76) — the controller merges it to staging at the checkpoint. Start the next build-step on a fresh `feat/<NN>-*` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **77 — Announcement landing pages + OMJ cross-Khundi** ✅ DONE · **FEATURE** · branch `feat/77-announcement-pages-v31` · tests 1507 (184 suites; +22) · **SCHEMA + ISOLATION ✅ (migration `20260720000000_announcement_pages`: NEW tenant-DATA `announcement_pages`, RLS ENABLE+FORCE + tenant FK cascade, NULLABLE `tenant_id` = OMJ-level at OMJ altitude, GLOBAL-unique `slug`; ADDITIVE)**. Richer landing pages via short `/a/<slug>`; **PUBLIC** unauth+rate-limited read returns ONLY sanitized fields + a **content guard** (`findRestrictedPublicContent`/`isPublicContentSafe`) that rejects a public page embedding CNIC/`{{welfare:…}}`-refs/restricted-store media (authoring + read boundary); **LOGIN-GATED** member view (RLS + audience match); **OMJ cross-Khundi** `announcements.broadcast` (NEW ALL_TENANTS key) authors OMJ-level pages + fans a broadcast out per Khundi through the build-step-76 engine (isolation absolute, broadcast-down, skip-not-abort). Web `/announcements` + `/a/[slug]` + `/platform/announcements`. 49 new `t()` keys both locales (+ delta `announcement-pages.json`).
- **76 — Notification/Announcement engine** ✅ DONE · **FEATURE** · branch `feat/76-notif-engine-v31` · tests 1485 (181 suites; +27) · **SCHEMA + ISOLATION ✅ (migration `20260719000000_notif_engine`: `notifications.attachments JSONB` + NEW tenant-DATA `notification_routing` per-(tenant,kind) default-channel, RLS ENABLE+FORCE + tenant FK cascade; both ADDITIVE)**. Deepens build-step-35/52: **media** attachment refs (vault keys, WhatsApp/push attach, **SMS degrades** to text via channel-aware `MessagingDispatcher`); **active-channels-only** (`computeActiveChannels` + `GET /active-channels`; composer offers + `send` accepts only active channels); **composite `custom` audience** (`memberIds[]` ∪ `roleIds[]` holders ∪ saved `audienceId` fwd-compat, deduped); **per-kind routing** (optional `channel` → `notification_routing` override → OMJ `DEFAULT_CHANNEL_FOR_KIND`; `GET/PUT /routing`); **per-channel template** `pickChannelTemplate`. Push tenant-scope re-asserted; no-auto-fire scheduler unchanged. NO new permission key/module (§17 creds intact). 10 new `t()` keys both locales (+ delta `notif-engine.json`).
- **75 — Accounting/reconciliation UI cleanup: tabbed IA + match/clear reconciliation** ✅ DONE · **FIX** · branch `feat/75-accounting-reports-cleanup-v31` · tests 1458 (177 suites; +0) · **NO-SCHEMA · Isolation ➖ (pure front-end; existing tenant-scoped accounting reads, no new API/query, RLS unchanged)**. Splits the crowded accounting page into build-step-38 tabs via `ModuleTabs`: Overview / Cashbook (both modes) / Funds & Donations (build-step-70) / Ledger / Vouchers / Banks & reconciliation / Chart of accounts / Reports (Pro-only `hidden: mode !== 'pro'` — a build-step-60 DENSITY view, no capability lost). Reconciliation cleanup: NEW `reconFilter` (all/matched/uncleared) over `JournalLine.reconciliationId` — a 3-button live-count filter keeps a long statement to the lines still needing a tick; `toggleCleared` operates over the full set. No schema/API change; double-entry engine untouched. 9 new `t()` keys both locales (+ delta `accounting-cleanup.json`).
> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
