> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 COMPLETE (build-steps 54–84).** v3 (38–53) + v2 (19–37) complete. **NEW PROGRAM: v3.2 — concern-resolution (build-steps 86–94, README-v3.2.md), Wave 1 = permissions + cross-cutting roll-out (86–89, + the 1b root-cause follow-up 92), Wave 2 = notifications + bug (90–91), Wave 3 = member UX (93–94).** **Wave 1 COMPLETE (86–89). Wave 2 COMPLETE (90–91).** **Build-step 92 — Permission catalog sync to DB ✅ DONE** (v3.2 Wave 1 / item 1b — the root-cause follow-up to 86, the testing blocker; FIX, NEW migration + boot-time durability hook + 2 API gate specs, GLOBAL `permissions` table; NO web/i18n/RLS-policy/auth change).
- **Last completed build-step:** **92 — Permission catalog sync to DB** ✅ **DONE** (built 2026-06-28) · **FIX** (v3.2 Wave 1 / item 1b — root-cause follow-up to 86) · branch `feat/92-permission-catalog-sync-v32` · **Touches the GLOBAL `permissions` catalog table + re-runs the build-step-86 Super-Admin reconcile; NEW migration + boot hook + 2 API specs; NO web/i18n/RLS-policy/auth change.** Problem: the `permissions` catalog is seeded ONLY once (`prisma/seed.ts`), so keys added to `@kc/shared` `PERMISSIONS` AFTER a long-lived DB was first seeded (`reports.view`, `polls.*`, +others) never reached it; build-step 86's reconcile then granted only the rows that existed → the live Super Admin showed **57**, not 66 (no Reporting/Polls). CI missed it (fresh-seed). Fix (order matters): (1) NEW migration `20260726000000_permission_catalog_sync` idempotently UPSERTs all **74** catalog rows (`INSERT … ON CONFLICT (key) DO UPDATE`, diff-verified == the live `perm()` set) THEN re-runs the 86 reconcile (excludes the 8-key `NON_TENANT_PERMISSION_KEYS` → 66 for a protected Super Admin); (2) NEW boot-time durability hook — `PermissionService implements OnApplicationBootstrap` runs `syncPermissionCatalog()` → `reconcileSuperAdminGrants()` on EVERY boot, so a permission added in future code reaches every DB + the protected Super Admin WITHOUT another migration (try/catch — never crashes boot). ADD/UPDATE only — never DELETE a catalog row. 2 new API suites prove it on a NON-fresh DB (catalog missing the later keys + an out-of-catalog row): catalog-sync inserts the missing keys (idempotent), the stale Super Admin reconciles to the full assignable set incl. Reporting/Polls, no platform/OMJ key granted, the stray row never deleted, President untouched, T2 isolated, authz backbone stays RLS-OFF / tenants RLS-ON. Gates: typecheck(6/6)/lint(clean)/full turbo build ✅; **full API jest 1681/1681 (205 suites; +2)** incl. the migration-regression + isolation + i18n parity/coverage ✅.
- **Current build-step:** **Build-step 92 COMPLETE.** Next: the controller advances to spec **93** (v3.2 Wave 3 — Family-tree convergent flow-chart) — STOPS with `[HUMAN_REQUIRED]` if `specs/93-*.md` is missing/empty. **Follow-up owed: 86b — fork-on-edit (§2.2)** for non-protected seeded roles.
- **Branch state:** build-steps 54–61 merged to staging; 62/63/64 on the 64 branch; 65–89 each on their `feat/<NN-*>` branch; 90 on `feat/90-notification-preferences-v32`; 91 on `feat/91-attendance-list-fix-v32`; **92 built on `feat/92-permission-catalog-sync-v32`** — the controller merges it to staging at the checkpoint. v3.2's remaining steps (93–94) each start on a fresh `feat/<NN>-*` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **92 — Permission catalog sync to DB** ✅ DONE · **FIX** (v3.2 Wave 1 / item 1b — root-cause follow-up to 86, the testing blocker) · branch `feat/92-permission-catalog-sync-v32` · **GLOBAL `permissions` table + re-runs the 86 reconcile; NEW migration + boot hook + 2 API specs; NO web/i18n/RLS-policy/auth.** The `permissions` catalog is seeded ONCE, so keys added to `@kc/shared` `PERMISSIONS` after a long-lived DB was seeded (`reports.view`, `polls.*`) never reached it → live Super Admin showed 57 not 66 (CI missed it: fresh seed). Fix (sync THEN reconcile): NEW migration `20260726000000_permission_catalog_sync` UPSERTs all 74 catalog rows (`ON CONFLICT DO UPDATE`, add/update only) then re-runs the 86 reconcile (excl. the 8 `NON_TENANT` keys → 66); NEW boot hook `PermissionService.onApplicationBootstrap` runs `syncPermissionCatalog()`→`reconcileSuperAdminGrants()` every boot for durability (never crashes boot). 2 new suites prove it on a NON-fresh DB (missing keys + an out-of-catalog row): inserts missing keys idempotently, Super Admin reconciles to full assignable set incl. Reporting/Polls, no platform/OMJ key granted, stray row never deleted, President untouched, T2 isolated, backbone RLS-OFF / tenants RLS-ON. Gates: typecheck(6/6)/lint/turbo build ✅; full API jest **1681/1681 (205 suites; +2)** ✅.
- **91 — Event attendance blank-table fix + list-registry coverage** ✅ DONE · **FIX** (v3.2 Wave 2 / item 2) · branch `feat/91-attendance-list-fix-v32` · **NO schema/RLS/auth/migration; shared registry + web + 1 API gate.** Concerns [12]/[14]. [12]: `<DataList listKey="events_attendance">` blanked because the key was unregistered → `columns=[]`; **registered `events_attendance`** in `@kc/shared` (mirrors `events`, same `cell` getter; ZERO new i18n keys — labels reuse `events` `col.*`). Audited all `listKey="…"` vs registry (only one missing). Added a dev-only `console.warn` in `data-list.tsx` on an unregistered key + a build-time gate `list-registry-coverage.spec.ts` (walks web source, fails CI on any unregistered rendered key). [14]: trimmed the over-long RSVP dump from `EventPanel` — removed the full `event_registrations` `<DataList>` from the expand, kept the concise fast-mark roster (toggle per active member). Gates: typecheck(6/6)/lint/turbo build ✅; full API jest **1669/1669 (203 suites; +1)** ✅.
- **90 — Per-type notification channel preferences + templates** ✅ DONE · **FEATURE** (v3.2 Wave 2 / item 1) · branch `feat/90-notification-preferences-v32` · **NEW `notification_preference` model + migration + RLS; NO auth/permission-catalog change** (reused `notifications.view`/`.config`). Generalises build-step-76's single-default `notification_routing` → a Khundi picks the SET of channels each kind delivers on (janaza → push+SMS+WhatsApp). Shared PURE `resolvePreferredChannels` (override ∩ live-active; empty → OMJ default) + `NotificationPreferenceView`. `send()` refactored: explicit channel unchanged; OMITTED channel FANS OUT (one log row/channel) via extracted `sendOnChannel`; refuses if none preferred-active. Web: `<NotificationPreferences>` in Settings→Providers (per-kind channel toggles, inactive greyed; per-(kind×channel) wording via existing template CRUD). 13 new `notifPrefs.*` keys both locales. No-auto + §17 preserved. Gates: typecheck/lint/web-build/api-build/prisma-validate ✅; full API jest **1668/1668 (202 suites; +15)** incl. new `notif-preference.isolation.spec.ts` + shared-pure + service fan-out ✅.
> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
