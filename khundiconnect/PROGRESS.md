> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 program underway (build-steps 54+, see `specs/README-v3.1.md`).** v3 (38–53) + v2 (19–37) complete. **Wave 1 — Core UI primitives & cross-cutting (54–60) COMPLETE.** **Wave 2 — Member model & identity (61–64) UNDERWAY — 61 done.** Next is 62. The cross-org welfare ledger stays deferred to v4; the data model is built forward-compatible.
- **Last completed build-step:** **61 — Marriage-linkage model & honorary removal** ✅ **DONE** (built 2026-06-27) · FIX · branch `feat/61-member-marriage-model-v31` · **Removed the `isHonorary` concept entirely** from `@kc/shared`/API/web/counts/labels (gender is now a separate facet, not a category); **added derived `isMarriedIn`** (a non-Okhai woman with an OPEN `SPOUSE_OF` link to an Okhai male — computed on read, never stored) via pure `isMarriedIn(facts, {hasOpenOkhaiMaleSpouse})` + API resolver `married-in.ts`. Demographics `honorary`→`female`+`male` split; insight/report `honorary`→`female`; `member_category` label `honorary`→`married_in` (EN+UR), `MemberView.isHonorary`→`isMarriedIn`. **`Relationship` gains nullable `endedAt`/`endedReason`** (migration `20260710000000_marriage_endedat`, additive — RLS untouched) so a SPOUSE_OF edge is **CLOSED-not-deleted** on divorce; the tree + `isMarriedIn` read OPEN links only. **`endMarriage`** (`POST /family/marriages/:memberId/end`, gated `members.edit`@OWN_TENANT) closes both directional rows + moves a Married-In woman to `inactive` via the existing `changeStatus` (status-history trail) — two explicit steps, never cascades; a re-marriage reopens by clearing `endedAt`. Web: Married-In chip (💍) replaces the Honorary badge; tree spouse-slot end-marriage `<ConfirmDialog>`→endpoint→toast; dashboard/oversight show female/male stats. Tests **1226/1226** (152 suites), incl. NEW `marriage-endedat.migration.spec.ts` (RLS preserved + isolation absolute + close-not-delete) + `marriage-model.spec.ts` (resolveMarriedInIds + endMarriage no-cascade). typecheck/lint/web-build/i18n parity+web-coverage green, zero physical-direction CSS. **Isolation gate ✅** (relationship `endedAt` + derivations).
- **Current build-step:** **Wave 2 underway — 61 done; next is build-step 62 (`member-timeline-notes-v31`).** Consult `specs/README-v3.1.md` for the Wave-2 build order and read `specs/62-*.md` (+ `specs/61-64-CODEREF.md`, which covers 62) before building. If that spec file is missing/empty, end with `[HUMAN_REQUIRED]`.
- **Branch state:** build-steps 54–60 merged to staging; **61 built on `feat/61-member-marriage-model-v31`** (off the staging head after 60) — the controller merges it to staging at the checkpoint. Start build-step 62 on a fresh `feat/62-*` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **61 — Marriage-linkage model & honorary removal** ✅ DONE · FIX · branch `feat/61-member-marriage-model-v31` · tests 1226 · removed `isHonorary` everywhere (gender now a facet not a category) + added derived `isMarriedIn` (non-Okhai woman ⟷ open SPOUSE_OF to Okhai male); `Relationship.endedAt`/`endedReason` (migration `20260710`, RLS untouched) → SPOUSE_OF closed-not-deleted on divorce, tree reads OPEN links only; `endMarriage` (`POST /family/marriages/:memberId/end`, `members.edit`) closes both rows + moves a Married-In woman to inactive via `changeStatus` (no cascade); demographics/insight/report `honorary`→`female`(+`male`); `member_category` `honorary`→`married_in` (EN+UR); web Married-In chip 💍 + end-marriage ConfirmDialog→toast. NEW migration-regression + behavior specs; isolation gate ✅.
- **60 — Mode-gating fix + labels + hygiene** ✅ DONE · FIX · branch `feat/60-mode-labels-hygiene-v31` · tests 1216 · admin/config now PERMISSION-gated only — removed `/settings` from `ROUTE_MODE_KEYS` (no toggle on Settings) + Roles/Providers/Storage tabs gate on `roles.manage`/`notifications.config`/`settings.manage` not `mode`; removed user-facing "Pro form/view" badges; NEW `mode-gating.access.spec.ts` proves `assignmentsGrant` has no `mode` arg (Simple admin with `roles.manage` can edit roles); image-ref sweep clean; `git rm` stray root files. Closes Wave 1.
- **59 — Modal submit shortcut + keyboard/ESC scoping** ✅ DONE · FIX · branch `feat/59-modal-keyboard-v31` · tests 1213 · NEW `lib/hotkeys.ts` (`useHotkeys`/`useEscapeDismiss` + pure matchers, reused by 72); `<Modal>` opt-in `onSubmitShortcut?` (Cmd/Ctrl+Enter saves, plain Enter still newlines) wired into ConfirmDialog + members/welfare/events/prize/roles forms; custom dropdowns close their own ESC via window-capture `stopPropagation` (no modal-close), second ESC closes the modal. Presentation/keyboard only.

> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
