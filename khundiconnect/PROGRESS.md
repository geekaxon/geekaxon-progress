> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 program underway (build-steps 54+, see `specs/README-v3.1.md`).** v3 (38–53) + v2 (19–37) complete. **Wave 1 (54–60) COMPLETE.** **Wave 2 (61–64) COMPLETE.** **Wave 3 — Form engine & census (65–69) UNDERWAY: 65, 66, 67, 68 done.** Next is build-step 69 (census/prize instance + season unification) — consult `specs/README-v3.1.md` for the Wave-3 build order. The cross-org welfare ledger stays deferred to v4; the data model is built forward-compatible.
- **Last completed build-step:** **68 — Polished census PDF report (branded cover · inline-SVG charts · Gatta-G sections · RTL · presentation view)** ✅ **DONE** (built 2026-06-27) · FEATURE · branch `feat/68-census-report-pdf-v31` · **Isolation ✅ (read-path; tenant-scoped report + branding via RLS — NO schema change)**. **Shipped path: server-rendered self-contained HTML deck → browser print-to-PDF** (RTL/Urdu correct because the browser shapes the text); a real binary-PDF lib was deferred — see decision note in PROGRESS-HISTORY. Shared (pure, tested): NEW `svgBarChart`(dir-aware RTL mirroring, drops zero rows, `safeColor`-guarded) + `svgDonutChart`(rotation-symmetric, center total + legend); `renderCensusReportHtml` rewritten into a polished branded deck — `CensusReportBranding` cover (brand name/logo/theme color, generated date, at-a-glance totals), inline-SVG charts per section, Gatta-G narrative, AGGREGATE-ONLY vulnerability COUNT + privacy footer ("no individual or CNIC data"); `@page` A4 + logical-property CSS. Reuses `buildCensusReport`/`deriveVulnerability` (no recompute). API `census.service.exportReport` now loads `loadReportBranding` (tenant displayName always; logo/colors only with paid `whitelabel` via `effectiveBranding`) and passes it in — export shape unchanged. Web `census/page.tsx`: NEW "Present" button opens the polished deck in its own window (on-screen presentation / print-to-PDF); Export still downloads it; Wave-1 toasts. Tests **1345/1345** (164 suites; +11). typecheck/lint/web-build/prisma-validate/i18n parity+web-coverage green; zero physical-direction CSS; 3 new `t()` keys both locales (+ delta `census-pdf.json`).
- **Current build-step:** **Wave 3 (65–69) UNDERWAY — 65, 66, 67, 68 done; next is build-step 69 (instance / season unification + cross-instance reporting).** Read `specs/69-*-v31.md` (+ CODEREF `specs/65-69-CODEREF.md`) before building. If that spec file is missing/empty, end with `[HUMAN_REQUIRED]`.
- **Branch state:** build-steps 54–61 merged to staging; 62 (`ea5b831`)/63/64 on the 64 branch; 65 on `feat/65-form-builder-advanced-v31`; 66 on `feat/66-taxonomy-governance-v31`; 67 on `feat/67-census-redesign-v31`; **68 built on `feat/68-census-report-pdf-v31`** (off the 67 head) — the controller merges them to staging at the checkpoint. Start the next build-step on a fresh `feat/<NN-*>` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **68 — Polished census PDF report (branded cover · inline-SVG charts · Gatta-G sections · RTL · presentation view)** ✅ DONE · FEATURE · branch `feat/68-census-report-pdf-v31` · tests 1345 (164 suites; +11) · **Isolation ✅ (read-path; tenant-scoped report+branding via RLS — NO schema change)**. **Shipped: server-rendered self-contained HTML deck → browser print-to-PDF** (RTL/Urdu correct via browser shaping); binary-PDF lib deferred (decision in history). Shared pure: NEW `svgBarChart`(RTL-mirroring, `safeColor`-guarded)/`svgDonutChart`(center total+legend); `renderCensusReportHtml` rewritten → branded `CensusReportBranding` cover + per-section inline-SVG charts + Gatta-G narrative + AGGREGATE-ONLY vulnerability COUNT + privacy footer; `@page` A4 + logical-property CSS. Reuses `buildCensusReport`/`deriveVulnerability` (no recompute). API `exportReport` loads `loadReportBranding` (displayName always; logo/colors only with paid `whitelabel`) — export shape unchanged. Web: NEW "Present" button opens the deck in its own window; Export downloads it. 3 new `t()` keys both locales (+ delta `census-pdf.json`).
- **67 — Census redesign (auto-fill · Gatta-G field set · unified "Other" · inline household links)** ✅ DONE · FIX/FEATURE · branch `feat/67-census-redesign-v31` · tests 1334 (164 suites; +14) · **Isolation ✅ (flow/read-path; schema-light — NO schema change, payload in `CensusEntry.data` JSON)**. Shared pure: `prefillCensusAnswers`/`extractCensusFacts`(enum-guarded)/`mapEducationLevel`/`mapEmploymentStatus`/`summarizeOtherAnswers`. `censusSpec` += Gatta-G step 3 (`home_ownership`/`has_transport`/`education_stage`/`in_education` conditional/`needs_asks`), unified allowOther, `income_band` realigned to IncomeBand enum. API: `buildEntryData` merges extracted facts (DTO scalars win); NEW `prefillAnswers`/`otherValueSuggestions` + 2 GET endpoints (`census.enter`/`census.view`); `createDefaultDefinition`+seed persist field `visibility`. Web: member auto-fill (editable) + Gatta-G fields render via the engine + "Frequent Other" review panel + inline household link panel (reuses `/family/relationships`, gated `family.edit`). No-auto-approval + audience gate preserved. 17 new `t()` keys both locales (+ delta `census-redesign.json`).
- **66 — Taxonomy governance (OMJ standard · Khundi locals · review queue promote/alias/remap)** ✅ DONE · FEATURE · branch `feat/66-taxonomy-governance-v31` · tests 1320 (163 suites; +35) · **Isolation ✅ + OMJ-scope test ✅** (NEW `taxonomy_local` RLS ENABLE+FORCE + Cascade; NEW global `taxonomy_standard` NO-RLS; migration `20260714000000_taxonomy_governance`; mapping folded into local). Shared: `TAXONOMY_DOMAINS`/`TAXONOMY_STANDARD_SEED` + pure `resolveTaxonomyValue`/`applyTaxonomyMapping`/`buildTaxonomyOptions`/`validateTaxonomyMapping`; perms `taxonomy.contribute`(OWN)+`taxonomy.govern`(ALL_TENANTS). API: Khundi `addLocal` usable-immediately (auto-slug, collision-reject, idempotent) + OMJ `reviewQueue`/`promote`/`mapLocal` (validated, audited, `runAsPlatform`). Web: OMJ governance `/platform/taxonomy` + Khundi `/taxonomy` + nav. 35 new `t()` keys both locales.
> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
