> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 COMPLETE (build-steps 54–84).** v3 (38–53) + v2 (19–37) complete. **Build-step 85 — Brand/logo deployment ✅ DONE** (FIX wiring the real KhundiConnect brand everywhere). **NEW PROGRAM: v3.2 — concern-resolution (build-steps 86–93, README-v3.2.md), Wave 1 = permissions + cross-cutting roll-out (86–89), Wave 2 = notifications + bug (90–91), Wave 3 = member UX (92–93).** **Build-step 86 — Permissions & roles correctness ✅ DONE** (v3.2 Wave 1 / item 1 — PRIORITY; unblocks Reporting/Polls testing).
- **Last completed build-step:** **86 — Permissions & roles correctness** ✅ **DONE** (built 2026-06-28) · **FIX** (the testing BLOCKER: an existing Khundi's protected Super Admin couldn't reach Reporting/Polls because `SUPER_ADMIN_KEYS` is dynamic but materialised into `role_permissions` only at provisioning time, so later-added perms never reached already-provisioned tenants) · branch `feat/86-permissions-fix-v32` · **PERMISSION-SENSITIVE — flagged** · tests 1649 (201 suites; +11, +2 suites). **Shipped §2.1 (the reconcile — the must-have/blocker); SPLIT §2.2 (fork-on-edit) to a follow-up (86b) per the spec's explicit sanction** (the fork model needs a schema/identity decision — `derivedFromRoleId` vs rename — and risks regressing the working name-based role editor; out of scope for the blocker fix). **Reconcile = two hooks:** (a) idempotent one-shot **data migration** `20260724000000_superadmin_perm_reconcile` (pure SQL — authz backbone is GLOBAL/no-RLS — backfills every protected TENANT Super Admin to the full assignable set; non-tenant keys excluded verbatim; per-role `NOT EXISTS` ⇒ idempotent + isolated) that fixes the operator's test Khundi NOW; (b) **boot-time safety net** `PermissionService.reconcileSuperAdminGrants()` on `onApplicationBootstrap` (dynamic to `SUPER_ADMIN_KEYS`, so any FUTURE permission reaches existing Super Admins with no new backfill; never crashes boot — DB-down is logged+skipped). **Preserved all 3 already-correct guards** (protected-not-editable, platform-perms-not-assignable via `sanitisePermissionKeys`, tenant-scoped) + the ≥1-Super-Admin lock-out. **NO schema change** (data-only migration; schema.prisma untouched). **No web/shared/`t()` changes** (API-only). Gates: typecheck/lint/build/full api test suite green; new migration-regression spec (real SQL on PGlite: backfill + idempotency + non-tenant exclusion + per-tenant isolation + RLS regression) + service spec (stale→full, idempotent, only-protected-TENANT, boot never-throws).
- **Current build-step:** **Build-step 86 COMPLETE.** Next: the controller advances to spec **87** (v3.2 Wave 1 — Dynamic-filter rollout to the ~18 remaining lists) — STOPS with `[HUMAN_REQUIRED]` if `specs/87-*.md` is missing/empty. **Follow-up owed: 86b — fork-on-edit (§2.2)** for non-protected seeded roles.
- **Branch state:** build-steps 54–61 merged to staging; 62/63/64 on the 64 branch; 65–85 each on their `feat/<NN-*>` branch; **86 built on `feat/86-permissions-fix-v32`** (off the staging head after 85) — the controller merges it to staging at the checkpoint. v3.2's remaining steps (87–93) each start on a fresh `feat/<NN>-*` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **86 — Permissions & roles correctness** ✅ DONE · **FIX** (PERMISSION-SENSITIVE; v3.2 Wave 1 / item 1 — unblocks Reporting/Polls testing) · branch `feat/86-permissions-fix-v32` · tests 1649 (201 suites; +11, +2 suites) · **NO SCHEMA** (data-only migration). Root cause: `SUPER_ADMIN_KEYS` is dynamic but materialised into `role_permissions` only at provisioning, so later-added perms (reports/polls) never reached already-provisioned tenants' protected Super Admin. **Shipped §2.1 reconcile (the blocker); SPLIT §2.2 fork-on-edit → 86b** (spec-sanctioned: fork model needs a schema/identity decision). Reconcile = (a) idempotent one-shot data migration `20260724000000_superadmin_perm_reconcile` (pure SQL on the GLOBAL/no-RLS authz backbone; backfills every protected TENANT Super Admin to the full assignable set; non-tenant keys excluded; per-role `NOT EXISTS` ⇒ idempotent+isolated) + (b) boot-time safety net `PermissionService.reconcileSuperAdminGrants()` on `onApplicationBootstrap` (dynamic to `SUPER_ADMIN_KEYS` so future perms reach existing Super Admins; never crashes boot). Preserved all 3 already-correct guards + ≥1-Super-Admin lock-out. New migration-regression + service specs.
- **85 — Brand/logo deployment** ✅ DONE · **FIX** (real KhundiConnect brand across favicon/PWA/maskable/iOS/login/header/push, replacing the placeholder `icon.svg`) · branch `feat/85-brand-logo-deploy-v31` · **NO schema/auth/permission** (pure web client + public static assets; isolation ➖). `layout.tsx` icons → favicon.svg/.ico + apple-touch-icon; **DEFAULT manifest icons** → four `icon-default-*` PNGs in `@kc/shared` `DEFAULT_PWA_MANIFEST` (build-step-83 dynamic route's fallback — per-Khundi `logoUrl` still wins); `sw.js` CACHE v3→v4 + real precache/push icons; login lockup `logo-full(.../-inverse)` (Tailwind `dark:`) + `sr-only` h1; header mark `logo-mark(.../-white)` (`dark:`, tenant-name precedence kept); deleted `public/icon.svg` (grep-clean). No new `t()` keys. Gates: typecheck/lint/web-build (44 routes)/i18n parity+coverage/pwa-manifest spec green.
- **84 — Reporting module** ✅ DONE · **FEATURE** (registry-driven report LIBRARY + PDF/Excel; Wave 6 / item 2 — CLOSES v3.1) · branch `feat/84-reporting-module-v31` · tests 1638 (199 suites; +14, +2 suites) · **NO SCHEMA** (reports READ existing data). **Pure `@kc/shared` `REPORT_REGISTRY`** (member-directory/financial-statements/lawazim-defaulters/welfare-summary/census-report/family-tree/khundi-dashboard + OMJ jamat-aggregate). **Hybrid export, both PURE:** PDF=`renderReportHtml` (build-step-68 browser-print HTML deck, RTL/Urdu, logical CSS, `safeColor`); Excel=`renderReportXlsx` (**zero-dependency OOXML writer** — hand-rolled ZIP+CRC32 + SpreadsheetML, RTL, numeric cells; validated via `unzip -t`). **Dependency choice = zero-dep** (no exceljs/SheetJS) — same rationale as build-step-68 PDF (ARM-safe/deterministic/testable/clean gates). **NEW `ReportLibraryService`** ORCHESTRATES existing domain services (REUSES derivations, never recomputes; inherits `runWithTenant` isolation). **Sensitive reports gated by SOURCE permission** (`requiresPermission`); only AGGREGATE reports `shareable`. Endpoints: Khundi `GET /reports/library[/:key[/export?format=pdf|excel]]` (`reports.view`@OWN_TENANT + per-report gate) + OMJ mirror (`crossKhundi.view`@ALL_TENANTS); legacy CSV exports UNCHANGED. Web `/reports` deepened with `ReportLibrary` (cards→filtered preview→PDF/Excel) + `openFileInNewTab` helper. 99 new `t()` keys both locales (+ delta `reports.json`).
> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
