> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 program underway (build-steps 54+, see `specs/README-v3.1.md`).** v3 (38–53) + v2 (19–37) complete. **Wave 1 — Core UI primitives & cross-cutting (54–60) COMPLETE.** **Wave 2 — Member model & identity (61–64) UNDERWAY — 61, 62, 63 done.** Next is 64. The cross-org welfare ledger stays deferred to v4; the data model is built forward-compatible.
- **Last completed build-step:** **63 — Card system (OMJ/Khundi/OMYS) + certificate-card canvas** ✅ **DONE** (built 2026-06-27) · FEATURE · branch `feat/63-member-cards-canvas-v31` · **NEW `members.khundiCardNumber` + `members.omysNumber` columns** (migration `20260712000000_member_card_numbers`, nullable ADD COLUMN — members RLS untouched; OMJ stays canonical+unique, the two new ones NOT unique). **Styling adds NO columns** — extends EXISTING JSON: `CertificatePlaceholder` gains `color?` + `normalizeCertificatePlaceholders` clamps font/size/color (back-compat); `CardLayoutField` gains `font?/size?/color?` + `CardLayout` gains `backgroundRef` + `normalizeCardLayout` attaches per-field styling only when valid (legacy fields round-trip identical). Shared: NEW `clampHexColor`/`clampFontSize`; `MemberView`+`CardView` gain the new numbers (read-through). **QR unchanged — `buildCardQrPayload` still OMJ#+ids only, `qrPayloadHasNoCnic` green.** API: member DTO/service/mapper + vault + member-portal wallet card read-through; template services validate via the extended normalizers automatically. Web: member-form Khundi#/OMYS# fields, `<MemberInfoBoxes>` tiles on the detail modal, certificate-canvas font/size/color controls, card designer background-image + per-field styling + live preview. Tests **1256/1256** (157 suites; +13): NEW `member-card-numbers.migration.spec.ts` + `member-cards-canvas.spec.ts`. typecheck/lint/web-build/i18n parity+web-coverage/QR-no-CNIC green, zero physical-direction CSS. **Isolation gate ✅** (two member columns).
- **Current build-step:** **Wave 2 underway — 63 done; next is build-step 64 (`family-tree-fix-v31`).** Consult `specs/README-v3.1.md` for the Wave-2 build order and read `specs/64-*.md` (+ `specs/61-64-CODEREF.md`, which covers 64) before building. If that spec file is missing/empty, end with `[HUMAN_REQUIRED]`.
- **Branch state:** build-steps 54–61 merged to staging; **62 committed as `ea5b831` on `feat/62-member-timeline-notes-v31`; 63 built on `feat/63-member-cards-canvas-v31`** (off the 62 head) — the controller merges them to staging at the checkpoint. Start build-step 64 on a fresh `feat/64-*` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **63 — Card system (OMJ/Khundi/OMYS) + certificate-card canvas** ✅ DONE · FEATURE · branch `feat/63-member-cards-canvas-v31` · tests 1256 · NEW `members.khundiCardNumber`+`omysNumber` columns (migration `20260712`, nullable ADD COLUMN, members RLS untouched; OMJ canonical+unique, new ones NOT unique); styling adds NO columns — extends JSON: `CertificatePlaceholder.color?` + `CardLayoutField.font?/size?/color?` + `CardLayout.backgroundRef`, normalizers clamp back-compat; NEW shared `clampHexColor`/`clampFontSize`; `MemberView`/`CardView` read-through the new numbers; **QR unchanged (no CNIC, `qrPayloadHasNoCnic` green)**; web member-form Khundi#/OMYS# + `<MemberInfoBoxes>` tiles + certificate canvas font/size/color + card designer background-image + per-field styling. NEW migration-regression + canvas/styling specs; isolation gate ✅.
- **62 — Member timeline + controller notes** ✅ DONE · FEATURE · branch `feat/62-member-timeline-notes-v31` · tests 1243 · NEW append-only `member_timeline` table (migration `20260711`, RLS ENABLE+FORCE) = per-member life-event log + NEW `members.controllerNotes` column (ADD COLUMN, members RLS untouched); NEW perm `members.notes` (CORE, OWN_TENANT; Super Admin/President/Gen-Sec defaults); shared `MemberTimelineEntryView`/`MemberTimelineType`/`sortTimeline`/`member_timeline_type` labels; `MemberTimelineService` + thin in-tx `appendTimeline(tx,…)`; endpoints `GET/POST /members/:id/timeline` (view/edit) + `GET|PUT /members/:id/notes` (gated `members.notes`, audited, never in MemberView/portal/exports); auto-hooks status_change/marriage/divorce/census/prize/card_issued; web Timeline + Notes panels in tree modal. NEW migration-regression + service + access specs; isolation gate ✅.
- **61 — Marriage-linkage model & honorary removal** ✅ DONE · FIX · branch `feat/61-member-marriage-model-v31` · tests 1226 · removed `isHonorary` everywhere (gender now a facet not a category) + added derived `isMarriedIn` (non-Okhai woman ⟷ open SPOUSE_OF to Okhai male); `Relationship.endedAt`/`endedReason` (migration `20260710`, RLS untouched) → SPOUSE_OF closed-not-deleted on divorce, tree reads OPEN links only; `endMarriage` (`POST /family/marriages/:memberId/end`, `members.edit`) closes both rows + moves a Married-In woman to inactive via `changeStatus` (no cascade); demographics/insight/report `honorary`→`female`(+`male`); `member_category` `honorary`→`married_in` (EN+UR); web Married-In chip 💍 + end-marriage ConfirmDialog→toast. NEW migration-regression + behavior specs; isolation gate ✅.

> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
