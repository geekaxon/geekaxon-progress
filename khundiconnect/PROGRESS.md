> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 program underway (build-steps 54+, see `specs/README-v3.1.md`).** v3 (38–53) + v2 (19–37) complete. **Wave 1 (54–60) COMPLETE.** **Wave 2 (61–64) COMPLETE.** **Wave 3 — Form engine & census (65–69) COMPLETE.** **Wave 4 — Finance & community modules (70–75) COMPLETE.** **Wave 5 — Communication, access & polling (76–82) COMPLETE (76–82 all DONE).** Consult `specs/README-v3.1.md` for the next build-step. The cross-org welfare ledger stays deferred to v4; the data model is built forward-compatible.
- **Last completed build-step:** **82 — Scoped Committee Roles** ✅ **DONE** (built 2026-06-28) · **FEATURE** (committee conventions + role-term support on the existing role engine; closes Wave 5) · branch `feat/82-committee-roles-v31` · **NO SCHEMA CHANGE** (committees = scoped custom roles on the existing `Role`/`RolePermission`/`UserRole` engine; the term reuses the existing `Role.timeLimitedUntil` column; `prisma validate` unchanged). Pure convention + seed + UX: NEW `@kc/shared` `CommitteeRoleTemplate` + `COMMITTEE_ROLE_TEMPLATES` (census/prize/welfare committee + committee chairman) — editable starting points, every permission key drawn from `TENANT_ASSIGNABLE_PERMISSIONS` so adopting one can NEVER grant more than a Khundi could by hand (spec-asserted). Committee TERM = the existing `Role.timeLimitedUntil`: role-editor DTOs gain `timeLimitedUntil?: string|null` (`@IsDateString`, null clears), `PermissionService.parseTerm()` is the one rule, `toSummary` exposes `RoleSummary.expiresAt`; the existing `getEffectiveAccess` already drops expired roles (no new enforcement). Chairman elections run through the build-step-81 polling module (committee-scoped eligibility) — the Chairman template carries `polls.manage`; no bespoke flow. Web role editor (Settings → Roles): "Start from a committee template" picker (new roles) + "Term end (optional)" date input + term badge on the role card. **First-class Committees entity DEFERRED to v4.** Tests **1616/1616** (196 suites; +4 — term set/expose/clear, bad-term-rejected, no-term→null, templates-tenant-assignable+adopt). typecheck (shared+api+web)/lint (web)/api+web-build/prisma-validate/i18n parity+web-coverage green; zero physical-direction CSS; 10 new `t()` keys both locales (+ delta `committee-roles.json`).
- **Current build-step:** **Wave 5 build-step 82 COMPLETE — Wave 5 (76–82) fully done.** Next: consult `specs/README-v3.1.md` for the next wave / build-step; read its spec (+ matching CODEREF) before building; if that spec file is missing/empty, end with `[HUMAN_REQUIRED]`.
- **Branch state:** build-steps 54–61 merged to staging; 62/63/64 on the 64 branch; 65–81 each on their `feat/<NN-*>` branch; **82 built on `feat/82-committee-roles-v31`** (off the staging head after 81) — the controller merges it to staging at the checkpoint. Start the next build-step on a fresh `feat/<NN>-*` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **82 — Scoped Committee Roles** ✅ DONE · **FEATURE** (convention + seed + UX on the existing role engine; closes Wave 5) · branch `feat/82-committee-roles-v31` · tests 1616 (196 suites; +4) · **NO SCHEMA** (committees = scoped custom roles on `Role`/`RolePermission`/`UserRole`; term reuses `Role.timeLimitedUntil`). NEW shared `CommitteeRoleTemplate`+`COMMITTEE_ROLE_TEMPLATES` (census/prize/welfare committee + chairman) — editable starting points, every key ∈ `TENANT_ASSIGNABLE_PERMISSIONS` (can't out-grant a Khundi; spec-asserted). Role-editor term: DTOs gain `timeLimitedUntil?:string|null`, `parseTerm()` one rule, `RoleSummary.expiresAt` exposed; existing `getEffectiveAccess` already drops expired roles. Chairman elections → build-step-81 polls (Chairman template carries `polls.manage`); no bespoke flow. Web: "Start from a committee template" picker + "Term end" date input + term badge. **First-class Committees entity DEFERRED to v4.** 10 new `t()` keys both locales (+ delta `committee-roles.json`).
- **81 — Polling & Elections** ✅ DONE · **FEATURE** (full NEW module) · branch `feat/81-polling-elections-v31` · tests 1612 (196 suites; +30) · **SCHEMA+RLS+PERMS+MODULE-FLAG ✅** (migration `20260723000000_polls_elections`: 5 NEW tenant-DATA tables, RLS ENABLE+FORCE, nullable `tenant_id`=OMJ-level; NEW `MODULES.POLLS` + `polls.view/manage/vote`). Lifecycle draft→nominating→open→closed→certified, each explicit (§3.5; pure tested `tallyPoll` fptp/majority/top_n/quota; certify freezes an immutable result). **SECRET BALLOT**: turnout (who, `@@unique` no-double-vote) + ballot (choice, NO voter id/FK, random UUID, day-coarsened `cast_at`) written in ONE tx — voter→choice structurally unlinkable (schema-introspection test). Voter = signed-in user's OWN member (by `omjCardNumber`, never a request id). Per-poll eligibility = build-step-78 audience/rule via the shared `AudiencesService` (NOT the OMJ franchise hardcoded). NEW `polls` module (`PollsService` + Khundi + OMJ `polls.manage`@ALL_TENANTS controllers); best-effort notify (build-step 76, never blocks). Web `/polls` + `/polls/[id]` (secret-ballot UX, live turnout, certified results) + nav. 77 new `t()` keys both locales (+ delta `polls-elections.json`).
- **80 — CSV bulk member import** ✅ DONE · **FEATURE** · branch `feat/80-csv-import-v31` · tests 1582 (193 suites; +26) · **NO SCHEMA** (reuses `members`; provenance stays `manual`). Labeled sample template + server-side per-row validation REUSING the single-add `CreateMemberDto` validators (+ build-step-58 CNIC/phone) — no second path — plus governed-taxonomy enforcement (build-step 66) + CNIC encrypt/token (build-step 48, §17). `POST /members/import/preview` (dry-run, per-row new/duplicate/error + reasons) and `/commit` (writes valid rows in ONE tenant tx, audited, explicit imported/updated/skipped/failed — no silent partial import). Dedupe on CNIC token / OMJ card = update-or-skip (never silent overwrite §3.5); intra-batch dups flagged; `status=suspended` rejected (OMJ-altitude); derived fields not importable. NEW pure shared `parseCsv`/`serializeCsv`/`MEMBER_IMPORT_COLUMNS`/`buildMemberImportSampleCsv`/`mapMemberImportHeaders`/`parseImportBoolean`/`buildImportErrorReportCsv`. NEW `MemberImportService`/`Controller` (gated `members.create`, MembersModule now imports TaxonomyModule); `main.ts` body limit → 8 MB (importer caps length + 5000 rows). Web Members → **Import** tab. 35 new `t()` keys both locales (+ delta `member-import.json`).
> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
