> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 program underway (build-steps 54+, see `specs/README-v3.1.md`).** v3 (38–53) + v2 (19–37) complete. **Wave 1 — Core UI primitives & cross-cutting (54–60) COMPLETE** — 54 + 55 + 56 + 57 + 58 + 59 + 60 all done. Next is Wave 2 (61+). The cross-org welfare ledger stays deferred to v4; the data model is built forward-compatible.
- **Last completed build-step:** **60 — Mode-gating fix + labels + hygiene** ✅ **DONE** (built 2026-06-27) · FIX · branch `feat/60-mode-labels-hygiene-v31` · **Admin/config is now PERMISSION-gated only, never mode-gated:** removed `['/settings','settings']` from `ROUTE_MODE_KEYS` (`apps/web/app/ui/nav.ts`) so the top-bar Simple/Pro toggle no longer appears on Settings; `settings/page.tsx` Roles/Providers/Storage tabs now gate on `roles.manage`/`notifications.config`/`settings.manage` (dropped the `!isPro` condition + the now-unused `useMode`). Removed the user-facing "Pro form"/"Pro view" badges on the members add/tree modals. NEW permission-gate spec `apps/api/src/permissions/mode-gating.access.spec.ts` (+3 tests) proves `assignmentsGrant` takes NO `mode` arg → a Simple-capped admin with `roles.manage` can still edit roles. Image-ref→URL sweep: clean — all image slots bind resolved URLs (`backgroundRef` only ever holds a `publicUrl` via the file-upload `servable` gate). Hygiene: `git rm` of `s`/`ss-test.txt`/`structure.txt`/`pnpm-workspace.yaml.disabled` (+ `.gitignore` guards). i18n parity + web-coverage already GREEN (no new keys; Wave-1 enums already route through `formatLabel`). Tests **1216/1216** (150 suites), typecheck/lint/build green, zero physical-direction CSS. Isolation ➖ (presentation/authz-metadata only — no data-model/endpoint change; the explicit PERMISSION gate replaces it).
- **Current build-step:** **Wave 1 complete — next is Wave 2 (build-step 61).** Consult `specs/README-v3.1.md` for the Wave-2 build order and read the matching `specs/61-*.md` (+ any `specs/<NN>-*-CODEREF.md` whose range covers 61) before building. If that spec file is missing/empty, end with `[HUMAN_REQUIRED]`.
- **Branch state:** build-steps 54 + 55 + 56 + 57 + 58 + 59 merged to staging; **60 built on `feat/60-mode-labels-hygiene-v31`** (off the staging head after 59) — the controller merges it to staging at the checkpoint. Start build-step 61 on a fresh `feat/61-*` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **60 — Mode-gating fix + labels + hygiene** ✅ DONE · FIX · branch `feat/60-mode-labels-hygiene-v31` · tests 1216 · admin/config now PERMISSION-gated only — removed `/settings` from `ROUTE_MODE_KEYS` (no toggle on Settings) + Roles/Providers/Storage tabs gate on `roles.manage`/`notifications.config`/`settings.manage` not `mode`; removed user-facing "Pro form/view" badges; NEW `mode-gating.access.spec.ts` proves `assignmentsGrant` has no `mode` arg (Simple admin with `roles.manage` can edit roles); image-ref sweep clean; `git rm` stray root files. Closes Wave 1.
- **59 — Modal submit shortcut + keyboard/ESC scoping** ✅ DONE · FIX · branch `feat/59-modal-keyboard-v31` · tests 1213 · NEW `lib/hotkeys.ts` (`useHotkeys`/`useEscapeDismiss` + pure matchers, reused by 72); `<Modal>` opt-in `onSubmitShortcut?` (Cmd/Ctrl+Enter saves, plain Enter still newlines) wired into ConfirmDialog + members/welfare/events/prize/roles forms; custom dropdowns close their own ESC via window-capture `stopPropagation` (no modal-close), second ESC closes the modal. Presentation/keyboard only.
- **58 — Validation/masking + phone library** ✅ DONE · FEATURE · branch `feat/58-input-masking-phone-v31` · tests 1213 · shared in-house E.164 lib (`PHONE_COUNTRIES`/`isValidE164`/`normalizePhoneE164`/`parsePhoneE164`, default PK) + CNIC/OMJ masks + validators; NEW web `<PhoneInput>`/`<CnicInput>`/`<OmjCardInput>`; two-level validation (shared rule reused by API DTO decorators in `common/dto-validators.ts`). Validation/presentation only.

> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
