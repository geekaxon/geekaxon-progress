> ⚠️ **PUBLIC FILE** — pushed to a separate public progress repo. **NO secrets, hostnames, real subdomains, API endpoints, credentials, or keys — ever.** Placeholders only.

# PROGRESS.md — KhundiConnect Build Tracker (SHORT)

> **This is the SHORT working tracker the build agent reads at the start of every step.** It holds only: current state, the next-step pointer, the checkpoint rule, and the last few steps as one-liners. The COMPLETE step-by-step detail is in **PROGRESS-HISTORY.md** (append-only archive).
>
> **At the END of the step you just finished (before `[CHECKPOINT]`, as part of finishing the spec — never deferred to the next step):** (1) mark the step you just finished as DONE/APPROVED in **Current Status** (do not leave it PENDING), (2) prepend a ONE-LINE entry to **Recent steps**, (3) drop the now-4th recent one-liner, and (4) append the FULL detailed entry for the step you just finished to **PROGRESS-HISTORY.md**. This recording is tied to the SPEC being done, NOT to any deploy/merge — record it regardless of deploy. Never let this file grow long.
>
> **Numbering:** build-step numbers are canonical (ARCHITECTURE.md §27). "Module N" = build-step N.
>
> **Checkpoint rule (continuous mode):** end every step with `[CHECKPOINT]` on its own final line — the controller records PROGRESS, then SILENTLY merges the branch to staging + pushes (deploy runs in the background; check it with `DEPLOY STATUS`) and continues to the next step. Do NOT use `[FIXED_CHECKPOINT]` — even data-model / RLS / auth changes just end with `[CHECKPOINT]`. Stop ONLY by ending with `[HUMAN_REQUIRED]` when the step's spec file is missing/empty or an infra problem is not code-fixable.

## Current Status
- **Project:** KhundiConnect (multi-tenant community SaaS)
- **Phase:** **v3.1 COMPLETE (build-steps 54–84).** v3 (38–53) + v2 (19–37) complete. **NEW PROGRAM: v3.2 — concern-resolution (build-steps 86–93, README-v3.2.md), Wave 1 = permissions + cross-cutting roll-out (86–89), Wave 2 = notifications + bug (90–91), Wave 3 = member UX (92–93).** **Wave 1 COMPLETE (86–89).** **Build-step 90 — Per-type notification channel preferences + templates ✅ DONE** (v3.2 Wave 2 / item 1 — the operator's actual notifications ask; NEW `notification_preference` model + migration + RLS).
- **Last completed build-step:** **90 — Per-type notification channel preferences + templates** ✅ **DONE** (built 2026-06-28) · **FEATURE** (v3.2 Wave 2 / item 1) · branch `feat/90-notification-preferences-v32` · **NEW TENANT-DATA model `notification_preference` (RLS ENABLE+FORCE) + migration `20260725000000_notification_preference`; NO auth/permission-catalog change** (reused existing `notifications.view`/`notifications.config` keys). Generalises the build-step-76 single-default `notification_routing`: a Khundi now picks the SET of channels each notification KIND delivers on (e.g. janaza `welfare_alert` → push+SMS+WhatsApp; `meeting_notice` → push only). `notification_preference` = `id, tenant_id, kind, channels Json, updated_by, ts`, `@@unique([tenant_id, kind])`. **Shared:** `NotificationPreferenceView` + PURE `defaultChannelsForKind`/`sanitizePreferenceChannels`/`resolvePreferredChannels`/`buildPreferenceTable` (a Khundi override ∩ live-active channels; empty override → OMJ default, never "no channels"). **Send path:** `send()` refactored — an EXPLICIT `dto.channel` is unchanged (build-step-76 behaviour, one row); an OMITTED channel now FANS OUT across the kind's preferred-and-active channels (one log row per channel), via extracted `sendOnChannel`. No preference → single build-step-76 default (so every existing send test is untouched — all pass channel explicitly). Refuses if NONE of the preferred channels are active. **API:** `listPreferences`/`setPreference` (empty set clears the override) + `GET/PUT /notifications/preferences[/:kind]`, mapper, `SetPreferenceDto`. **Web:** new `<NotificationPreferences>` rendered INSIDE Settings → Providers — per-kind channel toggles (inactive channels greyed/disabled) + per-(kind×channel) wording editors reusing the existing template CRUD (deterministic `notif-<kind>-<channel>` key). `CheckboxRow` gained an optional `disabled` prop. **13 new i18n keys** (`notifPrefs.*`) both locales in `messages-extra.ts` + delta `i18n-deltas/notification-preferences.json`. No-auto preserved (a preference ROUTES, it never auto-fires); §17 untouched (preferences carry no secret). Gates: typecheck(shared+api+web)/web-lint(clean)/web-build/api-build/prisma-validate ✅; **full API jest 1668/1668 (202 suites; +15)** incl. i18n parity+coverage + NEW `notif-preference.isolation.spec.ts` (real chain through 90 on PGlite, non-superuser: additive, RLS isolation read/update/delete/insert, deny-by-default, platform bypass, cascade) + shared-pure + service fan-out specs ✅.
- **Current build-step:** **Build-step 90 COMPLETE.** Next: the controller advances to spec **91** (v3.2 Wave 2 — event attendance blank-table fix; add `events_attendance` to the list-column registry) — STOPS with `[HUMAN_REQUIRED]` if `specs/91-*.md` is missing/empty. **Follow-up owed: 86b — fork-on-edit (§2.2)** for non-protected seeded roles.
- **Branch state:** build-steps 54–61 merged to staging; 62/63/64 on the 64 branch; 65–89 each on their `feat/<NN-*>` branch; **90 built on `feat/90-notification-preferences-v32`** (off the staging head after 89) — the controller merges it to staging at the checkpoint. v3.2's remaining steps (91–93) each start on a fresh `feat/<NN>-*` branch off the resulting staging head.

### Recent steps (one-liners — FULL detail in PROGRESS-HISTORY.md)
- **90 — Per-type notification channel preferences + templates** ✅ DONE · **FEATURE** (v3.2 Wave 2 / item 1) · branch `feat/90-notification-preferences-v32` · **NEW `notification_preference` model + migration + RLS; NO auth/permission-catalog change** (reused `notifications.view`/`.config`). Generalises build-step-76's single-default `notification_routing` → a Khundi picks the SET of channels each kind delivers on (janaza → push+SMS+WhatsApp). Shared PURE `resolvePreferredChannels` (override ∩ live-active; empty → OMJ default) + `NotificationPreferenceView`. `send()` refactored: explicit channel unchanged; OMITTED channel FANS OUT (one log row/channel) via extracted `sendOnChannel`; refuses if none preferred-active. Web: `<NotificationPreferences>` in Settings→Providers (per-kind channel toggles, inactive greyed; per-(kind×channel) wording via existing template CRUD). 13 new `notifPrefs.*` keys both locales. No-auto + §17 preserved. Gates: typecheck/lint/web-build/api-build/prisma-validate ✅; full API jest **1668/1668 (202 suites; +15)** incl. new `notif-preference.isolation.spec.ts` + shared-pure + service fan-out ✅.
- **89 — CNIC/OMJ masking rollout** ✅ DONE · **FIX** (completion/roll-out; v3.2 Wave 1 / item 4) · branch `feat/89-masking-rollout-v32` · **Web only — NO schema/RLS/auth/permission; NO shared change** (reused `normalizeCnic`). The build-step-58 masked-input was wired into only member-form; rolled it to the three remaining CNIC/OMJ *entry* sites: events attendance CNIC search → `<CnicInput>` (debounce normalizes the mask to bare digits before the `cnic=` search so dashes never leak); login OMJ-card (OTP+password) → `<OmjCardInput>`; platform/tenants Super-Admin OMJ card → `<OmjCardInput>`. Khundi-card/OMYS stay free-text (no canonical mask, matching member-form); members-import/onboard/roles/welfare/prize/census/graveyard/portal have no identifier entry. CNIC encrypt/token + reveal-audit untouched (presentation only). ZERO new i18n keys. Gates: typecheck(6/6)/lint/web-build(44)/i18n(8/8) green; acceptance grep clean.
- **88 — Real international phone input (libphonenumber-js)** ✅ DONE · **FIX** (v3.2 Wave 1 / item 3) · branch `feat/88-phone-library-v32` · **NO schema/RLS/auth/permission/migration** (web + shared validation + 1 server send-path change). Replaced the build-step-58 hand-rolled phone layer with `libphonenumber-js` + `country-flag-icons`. Shared `isValidE164`/`normalizePhoneE164`/`parsePhoneE164` now do real per-country validity; `PHONE_COUNTRIES` = full ITU list. Component rewrite kept the custom shell: searchable country combobox + SVG flags + as-you-type + real inline validation; country names via `Intl.DisplayNames` → ZERO new i18n keys. Server `normalizeRecipients` coerces recipients to real E.164 (drops un-dialable). Rolled out platform/tenants raw Input → `<PhoneInput>`; notifications bulk-paste stays free-text. Gates: typecheck(6/6)/lint/web-build(44)/i18n/**full API 1653 passed** green.
> _Note: build-step 52 (Notifications channels — PWA/SMS/WhatsApp providers) was the step between 51 and 53 in the build order; see PROGRESS-HISTORY.md for the full sequence and every step's detail._

---

> Older steps (everything before the three above) are archived in **PROGRESS-HISTORY.md**. Read that file only when you need to recover detail about an older step — never read it cover-to-cover each step.
